# 2026-09-02

## 前端页面结构整体重构（P11.6，未提交）

按用户要求「创建完整的前端页面结构（组件/layout/page）+ 开发清单」执行。

**结构重组（RCL SuperBuilder_AI.Components）**
- Pages 扁平 → 六组：`Analysis / Design / Platform / Admin / Account / Errors`；两个 Head 的 `_Imports.razor` 同步登记新命名空间（空目录会导致 CS0234，必须先填文件再 build）。
- Shared 拆为 `UI / BI / Feedback / Guard`；`ChartView` 迁入 `Shared/BI`，`PageHead` 迁入 `Shared/UI`。
- 重名规避：`Pages/Components.razor → Design/ComponentGallery.razor`，`Pages/Themes.razor → Design/ThemeEditor.razor`（与 `Admin/Themes` 类名冲突）。

**新增资产**
- Layout：`BlankLayout`（登录/错误页，且同样做主题+会话自举）、`AppBreadcrumb`、`IconSprite`（从 NavMenu 抽出）、`NavMenuItems.cs`（导航单一事实来源，含 `EnforcePermissions` 开关，默认 false 避免过度隐藏菜单）；MainLayout 加用户菜单/退出 + `SbToastHost`。
- 组件：19 个 `Sb*`（Panel/Toolbar/Search/Badge/StatTile/EmptyState/Spinner/Alert/Field/Progress/Segmented/KeyValue/Pagination/DataTable+Tabs/Modal/Confirm/ToastHost）+ BI 4 个（KpiGrid/SqlViewer/ResultTable/DataSourcePicker）+ Feedback 2 + Guard 2。
- 页面 9 个：`/` 工作台、仪表盘/应用/Agent计划/数据源 详情、个人设置、403/404/500、系统状态。
- 服务：`ToastService`；`AppState.SessionRestored` + `AuthStore.RestoreAsync` finally 置位；`IApiClient`/`ApiClient` 补 `SendAsync/Post/Put/Patch/Delete` + `GetTextAsync`；`Shared/JsonRows.cs`（松类型行抽取）。
- 样式：app.css 追加组件库样式（全部走主题变量）。

**重要修正**
- 原 `Login.razor` 同时占 `/` 与 `/login` → 侧栏「首页」跳登录。现已拆分：`/` = Home 工作台，`/login` 专用。

**文档**：`docs/Frontend_Structure_Plan.md`（结构规范/路由表/分层规则/5 步新增流程/踩坑）+ `docs/Frontend_DevChecklist.md`（S0 已完成 + S1~S5 待办清单）。

**验证**：RCL / Web / MAUI(Windows) 三端 build 0 error（剩余 2 警告为既有：Ask.razor CS8602、ThemeEditor._msg 未使用）。

## 新增踩坑（Blazor）
- 事件处理器内联 `Nav.NavigateTo("x")` 必须用**单引号**定界属性：`@onclick='() => Nav.NavigateTo("ask")'`；双引号版会提前闭合属性 → 生成代码 CS1026（9 处批量修正）。
- `<details>` 不支持 `@bind-open`（RZ9991），只能用普通属性。
- 页面登录判定必须等 `AppState.SessionRestored`（子组件 OnAfterRender 早于 MainLayout 的会话自举），否则预渲染/首帧误判未登录。
- 并行 `dotnet build` 同一解决方案会因 bin 文件被重建删除而报 MSB3030，串行执行。

---

## S1 组件化改造（续，三端 build 0 error）

承接 S0 完成后的 S1。

**S1-1 列表页统一**：Dashboards/Apps/Agent（上轮已转 `SbListPage`）→ 本轮补齐 SemanticLabels、BusinessModel、Audit、Identity、Localization、Quota、DataSources、ModelAccounts、Admin/{Tenants,Themes}。
- 单列表页统一用 `SbListPage TItem="JsonElement"`（含 `AuthGuard`、统计卡 `SbStatTile`、搜索、分页、空/错态）。
- 双视图页：BusinessModel 用 `SbTabs`（业务域 row-list / 实体 `SbDataTable`）；Identity 用两个 `SbPanel + SbDataTable`。
- DataSources/ModelAccounts 由 `SbField`+`SbPanel` 重写表单，占位 `_msg` 改 `ToastService`。

**S1-2 ComponentGallery**：增补 `SbTabs/SbModal/SbConfirm/SbDataTable` 交互示例 + Toast 四档按钮 + `PermissionGuard` 演示（Require 为 `IReadOnlyList<string>`，须传 `new[]{...}`）。

**S1-3 占位 Toast 清除**：SemanticLabels/BusinessModel/Tenants 的 `private void Toast(string)` + ThemeEditor 的 `_msg` 全部改为注入 `ToastService`。grep 确认全局无 `_msg` 占位。

**S1-4 详情页**：新增 `Analysis/SemanticLabelDetail.razor`(`/semantic-labels/{Id:long}`) 与 `Analysis/BusinessModelEntityDetail.razor`(`/business-model/entities/{Id:long}`)，列表行 `Open`/`RowActions` 已跳转到明细页（后端详情端点缺失时优雅空态）。

**S1-5 Ask 拆分**：`AskTurn`+`VizOverride` 抽到 `Models/AskTurn.cs`；单轮结果渲染 + 纯函数视图调整逻辑（EffType/BuildChart/ApplyViewCommand 等）移入 `Shared/Analysis/AskTurnCard.razor`；`Ask.razor` 446→~150 行，循环中 `<AskTurnCard Turn Index OnRefine=DoRefine OnPublish=Publish />`。`_Imports.razor` 增 `Shared.Analysis`。

**验证**：RCL(net10.0) 0 error / Web 0 error / MAUI(windows) 0 error。清单 `Frontend_DevChecklist.md` S1 标记 ✅。

### S1 新增踩坑
- `SbListPage` ErrorState `Message="@Error!"` → RZ9986（组件属性不支持混合 C#/标记），改 `@(Error ?? string.Empty)`。
- `SbStatTile Value` 是 `object` 类型，`Value="—"` 被当 C# 负号表达式（CS1525）→ 用 `@("-")` 强制字符串字面量；`Value="@_rows.Count"`（int→object）正常。
- 全角破折号 `—` 放在双引号属性值里触发 CS1056，UI 占位统一用 ASCII `-`。
- `PermissionGuard.Require` 为 `IReadOnlyList<string>`，示例须 `Require="@(new[]{"demo:write"})"`，勿传单字符串。
- 子组件 `AskTurnCard` 内修改 `turn.Overrides` 后无需手动 `StateHasChanged`：按钮点击事件由该组件处理，Blazor 自动重渲染；父页共享同一 `Turn` 实例，DoRefine/Publish 经 `EventCallback` 回父页修改后父重渲染、子随参重渲染。

---

## S2 写操作闭环（部分闭环，三端 build 0 error）

先核查 `src/Api/Controllers`：后端写接口比清单假设更完整（Dashboards/Apps/Agent/Tenants/Identity/SemanticLabels/Themes/DataSourceAccess 均有写端点）。

**已闭环（删除/新建/启停）**
- S2-1 仪表盘删除：`SbConfirm` → `DELETE api/dashboards/{id}?tenantId=`（`Dashboards.razor` RowActions + `DoDelete`）。
- S2-2 应用删除：`SbConfirm` → `DELETE api/apps/{code}`；卡片墙删除按钮加 `@onclick:stopPropagation="true"` 避免触发卡片跳转（`Apps.razor`）。
- S2-3 语义标签新建：模态表单（`SbModal`+`SbField`），`POST api/semantic-labels`，body 对齐 `UpsertSemanticLabelRequest`（tenantId/conceptType/conceptId/culture/labelKind/value/source/sortOrder）；概念类型/标签种类用下拉，cid 用 `long.TryParse` 守卫（`SemanticLabels.razor`）。
- S2-4 租户：列表改为真实加载（`GET api/tenant-management`）+ 新建（`POST`）+ 启停（`PATCH enable/disable`），错误码 409→「编码已存在」（`Tenants.razor`）。
- S2-中 Agent 计划删除：`SbConfirm` → `DELETE api/agent/plans/{code}`。

**代办**：仪表盘/应用 DSL 编辑器（用 `editor/blueprint`）、S2-5 身份权限写、S2-6 字段级 `SbField.Error`、S2-7 配额/多语言写（后端当前仅配额 check/consume、无语言写）。

**验证**：RCL(net10.0) / Web / MAUI(windows) 三端 build 0 error。`Frontend_DevChecklist.md` S2 标记 🟡（删除/新建/启停 ✅）。

## S2 续：编辑器 + 身份权限全量写（闭环，三端 0 error）

本轮补齐 S2 剩余写操作（已核查 `src/Api/Controllers` 契约）：

**S2-5 身份权限写（Identity.razor 全量闭环）**
- 新建用户：`POST api/identity/users`（tenantId/username/displayName/email/roleCodes[]），角色多选来自 `GET api/identity/roles`。
- 新建角色：`POST api/identity/roles`（tenantId/code/name/description/permissions[]），权限来自 `GET api/identity/permissions`。
- 分配角色：`POST api/identity/users/{id}/roles` `{roleCode}`（用户摘要不含 roles，故不做回显，仅指派）。
- 编辑角色权限：`PUT api/identity/roles/{code}/permissions` `{permissions[]}`（全量替换，预勾选来自 role.permissions 数组）。
- 新增 CSS：`form-stack`/`chk-grid`/`chk`（模态表单）于 `wwwroot/css/app.css`。

**S2-2 应用编辑器（Apps.razor）**
- 新建（描述生成）：`POST api/apps/generate`（tenantId/description/code?），LLM 非默认路径，502→错误提示。
- 编辑 DSL：卡片「编辑」→ `GET api/apps/{code}` 取 `dslJson` 注入 textarea → `PUT api/apps/{code}` `{dslJson}`；全局模板（tenantId==0）readonly 禁用保存。

**S2-1 仪表盘创建（Dashboards.razor）**
- 新建：拉取 `GET api/dashboards/editor/blueprint` 取 `skeleton`（序列化 JSON）预填 textarea，状态 draft/published 可选 → `POST api/dashboards`（tenantId/dslJson/status）。

**状态（S2 全部可补项完成）**：S2-1/S2-2/S2-3/S2-4/S2-5/S2-6 ✅；S2-7 ⬜（后端限制：Localization 仅 GET 无写端点、Quota 仅 check/consume 运行时无配额项维护端点）。三端 build 0 error（RCL 3 警告、Web 0、MAUI 2，均为既遗留）。
清单 `Frontend_DevChecklist.md` S2 区段已更新。

### S2 踩坑
- `SbConfirm`/`SbModal` 必须放在 `SbListPage` **之外**（同 AuthGuard 内），否则 RZ9996「Unrecognized child content」。
- 卡片墙内删除按钮需 `@onclick:stopPropagation="true"`，否则冒泡触发卡片 `@onclick` 跳转详情。
- `ApiClient.PostAsync` 等返回 `(Ok, Status, Error)`，409 等由调用方映射为文案，不抛异常。
- `btn-outline-danger` 是 Bootstrap 5.3.3 内置类，无需在 app.css 额外定义。

### S2 补全（租户设置 Upsert / 字段级校验）
- 租户设置：`POST api/tenant-management/{id:long}/settings`（`UpsertTenantSettingRequest{Key,Value,DataType}`）真实存在，已接入 `Tenants.razor`「设置」模态（GET 现有 → 列表 + 新增/更新一条）。
- 后端写操作错误体为扁平 `{ errors:["..."] }` 数组（非字段字典）。`ApiClient.ParseApiError` 原先不识别，回退为截断原始 JSON 文本；已补 `errors` 数组分支（逐条 join「；」），所有写操作失败提示友好化。
- 字段级校验走前端：`SbField Required` + `Error` 参数，提交前置校验未填/格式错写入对应字段（Tenants/SemanticLabels/Identity）。后端不返回字段名，故服务端错误仍聚合成表单级提示——S2-6 验收定为「客户端字段级 + 服务端错误可读化」。
- Razor 踩坑：`GetJsonAsync` 返回的 `data` 是 `JsonElement?`，模式匹配 `is { ValueKind: Array }` 后仍须 `.Value.EnumerateArray()`（不能直接 `data.EnumerateArray()`，CS1061）。
- `ApiClient.ParseApiError` 内 `foreach` 变量名勿与上层 `var e = Deserialize<ApiError>(...)` 冲突（CS0136），已改名 `item`。

---

## S3 Ask 旗舰体验增强（三端 build 0 error，未提交）

S3 全部完成，三端（RCL net10.0 / Web / MAUI-windows10.0.19041.0）+ API 均 0 error。

**S3-1 后端数据源枚举**：新增 `src/Api/Controllers/DataSourcesController.cs`（`GET api/data-sources`）。复用 `DataSourceAuthorizationService` 同款授权判定（用户/角色显式授权 + 本租户 + Enabled），返回 `DataSourceSummaryDto{Id,Name,DbType}`；需 `dashboard:view` 权限，无授权返回空数组（HTTP 200，避免前端闪烁）。只读 GET，不碰 Golden。
- Ask.razor 启动（`OnAfterRenderAsync` 等 `SessionRestored`）拉取 `api/data-sources` → 填充 `DataSourcePicker`（`DataSourcePicker.Option{Id,Name}`），默认选中首项；手输 number 改为下拉。

**S3-2 会话持久化**：新增 `Services/AskSessionStore.cs`（IJSRuntime + localStorage）。`JsonSerializer` 用 `IncludeFields=true` 序列化 `AskTurn` 列表（字段 Question/Response/Error/… 与 `VizOverride` 字段均需包含）。Key=`ask-session:{tid}:{uid}`，上限 50 轮；提问/细化/清空经 `PersistAsync` 落盘，会话自举后 `LoadAsync` 恢复。已给 `AskTurn` 加 `DataSourceIdHint` 字段（仅恢复时回填选择）。

**S3-3 导出**：新增 `Services/FileDownloadService.cs`（Blob + 临时 `<a download>` 下载，RCL `chart.js` 加 `downloadTextFile`/`setLocalJson`/`getLocalJson`/`removeLocal`）。Ask.razor 实现 `BuildCsv`（带 BOM）/ `BuildExcel`（HTML table xls）；`AskTurnCard` 每轮表格工具条「导出 CSV / 导出 Excel」经 `OnExport` 回调（`AskTurnExport` record）触发。MAUI WebView 内下载受沙箱限制（已知平台限制）。

**S3-4 图表扩展**：`ChartView` 增 `Stacked/Area/MultiAxis` 参数——面积图 `fill:true` + rgba 半透明（`ToRgba`）；堆叠 `stack:'s'` + y 轴 `stacked`；多轴第 2+ 序列走 `y1` 右侧轴（`grid.drawOnChartArea=false`）。`AskTurnCard` 工具栏增堆叠/面积/多轴切换 + 下钻（分类切片 chips 筛选表格，`_drillCat`）。`VizOverride` 增 `Stacked/Area/MultiAxis` 字段。

**S3-5 语义细化对比**：`Ask.razor` 的 `WithParents()` 计算每轮父轮（细化子轮绑定最近普通轮），经 `CompareWith` 传入 `AskTurnCard`；细化子轮「对比原结果」开关两栏并列渲染父/子答案文本 + 数据表 + SQL（`RenderCompact` RenderFragment）。

### S3 踩坑
- `AskTurnCard` 内 `@onclick="() => SetDrill("")"` 双引号属性含内嵌 `""` 会提前闭合属性 → CS1026/CS1525；事件处理器含 C# 字符串字面量必须 `@onclick='...'` 单引号定界（与 S1 踩坑一致）。导出按钮 `OnExport.InvokeAsync(new AskTurnExport(Turn, "csv"))` 同理改单引号。
- Ask.razor 在 `@else {` 标记块内再写 `@{ ... }` 代码块 → RZ1010；改为把父轮计算抽到 `@code` 的 `WithParents()` 方法，`@foreach` 渲染，规避嵌套代码块。
- 序列化 `AskTurn`：其 `Question/Response/Error/...` 是 public 字段（非属性），默认 `JsonSerializer` 不序列化 → 必须 `IncludeFields=true`，否则恢复后轮次内容全空。
- `ChartView` 多轴 `scales` 用匿名对象，勿对其做 `(IDictionary)scales` 强转（匿名类型非字典，会抛运行时异常）；多轴直接构造含 `y1` 的 scales 即可。

**验证**：RCL / API / Web / MAUI(windows) 四项目 build 0 error（RCL 含 IL2026 裁剪警告，为 ApiClient 既有，非新增）。清单 `Frontend_DevChecklist.md` S3 标记 ✅。
**待办**：所有改动仍未 `git commit`；后端单测 308/308 未在本沙箱重跑（新增控制器为只读、未改既有路径）。

---

## S4 权限与治理（三端 build 0 error，未提交）

S4 全部完成，RCL / Web / MAUI(windows) 三端 0 error。本次**刻意不改动后端**（见约束）。

**S4-1 权限码对齐**：新增 `Components/Constants/PermissionCodes.cs`（镜像后端 `SuperBuilder_AI.Models.Identity.IdentityPermissions` 真实码；RCL `_Imports.razor` 加 `@using ...Components.Constants`）。`NavMenuItems.Permission` 由占位码（`tenant.read` 等）改为真实后端码；`.cs` 文件需显式 `using ...Constants`（不继承 `_Imports`）。
- 映射：tenants→`platform:tenant:view`、identity→`identity:manage`、audit→`audit:view`、themes→`theme:view`；**quota/system/localization 保持仅登录（Permission 留空）**。

**S4-2 强制 + 守卫**：`NavMenuItems.EnforcePermissions=true`。6 个有码/保留管理页用 `<PermissionGuard Require="@(new[]{PermissionCodes.X})">` 包裹（Tenants/Identity/Audit/Themes 用码；Quota/System 用空 `<PermissionGuard>` 即仅登录）；SystemStatus 原有占位 `system.read` 改为真实/去除。`PermissionGuard` 内置 `owned.Count==0 → 放行` 兜底，避免首帧误隐藏。

**S4-3 审计筛选导出**：`Audit.razor` 一次 `GET api/audit/logs?tenantId=&limit=500` 全量加载，`_all` 派生 `_rows`（客户端筛选：action/entityType/actor 子串 + 时间范围 `ParseTs`）；`ToolbarRight` 放筛选输入（`.sb-filter` 宽 11rem，`app.css` 补），`Actions` 放「导出 CSV」（复用 `FileDownloadService.DownloadTextAsync`，BOM+CSV 转义），`Stats` 增「筛选命中」；SbListPage 内置搜索/分页仍生效。

**S4-4 敏感操作二次确认**：`Tenants.razor` 启用/停用 → `AskEnable` 开 `SbConfirm`（Title/Message 含 C# 字符串字面量须**单引号**定界）；`Identity.razor` 角色权限保存(`SavePerms`)与角色分配(`AssignRole`)各接 `SbConfirm`（`OnConfirm` 直指原方法）。SbConfirm 须置于 `SbListPage`/`SbModal` **之外**（RZ9996），新增确认框放在各页模态之后、`</PermissionGuard>` 之前。

### S4 关键约束与踩坑
- **严禁改后端 catalog**：`tests/IdentityServiceTests.cs:47` `Assert.Equal(31, permCount)` 精确断言全局权限数 31。任何往 `IdentityCatalog` 加权限码（如 localization:view）都会破坏 308/308。故 localization 等缺口只能前端记为「仅登录 + 待后端补码」，不碰后端。
- **对齐原则**：后端确有强制鉴权码（ tenants/platform、audit 经 `HasAuditPermission` 确认、identity/themes 目录）用真实码；后端无门禁/无码（quota=`api/quota` 仅租户隔离无 `[Authorize]` 权限、system=`/health`+`/metrics` 未挂 `platform:diagnostics:view`、localization 无码）保持仅登录——**绝不比后端更严格地隐藏可用页面**（即守卫注释避免的「过度隐藏」）。
- Razor 属性内 C# 表达式含字符串字面量（如 `Title='@(_x ? "启用" : "停用")'`）必须用**单引号**定界，否则内层 `"` 提前闭合属性（CS1056/CS1026）。
- `PermissionGuard` 是 Razor 组件，`.cs` 文件（NavMenuItems）引用其依赖的 `PermissionCodes` 需显式 `using`。

**验证**：RCL / Web / MAUI(windows) 三端 build 0 error。清单 S4 标记 ✅ 并附权限码映射矩阵。
**待办**：改动仍未 `git commit`；后端单测未重跑（本轮零后端改动，理论上 308/308 不受影响）。

---

## S5 质量与可观测（三端 build 0 error + 单测 426/426 通过，未提交）

S5 全部完成。本轮零后端改动（仅前端 RCL + 测试项目）。

**S5-1 错误边界**：`MainLayout.razor` 内容区用 `<ErrorBoundary @ref="_errBoundary">` 包裹 `@Body`；`ErrorContent Context="ex"` 渲染隔离卡片（`.error-boundary-card`，role=alert，异常 Message 可展开 + 「重试」`Recover()`/`_errBoundary.Recover()` + 「返回工作台」）。`main` 加 `id="main-content" tabindex="-1"` 供 skip-link 锚定。

**S5-2 无障碍**：`app.css` 加 `:focus-visible` 焦点环（链接/按钮/输入/图标按钮/菜单项）；`MainLayout` 加 `.skip-link`（默认 `left:-9999px`，`:focus` 显现）；`NavMenu` 链接补 `aria-label="@item.Title"`、图标加 `aria-hidden="true"`。

**S5-3 响应式**：既有断点 991（抽屉）/700/560；在 `@media (max-width:560px)` 内增堆叠（`.page-head` 纵向、`.ph-actions .btn/.sb-filter` 占满、`.stat-grid` 单列、`.toolbar` 筛选占满）。`.table-wrap` 既有 `overflow:auto`。

**S5-4 MAUI 双端回归**：RCL 三 TFM（net10.0/android/ios）0 error；MAUI Win 0 error。`MauiProgram.cs` `AddHttpClient` 基址增加 `#if ANDROID` → `http://10.0.2.2:5032`，否则 `https://localhost:5032`（修正 Android 模拟器回环）。iOS 完整 `.app` 需配对 Mac（平台限制，仅文档化）；Android 完整 apk 已在 P11.4 验证。

**S5-5 组件单测**：测试项目 `SuperBuilder_AI.Tests.csproj` 加 `bunit` 2.9.0 + 引用 `SuperBuilder_AI.Components.csproj`。新增 `ToastServiceTests`(6) / `SbDataTableTests`(4) / `AuthGuardTests`(3) 共 13 例（全部通过）。注意 bUnit 2.9 API 变更：`RenderComponent`→`Render`（返回 `IRenderedComponent<T>`，已过时旧 API），基类 `TestContext`→`BunitContext`（旧基类报 CS0618 过时，仍可用），服务注册用 `Services.AddSingleton`（需 `using Microsoft.Extensions.DependencyInjection;`）。`AuthGuard` 测试需注入 `AppState`（无参，可 new + `MarkSessionRestored()` 控登录态）+ 假 `NavigationManager`（`NavigateToCore` 空实现，仅满足 `@inject` 不触发导航）。

**S5-6 性能**：`ChartView` 已复用 canvas 实例（`renderChart` 先 `destroy` 旧 `_sbChart` 再 `new`，`_lastSig` 跳过重渲，无泄漏）——S5-6 图表复用天然满足。`SbDataTable` 增 `MaxHeight` 参数（如 "420px"）→ 加 `.table-scroll` 类 + 内联 `max-height`，`.table-wrap.table-scroll thead th` 粘性表头（`position:sticky;top:0`），约束大表高度防长页卡顿（轻量虚拟滚动替代，非逐行虚拟化）。

**验证**：RCL(net10.0/android/ios) + Web + MAUI(win) 五端 build 0 error；`dotnet test` 全量 **426 通过 / 0 失败**（后端单测不回归）。清单 S5 标记 ✅。
**待办**：所有改动（S0~S5 累积）仍未 `git commit`。
