# 2026-09-12 工作日志

## M9-15 开发库迁移升级闭环（用户指令：先应用 M9-15 开发库 5 个 Pending 迁移）

- **实际状态与文档登记不符**：文档 §7 登记的 5 个 pending
  （M1_ClosureIntegrity / M7_01_DashboardVersion / M7_02_AppVersion / M7_03_AgentRuntime /
  M7_11_PublishIdempotency）**已在此前被应用**；真实 pending 仅 1 个 =
  `20260911143327_M12_17_IdentityOrganizationUnits`（M12-17 建的 6 张组织目录表）。
  → 教训：**pending 必须实测，不能照抄历史文档**。
- 执行（先审后执）：
  1. `dotnet-ef migrations list --no-connect` 刷新 `scripts/schema/schema-version.json`
     42 → **44**（补 `20260910053000_M7_02_Fix_AppPlanPublishColumns`、`…M12_17_…`），
     schemaVersion 同步；
  2. `migrations script <from> <to> --idempotent` 生成脚本并扫描确认**纯 additive**
     （CREATE TABLE×6 + 索引 + 外键，无 DROP/DELETE/TRUNCATE）；
  3. `dotnet-ef database update` → `Done.`，`__EFMigrationsHistory` 写入 ProductVersion 10.0.11；
  4. 复核：sqlcmd 取 `__EFMigrationsHistory` 与程序集做集合差 → **44/44，pending 0，drift 0**。
- ⚠️ **新踩坑（重要）**：统计迁移文件时用 `grep -v Snapshot` 会把
  `20260908051650_M7_11_AskQuerySnapshot` **误过滤**（文件名含 Snapshot），
  一度误判为「库有程序集无」的漂移。正确过滤：
  `grep -vE '\.Designer\.cs$|SuperBIContextModelSnapshot\.cs$'`。
- ⚠️ **沙箱限制**：`scripts/schema/verify-schema.ps1` 在本沙箱的宿主脚本会话内无法自证 ——
  该会话**解析不到 `dotnet` / `sqlcmd`**（PATH 含目录但 `Get-Command` 失败；
  用全路径函数垫片后子进程仍返回 0 行输出）→ 脚本 EXIT=2（解析出 0 个迁移）。
  判定为**环境限制非脚本缺陷**；复核改用 bash 直连 sqlcmd + comm 集合差（更底层、更权威）。
  另需 `Set-ExecutionPolicy -Scope Process Bypass` 才能调用脚本（默认禁止运行）。
- 脚本健壮性顺带修复（无语义变更）：`$_ -match '^[0-9]{14}_'` 原在 `Trim()` **之前**执行，
  对带前导空白的宿主输出不鲁棒 → 改为先 `Trim()` 再匹配（online/offline 两处）。
- 文档：`docs/ops/migration-seed-schemaversion.md` §1/§3/§6 基线 42→44，§7 重写为
  「7.1 已过期的登记 / 7.2 实际执行 / 7.3 沙箱限制如实记录」。

## M12 增量 3-2：指标/维度编辑（计算口径 Expression + 数据类型 DataType）全链路落地

- **范围（8 层全链路）**：域模型 → EF 配置 → 仓储投影 → Service 写方法 → 请求 DTO →
  Controller PUT → 前端编辑弹窗 → i18n 四地 → 单测 → 迁移 → 提交。
- **新增列**：`BusinessEntityMetrics` / `BusinessEntityDimensions` 各加
  `Expression nvarchar(1000)` + `DataType nvarchar(50)`（均可空，纯 additive）。
- **合并语义（Upsert）**：按 `Name`（忽略大小写）全量替换 —— 命中更新标量并**保留指标物理绑定**；
  未命中新增；列表外存量删除（级联清物理绑定）。租户隔离双重：Controller `TenantDataPlanePolicy`
  + Service 父级（实体/业务域）归属校验 → `KeyNotFoundException` → 404。
- **端点**：`PUT api/business-model/entities/{id}/metrics`、
  `PUT api/business-model/domains/{domainId}/dimensions`（200/204/400/403/404）。
- **迁移**：`20260911231209_M12_18_MetricDimensionExpression`（4 个 AddColumn，无意外漂移）；
  开发库 43→44 已应用，**pending 0**；`scripts/schema/schema-version.json` 44→**45**，
  程序集↔清单集合差 **0**。
- **验收**：全量单测 **1140/1140 绿**（含 i18n 四地护栏 `MetricCenterUiTests` 等）；
  Api/Components/Web 编译 0 error（MAUI 因沙箱 `path1` 资产校验限制未纳入，本任务未改 MAUI）。
- ⚠️ **新增 3 条测试踩坑（都踩过）**：
  1. **`Tenant.TenantCode` 默认 `= string.Empty` 且 `IsRequired + IsUnique`** → 测试里
     `new Tenant{Id=tid}` 播种**两个**租户必撞 `UNIQUE constraint failed: Tenants.TenantCode`
     （不是 NULL 重复，是空串重复）。播种须给唯一 `TenantCode`（如 `"t"+tid`）。
  2. **bunit 渲染注入了新服务的页面必须补注册**：给 `MetricCenter.razor` 加 `@inject ToastService`
     后，`MetricCenterUiTests` 4 个用例全挂于
     `Cannot provide a value for property 'Toast'` → `Services.AddSingleton(new ToastService())`。
  3. **跨租户 403 与 404 的边界**：`TenantDataPlanePolicy.Resolve` 先于 Service → 若测试
     传 `tenantId` ≠ 认证租户，会在数据面直接 403（`ObjectResult`），**永远到不了 Service 的 404**。
     要测「实体属他租户→404」必须 **认证租户=调用方**、目标实体属另一租户。
  4. 次要：给行内操作加按钮会改变 `FindAll(...).First()` 的顺序假设 —— 把「详情」放回首位
     （主操作优先）即修复既有导航回归测试。
