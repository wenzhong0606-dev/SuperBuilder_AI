# 2026-09-07 工作日志

## M7-07 交付：ModelAccounts 加密绑定与模型目录（占位清零批次次件）
- 后端：ModelAccount 实体 + DbSet + 受控迁移 `P7_3_ModelAccounts`（唯一索引 `IX_ModelAccounts_TenantId_Provider_ModelId` + 租户查询过滤器）；`ISecretStore`(`AesGcmSecretStore`, AES-256-GCM, 密文 `v1:base64(nonce|cipher|tag)`) + `Program.cs` 延迟到解析期 fail-fast（不阻断 `dotnet ef`）；静态 `ModelCatalogProvider`(5 项) + `GET /api/model-catalog`；`ModelAccountService`(加密落库/掩码/首绑默认/设默认唯一性/服务端 `ResolvePlaintextKeyAsync` 供 LLM 注入) + `ModelAccountsController`(POST/GET/GET{id}/PUT/DELETE + set-default，租户隔离，掩码返回，明文绝不下发)；错误码补 `Conflict`(409)。
- 前端：`ModelAccounts.razor` 重写（目录驱动 + 真实 CRUD + 设默认 + 删除确认 + 失败显式报错）；本地化 4 向新增 `ModelAccountsDeleted/DeleteConfirm/BindFailed` 并修订 `ModelAccountsBindSubmitted` 为「绑定已保存。」。
- 红线清理：`appsettings.Local.json` 明文 `Qwen:ApiKey`/`Embedding:ApiKey` → 环境变量占位符（与已提交 `appsettings.json` 一致）+ 新增 `SecretStore:MasterKey`(32 字节 base64，文件已被 `.gitignore:14` 忽略)；连接串本地 DB 密码未动。
- 测试：`ModelAccountServiceTests`(8) / `ModelAccountsControllerTests`(8) / `ModelCatalogTests`(3)。全量 **942 零回归**（基线 923 → +19，Golden 18/18 不变）；API / RCL(net10.0/android/ios) build **0 error**。
- 提交（双提交，不推送 origin）：feat `0e781ce`、docs `0c3a813`；排除 phantom `nul`。
- 关键修复：`ModelAccountsController.ScopeTo`（跨租户抛 `SuperBuilderException`）原在 `try` 外调用，导致跨租户异常未转 403；已将所有端点 `ScopeTo` 移入 `try`，跨租户一律返回 403（同时修正对应测试由「抛异常」改断言 403）。
- 约束：租户隔离沿用 `ThemeController` 范式（`ResolvePlatformScope` + `StorePlatformScope` + `ApplyTenantScope`）；ModelAccount 不注册为数据面路径（`IsDataPlanePath` 不补）。

## M8-01 计划块：统一视觉系统（仅计划，未实施）
- 勘察 `SuperBuilder_AI.Components/wwwroot/css/app.css` 真实现状：`:root`/`[data-theme="dark"]` 已有完整 `--sb-*` 古风令牌 + 全套 `--bs-*` 覆盖；`.btn-primary` 已全态 token 化（L163-177）。核心缺口：① `.badge-primary` 双定义（L178 color-mix 自适应 vs L732 硬编码 `rgba(47,79,111,.12)` 胜出 → 深色错位）；② `.badge-*` 背景 L732-737 硬编码 5 浅色 RGBA → 深色漂移；③ `btn-secondary/light/dark` 的 `--bs-secondary` 未覆盖 → 默认灰蓝渗透；④ 原生 `select`/`checkbox`/`radio`/`range`/`date` 未 `appearance` 重置；⑤ `--sb-ring` 未用于 `:focus-visible`。`#0d6efd` 仅存 `bootstrap.min.css`，应用层无硬编码默认蓝。
- 已写入 `docs/Master_Development_Plan.md` §12 M8-01：目标 + 现状盘点 + 5 阶段实施（阶段0 令牌契约固化 / 阶段1 按钮全态 / 阶段2 徽标令牌化+深色对齐 / 阶段3 原生控件令牌化 / 阶段4 焦点·深色收口与默认蓝扫描）+ 可量化验收 + 测试门禁 + 红线 + 范围外。
- 节奏：本回合仅产出计划块（docs），未写代码、未实施，待用户确认切入点（阶段0 或 直接阶段1 安全首刀）后再落地，届时遵循双提交（feat+docs）、不推送 origin、排除 `nul`。

## 状态：已暂停，等待用户通知（2026-09-07 午后）
- 用户决定自行用 ChatGPT 推进 M7 剩余项（M7-08~11），我方暂停执行、不碰代码。
- 恢复触发：用户通知 ChatGPT 的 M7 工作完成。届时动作：① `git pull`/拉最新 → ② 分析实际进度（哪些 M7 项落地、是否回归/冲突/红线问题、是否引入默认蓝或破坏 `--sb-*` 令牌体系）→ ③ 更新 Master 计划（§11 M7 进度标记、§12 M8 联动）→ ④ 续推 M8-01 或补齐缺口；仍遵循双提交、不推送 origin、排除 `nul`。
- 关键风险点（恢复时重点核查）：M7-09~11 样式若由 ChatGPT 产出，可能硬编码颜色/引入 Bootstrap 默认蓝，与 M8-01 目标冲突；需核 `app.css` 与组件 `.razor` 是否仍只用 `--sb-*` 令牌。

## 恢复 + 执行 A：复验 ChatGPT M7-08/09/10 提交（2026-09-07 晚）
- 用户确认：ChatGPT 完成 M7-10；M7-11/12 未进行。实际 git 显示 ChatGPT 提交了 **M7-08/09/10 三项**（b5056a8 / a788254 / c098fa4），不止 M7-10。M7-11、M7-12 确为待办。
- 复验结果（选项 A）：
  - **构建**：`dotnet build SuperBuilder_AI/SuperBuilder_AI.csproj -c Debug` → **28 警告、0 错误**（均为预存 nullable/过时 API 告警，与 ChatGPT 改动无关）。
  - **全量测试**：`dotnet test tests/SuperBuilder_AI.Tests` → **957 通过、0 失败、0 跳过**（含 Golden 18/18；相对 942 基线零回归，三个提交净增 15 测试全绿）。
  - **红线巡检**：`git ls-files | grep nul` → 未跟踪（OK）；应用层 `#0d6efd`/默认蓝 → 无（仅 `bootstrap.min.css` 内）；`app.css` 三个提交**均未触碰** → 令牌体系结构完整；`Quota.razor` 无硬编码颜色；`ComponentGallery.razor` 用 `btn btn-primary`(已 token 化)/`btn-outline-secondary`(依赖未覆盖的 `--bs-secondary` 灰，属已知 M8-01 债务，非默认蓝)。
- 计划漂移修正（docs-only）：L653「M7-03~M7-11 待办」→ 改为「M7-11、M7-12 待办（M7-03~M7-10 已交付），蓝图编辑器已于 M7-09 接入」。L642-646 表已含 M7-12 行（先前说缺 M7-12 是误判，更正）。
- 结论：ChatGPT 的 M7-08/09/10 零回归、未破坏令牌体系/红线，可放心继续。下一步待用户定：B(推进 M8-01) 或 C(收 M7-11 或 M7-12)。
- 提交：docs `0eacadd`(M8-01 计划块) 此前已提交；本次 L653 修正待提交（docs-only，不推送 origin、排除 `nul`）。

## M8-01 交付：统一视觉系统（纯前端 app.css，五阶段全落地）
- 用户「Please continue.」→ 按推荐路径落地 M8-01（计划块 L774-812 已含分阶段范围与验收）。
- 阶段0 令牌契约：`:root`/`[data-theme="dark"]` 新增 `--sb-btn-hover/-active` + 全套 `--sb-badge-*`(bg/fg/bd × primary/success/warning/danger/info/secondary/light/dark)。
- 阶段1 按钮：`.btn-primary` 改引用 `--sb-btn-*`；新增 `.btn-secondary/.btn-light/.btn-dark` 映射品牌中性色（消除默认灰蓝）；其余 btn 变体沿用既有 `--bs-*` 覆盖。
- 阶段2 徽标：删除重复 `.badge-primary`(早期 color-mix 与硬编码 RGBA 双定义)；L732-737 硬编码 RGBA → `color-mix(var(--sb-*))` 浅深自适应；`badge-warning/info` fg 深色模式切换品牌亮色；补 secondary/light/dark。
- 阶段3 原生控件：`select`(appearance:none+SVG 箭头)/`checkbox`/`radio`/`range` 令牌化，焦点复用 `--sb-ring`；**日期输入保留原生选择器**（刻意偏差，维持日历拾取器可用）。
- 阶段4 焦点/深色收口：`:focus-visible` 改用 `--sb-ring`；深色 `--sb-*`/`--bs-*` 成对复核；`git grep` 确认应用层无 `#0d6efd`/默认蓝/灰蓝（仅 bootstrap.min.css 内含，不改动）。
- 红线：深色仅切 `data-theme` 不落库；仅样式无行为语义；令牌集中 `:root`/`[data-theme="dark"]`、无 `.razor` 内联硬编码色。
- 验收：RCL `dotnet build` 0 error（130 预存 IL2xxx 裁剪告警，与本次无关）；`.badge-primary` 全局仅一处；零回归（Golden 18/18 不变、无后端改动/测试新增）。
- 提交（双提交 + docs，不推送 origin、排除 nul）：feat = app.css；docs = Master 计划 M8-01「计划→交付」标记 + 交付要点 + 本记忆。

## M8-02 交付：列表、表格、菜单与响应式（纯前端 app.css，增量收口）
- 用户「Please continue.」→ 按里程碑序列推进 M8-02（M8-01 已完成）。
- 勘察结论：多数原则已满足——`.data-table` 100%宽+sticky(L630)、`.table-wrap` 横向滚动(L623)、`.nav` 单列+`overflow-y:auto` 超高滚动(L370)、`.ds-overview`/`.connector-picker` 网格 900/560px 变单列(L682-683)；状态组件 `.sb-loading`/`.sb-spinner`(L1010)、`.empty-state`(L844)、`.sb-alert`(L1025) 已存在。**真实缺口三处**（纯增量、低风险）。
- 实施：① 补 `.sb-state`(五态容器)+`.sb-state.success`(品牌绿)+`.sb-retry`(重试按钮复用 `.sb-spinner`、hover/active/disabled) → 加载/空/错误/成功/重试 五态一致；② 新增 `.sb-grid`(`repeat(auto-fit, minmax(260px,1fr))`) 标准化响应式网格工具类；③ `.content` 加 `overflow-x: clip` 防页面级横向溢出（保留 `overflow-y:auto`）。
- 红线：深色仅切 `data-theme` 不落库；仅样式无行为语义；令牌集中 `:root`/`[data-theme="dark"]`、无 `.razor` 内联硬编码色；`git grep` 应用层无 `#0d6efd`/默认蓝。
- 验收：RCL `dotnet build` 0 error（130 预存 IL2xxx 裁剪告警）；零回归（无后端改动/测试新增）。
- 提交（双提交 + docs，不推送 origin、排除 nul）：feat = app.css；docs = Master 计划 M8-02「交付」标记 + 交付要点 + 本记忆。

## M8-03 交付：导出标准化（真实功能修复 + 四向资源键对齐）
- 用户「Please continue.」→ 按里程碑序列推进 M8-03（M8-01/02 已完成）。
- 真实缺口（非纯样式）：`FileDownloadService.DownloadTextAsync` 原 `void` + `catch{}` 静默吞异常 → 违反红线「禁止假成功」；Ask/Audit 导出失败无 Toast；`AskTurnExportExcel` 文案未声明兼容格式。
- 实施：① 服务改 `async Task<bool>`（成功 true / JS 异常 false，不向上抛）；② `Ask.razor` 注入 `ToastService`、空数据 `Toast.Warning(ExportNoData)`、失败 `Toast.Error(ExportFailed)`；③ `Audit.razor` `ExportCsv` 捕获 `ok` 失败 `Toast.Error`+return；④ `AskTurnCard.razor` 标签改「导出 Excel 兼容」；⑤ RCL `Keys.cs` 新增 `ExportFailed`/`ExportNoData` 并改 `AskTurnExportExcel` 默认值，同步后端 `ResourceKeys.cs`(常量+Catalog DefaultValue) 与 `LocalizationSeedService.cs`(`ZhCnDefaults`)，满足 M3-05 护栏 `ResourceKeyRegistryTests` 四向一致；⑥ 新增 `FileDownloadServiceTests`(手写 OkJsRuntime/ThrowJsRuntime fake，不依赖 Moq)。
- 踩坑：首次新测试编译 `error CS0738`(IJSRuntime fake 缺 TimeSpan 重载)→ 实现 3 重载 + `using System;`；随后 `ResourceKeyRegistryTests` 2 失败（RCL 镜像键集/默认值与后端不一致，因新增键未进后端 + `AskTurnExportExcel` 默认值改动）→ 后端四向登记后复绿。
- 验收：RCL + 后端 build 0 error；全量 **959/959 零回归**（净增 2 行为测试，相对 957 基线）；`ResourceKeyRegistryTests` 四向一致全绿。
- 提交（双提交 + docs，不推送 origin、排除 nul）：feat = FileDownloadService.cs / Keys.cs / Ask.razor / AskTurnCard.razor / Audit.razor / FileDownloadServiceTests.cs / ResourceKeys.cs / LocalizationSeedService.cs；docs = Master 计划 M8-03「计划→交付」标记 + 交付要点 + 本记忆。

## M8-04 交付：性能与图表真实性（Chart.js 增量/卸载 + 大表分页 + 下钻误标修正）
- 用户「Please continue.」→ 按里程碑序列推进 M8-04（M8-01/02/03 已完成）。
- 真实缺口（非样式）：① `ChartView.razor` 无 `IAsyncDisposable` → 组件卸载时 Chart.js 实例未 destroy（内存泄漏）；`renderChart` 每次数据变化 destroy+recreate（非增量 update）。② `AskTurnCard` 主结果表 + `ResultTable`(DashboardDetail) 一次渲染全部行；后端 `QueryExecutionService` 无行数封顶 → 10k DOM 风险真实。③ `AskTurnCard`「下钻」实为客户端按分类切片过滤（非后端下一层真实数据）→ 应标「筛选」。
- 实施：① `chart.js` `renderChart` 同类型走增量 `chart.data/options=...; chart.update()`，类型变化/首次才 destroy+new；`catch` 补 destroy 兜底。② `ChartView` 实现 `IAsyncDisposable`→`DisposeAsync` 调 `destroyChart`，`OnAfterRenderAsync` 加 `_disposed` 守卫。③ `AskTurnCard` 结果表客户端分页（PageSize=100 + Skip/Take，复用 `SbPagination`，切下钻重置页码）；「下钻：」→「筛选：」、「清除下钻」→「清除筛选」。④ `ResultTable` 加客户端分页（PageSize 参数默认 100 + 越界夹紧）。⑤ `AskTurnDrill`/`AskTurnClearDrill` 默认值改「筛选：/清除筛选」(zh-CN) 与「Filter:/Clear filter」(en-US)，三向同步（RCL Keys.cs + `ResourceKeys` Catalog + `LocalizationSeedService.ZhCnDefaults`），键名保留。
- 注意：`RateLimitMiddlewareTests.Expired_Windows_Are_Evicted_To_Prevent_Unbounded_Growth` 并行全量套件下偶发失败（1/959），隔离复跑 3/3 通过 → 预存时序偶发、与 M8-04 无关，未改动（避免掩真）。后端未聚合查询仍可能返回大结果集，服务端分页属 S6 后续议题。
- 验收：RCL + 后端 build 0 error；全量 959/959（干净跑）；`ResourceKeyRegistryTests` 四向一致全绿。
- 提交（双提交 + docs，不推送 origin、排除 nul）：feat = chart.js / ChartView.razor / AskTurnCard.razor / ResultTable.razor / Keys.cs / ResourceKeys.cs / LocalizationSeedService.cs；docs = Master 计划 M8-04「交付」标记 + 交付要点 + 本记忆。
