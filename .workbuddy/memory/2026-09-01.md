# 2026-09-01 工作日志

## 源码级核验外部《全量源码审计最终结论》

用户提供了一份 60+ 节的 SuperBuilder 全量源码审计报告，要求核验其准确性。逐项对照 `master` 源码验证（`SuperBuilder_AI/src/**` + `SuperBuilder_AI.Components/**`）。

### 核验结论：审计整体准确，架构判断与源码高度吻合，但有 2 处需修正

**核实为准确的断言（证据固化）**
- QueryPlan 为核心资产：`Domain/BiQuery/QueryPlan.cs` 字段与审计所列完全一致
- 多表 JOIN 真实落地：`QueryJoinInferenceService` / `JoinBuilder` / `QueryJoinCandidate` / `QueryPlanBuilder.BuildJoinsAsync` 均在 → 旧"仅单表"文档确已过时
- Validation / Semantic / Repair / Confidence / DecisionGate / Explainability 全部有实现
- SQL 三方言：`SqlServerDialect` / `MySqlDialect` / `PostgreSqlDialect` + `ISqlDialectResolver`
- Golden/Evaluation 体系齐全（`Application/Golden/` 下 20+ 组件）
- **P0-02** 签名密钥回退：`TokenService.cs:54` `signingKey ?? "dev-insecure-signing-key-P11-change-in-prod"`，`Program.cs:207-209` 传入
- **P0-01** 登录无口令：`AuthController.cs:24` 注释"本阶段登录不校验口令"；前端仅发 `{username, tenantId}`
- **P0-06** CORS：`Program.cs:215-218` 缺 `Cors:AllowedOrigins` 即 `AllowAnyOrigin()`
- **P0-04** DataSourceId 全链路丢弃（最实锤）：`AskController.cs:72/83` 仅用作缓存键；`:81` `_bi.AskAsync(question, tenantId)` 不传 dataSourceId；`BIConversationService.cs:240-244` 用 `plan.DataSourceId`（管道推断）执行
- `/metrics` 匿名成立：AuthMiddleware 只对 `/api/*` 强制令牌，`/metrics` 非 /api 路径被"放行"分支穿透

**需要修正的 2 处**
1. **P0-03 机制错误（结论对、机制错）**：审计称前端 `X-Tenant-Id` 透传决定租户、改头即可跨租户查数。实际——
   - Ask 路径**已由令牌驱动、安全**：`AskController.ResolveTenantId()` 读 `User.FindFirst("tid")`（AuthMiddleware.cs:70 写入），`BIConversationService.cs:179` `ApplyTenantScope(tenantId)` 用令牌租户，后端**不读 X-Tenant-Id 定租户**
   - `X-Tenant-Id` 头**仅**被 `ObservabilityMiddleware.cs:104` / `AuditMiddleware.cs:64` 读取做指标/审计打标，篡改只污染标签
   - **真实缺陷在别处**：Dashboard/App/Agent/Identity/Theme/Quota/Audit 七类控制器用 `ScopeTo(request.TenantId)` 直接信任**请求体/查询串的 TenantId**，无"用户是否属于该租户"校验（`ThemeController.cs:49`、`DashboardController.cs:70/99`、`IdentityController.cs:56/73`、`AppBuilderController.cs:62/86`、`AgentController.cs:67/88`、`QuotaController.cs:30/38/47/56`、`AuditController.cs:36`）
   - → 等级维持 P0，但施工入口完全不同，按原审计封堵 X-Tenant-Id 头会**修错地方**
2. **P0-44 已落地/过时**：审计称"缓存需增加 DataSourceId"，实际 `AskController.cs:72/83` 缓存键已是 **租户 + 归一化问题 + DataSourceId**，该子项关闭，降为 P1（仍缺权限上下文 / metadata 版本 / QueryPlan 版本 / 模型版本）

**本次核验新发现的联动风险**
缓存键用前端 `request.DataSourceId`，实际执行用管道推断的 `plan.DataSourceId` → 两者不一致时会把数据源 B 的结果缓存到键 A 下。P0-04 修复后自动消除，建议与 P0-04 联合验收。

### 交付物
- `docs/Audit_P0_Corrected_DefectList.md` — 修正后可下发的 P0 缺陷清单（含修订摘要、三条现状事实澄清、缺陷位置表文件:行号、风险、分步修复方案、验收标准、回归约束、下发顺序建议、核验方法）

### 复用要点
外部审计报告不可直接采信机制描述；核验租户/数据源这类"上下文传递"类断言时，必须跟踪**控制器层该值的实际取值来源**（令牌声明 vs 请求体 vs 请求头），并交叉验证下游消费方是否真的用它做数据定界——审计常在此处把"打了标的头"误判为"定界的头"。

---

## 整改开发清单定稿（第二轮）

用户基于审计结论自拟了一版「缺陷整改开发清单」（SB-P0-01~P0-10、SB-P1-01~P1-15、SB-P2-01~P2-10，共 35 条，含核心改造点/验收标准/五 Sprint 排期），要求比对并重写定稿。

**比对结论**：用户清单**更全面、更可执行**（35 条 vs 我方 10 条；每条带改造点+验收标准；Sprint 依赖顺序正确），确定以用户清单为 master 主清单。我方价值仅在纠正 2 处会导致施工错位的机制错误。

**写入定稿的 6 项修订**
1. SB-P0-02 核心改造点重写：删除"Tenant 由 X-Tenant-Id 决定"的错误前提，改为"请求体/查询串 TenantId + 零成员校验"的真实机制，验收标准补 403（非 404，避免资源枚举）
2. SB-P0-01 验收标准补"缓存写入键与执行上下文同源"，且不许延后到 Sprint 4
3. SB-P1-09 删除"Key 加入 DataSourceId"（已实现），仅保留 PermissionContext / SemanticVersion / MetadataVersion
4. SB-P0-09 严重性上调：`/metrics` 是**完全匿名**（非"普通用户可访问"），验收按"无令牌 401"设计
5. SB-P0-04 补充：`User.PasswordHash` 已建模，无需迁移，可省一轮
6. 新增两条横切约束（用户清单原缺）：
   - **Golden 18/18 硬门禁** + 全量单测 308/308，每项完成即复跑
   - **门控隔离**：默认路径逐字节不变、禁止新增全局中间件分支、不碰 Golden 依赖文件
   - 另注：Golden 运行时走 `evaluation/golden-runtime`（AuthMiddleware 匿名白名单），是刻意隔离，不得被鉴权改造破坏

### 交付物
- `docs/DevChecklist_Final.md` — 定稿主清单（v1.0）：横切约束、P0/P1/P2 三表（含状态列）、五 Sprint 顺序、P3 长期项、附录 A 源码落点表（文件:行号）、附录 B 三条易错提醒、附录 C 修订记录

### 下一步（用户已确认从此处开工）
SB-P0-01 DataSourceId 全链路修复 —— 需给出施工方案：改哪些文件、签名怎么调、如何在不碰 Golden 依赖文件前提下保持门控隔离。

---

## 增强版清单可行性评审（第三轮）

用户在 `docs/DevChecklist_Final.md` 基础上产出增强版（`~/Downloads/SuperBuilder_AI_缺陷整改开发清单_v1.0_定稿.md`），要求评审可行性。

**结论：方向可行、结构比我那版更成熟，但有 3 处须先补（2 处会造成返工/破坏 Golden）。**

### 增强版的 4 处改进（确认优于我方）
1. P0-02 拆为 02（授权决策）/02A（Controller Scope 收口）/02B（Observability+Audit 双租户上下文）/02C（移除前端 X-Tenant-Id 头）。其中 **02B 是我漏的**：合法跨租户切换须同时记录 AuthenticatedTenantId 与 EffectiveTenantId，否则审计错账。
2. P0-01 引入 `RequestedDataSourceId` / `ResolvedDataSourceId` 二分，回答了我上轮留的开放问题 =「显式指定优先 + 未指定时推断」。
3. P1-09 改用稳定 `PermissionFingerprint` + `DataPolicyVersion`，非拼接权限文本。
4. 横切约束承认「安全修复可受控改变默认行为」——必要，否则 P0 无法实施。

### ⚠️ 关键发现 1：Golden 依赖边界（决定 P0-01/06/07 生死）
实测 `GoldenDatasetRunner` 构造依赖：
- ✅ `IQueryPlanBuilder`、`IQueryPlanContextBuilder`、`IQueryPlanValidationPipeline`
- ❌ **不依赖** `IBIConversationService`、`ISqlQueryBuilder`、`IQueryExecutionService`

→ **Golden 在 ValidationPipeline 处终止，从不进入 BIConversationService，也不生成/执行 SQL。**
- 改 Builder / ValidationPipeline → Golden 18 例全在爆炸半径
- 改 `BIConversationService`（管线之后、SqlQueryBuilder 之前）→ **Golden 完全免疫**

**P0-01 硬约束**：不能在管线之后覆盖 `plan.DataSourceId`（元数据检索阶段就必须限定数据源，否则造出「表属 B 但 plan.DataSourceId=A」的非法计划，触发 Validator `TABLE_DATASOURCE_MISMATCH`）。
→ 正解：给 Builder 加**可选** `dataSourceId` 入参（默认 0，既有路径逐字节不变），**只由 BIConversationService 传入**，Golden 不传 → 行为不变。这正是门控隔离手法。
→ P0-06(RLS)/P0-07(Security Stage) 一律放 `BIConversationService` 的「管线后、SQL 前」缝里，天然 Golden 免疫。

### ⚠️ 关键发现 2：路由实测（P0-09 会破坏 Golden + 点名了不存在的控制器）
全量 `[Route(` 扫描结果：
- **`/test/*`（AITestController `[Route("test")]`）→ 完全匿名**，暴露 `understand`/`metadatasearch`/`sql` 三个内部能力，无令牌即可调用——比 `/metrics` 更危险
- 11 个 `/evaluation/*` 控制器在匿名白名单（Golden 依赖）
- `api/metadata-vector`（MetadataVectorController）需令牌、无 admin
- **审计点名的 `QdrantController` 在代码库中不存在**（实际对应 MetadataVectorController）→ 照清单施工会找不到文件

→ **P0-09 若按「诊断接口加 Admin 权限」施工，会波及 `/evaluation/*`，直接打破 Golden 18/18（违反约束 1）**。
→ 正确分三类：`/test/*`+`/metrics` 加鉴权（无令牌 401）；`api/metadata-vector` 补 admin；**`/evaluation/*` 保持匿名但做网络隔离**（内网/localhost + 生产 feature flag），P0-09 明确豁免。

### 其他建议
- 约束 2 新增的「契约变化可调整 Golden 基线」缺收口：须指定评审人 + 在 docs 记录原因与影响用例清单 + 单次仅允许因一条 P0 调整，否则成为 Golden 漂移后门。
- P0-03（SigningKey fail-fast）仅数行却是生产硬阻塞，应从 Sprint 2 前移到 Sprint 1 首位。
- Sprint 2 有 7 项且 P0-06/P0-07 均动查询主链路且互相依赖，建议把 P0-08（CORS，配置级）前移。

---

## v1.0(1) 复核：3 处修订均已正确落地 + 新发现数据模型约束（第四轮）

### ✅ 上一轮 3 处修订全部确认落地
1. P0-09 三级处置已写入：`/test/*`+`/metrics` 加鉴权（无令牌 401）、`api/metadata-vector` 补 Admin、`/evaluation/*` Golden 豁免改网络隔离+Feature Flag；`QdrantController` 已更正为 `MetadataVectorController`
2. 新增「约束 3：Golden 爆炸半径 / 安全落点规则」，并把落点分别写死进 P0-01/06/07 三行
3. Golden 基线治理收口为 reviewer 书面确认 + docs 记录 + 单次仅一条 P0 + diff 可审计

### ✅ 好消息：P0-01 有现成先例，风险低于预期
`IQueryPlanBuilder` 现已是：
- `Task<QueryPlan> BuildAsync(QueryIntent intent)`
- `Task<QueryPlan> BuildAsync(QueryIntent intent, QueryPlanSemanticResolution? resolution)` — 注释「Resolution 为 null 时保持原有行为」

→ **"可选入参 + null 保持旧行为"本就是该接口既有范式**，P0-01 加可选数据源约束照抄即可，Golden 不传 → 行为不变。文档附录 A 该行描述准确。

### 🔴 新发现：P0-02 验收标准 ② 在当前数据模型下不可实现
- `Domain/Identity/User.cs:18` 为**标量** `TenantId`，类注释「平台用户（租户作用域，TenantId 为所属租户）」
- `SuperBIContext.cs:343` 注释明确：**「User：租户作用域，不放行 TenantId == 0（用户恒归属某一租户）」**
- DbSet 全集无 `UserTenant` / 成员表（仅 Tenants/Users/Roles/Permissions/UserRoles/RolePermissions）
- `TenantId == 0` 语义是**平台级共享定义**（内置 Role/Permission/Theme/Dashboard/Quota/AuditLog 放行），**不含 User** → 不存在跨租户的平台管理员

→ **一用户恒属单租户，不存在"合法成员切换 B"**。故：
- P0-02 验收标准 ②「合法成员切换 B → EffectiveTenantId=B」**无法测试**（成功分支是死代码）
- P0-02B 验收标准 ②「合法从 A 切换到 B 时日志同时保留 A 与 B」同样无法达成

**建议改为（P0 范围）**：
- `EffectiveTenantId := AuthenticatedTenantId`（恒等）
- 任何 `RequestedTenantId != AuthenticatedTenantId` → 403，**无成员表、无切换、零迁移**
- P0-02C 移除 `X-Tenant-Id` 头后**无需替代通道**
- 好处：Sprint 1 显著变轻（省成员表+迁移+种子+切换 UI），且完全消除越权
- 「一用户多租户成员」登记为独立 P1/P2 backlog（需新增成员表 + 迁移 + 产品决策），避免能力丢失

### 🟡 其余两项（非阻塞）
- P0-01 未定义"用户指定 A 但 A 内无匹配表"的行为 → 当前会退化为 `TABLES_EMPTY`/`FIELDS_EMPTY` 泛化校验错误。建议明确为「所选数据源下未找到相关表」。安全性无碍（是报错不是错误答案），属体验打磨。
- 上轮建议的 P0-03 前移 / Sprint 2 减负未被采纳，非阻塞，顺手即可。

---

## 平台超级管理员必要性论证（第五轮，用户提问）

### 先纠正我自己上一轮的一个假设
我曾假设「普通用户传 `tenantId=0` 可写平台级定义」——**该假设不成立**。实测各控制器已系统性拦截租户 0 写入：
- `IdentityController.cs:70`（全局用户）、`:195`（全局角色）、`:241`（role.TenantId==0 权限指派）
- `ThemeController.cs:63`（全局主题）、`:249`（复制创建内置主题）、`:156/:178`（IsBuiltIn||TenantId==0 禁删改）
- `AppBuilderController.cs:74`、`:185/:215`；`AgentController.cs:79`、`:184/:213`
- `QuotaController.cs:32/40/49/58`（配额仅租户作用域）
- `AuthController.cs:51`（登录 tenantId 必须 > 0）
→ **租户 0 平台层已受保护，不需要为它单独引入超管。**

### 🔴 新发现（清单中缺失且更严重）：租户创建完全无鉴权
`TenantManagementController`：
- `[HttpPost]`（:47）→ `new Tenant`（:59）→ `_db.Tenants.Add`（:65）
- 全文件**无 `HasPermissionAsync`、无 `ScopeTo`、无任何权限校验**
→ **任何持有合法令牌的用户都能创建租户**。这比七类 `ScopeTo` 跨租户写更严重，因为受影响的是**租户边界本身**。

### 结论：需要「平台治理角色」，不需要「跨租户数据访问者」
**真正需要它的 3 处**
1. 租户创建/启用停用 —— 当前零校验（上述新发现）
2. P0-09 诊断面（`/test/*`、`/metrics`、`api/metadata-vector`）需要一个非租户作用域的身份承载 Admin/Internal 权限，否则只能赋给某个租户用户（租户 A 用户看全平台指标，错误）
3. 未来运行时治理 tenant 0 共享定义（内置主题/角色/模板）

**明确不该做的 3 处**（否则与 P0-06 RLS 冲突且是合规灾难）
- 不得读取任何租户业务数据（不进 BI 数据链路）
- 不得受理 `RequestedTenantId` 切换（超管 ≠ 能变成别人）
- 不得绕过审计（全部操作进 P0-10）

### 最小落地方案（零新增表、零迁移，兼容「用户恒属单租户」）
**方案 A（推荐）平台租户 + 平台权限码**
- Seed 一个平台租户（如 `TenantCode=platform`），平台管理员的 `User.TenantId` 指向它
- 复用现有 `Role`/`Permission` 的 `TenantId==0` 放行机制，定义 `platform:*` 权限码
- 只加种子 + 权限码 + guard，**不改 User 模型、不加成员表、不需要迁移**
- 与 P0-02「单租户恒等」简化完全兼容：平台管理员也只是「属于平台租户的用户」，仅权限码不同
**方案 B** `User` 加 `IsPlatformAdmin` 布尔：最省事但授权语义硬编码进实体，不利于扩展（P0 可接受，不推荐）
**方案 C** `UserTenant` 成员表：过度设计，登记 backlog

### 落到清单的动作
1. **P0-02A 增补**：`TenantManagementController` 租户创建/停用必须加平台权限校验（清单原未覆盖，且严重性高于七类 ScopeTo）
2. **新增 SB-P0-11**：平台超级管理员角色（权限码定义 + 种子 + guard），并作为 P0-09 诊断面 Admin/Internal 的权限承载
3. 约束补充：平台管理员不进入 BI 数据链路、不受理租户切换、全部操作进审计

---

## ⚠️ 重要纠错：platform-admin 全局全权限角色确实存在（第六轮）

### 我第五轮的判断是错的，须永久记住
我断言「不存在跨租户的平台管理员概念」——依据是在 `SuperBIContext.cs` 里 grep `PlatformAdmin|IsPlatform|GlobalAdmin` 无结果。**该检索方式错误**：该角色是**种子角色码字符串**，不在 DbContext 而在 `Domain/Identity/IdentityConstants.cs`。
→ **教训：判断"某概念是否存在"时，不能只 grep 实体属性/字段名，必须同时检索常量/种子目录与角色码字符串。**

### 已验证证据（用户 v1.2 论断全部成立）
1. `IdentityConstants.cs:3` 注释「内置角色码（TenantId=0 全局角色）」；`:6` `PlatformAdmin = "platform-admin"`
2. `IdentityConstants.cs:94-105`：`platform-admin` = 「平台级全权限」，含 Dashboard×5、App×5、Agent×4、Theme×3、**Metadata view/edit/scan**、AuditView、Billing×2、IdentityManage → **全量业务权限，非治理权限**
3. `IdentityService.cs:190-203` `ResolveRoleIdsAsync`/`ResolveRoleIdAsync` 用 `(r.TenantId == tenantId || r.TenantId == 0)` → **全局角色可被租户作用域解析并绑定给租户用户**
4. `P10AcceptanceTests.cs:62/:71-74`：测试直接 `CreateUserAsync(Tenant1, "admin1", ..., new[]{ IdentityRoles.PlatformAdmin })` 并断言「全权限」（IdentityManage/DashboardDelete/BillingManage）→ **该行为已被测试固化**，修正须同步改测试

### 确认的提权链
`tenant-admin` 自带 `identity:manage`（`IdentityConstants.cs:115`）→ 调 Identity 接口分配 `platform-admin` → `ResolveRoleIdAsync` 命中 TenantId=0 全局角色 → 建 `UserRole{TenantId=本租户, RoleId=platform-admin}` → `GetPermissionsAsync` 返回全量业务权限（含 `metadata:scan`、`billing:manage`）。
→ 若 P0-09/P0-11 以「持 platform-admin」作门禁，**自我提权的租户管理员即可通过**。v1.2「必须重定义/废弃既有 platform-admin，不能旁加一套角色后保留旧越权通道」判断正确。

### v1.2 其余修订核验：均正确
- 约束 1 改「308 为记录基线 + master 动态下限 + 零失败」✅（固定数字会过期）
- P0-03/P0-08 由「Production」扩展为「非 Development（含 Staging/未知环境）」✅（`IsDevelopment()` 对 Staging 为 false，原表述会漏）
- P0-04 拆 04A 密码 / 04B Token 生命周期（SecurityStamp/TokenVersion）✅（TokenService 载荷含静态 `perms`、仅 60min 过期，撤权不即时生效属实）
- P0-06 强调策略值参数化、禁拼 SQL ✅；P0-07 改为校验 **RLS 注入后的最终 Plan** ✅（比注入前校验更正确）
- P0-09 改「Production 默认不映射 + 独立内部监听/受控反代 ACL + 禁止伪造 Header 判内网 + 依赖 IP 须配可信代理与 Forwarded Headers」✅（单服务无法按路径绑 localhost，技术上更准确）
- P0-11 明确 Seed **正 Id** 平台租户 ✅（`SuperBIContext.cs:343` User 不放行 TenantId==0，必须正 Id）
- P1-09 的 `PermissionFingerprint` 前移 P0-05、`DataPolicyVersion` 前移 P0-06 ✅（否则撤权/RLS 变更后仍复用高权限缓存）

### 🔴 v1.2 仍存的一处逻辑冲突（需补）
**P0-02「任何 RequestedTenantId ≠ AuthenticatedTenantId → 403」与 P0-02A「治理角色可管理所有租户」互相矛盾。**
治理用户 `AuthenticatedTenantId` = 平台租户 Id，但需对租户 B 执行创建/停用/设置读写，按 P0-02 规则会被自身 403 挡死。
→ 须在规则层显式区分**管理面 / 数据面**：
- 管理面（租户管理、诊断、平台审计/配额）：治理角色可跨租户
- 数据面（BI 查询、元数据业务读取、DataSource）：恒等，一律不得跨租户
建议 P0-02 与 P0-02A 各加一句例外说明。

### 小问题
P0 表内第 66-68 行残留 HTML 注释的 v1.1 旧行（`<!-- v1.1 legacy row retained...`），夹在 P0-08 与 P0-09 之间。附录 C 已记录修订，建议删除该注释块保持定稿整洁。

---

## v1.3 复核：管理面/数据面分离已落地，清单定稿（第七轮）

用户按建议修订后复核：

### ✅ 管理面/数据面分离落地质量优于建议
不只加了"例外"，而是引入了独立的 `ManagementTargetTenantId` 概念，并显式禁止其污染业务上下文：
- 约束 4（:42）：管理面目标租户记为 `ManagementTargetTenantId`，由 `platform:*` 授权；**不是** EffectiveTenant 切换，不得写入 BI 数据上下文
- P0-02（:56）：标题改为「业务数据面单租户恒等」，规则限定于数据面
- P0-02A（:57）：管理面例外写明「不得改写 EffectiveTenantId、不得开启目标租户 EF 业务数据 Scope、不得复用 BI 数据面切换通道」；验收②治理角色管理 B 时其 Authenticated/EffectiveTenantId **仍为平台租户**
- P0-02B（:58）：管理面另记 `ManagementTargetTenantId`/`ManagementAction`/`ManagementActionAuthorized`，禁止把管理目标伪装成 EffectiveTenant
- 附录 B 新增第 13 条；附录 C 增 v1.2 修订行

### 🔧 我代为收尾的一处
P0 表格内 v1.1 遗留 HTML 注释块（`<!-- v1.1 legacy row retained...`）**用户未删除**，我已移除，表格现为 P0-08→09→10→11 连续。

### 清单状态
`docs/DevChecklist_Final.md` 已至可开工状态：11 项 P0 + 16 项 P1 + 10 项 P2。
Sprint 1 顺序：`P0-03 → P0-08 → P0-09 → P0-11 → P0-02 → 02A → 02B → 02C → P0-01 → P0-05`。

### 下一步待用户选择
出施工方案，三选一：P0-03/P0-08（配置型收口，最轻）、P0-11（platform-admin 重构，最硬）、P0-01（DataSourceId 全链路）。
