# 2026-08-31 工作日志

## P11 前端方案：核实并闭合 §11 待确认项（✅ 完成 · 仅方案，未落代码）

- **已提交**：`5ee9b2a P11 前端方案：MAUI Blazor + Web 双端 + 优化轨道 + 用户自定义能力 + 平台扩展能力`（`docs/P11_Frontend_MAUI_Blazor_Plan.md` + 记忆同步）。此前方案文档与记忆更新一直未提交，本次补齐。

### 核实结论（重要现状事实，后续开发须据此调整）

1. **核心 NL 问数端点确实缺失（旗舰页硬前置）**
   - `BIConversationService.AskAsync` 存在且已 DI 注册（`Program.cs` 162–163），但**仅被 `QueryPlanWidgetDataResolver` 消费**（仪表盘取数）。
   - 全量 26 个控制器（11 生产 + 15 诊断）中**无任何 NL 问数端点**（诊断类仅有 `query-plan-*` 评估端点）。
   - → P11.2 前必须新增 `api/ask`（包装 `IBIConversationService.AskAsync`）。

2. **P10.2「全局守卫」不存在 —— api/* 当前零鉴权保护（安全硬缺口）**
   - 全库 `grep -rln "Guard"` **零命中**；`Program.cs` 仅注册 `ObservabilityMiddleware` + `AuditMiddleware`，**无鉴权中间件**。
   - `IdentityController` 只做权限 CRUD，无逐请求校验。
   - → P11 安全轨道从「补接线」升级为「**新建鉴权中间件**」（Token + 租户上下文 + 复用 `IIdentityService.HasPermissionAsync`） + CORS + 限流。列为 **P11.0 前置、最高优先级**。

3. **用户自定义组件库后端面缺失**
   - `ComponentType` 仅存在于 `Domain/AppBuilder/AppDsl.cs` 的 `AppComponentTypes`（P8 白名单 `chart/table/kpi/text/filter/form`）。
   - **无 `Component` 实体、无自定义组件持久化** → P11.3 须新建领域模型 + 持久化 + `api/components`。

4. **P8 发布契约：部分支持**
   - **主题引用已支持** ✅：`AppDsl.ThemeKey`（`AppDsl.cs:82`）+ `AppPlan.ThemeKey`（`AppPlan.cs:74`）→ 发布应用套用自定义主题无需改 P8。
   - **自定义组件引用不支持** ⚠️：受 `AppComponentTypes.Supported` 硬编码白名单约束（校验点 `AppDslSerializer.cs:160`）→ 需扩白名单或引入 `CustomComponentRef`。

5. **默认路由 `Home/Index` 无法渲染**：`Api/Diagnostics/HomeController.cs` 存在且 `Index()` 返回 `View()`，但项目**无 `Views/` 视图文件** → 前端接入后应移除或改指向前端入口。

### 方案调整
- 新增阶段 **P11.0 后端前置**：① `api/ask` 公开问数端点；② 鉴权中间件 + CORS + 限流。闸门：build 0 error + 单测绿 + Golden 18/18 不变（均新增文件、不碰 Golden 依赖）。
- §9.3 安全轨道改写为「需新建」而非「补接线」。
- §11 风险项 1/2/6 标记「已核实 ✅ 确认缺失」，风险项 8 标记「已核实 ✅ 部分支持」。

### 任务状态清理
- `#97 P10.5 安全/审计基线验收测试` → completed（交付物 `P10AcceptanceTests.cs` 已随 `69b6c2b` 提交，此前状态陈旧）。
- `#78 / #79`（P7.3 / P7.4）→ completed（已被 `#80 / #81` 取代完成）。
- `#49 A3` / `#53 A5` 仍 pending —— 用户指令暂缓。

### 下一步建议
P11.0 后端前置应先于前端脚手架落地（旗舰 Ask BI 依赖 `api/ask`，安全项优先级最高）。待用户确认后动工。

## P11.0 后端前置：已落代码（BUILD/TEST/GOLDEN/COMMIT 待执行）

用户确认「继续」后动工。已按 P11.0 范围完整实现（全为新增文件 + Program.cs 编辑，**未碰 Golden 依赖文件**）：

- **新增 `src/Application/Auth/TokenService.cs`**：HMAC-SHA256 手动 JWT 风格无状态令牌（零 NuGet 依赖）；`ITokenService.Issue/Validate`；`TokenPrincipal(UserId,TenantId,Username,Permissions)`。
- **新增 `src/Api/Middleware/AuthMiddleware.cs`**：Bearer/X-Api-Token 解析 → 校验 → 设 `HttpContext.User` + `Items["TenantId"]`；匿名白名单 `/evaluation`、`/health`、`/api/auth/login`、`/`、静态资源（保证 Golden 不受影响）；其余 `/api/*` 无令牌 → 401。
- **新增 `src/Api/Middleware/RateLimitMiddleware.cs`**：`/api/*` 按 IP/令牌固定窗口限流（120/分），异常静默。
- **新增 `src/Api/Controllers/AuthController.cs`**（`api/auth`）：`POST /login {username,tenantId}` → 查活跃用户 → `GetPermissionsAsync` → 签发令牌；`GET /me` 返回当前主体。注：本阶段登录不校验口令（PasswordHash 已建模，认证在 P13 BYO 补齐）。
- **新增 `src/Api/Controllers/AskController.cs`**（`api/ask`）：`POST {question,dataSourceId?}` → 需有效令牌 + `DashboardView` 权限（复用 `HasPermissionAsync`，deny-by-default 与 P10 一致）→ `IBIConversationService.AskAsync(question, tenantId)` 返回 `BIResponse`。租户隔离由令牌 `tid` 声明驱动。
- **编辑 `src/Api/Program.cs`**：注册 `ITokenService` 单例（密钥 `Auth:SigningKey` 配置，缺失用 dev 默认）+ CORS 策略 `P11Cors`；中间件顺序 `UseRouting → UseCors → UseMiddleware<RateLimitMiddleware> → UseMiddleware<AuthMiddleware> → UseAuthorization → Observability → Audit`；`MapGet("/health")`。
- **新增测试（确定性、无 LLM/DB）**：`TokenServiceTests`(5)、`AuthMiddlewareTests`(5 匿名白名单/401/设User/登录/静态)、`AskControllerTests`(4 权限/403/空问/401)。预计累计 ~308 单测。

### ✅ P11.0 验证与提交（shell 恢复后，2026-08-31 上午）
- shell 恢复后按闸门顺序跑通：
  1. `dotnet build` → **0 error**（仅 12 个预存 nullable 警告）。
  2. `dotnet test` 全量 → **308/308 通过**（原 294 + 新增 14：TokenService5 + AuthMiddleware5 + AskController4）。
  3. 启动 5032 + Qdrant → `run_golden.py` → **Golden 18/18 PASS**（`decision=PASS`、`expectedOutcomePassed=18`、`expectedOutcomeFailed=0`、`failedGates=None`）。Auth 匿名白名单正确覆盖 `/evaluation`，Golden 不受影响。
- 预修与测试修正（均非实现 bug，是测试预期/缺失 using）：
  - `AskControllerTests` 补 `using System.Collections.Generic;`（缺 using 致 `IReadOnlyList<>` 无法解析）。
  - `Ask_Without_Authenticated_User_Returns_401` 断言由 `UnauthorizedResult` 改为 `UnauthorizedObjectResult`（控制器返回 `Unauthorized(对象)`）。
  - `TokenServiceTests.Issue_Is_Deterministic_Per_Call_But_Unique_Signature` 改写为 `Issue_Is_Deterministic_For_Identical_Payloads`：相同载荷(同秒精度)生成相同令牌是正确确定性行为，断言改为 `Assert.Equal` + 不同载荷断言 `NotEqual`。
- 文档同步：PhaseChecklist.md 新增 `### P11` 段（P11.0 ✅ + P11.1~P11.5/§12/§13 待办）；DevelopmentPlan.md §8 补 P11.0 完成 + P11.1 下一步；MEMORY.md P11 行更新。
- **下一步**：git 提交 P11.0（commit message 见提交记录）；随后启动 P11.1 前端脚手架（RCL + Blazor Web Head + MAUI Head，现有 API 零改动）。

## P11.1 前端脚手架（✅ 完成 · 2026-08-31）

三个新项目全部 build 0 error，现有 API `src/` 零改动，Golden 18/18 不受影响（前端不碰后端）。

- **RCL `SuperBuilder_AI.Components`**（net10.0 Razor Class Library）：`Routes`/`MainLayout`/`NavMenu` + 全页面占位（`Login`/`Ask` 真实调用 `api/ask`·`api/auth`，其余占位）+ `ApiClient`(`IApiClient`)+`AppState`+`ThemeService`+`LocalizationService` 运行时骨架 + `wwwroot/css/app.css` + `wwwroot/js/chart.js`（`.razor.js` 命名会触发 BLAZOR106，须用 `.js`）。
- **Web Head `SuperBuilder_AI.Web`**（**Blazor Server 经典模型**）：`_Host.cshtml` HTML 壳 + `MapBlazorHub` + `MapFallbackToPage("/_Host")`；`App.razor` 用 `Router` + `AdditionalAssemblies` 扫描 RCL 页面；`dotnet run`（务必从项目目录启动，内容根为项目目录，否则 RCL `wwwroot` 静态资源 404）验证首页/CSS/JS 均 HTTP 200。注：初版用 Blazor Web App(`MapRazorComponents`) 导致 `/` 持续 404，改经典 Blazor Server 后稳定。
- **MAUI Head `SuperBuilder_AI.Maui`**（**Blazor Hybrid**）：`MauiProgram` 用 `builder.UseMauiApp<App>()` + `builder.Services.AddMauiBlazorWebView()`（**.NET 10 已移除 `UseMauiBlazorWebView()` 构建器扩展**）；`MainPage.BlazorWebView` 以 RCL `Routes` 为 RootComponent、`wwwroot/index.html` 为 HostPage；`WindowsPackageType=None` 免打包；Windows 目标 `net10.0-windows10.0.19041.0` 编译 **0 error**（仅 `MainPage` setter 过时警告）。
- **关键坑**：① MAUI `UseMaui=true` 在 .NET 8+ 不再自动引入 NuGet 包，须显式引用 `Microsoft.Maui.Controls`+`Microsoft.AspNetCore.Components.WebView.Maui`(10.0.20)；② RCL 静态资源经 `dotnet run`（项目目录内容根）才能正确服务，直接跑 `bin/*.dll` 会因内容根在 bin 而 404。
- 图表库选型 **Chart.js**（轻量、纯 JS、Web 与 MAUI WebView 双端通用；`ChartView.razor` 经 JS 互操作封装，P11.2 细化）。
- 解决方案 `SuperBulider_AI.slnx`（历史拼写，未改名）已纳入三个新项目。
- **下一步**：P11.2 旗舰页（登录/租户 + Ask BI 端到端 + 视图层多轮调整）。
