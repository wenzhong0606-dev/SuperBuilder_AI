> **HISTORICAL SNAPSHOT / 历史快照**：本文仅记录当时的工作状态、判断或证据，不是当前缺陷清单、当前测试基线、当前 HEAD 或当前执行计划。任何“当前/唯一/已完成/未完成/风险”等表述均按本文记录日期理解；现状必须按 `docs/README.md` 的治理顺序重新核验：当前源码/测试 → Master → Milestone → Active Plan → Backlog。\n\n# 2026-08-26 工作日志

## 项目进度梳理
- 读取了 SuperBuilder AI Native BI 项目全部核心文件
- 梳理了系统架构、Phase 开发进度、D14 当前状态
- 生成了架构图和进度图表

### 关键发现
- 项目当前处于 Phase 2.7 D14 (DimensionAware QueryPlan) 阶段
- D14 Golden Regression BLOCKED: 18 cases 中 11 pass / 7 fail
- Overall Pass Rate 61.11% (需>=90%), Positive Pass Rate 36.36% (需>=95%)
- Build PASS, Startup PASS, 但 Release Gate BLOCK
- 7个失败 Positive Case 分为4个根因簇: Multi-DB DirectKey, Ranking Eval, EntityCount Ambiguous, Multi-Metric

### 代码结构
- ~34,127 行 C# 代码
- 核心服务在 Services/BI/ 下 (74文件)
- 评估框架在 Services/BI/Evaluation/ 下 (含 Golden Dataset 回归测试)
- QueryPlanBuilder 采用 partial class 拆分: Search/TableSelection/FieldResolution/PlanAssembly/SemanticResolution/Diagnostics

## 逐文件逐行代码审计完成

- 完成全项目 206 个源文件逐行审计,产出报告: `.workbuddy/audits/2026-08-26-code-audit.md`
- 发现 3 个 Critical 问题: BIConversationService 未注册 DI (C1)、7 处接口/服务 DI 注册缺失 (C2)、死代码重复 QueryIntentNormalizer (C3)
- 发现 12 个 Major 问题: 输入突变(M1)、硬编码时间字段(M2)、硬编码阈值(M3)、9+空catch(M4)、Console.WriteLine(M5)、NormalizeOperator不一致(M6)、SqlBuilder双路径(M7)、重复搜索(M8)、LooksLikeIdentifier过宽(M9/M10)、O(n^3)性能(M11)、硬编码JOIN上限(M12)
- D14 根因分析: 4 个簇 — R1 DirectKey(GQ-005/009)、R2 Ranking Eval(GQ-006/010)、R3 EntityCount(GQ-002/008)、R4 Multi-Metric(GQ-007)
- 工作计划 5 阶段: A(DI修复) → B(D14回归) → C(代码质量) → D(架构改进) → E(后续Phase)
- **下一步**: 用户确认后执行阶段 A (Program.cs DI 注册修复 + 删除死代码)

## 阶段 A+B 执行完成

### 阶段 A: DI 注册修复 (8项全部完成)
- Program.cs 新增 14 行注册: BIConversationService+IBIConversationService, IQueryPlanExplainabilityService, ISqlDialectResolver, ResultUnderstandingService+IResultUnderstandingService, QueryPlanMetadataValidator, IMetadataPromptBuilder, MetadataSearchService+IMetadataSearchService
- 死代码 QueryIntentNormalizer (AI版本) 已确认删除 (目录已空)
- **意外发现**: LocalRuntimeDiagnosticsController.cs 被 commit 252cf2b 意外截断 (395→22行)，从 commit 08c4375 恢复完整版本，同时保留了对 GetValue 方法的 DbConnectionStringBuilder.Keys 修复
- **构建验证**: 0 error, 0 warning (首次), 后续 5 pre-existing warnings

### 阶段 B: D14 Golden Regression 4根因修复 (全部完成)
- **R1 (GQ-005/009 DirectKey)**: DimensionResolutionEvidenceService.ScoreFactCandidate — IsPrimaryKey 惩罚 -0.20 改为 +0.10 bonus (key candidate 就是 PK,惩罚反了)
- **R2 (GQ-006/010 Ranking)**: QueryPlanBuilder.cs L1129-1150 — QueryOrder.Aggregation 从硬编码 None 改为 orderingMetric.GetAggregation(); MetricName 从 Name 改为 SemanticText
- **R3 (GQ-002/008 EntityCount)**: QueryIntentNormalizer — 添加 InferMetricAggregation 方法, "单数量"/"供应商数量"/"客户数量" → COUNT 而非 SUM
- **R4 (GQ-007 Multi-Metric)**: SetQuantity/SetAmount — 不再覆盖 Name 为硬编码 "入库数量"/"入库金额", 只设 Field+SemanticType

### 附带代码质量修复 (阶段C部分, 5项)
- M5: 移除 QueryPlanBuilder.TableSelection.cs Console.WriteLine 调试输出
- M6: NormalizeOperator 统一 — 未知操作符默认 "=" (Helpers.cs 与 SqlQueryBuilder.cs 一致)
- M8: DimensionResolutionEvidenceService 重复搜索 — 复用 factSearch 而非重新 SearchAsync
- M9: LooksLikeIdentifier — 词边界感知匹配 (_id/_code/_key/_no + camelCase) 替代 EndsWith("id")
- M10: ResolveMetricField "id" 后缀 — 同样改为词边界感知匹配

### 变更文件清单
1. Program.cs — +14行 DI 注册
2. Controllers/LocalRuntimeDiagnosticsController.cs — 恢复完整 395 行 (从 22 行截断版恢复)
3. Services/BI/DimensionResolutionEvidenceService.cs — R1 评分 + M8 重复搜索 + M9 LooksLikeIdentifier
4. Services/BI/QueryPlanBuilder.cs — R2 QueryOrder Aggregation+MetricName
5. Services/BI/Planning/Intent/QueryIntentNormalizer.cs — R3 InferMetricAggregation + R4 SetQuantity/SetAmount
6. Services/BI/QueryPlanBuilder.Helpers.cs — M6 NormalizeOperator
7. Services/BI/QueryPlanBuilder.TableSelection.cs — M5 Console.WriteLine
8. Services/BI/QueryPlanBuilder.FieldResolution.cs — M10 "id" 后缀匹配
9. Services/AI/QueryUnderstanding/QueryIntentNormalizer.cs — 死代码确认已删
