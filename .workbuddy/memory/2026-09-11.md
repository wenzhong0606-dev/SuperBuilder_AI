> **HISTORICAL SNAPSHOT / 历史快照**：本文仅记录当时的工作状态、判断或证据，不是当前缺陷清单、当前测试基线、当前 HEAD 或当前执行计划。任何“当前/唯一/已完成/未完成/风险”等表述均按本文记录日期理解；现状必须按 `docs/README.md` 的治理顺序重新核验：当前源码/测试 → Master → Milestone → Active Plan → Backlog。\n\n# 2026-09-11 工作日志

## M12-P0 权限矩阵 CI 实跑根因定位与修复

目标：让 `PermissionMatrixE2ETests`（17 用例）在 CI 真实执行并全绿。

### 现象（CI run 34566854796）
- 17 总数 / 5 通过 / 12 失败。
- 经 GitHub REST API 拉取 E2E job 日志（`actions/jobs/103160847608/logs`）分析。

### 根因
1. **登录 429（主因，12 例全因此失败）**
   - `RateLimitMiddleware` 默认 `LoginLimit=10`/`LoginWindowSeconds=60`（见 `RateLimitOptions.cs`）。
   - CI 无 gitignored `appsettings.Local.json`（本地 LoginLimit=500/GlobalLimit=1000），回退默认。
   - 限流键：登录路径以 `ip:{RemoteIp}+{UA 哈希}` 为键；所有 E2E 登录来自 `127.0.0.1` 同一 Playwright UA → 共用一个桶。
   - 矩阵 ~19 次登录在 60s 窗口内触顶 10 → 其余用例 `SB_TOO_MANY_REQUESTS`(429)。
   - 证据：日志 `Microsoft.Playwright.PlaywrightException : Error: login 429 {"Code":"SB_TOO_MANY_REQUESTS"...}`。

2. **`ThemeEditor` 调色板 `KeyNotFoundException 'primary'`（次因，admin `/themes` 必崩）**
   - `_tokens` 初值空字典，仅 `InitTokensFromNode` 填充；`OnAfterRenderAsync(firstRender)` 在会话自举前触发 → `api/themes/editor/blueprint` 以 `authTenant=0` 匿名发出（日志 REQ ... authTenant=0）→ 401 → catch → `_tokens` 仍空 → 渲染 `_tokens[key]` 抛 `KeyNotFoundException`。
   - 证据：日志 `Unhandled exception rendering component: The given key 'primary' was not present in the dictionary.` @ `ThemeEditor.razor:96`。

### 修复
- `.github/workflows/dotnet-build.yml`「启动 Web API」步骤注入 `RateLimit__LoginLimit=500`/`RateLimit__GlobalLimit=1000`（仅 CI）。
- `ThemeEditor.razor`：`_tokens` 防御性初始化 `TokenOrder.ToDictionary(k=>k,"")`；仿 `AppPreview`/`AppRun` 在 `OnInitialized` 订阅 `State.SessionRestoredChanged`、`OnAfterRenderAsync` 仅 `State.SessionRestored` 后取数；加 `@implements IDisposable`+`Dispose`。
- Components 工程 `dotnet build -p:TargetFrameworks=net10.0 --no-restore` 0 错（沙箱内 `dotnet restore` 因 `ProgramData` 不可解析失败，属环境限制，CI 不受影响）。

### 待办
- commit + push → `workflow_dispatch` 触发 → 轮询至矩阵 16 通过 / 1 跳过 / 0 失败。
- 全绿后同步 `M12.md` §8 状态，更新项目记忆。

## V2.6 Evaluation Controller Runtime Smoke job 长期 failure 根因（用户追问）

目标：解释 `dotnet-build.yml` 中独立 job `V2.6 Evaluation Controller Runtime Smoke` 为何失败。

### 现象
- 该 job 最后一步 `curl POST /api/metadata-vector/rebuild` 返回 **HTTP 401** → 后续 `grep '"success":true'` + 三项 `test ... -gt 0` 全失败 → `exit code 1`。
- 日志证据：`eval.txt` line 3995 `curl: (22) The requested URL returned error: 401`；line 3996 `##[error]Process completed with exit code 1.`

### 根因
- **CI 脚本鉴权缺口，非产品/M12 代码缺陷**。
- 该 job 全程匿名 curl（dotnet-build.yml line 122~165 无任何登录/Bearer 注入）。
- `/evaluation/diagnostics/*` 系列为公开诊断端点（line 124/135/139/144/148/156/160/164 均匿名成功）。
- `/api/metadata-vector/rebuild`（`MetadataVectorController.cs` `[Route("api/metadata-vector")]`、`Rebuild()` line 107-108）**受认证保护**；`DiagnosticsAccessPolicy.cs:33/36` 对 `/api/metadata-vector` 走独立授权分支，`GovernanceDataPlanePolicy.cs:18` 将其排除出治理策略 → 证实需令牌。
- 匿名调受保护端点 → 401。

### 结论与修复方向（未实施）
- 与权限矩阵（16通过/1跳过/0失败）、Agent 新建/运行（M12-09/10）无关。
- 修复：在 job 内登录取 Bearer 令牌，line 128 加 `-H "Authorization: Bearer $TOKEN"`；或改用已认证的 evaluation 诊断端点。
- 属独立范围，不在 M12-P1 内，待用户决定是否修。

## M12-P1 进度（2026-09-11）
- ✅ M12-09（Agent 新建接真实 API）、M12-10（Agent Run 执行真实 Workflow）已实施，CI run 34570740857 编译检查+矩阵全绿。
- 待排期：M12-11（Agent Runs 页面）、M12-12（DataSource 测试连接真实执行）、M12-13~18（设计器/语义关系图/指标中心/Identity 组织·部门·用户组/数据权限 RLS）。

## V2.6 401 修复实施（用户明确"先修复 V2.6 这个 401"）
- 根因再确认：`/api/metadata-vector/rebuild` 受 `DiagnosticsAccessPolicy` 保护，需 `platform:diagnostics:manage`；`platform-admin` 角色含该权限（IdentityConstants.cs:120），但 CI 的 Development 环境未配置 `PlatformBootstrap:Username/Password` → 无平台管理员 → 无账号可持该权限 → 匿名 401。
- 修复（`dotnet-build.yml` controller-runtime job）：
  1. 「启动 Web API」env 注入 `PlatformBootstrap__Username=ci-platform-admin` / `PlatformBootstrap__Password=C13_3_CI_Admin_Pass_2026`（幂等落地平台管理员，仅 CI）。
  2. 新增「获取平台管理员令牌」步骤：经 SQL 容器 `sqlcmd` 读 `platform` 租户 Id（避免硬编码）→ `POST /api/auth/login` 取 `token` → `::add-mask::` 后写入 `$GITHUB_ENV`。
  3. 「通过现有 Controller 重建 Metadata Vector（持令牌）」改为带 `-H "Authorization: Bearer $CI_PLATFORM_TOKEN"` 调用。
- 校验：python yaml.safe_load 通过（25 步，新步骤就位）。
- 状态：已提交并推送，触发 workflow_dispatch 验证（待轮询 controller-runtime job 是否转绿）。

### V2.6 token 步骤首轮失败根因（CI run 34572710557）
- **现象**：新步骤「获取平台管理员令牌」`completed/failure`；rebuild 被 skipped。
- **根因**：`sqlcmd` 输出含 `(1 rows affected)` 行，`-h -1` 只抑制列头不抑制该消息 → `TID=1(1rowsaffected)` → `jq --argjson t "1(1rowsaffected)"` 非法 JSON → jq 报错 `exit code 2`。
- **日志证据**：`Platform tenant id = 1(1rowsaffected)` → `jq: invalid JSON text passed to --argjson` → `##[error]Process completed with exit code 2.`
- **二次修复**（commit `71502f1`，已推送）：sqlcmd `-Q` 改为 `SET NOCOUNT ON; SELECT ...`，并 `| tr -d '[:space:]' | grep -E '^[0-9]+$'` 仅保留纯数字租户 Id。YAML 校验通过。
- 状态：已重新触发 workflow_dispatch，后台轮询 task `1aRbEK` 监控 controller-runtime job 直至完成。

### V2.6 job 复跑结果（CI run 34573634830，head 71502f1）
- ✅ 401 修复确认生效：步骤 15「获取平台管理员令牌」与 16「重建 Metadata Vector（持令牌）」均 **success**。原 401 问题已闭环。
- ❌ job 仍 failure，但失败点已**转移**到步骤 20「Controller C.13.3 Multi-Metric Evaluation」（GQ-007）。该步骤此前因 rebuild 401 被 skipped，故本次是首次暴露的**独立既有故障**，与 401 修复无关。
- GQ-007 失败证据（v26b.log line 4075）：`{"passed":false,"stage":"SemanticApplicabilityGate","decision":"BLOCK","reason":"Top Candidate 缺少 Golden SemanticText 的直接语义证据。"}`，metric "入库数量" 的 top candidate = `wms_shelf_down_strategy.note`（businessMeaning "下架策略的补充说明或备注信息"，score 0.785）。即 CI 元数据语义检索把"入库数量"错配到备注列 → 语义适用性闸门 BLOCK。
- 附带 **workflow 断言 bug**（同步骤）：`test ... '.evaluation.metrics.details[1].semanticText' = "入库金额"` 与 golden GQ-007 定义不符——GQ-007 期望 `[入库数量, 入库单数量]`（见 query-plan-golden-v1.json GQ-007 expected.metrics），并非"入库金额"。属拷贝错误，但当前被 SemanticApplicabilityGate 阻断在前，断言本身还不是首要阻塞。
- 结论：本次"修复 401"目标已达成；GQ-007 是语义评估/检索质量的独立既有问题（CI WMS 元数据下"入库数量"无直接语义证据），需单独立项排查（语义闸阈值/embedding/元数据 fixture），不属 401 修复范围。
- **用户决策（选 B，15:25）**：认定 401 修复已完成（原始需求达成），GQ-007 作为**独立既有故障**记入待办，本轮回写、不在此轮处理。
  - 待办项：① GQ-007 `SemanticApplicabilityGate` BLOCK（metric "入库数量" 错配到 `wms_shelf_down_strategy.note`）→ 排查语义闸阈值/embedding/CI 元数据 fixture；② 同步骤 workflow 断言笔误 `入库金额`→`入库单数量`（golden GQ-007 期望 `[入库数量, 入库单数量]`）。两处均不属 401 修复范围。

## M12-11 Agent Runs 页面（实施完成 + CI 验证中）
- commit `a251104`（5 文件，+484 行）已推送 `71502f1..a251104 master -> master`（首次 push SSL 握手失败、重试成功）。
- 交付物：
  - 新增 `SuperBuilder_AI.Components/Components/Pages/Analysis/AgentRun.razor`（`/agent/runs/{Id}` 运行详情页：执行历史时间线 / 工具调用 / 风险 / 模式 / 时长 / 输出 / 错误 / 待审批；`approval_pending` 时 `PermissionGuard AgentManage` 包裹 approve/reject，调 `POST /api/agent/runs/{id}/approve|reject`）。
  - 改写 `Agent.razor` 的 `Run()`：触发成功后 `Nav.NavigateTo($"agent/runs/{runId}")` 跳详情页（runId<=0 才 `Reload()`）。
  - i18n 四地一致：后端 `ResourceKeys.cs`(常量+Catalog)、RCL `Keys.cs`(常量+Defaults)、`LocalizationSeedService.cs`(ZhCnDefaults) 各 +38 个 `AgentRun*` 键；`C:/tmp/verify_i18n.py` 校验四处 zh/en/占位符一致通过。
- **CI 验证**：已 `workflow_dispatch` 触发 `dotnet-build.yml`（master，head a251104，run `34576806667`）。
  - ✅ 编译检查: success（含 `ResourceKeyRegistryTests` 38 新键通过 + 三端 build 0 error + AgentRun.razor 编译通过）。
  - ✅ Web/Blazor E2E (Playwright): success。
  - ❌ V2.6 Evaluation Controller Runtime Smoke: failure——**仅**步骤「Controller C.13.3 Multi-Metric Evaluation」(GQ-007 `SemanticApplicabilityGate` BLOCK)，属用户「选 B」已登记的独立既有故障，与 M12-11 无关。
- **结论**：M12-11 在自身范围内 CI 全绿（编译检查 + E2E + i18n 护栏）。V2.6 GQ-007 不在本轮范围。
- 下一步：汇报后询问是否继续 M12-12（DataSource 测试连接真实执行）。

## M12-12 DataSource 测试连接（按方案 B，收口并 commit/push/CI 中）
- 用户指令"按B进行"。排查发现：**M12-12 方案 B 早已在先前会话实现但从未 commit/push**——`git status` 显示 `DataSourcesController.cs`(+38 端点)、`DataSources.razor`(+34 接线)、`DataSourcesControllerTests.cs`(+54)、`AuthStoreTests.cs`(+1)、新 `DataSourcePreviewUiTests.cs`、`docs/Master_Development_Plan.md`(+5)、`docs/milestones/M12.md`(M12-12 交付记录) 全部未提交。M12.md §4/§8 已预写"已实施方案 B""未提交或推送"。
- 后端端点 `TestConnectionString`（`POST /api/data-sources/test-connection`，`DataSourcesController.cs:367`）：收 `{DbType, ConnectionString}`，`CreateConnection` 开三驱动连 `SELECT 1`，15s 超时，返回 `{status, elapsedMs, errorCode}`，不落库/不回显/脱敏。要求 `metadata:edit` + tid/uid。
- 前端 `DataSources.razor.TestConn`（:201）新建态 `POST /api/data-sources/test-connection`；行内 `Test`（:222）`POST /api/data-sources/{id}/test-connection`。`IApiClient.PostJsonAsync` 接口+实现（`ApiClient.cs:85`/`ApiClientBase.cs:163`）已就位，AuthStoreTests 测试替身补 `PostJsonAsync` 修复编译。
- **本轮清理**：死键 `DataSourcesTestSubmitted`（文案"计划于 P12 实现"，未被任何 razor 引用）从四地一致移除（ResourceKeys.cs const+Catalog、Keys.cs const+Defaults、LocalizationSeedService.cs ZhCnDefaults）——注意同文件并行 Edit 会互相覆盖，须顺序编辑。
- commit `80b4317`（11 文件，+231/-15，含死键清理），已 push `94c7f7c..80b4317`。
- **CI 验证**：`workflow_dispatch` 已触发（head 80b4317，run `34580960129`）。
  - ✅ 编译检查: success——`ResourceKeyRegistryTests` 死键移除后仍一致、新增 8 后端 + 2 bUnit 测试编译通过、三端 build 0 error。
  - ✅ Web/Blazor E2E (Playwright): success。
  - ❌ V2.6 Evaluation: failure——**仅** GQ-007 步骤（已知既有故障，选B登记），与 M12-12 无关。
- **结论**：M12-12（方案 B）在自身范围内 CI 全绿（编译检查 + E2E + i18n 护栏 + 新增测试）。V2.6 GQ-007 不在范围。M12-12 正式收口。
- 注意：`DataSourcesControllerTests` 用 `TestConnectionString(new(type,value), ct)` 语法——该方法签名第二参为 body、第三参 `CancellationToken`，与 `HttpContext` 无关，测试直调控制器方法。

## M12-13 Dashboard 可视化设计器（实施完成，待 commit/push/CI）
- 交付物：
  - 新增 `SuperBuilder_AI.Components/Components/Pages/Analysis/DashboardDesigner.razor`（路由 `/dashboards/{Id:long}/design`）——双模式（设计 / 源码）。设计模式：12 栅格 CSS Grid 拖拽画布 + 调色板拖放 + 属性面板（6 类组件通用+专属字段）；源码模式：`DashboardDsl` JSON 直编，`JsonNode.Parse` 校验失败回退。保存 `PUT api/dashboards/{id}`，发布 `POST api/dashboards/{id}/publish`。
  - 新增 `SuperBuilder_AI.Components/wwwroot/js/dashboard-designer.js`——`window.SuperBuilder.designer.attach(canvas, dotNetRef)/detach()`，原生 Pointer Events + `getComputedStyle` grid 指标换算像素位移→(x,y,w,h)，松手 `[JSInvokable] OnWidgetGridChanged` 回写；调色板 `drop` → `OnPaletteDrop(type,x,y)`。零第三方拖拽库。
  - `Components/wwwroot/css/app.css` +~40 行 `.sb-dz-*` 样式（复用 `--sb-*` 令牌，含 1100px 响应式降级）。
  - 脚本注册：`SuperBuilder_AI.Web/Pages/_Host.cshtml`、`SuperBuilder_AI.Maui/wwwroot/index.html` 各加一行 `_content/SuperBuilder_AI.Components/js/dashboard-designer.js`。
  - 入口：`Dashboards.razor` 行操作区新增「设计」按钮（`PermissionGuard Require=DashboardEdit Silent`）→ `OpenDesign` 跳转。
  - i18n：新增 **70** 个 `Content.DashboardDesigner*` 键，四地一致（`ResourceKeys.cs` const+Catalog / `Keys.cs` const+Defaults / `LocalizationSeedService.cs` ZhCnDefaults），页面内无裸中文（全 `L10n.T(Keys.Content.*, fallback)`）。校验脚本 `C:/tmp/verify_designer_i18n2.py`（输出 `OK: all 70 ... consistent across 4 places`，且 reported unused=[]，即 70 键全部被 razor 引用）。
  - 新增 `tests/SuperBuilder_AI.Tests/DashboardDesignerUiTests.cs`——2 个 bUnit 用例（DSL→渲染组件+调色板新增；源码模式 textarea 出现），用 `DispatchProxy` 替身 `IApiClient`。
  - `docs/milestones/M12.md`：§4 M12-13 行更新为「✅ 已实施（70 键过护栏 + 2 bUnit + 全量 1073/1073 绿）」，新增「M12-13 交付记录」段。
- ⚠️ **i18n 四处一致性的正确不变量**（本轮踩到）：后端 Catalog 的 `DefaultValue` 存的是 **en-US**，RCL `Keys.cs` 的 `new(zh, en)` 第一参是 zh。故校验应为 `be_catalog.DefaultValue == rcl.en` 且 `seed.zh == rcl.zh`；反向比较会得到全量假失败。（此前 `verify_i18n.py` 已隐含此约定。）
- 验证：Components / Tests / Web 宿主三工程 `dotnet build -p:TargetFrameworks=net10.0 --no-restore` **0 错误**；全量单测 **1073/1073 通过、0 失败、0 跳过**（基线 1071 + 新增 2）。
- 环境坑：Web 宿主构建两次报 `MSB3027/MSB3021` 文件锁（`.NET Host (16112)` 占 `Components.dll/.pdb`）——实为**本会话先前启动的 Web 宿主进程**；`taskkill /F /PID` 在 Git Bash 下需 `MSYS_NO_PATHCONV=1 MSYS2_ARG_CONV_EXCL='*'` 前缀（否则 `//F` 被破坏）。停掉后构建即 0 错误，非代码缺陷。
- 验证边界：仅 bUnit 渲染级 + 本地构建；未做真实浏览器拖拽手势 E2E；未在 MAUI 真机验收。
- 下一步：commit（仅 M12-13 相关文件，排除 `e2e_m12p1.*`/`eval.*`/`nul` 散落日志）→ push → `workflow_dispatch` 触发 CI → 轮询结论。

### M12-13 CI 结果（run 34596673160，head c677d10，commit 已 push）
- ✅ 编译检查: success（三端 build 0 error + 70 新键过 `ResourceKeyRegistryTests`）
- ✅ Web/Blazor E2E (Playwright): success
- ❌ V2.6 Evaluation Controller Runtime Smoke: failure——**仅**步骤「Controller C.13.3 Multi-Metric Evaluation」(GQ-007 `SemanticApplicabilityGate` BLOCK)，用户「选 B」已登记的独立既有故障，与 M12-13 无关。
- **结论**：M12-13 在自身范围内 CI 全绿（编译检查 + E2E + i18n 护栏）。M12-13 正式收口。
- ⚠️ **本沙箱无 `gh` CLI**：触发/轮询 CI 须走 GitHub REST API，令牌从 `git credential fill`（GCM 持有的 `gho_*`）取，勿 echo。另：`python.exe /c/tmp/x.py` 会被 MSYS 转成 `C:\c\tmp\x.py` 报 file not found，须写 `"C:/tmp/x.py"`。

### M12-14 闭环（App 可视化设计器，承接续做）
- 交付：`SuperBuilder_AI.Components/Components/Pages/Analysis/AppDesigner.razor`（路由 `/apps/{Code}/design`，~930 行）；入口接线 `Apps.razor` 行操作 + `AppDetail.razor` 头部「设计」按钮（`PermissionGuard Require=AppEdit`）；`tests/.../AppDesignerUiTests.cs` 2 例 bUnit。
- 设计：**复用 M12-13 的 Dashboard 12 栅格拖拽运行时**（`.sb-dz-*` + `window.SuperBuilder.designer.attach/detach`，零第三方库），差别仅 App DSL 用 `pages[].components[]`（Dashboard 用 `widgets[]`，高度键 `h`）。
- i18n：新增 **60** 个 `Content.AppsDesigner*`（含 `AppsDesignerSaving`），四处严格一致（`verify_appdesigner_i18n.py` 校验 `OK`），过 `ResourceKeyRegistryTests`。
- 红线遵守：App DSL **无 Events 模型** → 按不造假占位原则剔除 Events 编辑；Permissions 渲染为说明面板（引用 RBAC 码），非伪表单。
- 验证：Components(`-f net10.0`)/Tests 工程 build **0 错误**；全量单测 **1075/1075 绿**（基线 1073 + 2 bUnit）。
- 编译坑（已修，供复用）：① `P.Hh="h"` 是高度常量，勿写 `nameof(P.H)`；② 调色板拖拽须用 HTML 属性 `ondragstart="event.dataTransfer.setData('text/sb-widget','@t.Type')..."`（Razor C# handler 的 `e.DataTransfer?.SetData` 编译失败）；③ 调用注入 `L10n` 的方法不可标 `static`（CS0120）；④ 属性内 `?? "1"` 双引号嵌套破坏属性解析 → 改 `?? string.Empty`。
- 验证边界：仅 bUnit 渲染级 + 本地构建；未做真实浏览器拖拽手势 E2E；未在 MAUI 真机验收。**沙箱缺 Android SDK** 致 RCL 的 `net10.0-android/ios` 目标本地 `XA5300` 失败（CI 环境有 SDK 不受影响）——本地验证用 `-f net10.0` 单目标。
- push：经 `127.0.0.1:7892` 代理，**偶发 TLS 握手失败**（`schannel failed to receive handshake`），重试 1~3 次即成功。commit `83ea583`(代码)+`404fc51`(docs M12.md) 已 push master。
- CI：已 `workflow_dispatch` 触发 `dotnet-build.yml`（HTTP 204）。实际 run **34600842945**（head 83ea583）：✅ 编译检查 success（三端 0 error + 60 新键过护栏）、✅ Web/Blazor E2E success、❌ V2.6 Evaluation Smoke failure（**仅** GQ-007 `SemanticApplicabilityGate` BLOCK，用户「选 B」登记的独立既有故障，与 M12-14 无关）。M12-14 自身范围 CI 全绿，正式收口。
- push 重试提示：Git push/API 经 `127.0.0.1:7892` 代理**稳定偶发 `schannel failed to receive handshake`**，循环重试 2~4 次必成功；CI dispatch 与 runs 查询同样会偶发 TLS 失败需重试。
- M12.md：M12-14 行改「✅ 已实施」，§8 结论/交付列表同步，新增「M12-14 交付记录」段。

### M12-15 闭环（Semantic Model 关系图）
- 交付：后端新增只读端点 `GET api/business-model/relationships?tenantId=`；`IBusinessEntityRepository`/`IBusinessEntityRegistryService` 增 `ListRelationshipsAsync`（`BusinessEntityRepository` 实现：Include 两端实体 + 「两端 TenantId 均须匹配」过滤）；前端新增 `SemanticModelGraph.razor`（零依赖 SVG 关系图，节点=实体按业务域分列、边=关系带箭头、点击节点→`/business-model/entities/{id}`）+ `BusinessModel.razor` 新增「关系图」Tab + `app.css` `.sb-graph-*`；`SemanticModelGraphUiTests.cs` 4 例 + `BusinessModelControllerTests` +3。
- **关键事实**：`BusinessEntityService.ListAsync` **不 Include 关系**（只 `Where(TenantId).OrderBy(Name)`），故实体列表 JSON 无关系；画关系图必须新增独立关系端点（纯增量，未改 entities 契约）。关系模型 `BusinessEntityRelationship`：SourceEntityId/TargetEntityId/Name/RelationshipType/Cardinality/IsRequired，**无自身 TenantId**（租户归属靠两端实体）。
- **bUnit 导航断言坑**：自定义 `NavigationManager` 子类因 **BaseUri 未初始化**，`NavigateTo` 相对路径会抛异常；应用 bUnit 2.9 内置 `Bunit.TestDoubles.BunitNavigationManager`（`Services.GetRequiredService<NavigationManager>()`，已初始化，`.Uri` 会更新，另有 `.History` 栈）。bUnit 2.9 无旧版 `FakeNavigationManager` 类型。
- i18n：新增 4 个 `Content.BusinessModelGraph*`（Title/Empty/NoRelationships/ClickHint），四处一致（ResourceKeys 8=4const+4catalog、Keys 8、Seed 4），过 `ResourceKeyRegistryTests`。踩坑：**种子键名误植 `]`**（如 `["Content.X]"]`）——插入后务必回读核查。
- 验证：全量单测 **1082/1082 绿**（基线 1075 + 3 后端 + 4 bUnit）；后端主工程/Components(net10.0)/Tests/Web 四工程 build 0 错误。
- 诚实边界：演示租户 `BusinessEntities`/`BusinessEntityRelationships` 多空集 → 图实际为「节点（若有）+ 无关系说明」；能力就绪，待治理层数据填充。
- commit `5699f71` 已 push master；CI run **34605047205**（push 触发）：✅ 编译检查 success、✅ E2E success、❌ V2.6 Evaluation Smoke（仅 GQ-007，独立既有故障）。⚠️ **CI dispatch 陷阱**：`curl ... -o /dev/null -w ... 2>/dev/null` 在 Git Bash 下会「HTTP 204 却退出码非 0」→ `&& break` 失效导致**重复触发多个 run**；应改用 `|| true` 或检查响应体，多余 run 用 `POST /actions/runs/{id}/cancel` 取消。
- M12.md：M12-15 行改「✅ 已实施」，§8 结论/交付列表同步，新增「M12-15 交付记录」段。

### M12-16 闭环（指标中心 Metric Center）
- 交付：后端新增只读端点 `GET api/business-model/metrics?tenantId=` 与 `GET api/business-model/dimensions?tenantId=`；`IBusinessEntityRepository`/`IBusinessEntityRegistryService` 增 `ListMetricsAsync`/`ListDimensionsByTenantAsync`；前端新增 `MetricCenter.razor`（路由 `/metrics`，统计瓦片 + 三 Tab 指标/维度/计算字段 + 搜索 + 行操作跳实体详情）+ `NavMenuItems`「分析」组 `metrics` 入口（Permission `metadata:view`）；`MetricCenterUiTests.cs` 4 例 + `BusinessModelControllerTests` +5。
- **关键事实（踩坑）**：`BusinessEntityMetric` 继承 `BaseEntity`——**BaseEntity 无 TenantId** ⇒ 指标表本身无 TenantId，租户隔离只能经所属 `BusinessEntity.TenantId`（EF 翻译为 INNER JOIN）；`BusinessEntityDimension` 自带 `TenantId`，直接过滤。`BusinessDomain` 有 `TenantId/Name/Description`。
- **投影 DTO 而非返回 EF 实体**：`BusinessEntityMetric.BusinessEntity` 与 `BusinessEntity.Metrics` 互为反向导航 → 直接序列化有循环引用风险；故新增只读 record `BusinessMetricView`/`BusinessDimensionView`（`src/Application/Ports/BI/Entity/BusinessModelGovernanceViews.cs`，命名空间 `Interfaces.BI.Entity`），同时把实体名/业务域/`PhysicalBindings.Count` 一次算好。
- **测试坑**：SQLite 内存库 `Tenants.TenantCode` 有唯一约束 → 同一测试内种第二个租户必须显式给 `TenantCode`（默认空串冲突，报 `UNIQUE constraint failed: Tenants.TenantCode`）。另：`dotnet test` **不带 `--no-restore`** 会触发还原报 `Value cannot be null. (Parameter 'path1')`（本沙箱还原不可用）→ 统一 `build --no-restore` + `test --no-build`。
- i18n：新增 30 键（`Nav.MetricCenter` + 29 个 `Content.MetricCenter*`），四处一致（ResourceKeys 58=29const+29catalog、Keys 58、Seed 29；Nav 2/2/1），过 `ResourceKeyRegistryTests`。
- 验证：全量单测 **1091/1091 绿**（基线 1082 + 5 后端 + 4 bUnit）；后端主工程/Components(net10.0)/Tests/Web 四工程 build 0 错误。
- 诚实边界：本项为**只读治理视图**；指标/维度编辑（含计算口径）需扩展 `BusinessEntityUpsertRequest` 写契约，留待后续里程碑，页面以 i18n 说明面板明示。
- commit `5010fb6` 已 push master；CI run **34608796259**（push 触发）：✅ 编译检查 success（30 新键过 `ResourceKeyRegistryTests`）、✅ Web/Blazor E2E (Playwright) success、❌ V2.6 Evaluation Smoke（仅步骤 20「Controller C.13.3 Multi-Metric Evaluation」= GQ-007，独立既有故障）。
- M12.md：M12-16 行改「✅ 已实施」，§8 结论/交付列表同步，新增「M12-16 交付记录」段。

### M12-17 闭环（Identity 组织目录：组织 / 部门 / 用户组）
- 交付：域模型 6 张新表（`Organization`/`Department`/`UserGroup`/`UserGroupMember`/`UserGroupRole`/`UserDepartmentMember`，`Domain/Identity/`，namespace `Models.Identity`）+ SuperBIContext DbSet/索引/FK/查询过滤器；EF 迁移 `20260911143327_M12_17_IdentityOrganizationUnits`；服务 `IIdentityDirectoryService`+`IdentityDirectoryService`；控制器 `IdentityDirectoryController`（`api/identity-directory`，11 端点）；前端 `Components/Pages/Admin/IdentityDirectory.razor`（`/admin/identity-directory`，三 Tab）+ NavMenuItems 入口；`IdentityDirectoryControllerTests` 13 例 + `IdentityDirectoryUiTests` 4 例。
- **🔑 本项核心 = RBAC 真实联动**：`IdentityService.GetPermissionsAsync` 由单一 `UserRole` 改为 **有效角色 = 直接 UserRole ∪ 组角色 UserGroupRole**（join `UserGroupMembers`）。这样用户组不只是组织架构展示，成员入组即获得组角色的权限、退组即撤销（测试覆盖）。
- **🔑 沙箱内可用真实 EF 工具（突破）**：`dotnet-ef 10.0.10` **已全局安装**于 `~/.dotnet/tools/dotnet-ef.exe`，但沙箱 `APPDATA` 为空 → `dotnet ef`/`dotnet nuget` 报 `Value cannot be null. (Parameter 'path1')`。**先 `export APPDATA="C:\Users\ThinkPad  X1\AppData\Roaming"`，再直接调 exe**（`"$HOME/.dotnet/tools/dotnet-ef.exe" migrations add <Name> --project ... --startup-project ... --framework net10.0 --no-build`）即可生成迁移。此前手工迁移是本项目的主要缺陷来源；今后新表一律走此路径。
- **控制器安全范式**：新控制器**服务端强制权限码**（`User.HasClaim("perm", IdentityPermissions.IdentityManage)`，缺则 403），而非像既有 `IdentityController` 只靠前端 `PermissionGuard`。这是 M12-P0 已知缺口的局部收敛方向。
- **服务查询范式**：`IgnoreQueryFilters()` + 显式 `TenantId` 过滤 → 行为**不依赖**调用方是否已 `ApplyTenantScope`；测试直调（无中间件）也正确。跨租户写入校验一律显式查 `Id == x && TenantId == tid`。
- **测试基建**：目录测试的 `CreateContext` 必须种 RBAC 目录（`new IdentityService(ctx, new PasswordHasher()).SeedAsync(...)`），否则「组→角色」解析为空、组角色关联静默不建立（曾误判为 EF 投影 bug）。另一坑：**同一文件内并行 `Edit` 会互相覆盖**（第二个写入胜出，第一个静默回滚）——i18n 技能已记录，本次再次踩到。
- **Blazor 页面注入**：新页面注入 `ToastService` → bUnit 必须 `Services.AddSingleton(new ToastService())`，否则 `CreatePropertyInjector` 抛 DI 异常（4/4 全失败）。
- i18n：新增 49 键（`Nav.IdentityDirectory` + 48 个 `Content.IdentityDirectory*`），四处一致（RK 98、Keys 98、Seed 49），过 `ResourceKeyRegistryTests`。**生成脚本前缀写错（`Content.Desc` 而非 `Content.IdentityDirectoryDesc`）导致需 `git checkout` 回滚三个文件重跑**——脚本生成 key 名务必 `PREFIX` 显式拼接并回读校验。
- 验证：全量单测 **1108/1108 绿**（基线 1091 + 13 后端 + 4 bUnit）；后端主工程/Components(net10.0)/Tests/Web 四工程 build 0 错误。
- 诚实边界：仅「新建 + 关联」闭环；重命名/停用/删除/分页/部门树 UI 未做，页面不显示假操作入口；演示租户目录为空集 → 实际展示「瓦片全 0 + 空状态」。
- commit `3e1330c` 已 push master；CI run **34613229420**（push 触发）：✅ 编译检查 success ✅ Web/Blazor E2E success；❌ 仅 V2.6 Evaluation Smoke 步骤 20 GQ-007 独立故障（选 B），与 M12-17 无关。
- M12.md：M12-17 行改「✅ 已实施」，§8 结论/交付列表同步，新增「M12-17 交付记录」段。

## M12-18 数据权限 / RLS 可视化管理（2026-09-11，commit `70a3395`）

- 起点复盘：RLS **基础设施早已存在**（`RowLevelSecurityPolicy` + `RowLevelSecurityService` +
  `QueryPlanSecurityGate` + `RowLevelSecurityController` @ `api/data-policies/row`），
  真正的缺口是**前端无管理页**且 `List` 只吐原始实体（无表名/列名/主体名）。
  故本项 = 富化列表 + 启用切换端点 + 可视化页面，**未动执行链路**。
- 后端新增：`GET api/data-policies/row/manage`（跨数据源富化投影，可选 dataSourceId 过滤）、
  `POST api/data-policies/row/{id}/toggle`（省略 body 取反）。复用既有 `ScopeAsync`
  （服务端强制 `identity:manage` **且** `metadata:edit`，缺任一 403）。
- 前端新增 `Components/Pages/Admin/DataPolicies.razor`（`/admin/data-policies`）：统计瓦片 +
  筛选（数据源/状态/关键字）+ 表格 + 数据源→表→列级联新建编辑 + 启用切换 + 删除。
  级联数据复用 `api/data-sources/manage` 与 `api/data-sources/{id}/metadata`。
- i18n：65 键（Nav.DataPolicies + 64 Content），四处一致（**精确计数核验**：
  `const`(带引号) + `[Key]`(catalog/defaults 无引号) = 2/2/1；子串计数会误报，勿用）。
- 验证：全量 **1119/1119 绿**（基线 1108 + 7 后端 + 4 bUnit）；四工程 build 0 错误。
- 踩坑：① 匿名数组两个元素 `subjectId = 55`(int) 与 `(long?)null` 混用 → CS0826，须统一 `(long?)55`；
  ② `SbEmptyState` 根类是 `.empty-state`（不是 `.sb-empty`）；③ 控制器返回匿名投影时
  `JsonSerializer.Serialize` 默认 **PascalCase**，测试断言须显式 `CamelCase` options；
  ④ `Num()` 返回 long 赋给 int 字段须显式强转；⑤ razor 行操作按钮调 async 方法须写
  `async () => await M(context)`，否则 lambda 返回类型不匹配。
- 诚实边界：仅**行级**策略；**列级（字段）授权未提供**，页面说明文案明示，不做假占位。
- M12.md：M12-18 行改「✅ 已实施」，§8 结论改为「M12-P1（M12-09~18）至此全部闭环」，
  §8 交付列表同步，新增「M12-18 交付记录」段。
- commit `70a3395` 已 push master；CI run **34617329087**（push 触发）：✅ 编译检查 success ✅ Web/Blazor E2E success；❌ 仅 V2.6 Evaluation Smoke 步骤 20 GQ-007 独立故障（选 B），与 M12-18 无关。**M12-P1（M12-09~18）全部闭环。**

## 剩余工作大盘点（2026-09-11 23:50，用户要求"先整理"）
- 实测基线：HEAD `c43d833`，**已全部推送 origin/master**（`git log FETCH_HEAD..HEAD` 空）；
  全量单测 1119/1119；CI 仅 GQ-007（V2.6 Smoke 步骤 20）独立失败。
- **文档滞后是最大问题**：MDP §3 总表仍写「M9-04~15 未启动」（实际 04~11/13~15 均已交付，
  M9.md 有完整交付记录；仅 M9-12 用户决策跳过）、「M12 草案未排期」（实际 P0+P1 09~18 全闭环）、
  §3.2 快照停留在 09-09 / HEAD `0afce59`；PhaseChecklist 12 项未勾选含已交付项
  （A5 循环已消除、自定义组件库/主题已 M7-09/10 交付）；DevChecklist 勾选框整体未维护。
- **真实功能缺口（代码实测）**：
  ① **M7-12 未交付**——`ToolRegistry` 中 Query/Dashboard/Report/Forecast/Alert 5 工具仍
  `BackendPending`（仅 Metadata/Semantic 为 `live`，Workflow 已 `Enabled=false`）；
  验收要求「全 live 或明确禁用且不可执行」，当前 5 个 pending 且 Enabled=true → 未达标。
  ② Agent 无回滚体系（无 `AgentVersion`）；③ 组织目录缺重命名/停用/删除/分页；
  ④ 指标中心只读，缺指标·维度编辑；⑤ App DSL 无 Events 模型；⑥ 列级（字段）授权未提供；
  ⑦ 自定义组件不能被 App 引用（`AppComponentTypes` 6 类白名单，无 `CustomComponentRef`）。
- **未启动里程碑**：M10-01 多库连接器 / M10-02 多模型 BYO / M10-03 外部 IdP；M11 12 项暂缓。
- **环境门禁**：CI E2E 缺 e2e 账号种子（权限矩阵在 CI 为假绿）；M7-11 P0-D1 人工验收未做；
  M9-15 检出的 5 个 Pending 迁移未应用开发库；M9-14 Qdrant 备份下载（≈592MB）未跑通。
- 工作区：`e2e_m12p1.gz/txt`、`eval.gz/txt`、`nul` 未跟踪待清理；`.workbuddy/memory/*` 有未提交改动。
- **结论：不是漏项，是提前到 M7-09 交付了**。MDP:111 把「自定义组件」写在 M10 范围列里，
  但同格备注已注明「组件/主题已在 M7-09/10 先行」；MDP:242 明确「自定义组件/风格/发布应用 → M7-09~11」。
- 代码实证：`Domain/Components/CustomComponentDefinition.cs` + `CustomComponentVersion`、
  `Application/Components/CustomComponentDslSerializer.cs`、`Api/Controllers/ComponentsController.cs`
  （`api/components`）、`tests/CustomComponentsTests.cs`；迁移 `20260907053931_M7_09_CustomComponents`。
- **真实缺口（非 M10）**：自定义组件**不能被 App DSL 引用**——`AppComponentTypes.Supported`
  是 6 类硬编码白名单（chart/table/kpi/text/filter/form），全仓无 `CustomComponentRef`；
  打通需扩白名单或引入引用机制（归 M7-11 App DSL 演进，不属 M10）。
- 文档滞后 3 处（待修，用户未授权）：① MDP:111 范围列仍写「自定义组件」易误读；
  ② `PhaseChecklist.md:256` 自定义组件库仍为 `- [ ]` 未勾选（与 M7-09 已交付矛盾）；
  ③ `docs/milestones/M10.md` 仅 01~03 三项，无任何指向 M7-09 的说明。
