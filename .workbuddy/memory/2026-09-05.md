> **HISTORICAL SNAPSHOT / 历史快照**：本文仅记录当时的工作状态、判断或证据，不是当前缺陷清单、当前测试基线、当前 HEAD 或当前执行计划。任何“当前/唯一/已完成/未完成/风险”等表述均按本文记录日期理解；现状必须按 `docs/README.md` 的治理顺序重新核验：当前源码/测试 → Master → Milestone → Active Plan → Backlog。\n\n# 2026-09-05 工作日志

## M1-05 已推送 origin/master
- 用户于 2026-09-05 完成 `git push`；本地 13 个提交（M1-04 `020ffa9`、M1-05 `747b876` 等）全部上推，`git rev-list origin/master..HEAD = 0`，`origin/master` tip = `747b876`。
- 验证回顾：M1-05 新增 16 例 `MetadataVectorIntegrityTests`（16/16 通过）；全量回归 **603/603 通过 0 失败**；全解决方案构建 **0 error / 110 warning**（warning 均为既有 IL2026/obsolete/trimming）。
- 主计划 `docs/Master_Development_Plan.md` 的 M1-04/M1-05 段状态由"提交待 push"更正为"已推送 origin/master"。

## BOOTSTRAP 工作区身份初始化
- 已建立 `IDENTITY.md`（名 WorkBuddy、风格 专业严谨、🛠️）、`USER.md`（偏好简体中文；SuperBuilder_AI 审计/整合上下文；姓名/城市待用户提供）、`SOUL.md`（追加 Established 段）。
- `BOOTSTRAP.md` 经确认已不存在（身份初始化文件 IDENTITY.md/USER.md/SOUL.md 均就位），身份初始化完成，无需再删除。

## M1-06 PhysicalBinding / 授权 / RLS ✅ 完成（本地提交待推送）
- 源码：SuperBIContext M1-06 region（PriorityNonNeg + RLS 两 CHECK + BusinessDomain/MetadataSemantic.BusinessDomainId FK + DataSourceAccessGrant Tenant FK）、`BusinessEntityService.ValidateBindingsAsync`（写入路径「恰好一个 Owner」，按父集合归属判定，修复 detached-graph FK 未 fixup 回归）、`DataSourceAuthorizationService`（RevokeBySubjectAsync + DetectOrphanGrantsAsync）、`RlsVocabularies.AllowedOperators/NormalizeToSymbol`。
- **关键 gotcha（必记）**：`PhysicalBinding` 的 CHECK/索引在**两处**配置——`SuperBIContext` M1-06 region **与** `Configurations/Phase31EntityModelConfiguration.cs`（`PhysicalBindingConfiguration` 仍声明 `CK_PhysicalBindings_ExactlyOneOwner`）。仅改 SuperBIContext 不够；移除旧硬 CHECK 必须同步改 Phase31。本次按 M1-02~05 约定**删除**该硬 CHECK（既有 0-owner 种子不兼容），改写入路径强制。
- **另一 gotcha**：`EnsureCreated()`（SQLite 测试）用的是**完整模型**（含所有 IEntityTypeConfiguration），故 Phase31 里的 CHECK 会在测试库生效；且 SQL Server `N'...'` 字面量会令 SQLite `EnsureCreated()` 报 `near "'='": syntax error`——CHECK 字面量须用纯 `'...'`（SQL Server/SQLite 通用）。
- 迁移：`20260905020528_M1_06_PhysicalBindingAuthorizationRls`（先 DROP `CK_PhysicalBindings_ExactlyOneOwner` 再新增约束/索引/FK/列）；因手动改过模型需 `dotnet ef migrations remove` 后重新 `add` 以同步 Designer/ModelSnapshot，否则未来迁移会对已删约束重复 DROP。
- 修复 3 个测试 fake（AskControllerTests/DataSourceAuthorizationServiceTests/QueryPlanWidgetDataResolverTests 的 IDataSourceAuthorizationService 实现）补 `RevokeBySubjectAsync`/`DetectOrphanGrantsAsync` 桩；QueryPlanWidgetDataResolverTests 缺 `using System;` 补之。
- 验证：新增 16 例 `M1_06_PhysicalBindingAuthorizationRlsTests`（16/16）；全量回归 **619/619 通过 0 失败**，构建 0 error。主计划 M1-06 段已标 ✅（本地提交待推送）。

## M2-01 多平台管理员（后端治理 ✅ / Blazor 页面待补）
- 新增 `src/Application/Identity/PlatformAdminService.cs`（`IPlatformAdminService`：List/Add/Disable/Enable/ResetPassword + "至少保留一名有效管理员"守卫 + 审计）、`src/Api/Controllers/PlatformAdminController.cs`（路由 `api/platform-admin`，`GovernanceDataPlanePolicy.IsGovernancePrincipal` 守卫返回 403）、Program.cs 注册 `IPlatformAdminService`。
- **关键 gotcha（必记）**：`IdentityService.ResolveRoleId(s)Async` 刻意排除 `platform-admin`（防普通身份流越权），故 `AddAsync` 必须**直接插入 `UserRole`**（与 `PlatformAdminBootstrapper` 一致），不能走 `CreateUserAsync(roleCodes)` 或 `AssignRoleAsync`——后者对 platform-admin 返回 Fail/null。另：`User` 实体**无** `UserRoles` 导航属性，列表/计数须直接查 `_db.UserRoles`。
- 架构沿用既有底座：`IAuditLogService.LogAsync(AuditLogEntry)`（append-only）、`UserStatus` 枚举、`IdentityService.CreateUserAsync/SetPasswordAsync`（含安全戳轮换）。
- 新增 10 例 `M2_01_PlatformAdminTests`（服务层：列表/新增/幂等复用/停用守卫/启用/重置密码/审计/短口令拒/CreateUserAsync 排除 platform-admin 不变量）；全量回归 **629/629 通过（619+10），0 失败**，构建 0 error。主计划 M2-01 段已更新（后端 ✅，Blazor 页面待补）。
- 待续：M2-01 的 Blazor 管理页面（列表/新增/停用/重置密码/审计视图，跨 Web/Maui/RCL）为后续子项。

## M2-01 Blazor 管理页面 ✅ 完成（含本次提交待推送）
- 新增 RCL 页面 `Components/Components/Pages/Admin/PlatformAdmins.razor`（`@page "/admin/platform-admins"`）：`AuthGuard`+`PermissionGuard(PlatformAdminManage)` 守卫；`SbTabs` 两页签「管理员 / 操作审计」；管理员页签 = `SbDataTable`（用户名/显示名/邮箱/状态徽标/创建时间）+ 行操作「停用·启用 / 重置口令」；新建/重置口令走 `SbModal`，停用·启用走 `SbConfirm` 二次确认；审计页签拉 `api/audit/logs?tenantId={State.TenantId}&entityType=PlatformAdmin`，列 操作/操作人/结果/时间。沿用既有组件与加载约定（数据在 `OnAfterRenderAsync(firstRender)` 拉取，避免 prerender 期 JS/NU）。
- 后端：`PlatformAdminView.Status` 加 `[property: JsonConverter(JsonStringEnumConverter)]`（默认 STJ 按数字序列化枚举，前端徽标需 "Active"/"Disabled" 字符串）；新增客户端权限码 `PermissionCodes.PlatformAdminManage="platform:admin:manage"`，并补入 `IdentityCatalog`（PlatformAdmin 角色 + PermissionDef），种子幂等授予。
- `NavMenuItems` 管理后台组登记「平台管理员」入口（图标 `sb-ico-user`，权限 `PlatformAdminManage`）。
- 测试：`IdentityServiceTests.SeedAsync_Is_Idempotent` 因目录新增 1 权限，断言 31→32（目录增长预期内）。
- 验证：RCL+Web 构建 **0 error / 0 warning**；Maui 三 TFM 构建 **0 error**（111 既有 IL2026/CA1416/CS0618 警告，与本变更无关）；全量回归 **629/629 通过 0 失败**。主计划 M2-01 段标 ✅（含页面）。
- M2-01 全部子项完成，可进入 M2-02。

## M2-02 管理员—租户范围 ✅ 完成（本地提交待推送）
- 需求：默认管理员管理全部租户；新增 `PlatformAdminTenantScope` 支持限定范围；服务端强制校验并记录 Actor/ManagementTargetTenantId/Action/Result/CorrelationId；平台治理身份不得读取租户业务数据。
- 实体 `src/Domain/Identity/PlatformAdminTenantScope.cs`：无行=全部租户（默认）；有行=显式白名单。SuperBIContext 加 `PlatformAdminTenantScopes` DbSet + M2-02 region 配置（表注释/AdminUserId/TenantId 索引/唯一(AdminUserId,TenantId)/GrantedBy maxlen128/双 FK Cascade）。
- 服务 `src/Application/Identity/PlatformAdminScopeService.cs`：`IPlatformAdminScopeService`（`HasFullScopeAsync`/`CanManageAsync`/`GetScopedTenantIdsAsync`/`GetScopeAsync`/`SetScopeAsync`）。`CanManageAsync`：scoped 空=全部，否则 Contains；`SetScopeAsync` 仅保留真实存在租户，幂等删除旧行后写新行（空=恢复默认）。注册 `AddScoped`。
- 控制器 `src/Api/Controllers/PlatformAdminScopeController.cs`：路由 `api/platform-admin`，`GET {id}/scope`、`PUT {id}/scope`（`SetPlatformAdminScopeRequest{tenantIds}`），`RequireManage()` 校验 `perm=PlatformAdminManage`。**需 `using SuperBuilder_AI.Models.Identity;` 否则 CS0103 IdentityPermissions 未定义。**
- 强制校验：`TenantManagementController` ctor 注入 `IPlatformAdminScopeService`；新增 `CallerId()`（NameIdentifier→long）、`RequireInScopeAsync(target,permission,ct)`（先 `RequirePlatformPermission` 再 `CanManageAsync`→403）。应用到 `Get`/`SetEnabledAsync(Enable/Disable)`/`Update`/`ListSettings`/`UpsertSetting`；`List` 按 `GetScopedTenantIdsAsync` 在 count>0 时过滤。
- 审计字段扩展：`AuditLog` 加 `ManagementTargetTenantId`/`CorrelationId`；`AuditLogEntry` 记录尾部加同名可选参；`AuditLogService.LogAsync` 映射；`AuditMiddleware` 写入 `ManagementTargetTenantId: tenantCtx.ManagementTargetTenantId, CorrelationId: traceId`（traceId 取自 HttpContext.Items["CorrelationId"]，由 ObservabilityMiddleware 从 X-Correlation-Id 注入）。
- 治理数据面隔离（需求#3）**已由既有 `AuthMiddleware`+`GovernanceDataPlanePolicy.IsForbiddenDataPlane` 允许清单结构性满足**（治理主体命中非允许 `/api` 路径→403），本步仅确认/记录，不重建。
- 迁移 `20260905030350_M2_02_PlatformAdminScopeAudit`：`AuditLogs` 加 `CorrelationId`(nvarchar max)、`ManagementTargetTenantId`(bigint)；新建 `PlatformAdminTenantScopes` 表 + 双 FK Cascade + 三索引。`dotnet ef migrations add` 工具生成（tools 10.0.10 / runtime 10.0.11，仅有全局查询筛选既有告警，非阻断）。
- 前端 RCL `Components/Components/Pages/Admin/PlatformAdminScopes.razor`（`@page "/admin/platform-admin-scopes"`）：`AuthGuard`+`PermissionGuard(PlatformAdminManage)`；列 用户名/显示名/邮箱/管理范围；行操作「设置范围」→`SbModal`（管理全部租户 checkbox + 租户多选 ToggleTenant）→`PUT api/platform-admin/{id}/scope`。`NavMenuItems` 管理后台组登记「管理员租户范围」(sb-ico-scope)。
- **本步 gotcha（必记）**：① Razor 渲染变量写 `@(code)` 否则 `@code` 被当指令→RZ2005/RZ1017（PlatformAdminScopes.razor:62 修复）。② 测试桩类 `SuccessScopeService`/`ScopedScopeService` 误置于 namespace 顶层且标 `private`→CS1527；改为 `internal sealed`（namespace 层类型不可 private）。③ `List` 测试需 `PlatformTenantView` 权限，默认 Build 用户仅含 `PlatformTenantManage`→403(ObjectResult 非 OkObjectResult)；测试内补 `PlatformTenantView` claim。
- 验证：后端/RCL/Web/Maui 四端构建 **0 error**；新增 3 例 `TenantManagementControllerTests`（越权停用 403 / 授权内停用成功 / 全范围管理员见全部租户）；全量回归 **632/632 通过 0 失败**。主计划 M2-02 段标 ✅。
- 待续：M2-03 首次初始化。

## M2-03 首次初始化 ✅ 完成（本地提交待推送）
- 既有 `PlatformBootstrapController`+`PlatformAdminBootstrapper` 已具备 Loopback 校验、一次性关闭（409）、结构化错误；M2-03 补全缺口：Email 字段、服务端 confirm/email 校验、生产匿名开关、前端表单。
- 后端：`PlatformBootstrapRequest` 增 `Email`/`ConfirmPassword`；`CreateAsync(username,password,displayName,email="",confirmPassword=null,ct)`——交互式（confirmPassword!=null）校验 口令一致+邮箱含'@'+非空，配置路径（confirmPassword=null）邮箱可空；`user.Email=email` 落库。`PlatformBootstrapController` 注入 `IConfiguration`：`AllowAnonymous` 默认 true，false 时 `POST`/`Status.anonymousAllowed` 体现；`Create` 顺序 匿名开关→Loopback→`CreateAsync`。`appsettings.json` 加 `PlatformBootstrap:AllowAnonymous=true`（生产改 false 或 env 覆盖）。
- 前端 `Login.razor`：初始化表单补 Email 输入+客户端邮箱校验、POST 携带 email/confirmPassword、失败保留输入（仅置 Error）、按 `status.anonymousAllowed` 隐藏表单改提示部署配置；`RefreshBootstrapStatus` 读 `anonymousAllowed`。
- 治理数据面/代理信任：沿用 `Program.cs` 受控 `ForwardedHeaders`（仅 KnownProxies/KnownNetworks 消费 X-Forwarded-*，默认不消费）→ `RemoteIpAddress` 即真实连接对端，规避伪造客户端 IP；M2-03 仅确认复用，未改动。
- 测试：新增 `PlatformBootstrapControllerTests`（5：匿名禁用 403 / 非 Loopback 403 / Loopback 成功 / 二次调用 409 / Status.anonymousAllowed 标志）+ `PlatformAdminBootstrapperTests` 扩 4（口令不一致 400 / 邮箱非法 400 / 合法邮箱落库 / 配置路径空邮箱）；共 15 例全过。
- **flaky 提示（非本步引入）**：`RateLimitMiddlewareTests.Expired_Windows_Are_Evicted_To_Prevent_Unbounded_Growth` 依赖后台逐出轮询计时（Delay 250ms），全量并发下偶发失败；隔离运行 7/7 通过，与 M2-03 无关，建议后续将断言改为轮询重试而非固定延时。
- 验证：后端/RCL/Web/Maui 四端构建 0 error；全量回归 640/641（唯一失败为上述 flaky 限流测试），M2-03 新增 15 例全绿。主计划 M2-03 段标 ✅。
- 待续：M2-04 租户事务与生命周期。

## M2-04 租户事务与生命周期 ✅ 完成（本地提交待推送）
- 既有 `TenantManagementController.Create` 已满足 M2-04 全部要求：`BeginTransactionAsync` 单事务内依次落库 Tenant → 2 条锁定 `TenantSetting`(availableCultures/defaultCulture) → `IIdentityService.CreateUserAsync`(首位 TenantAdmin+安全戳) → `SetPasswordAsync`(pbkdf2 口令哈希+安全戳轮换)；任一步失败 `RollbackAsync` 回 Conflict/BadRequest。关键：`IdentityService` 复用控制器注入的同一 `SuperBIContext` → 参与环境事务，回滚覆盖管理员写入。**页面 `Tenants.razor` 已完整**（统计总数/启用/停用、`SbListPage` 编码/名称搜索、创建/编辑弹窗含语言授权与默认语言、启停 `SbConfirm` 影响提示、设置弹窗），无需改动。
- 测试证据（核心交付）：`TenantManagementControllerTests` 新增 2 例。① `Create_WhenAdminCreationFails_RollsBackEntireTenantCreation`——新增 `FailingIdentityService` 桩(`CreateUserAsync` 恒 `Fail`) + `Build(db, IIdentityService)` 重载；断言返回 `ConflictObjectResult` 且 `Tenants`/`TenantSettings` 均为 0 行，证明原子回滚。② `Create_Success_CreatesTenantAdminUserAndSettingsAtomically`——用**真实** `IdentityService(ctx, new PasswordHasher())` 并先 `SeedAsync()` 注入全局角色目录；断言提交后 租户+2 条默认设置+管理员 User(TenantAdmin 角色、pbkdf2 口令、非空安全戳)全部落库，证明原子提交。
- **实现细节提示**：`ResolveRoleIdsAsync` 要求 `TenantAdmin` 角色已存在于 DB（TenantId==0 或租户级），不自动创建；故成功路径测试须先 `SeedAsync()`。真实 `IdentityService` ctor 需 `SuperBIContext`+`IPasswordHasher`（`PasswordHasher` 为零参具体类）。
- 验证：四端(`SuperBulider_AI.slnx`)构建 **0 error**（112 既有 CS8767 可空性告警/Maui IL2026 等，非阻断）；M2-04 类 15 例全过；**全量回归 643/643 通过 0 失败**（此前 flaky 限流测试本次通过）。主计划 M2-04 段标 ✅（含实现要点）。
- 待续：M2-05 多租户成员关系（SB-P1-16）。

## M2-05 多租户成员关系（SB-P1-16）✅ 完成
- 实体/映射：`src/Domain/Identity/UserTenant.cs`（UserId/TenantId/IsDefault/CreatedAtUtc/CreatedByUserId）+ `SuperBIContext` DbSet/配置（唯一约束(UserId,TenantId)、索引、级联 FK）+ 迁移 `20260905035637_M2_05_UserTenant`。主租户(`User.TenantId`)恒为隐式成员、不落表。
- 服务：`ITenantMembershipService`/`TenantMembershipService` 提供 AddMemberAsync(幂等/主租户免记录)/RemoveMemberAsync(主租户不可移除, 抛 InvalidOperationException)/SetDefaultAsync/IsMemberAsync/GetMembershipsAsync/GetSwitchableTenantIdsAsync/**ListAllAsync**(治理面跨租户汇总, 刻意 `IgnoreQueryFilters` 绕过租户作用域过滤器；EF 不能翻译 record 构造器进 OrderBy，故先匿名类型投影→ToListAsync→内存映射)。
- 控制器：`TenantMembershipController` GET/POST/DELETE `api/tenant-membership` + POST `switch` + 新增 `GET admin`(PlatformTenantManage 守卫)。Switch 采用用户确认机制「重签 JWT」：`tid=目标、htid=主租户`；`AuthMiddleware` 改依 `htid` 做用户/安全戳查找(防切换后误判 token 失效)并新增 `htid` 声明 + 「生效租户是否启用」守卫。
- `TokenService` 贯通 `HomeTenantId`/`Htid`；`TokenPrincipal.HomeTenantId`。
- 前端 RCL：`TenantSwitcher.razor`(顶栏切换、主租户标 `·`、切换后 `State` 刷新 + `Nav.NavigateTo(forceLoad)`) 接入 `MainLayout`；新增 `TenantMembers.razor`(`/admin/tenant-members`, PlatformTenantManage 守卫，增删成员关系)；`PermissionCodes.PlatformTenantManage` 新增 + `NavMenuItems` 登记「租户成员」；`ApiClient` 新增 `SwitchTenantAsync`/`TenantSwitchResult`/`AuthResult.HomeTenantId`；`AppState.HomeTenantId`/`AuthStore` 持久化。
- **TenantSwitcher 关键修复**（前序会话遗留 bug，本次构建暴露）：①缺 `@using System.Text.Json`→`JsonValueKind` 未定义；②`Toast(...)` 误用（`Toast` 是 `ToastService` 属性，须 `Toast.Error(...)`）；③`result.AvailableCultures ?? new[]{...}` 类型不匹配(`List<string>?` vs `string[]`)→改 `new List<string>{...}`。
- 测试：`TenantMembershipTests` 新增 3 例(ListAllAsync 仅含显式成员/无权限 403/有权限 200)；`AuthStoreTests.StatusApiClient` 桩补 `SwitchTenantAsync` 以匹配 `IApiClient`。
- **验证**：四端(`SuperBulider_AI.slnx`: 后端/RCL/Web/Maui)构建 **0 error**；全量回归 **646/646 通过 0 失败**（643+3）。主计划 M2-05 段标 ✅（含实现要点），roadmap SB-P1-16 标记 ✅。
- 待续：M2-06 租户自注册（待裁决）/ M2-07 默认种子与演示数据。

## M2-06 租户自注册（默认关闭骨架）✅ 完成（本地提交待推送）
- 按用户裁定以「默认关闭」形式落地骨架：平台开关 + 受控匿名注册端点（默认 off，不对外暴露），待裁决子特性（审批/验证码）以配置占位、开启即拒绝（返回 501 feature_not_implemented），避免不经治理评估对外开放公网（DEC-04 默认关闭，平台按环境开启）。
- 新增：`src/Application/Common/Options/SelfRegistrationOptions.cs`（配置节 `SelfRegistration`，`Enabled` 默认 false；`ApprovalRequired`/`RequireCaptcha`/`AllowedEmailDomains`/`DefaultCulture`/`DefaultAvailableCultures`/`DefaultQuotaPolicyCode` 占位）、`src/Application/Ports/Identity/ISelfRegistrationService.cs`（记录 + 接口 `RegisterAsync`）、`src/Application/Identity/SelfRegistrationService.cs`、`src/Api/Controllers/SelfRegistrationController.cs`（`GET status`/`POST register`/`GET config`，后两者匿名、config 受 `PlatformAdminManage` 守卫）。
- `SelfRegistrationService.RegisterAsync`：开关关闭→`disabled`(403)；待裁决特性开启→`feature_not_implemented`(501)；通过后与 M2-04 一致事务原子创建「租户 + 租户设置(localization) + 首位租户管理员(TenantAdmin) + 口令(pbkdf2)」，读取真实 `SecurityStamp` 后以 `tid=htid=新租户` 重签令牌（注册即登录），审计 `tenant.self-register`；冲突→409。
- `Program.cs` 注册 `Configure<SelfRegistrationOptions>` + `AddScoped<ISelfRegistrationService>`；`appsettings.json` 加 `SelfRegistration:Enabled=false` 等默认。
- 前端 RCL：`ApiClient`/`IApiClient` 增 `RegisterSelfAsync`/`GetSelfRegistrationConfigAsync`；新增 `SelfRegistration.razor`(`/register`, BlankLayout, 查 status→开放时注册并自动登录跳 `/ask`)、`SelfRegistrationAdmin.razor`(`/admin/self-registration`, PlatformAdminManage 守卫只读展示)；`Login.razor` 加「申请开通」入口；`NavMenuItems` 加「自助注册」项。
- 测试：新增 `SelfRegistrationTests` 4 例（关闭→disabled 无数据 / 审批开启→feature_not_implemented 无数据 / 开启→原子创建且令牌非空 / 编码冲突→conflict）；`AuthStoreTests.StatusApiClient` 桩补两方法。
- **RCL 构建 gotcha**：`ApiClient.cs` 中 `out var d` 的 `JsonElement` 不可 `.Value`（非可空），须直接用 `d.EnumerateArray()`（CS1061，跨 net10.0/android/ios 三 TFM）。
- **验证**：四端(`SuperBulider_AI.slnx`)构建 **0 error**；全量回归 **650/650 通过 0 失败**（646+4）。主计划 M2-06 段标 ✅（默认关闭骨架）。
- 待续：M2-07 默认种子与演示数据（DEC-05 已裁定：独立 Demo 安装器，不进生产默认种子）。

## M2-07 默认种子与演示数据 ✅ 完成（本地提交待推送）
- **默认主题种子**（补齐 M0 启动序列缺口）：`src/Application/Seed/ThemeSeedService.cs`（`IThemeSeedService`）+ `Program.cs` 第 4 步（配额之后）`IThemeSeedService.EnsureSeededAsync()`，幂等写入 `Theme(TenantId=0, Key="default", DslJson=BuiltInThemes.DefaultDsl())`。需注意 `DemoDataInstaller`/`ThemeSeedService` 的 `Theme`/`BuiltInThemeKeys` 在 `SuperBuilder_AI.Models.Theme`，缺 `using` 会 CS0103。
- **独立 Demo 安装器（DEC-05：不进启动序列、生产默认不安装）**：`src/Application/Ports/Seed/IDemoDataInstaller.cs`（记录 + 接口 `PreviewAsync`/`InstallAsync`）+ `src/Application/Seed/DemoDataInstaller.cs`（`SuperBuilder_AI.Services.Seed` 命名空间）。`PreviewAsync` 返回 8 项计划（Tenant/User/DataSource/MetadataTable/MetadataColumn×5/MetadataSemantic×5/BusinessEntity/Dashboard），含重复安装保护（按 `TenantCode=="demo"` 判定）；`InstallAsync` 在 `BeginTransactionAsync` 内事务原子创建「租户 + 2 条本地化租户设置 + 管理员(TenantAdmin, pbkdf2, SecurityStamp) + 数据源(MySQL) + 元数据表(sales_order) + 5 字段(各带 Manual 语义) + 业务实体 + 已发布仪表盘(ThemeKey=default, DSL 序列化)」，失败整体回滚，成功后写审计 `demo.seed`。`DemoAdminPassword="Demo@123456"`。
- **控制器**：`src/Api/Controllers/DemoDataController.cs`（`api/demo-data`：`GET preview`/`POST install`）受 `IdentityPermissions.PlatformAdminManage` 守卫（→403）。
- **前端 RCL**：`ApiClient`/`IApiClient` 增 `GetDemoDataPlanAsync`/`InstallDemoDataAsync` + DTO `DemoPlanItem`(含 3 参 ctor)/`DemoInstallPlan`/`DemoInstallResult`；新增 `DemoData.razor`(`/admin/demo-data`, AuthGuard+PermissionGuard)；`NavMenuItems` 加「演示数据」项。
- **测试**：`tests/.../DemoDataInstallerTests.cs` 3 例（预览 8 项 / 事务原子安装幂等落库 / 重复安装返回 already_installed 无重复）。关键坑：① `MetadataColumn` 无 `TenantId`，断言须经父表 `MetadataTable.TenantId` 或 `MetadataTableId` 过滤；② 序列化器按 RFC8259 将非 ASCII 转义为 `\uXXXX`，故 DslJson 断言须解析 `JsonDocument.RootElement.GetProperty("title").GetString()` 而非字面子串；③ 测试原用 `NoopAuditService`（不落库），审计断言改用 `RecordingAuditService` 记录桩验证 `demo.seed` 事件。
- **验证**：四端(`SuperBulider_AI.slnx`)构建 **0 error**；全量回归 **653/653 通过 0 失败**（650+3）。主计划 M2-07 段标 ✅。
- 待续：M3 多语言产品化（M3-G0 起）。

## M3-G0 多语言产品化核心可用子集 ✅ 完成（本地提交待推送）
- **范围**：M4/M7/M8 的最小前置——稳定资源键与加载机制、登录/Layout/NavMenu、共享按钮/验证/错误组件、租户语言范围、用户语言服务端恢复。不等同于完成全部 i18n（M3-01~06 仍为发布门禁）。
- **G0-A 权限码**：`IdentityPermissions` 新增 `localization:view`/`localization:manage`；`IdentityCatalog.Permissions` 全局 32→34；`PlatformAdmin`+`TenantAdmin` 角色种子纳入；`LocalizationController` 守卫切到 `LocalizationManage`/`LocalizationView`；`PermissionCodes.cs`/`NavMenuItems.cs` 同步；`IdentityServiceTests` 权限数 32→34。
- **G0-B 实体+迁移**：`UserLanguagePreference(TenantId,UserId,Culture)`，唯一索引 `(TenantId,UserId)`，`Culture` 限长16；迁移 `20260905060434_M3G0UserLangPref`（无设计期工厂，`dotnet ef` 须从 `SuperBuilder_AI/` 目录运行）。
- **G0-C 服务**：`IUserLanguagePreferenceService`/`UserLanguagePreferenceService`，按租户 `localization:availableCultures`/`defaultCulture` 校验（越界回退 available[0]）+ upsert + 审计 `user.language.set`。
- **G0-D 控制器**：`UserPreferenceController` `api/user/preferences/language` GET/PUT，匿名 401、越界回退仍 200、返回服务端生效文化。
- **G0-E 前端**：`IApiClient`/`ApiClient` 增 `GetUserLanguageAsync`/`SetUserLanguageAsync`；`LocalizationService.InitializeAsync` 回退链「服务端偏好→localStorage→租户默认」，`SetCultureAsync` 同步持久化（仅登录用户）。
- **G0-F 共享切换器**：`LanguageSwitcher.razor`（仅 `AvailableCultures>1` 显示，原生名）；登录与 `MainLayout` 内联 `<select>` 已替换。
- **G0-G 组件**：`FieldError.razor`、`ValidationSummary.razor`（role=alert / validation-summary）。
- **G0-H 键注册表+5语种子**：`ResourceKeys`（Common/Login/App/Document/Validation/Error/Empty，`All()` 反射枚举）；`LocalizationSeedService.defaults` 扩至 5 语言（zh-CN/zh-TW/en-US/ja-JP/ko-KR）各 17 键；新增 `EnsureSeedAsync_Covers_Registered_ResourceKeys`。
- **关键决策**：PlatformAdmin 治理不变量（`Assert.StartsWith("platform:")` 改为 `platform: || localization:`）——`localization:*` 视为平台治理面，不授予任何租户业务能力（dashboard/app/agent/metadata 等），租户目录隐藏 8 项 `platform:*` 故全局 34−8=26（`IdentityControllerTests` 断言 24→26）。
- **验证**：四端(`SuperBulider_AI.slnx`)构建 **0 error**；全量回归 **661/661 通过 0 失败**（653+8）。主计划 M3-G0 段标 ✅。
- 待续：M3-01~06（关系模型/平台语言维护/租户覆盖/键治理/全页接入/偏好硬化）作发布门禁；之后 M4 数据源与元数据闭环。

## M3-01 语言关系模型 ✅ 完成（已本地提交 f4de6b3，待推送 origin/master）
- **M3-01-A 实体+迁移**：`src/Domain/Localization/TenantUiLanguage.cs`（`BaseEntity` 派生，`Enabled`/`IsDefault`/`SortOrder`）；`SuperBIContext` 加 `TenantUiLanguages` DbSet + 配置（唯一索引 `(TenantId,UiLanguageId)`、索引 `(TenantId,IsDefault)`、级联删租户、限制删语言）；迁移 `20260905064209_M3_01_TenantUiLanguage`。
- **M3-01-B 服务+约束**：`ITenantLanguageService`/`TenantLanguageService`/`TenantLanguageException`。`SetLanguagesAsync` 强制（≥1 启用、恰 1 默认、默认须启用、禁用平台语言不可启用→`TenantLanguageException`→控制器 400）；`GetAvailableCulturesAsync`(Enabled 文化, 空回退 zh-CN)/`GetDefaultCultureAsync`(IsDefault&&Enabled→首启用→zh-CN)/`GetLanguagesForTenantsAsync`(批量租户列表用)；`EnsureTenantLanguagesAsync`(按 `localization:*` JSON 或平台默认 zh-CN 幂等播种)/`EnsureAllTenantsLanguagesAsync`(启动序列调用)/`DisablePlatformLanguageAsync`(迁移默认租户→首其他启用语言，单语言租户回退首个其他启用平台语言新建关系，并停用所有租户侧该语言行)。
- **M3-01-C 启动播种**：`Program.cs` 受控启动序列第 4.5 步（主题种子后、`Bootstrap` 前）调用 `EnsureAllTenantsLanguagesAsync`，独立异常隔离，幂等。
- **M3-01-D 消费者改走服务**：`AuthController`(登录 locale / LoginOptions 批量)、`TenantMembershipController`(切换 locale)、`TenantManagementController`(List 批量投影 / Create / Update)、`LocalizationController.AllowedCultures`、`UserLanguagePreferenceService`、`SelfRegistrationService`、`DemoDataInstaller` 全部经 `ITenantLanguageService`；不再写 `localization:availableCultures/defaultCulture` JSON。`TenantSettingPolicy` 锁定键（含 `localization:*`）保持不变以兼容 `TenantSettingPolicyTests`（JSON 仅作播种期向后兼容读）。
- **新增测试**：`TenantLanguageServiceTests` 13 例（约束/JSON 迁移/幂等/停用迁移/单语言回退/已停用幂等）。既有 `UserLanguagePreferenceServiceTests`/`UserPreferenceControllerTests`/`TenantManagementControllerTests`/`SelfRegistrationTests`/`DemoDataInstallerTests` 改为基于 `TenantUiLanguage`+`UiLanguage` 行断言。
- **验证**：四端(`SuperBulider_AI.slnx`)构建 **0 error**（127 既有警告）；全量回归 **674/674 通过 0 失败**（661+13）。主计划 M3-01 段标 ✅。
- 待续：M3-02~06 发布门禁；之后 M4 数据源与元数据闭环。

## 停止后台 git 推送（用户指令）
- 用户要求停止后台 push。核查结果：**无任何 git 进程**（git.exe / git-remote-https.exe / ssh.exe 均无），此前那次 `git push origin master` 已因 `could not read Username for 'https://github.com'` 同步失败退出（EXIT=128），从未转入后台。
- **沙箱网络到 github.com 不通**：`git ls-remote origin master` 25s 超时（SIGTERM）。因此后续推送只能由用户在本地终端执行。
- **同步状态**：`git rev-list --count origin/master..HEAD` = 0，`HEAD..origin/master` = 0，本地 tip = `f4de6b3`（M3-01）。即 M0-09 明细补列那批修复已随 M1/M2/M3 一起在远端（hash 因 rebase 变为 `701e298`/`cf43bd8`/`60212d9`/`8866f83`/`6845a5f`/`e7647df`，摘要里记的 `0557519` 等旧 hash 已失效，勿再引用）。代码在：`QueryPlanBuilder.Helpers.cs:94 IsDetailListQuery` + `QueryPlanBuilder.cs:811/1395`。
- **未提交工作（M3-02 进行中）**：9 改 + 5 新增，含 `PlatformLanguageService`(Application/Localization)、`IPlatformLanguageService`(Ports)、`PlatformLanguageException`(Domain)、迁移 `20260905072839_M3_02_UiTextResource_IsTranslated`、`PlatformLanguageServiceTests`。处理待用户决定。
- **垃圾文件**：工作区根目录有 0 字节 `nul`（Windows 下 `2>nul` 重定向误生成），建议删除或加入 .gitignore（未擅自删除）。

## M3-02 平台语言维护 ✅ 完成（本地提交待推送 origin/master）
- **M3-02-A 基础+迁移**：`LocaleContext.NormalizeCulture` 公开（BCP 47 归一化 `_`→`-`、语言小写、区域大写）；`UiTextResource` 增 `IsTranslated`(默认 true) + 迁移 `20260905072839_M3_02_UiTextResource_IsTranslated`（Designer+Snapshot）+ 种子置 true。
- **M3-02-B 服务**：`IPlatformLanguageService`/`PlatformLanguageService`/`PlatformLanguageException`（namespace `Models.Localization`/`Services.Localization`/`Interfaces.Localization`）。`CreateLanguageAsync` 归一化→唯一性→必填 DisplayName/NativeName→追加并复制 `CopyFromCulture`(默认 en-US) 平台基线键集合且标记 `IsTranslated=false`；`SetEnabledAsync(false)` 委托 `TenantLanguageService.DisablePlatformLanguageAsync` 完成租户默认语言迁移；`ReorderLanguagesAsync` 按给定 Id 序赋 `SortOrder`；`ListLanguagesAsync`/`GetLanguageAsync` 统计 `TotalKeys`/`TranslatedCount`（按 `TenantId==0` 分组）。
- **M3-02-C 控制器+DI**：`LocalizationController` 新增 `GET admin/languages`(含停用)、`GET admin/languages/{id}`、`PUT languages/{id}`、`POST languages/{id}/enabled`(`LanguageEnabledRequest{Enabled}`)、`POST languages/reorder`(`ReorderLanguagesRequest{OrderedIds}`) 五端点，均 `LocalizationManage` 守卫；`Program.cs` 注册 `IPlatformLanguageService`；`PublicTexts`/`Texts` 投影新增 `IsTranslated`。注意：`SetEnabledRequest` 已存在冲突，本步新记录改名 `LanguageEnabledRequest`。
- **M3-02-D 前端**：`IApiClient`/`ApiClient` 增 6 方法 + DTO `AdminLanguageView`/`AdminLanguageCreate`/`AdminLanguageUpdate`/`PublicLanguageView`（`Models/LocalizationModels.cs`，`PendingCount=TotalKeys-TranslatedCount`）；`Localization.razor` 改强类型目录（启停/上移下移/改名弹窗/待翻译徽标，平台视角含停用项）；`LanguageSwitcher`+`LocalizationService` 从 `public/languages` 动态取 `NativeName`，移除硬编码 5 语言映射。
- **M3-02-E 测试+验证**：新增 `PlatformLanguageServiceTests` 13 例（归一化 es_es→es-ES/非法/重复/必填/复制待翻译/列表排除停用/查看统计/改名/启停委派/排序）+ `LocalizationControllerAdminTests` 4 例（声明守卫 403/含停用项/复制待翻译/非法 Culture 400）；`AuthStoreTests.StatusApiClient` 桩补 6 方法；`LocalizationControllerPublicTextsTests` harness 注入 `PlatformLanguageService`。**验证**：后端 + 三端(Components/Web/Maui) 构建 **0 error**；全量回归 **691/691 通过 0 失败**（674+13+4）。主计划 M3-02 段标 ✅。
- **待续**：M3-03~06 发布门禁；之后 M4 数据源与元数据闭环。本地提交须排除 `.workbuddy/` 与根目录 `nul`（stage `SuperBuilder_AI/src tests/SuperBuilder_AI.Tests docs`）。

## M3-03 租户文本覆盖 ✅ 完成（本地提交待推送 origin/master）
- **核心不变量已由 M3-G0/M3-01 落地**：`LocalizationController.Languages` 对租户用户经 `ITenantLanguageService.GetAvailableCulturesAsync` 按 `TenantUiLanguage` 关系过滤（平台启用的非授权语言不出现）；`Texts` 合并平台基线(tenantId=0)+租户覆盖并标注来源；`SaveText` 写租户覆盖置 `IsTranslated=true`；`ResetText` 删租户覆盖恢复继承。前端 `Localization.razor` 非平台分支展示 platformValue/value/isOverridden + 「租户专属」恢复继承。
- **本次补强**：新增 `LocalizationControllerTenantTests` 2 例（租户仅见授权语言 / 平台见全部启用语言）锁定"租户只见被授权语言"；主计划 M3-03 段标 ✅。
- **验证**：全量回归 **693/693 通过 0 失败**（691+2）；四端构建 0 error（M3-03 仅改测试+文档，无源码改动）。
- **待续**：M3-04 资源键治理、M3-05 全页面接入、M3-06 用户语言偏好硬化；之后 M4 数据源与元数据闭环。本地提交（M3-02 `89d5862` + M3-03 `b716b13`）均待用户推送 origin/master。

## M3-04 资源键治理 ✅ 完成（本地提交待推送 origin/master）
- **M3-04-A 资源键目录元数据**：`src/Domain/Localization/ResourceKeys.cs` 新增嵌套分组 `Nav`(Dashboard/Ask/DataSources/Admin)、`Accessibility`(SkipToContent)、`Theme`(Light/Dark)；新增 `public sealed record ResourceKeyMeta(string Module, string? Page=null, string? DefaultValue=null, bool Deprecated=false)` 与 `public static IReadOnlyDictionary<string, ResourceKeyMeta> Catalog` 字典（键→元数据，含 Module/Page/DefaultValue），为后续 CI 键空间扫描提供权威注册表。`All()` 不变。
- **M3-04-B 键覆盖与种子补齐**：`src/Application/Platform/LocalizationSeedService.cs` 在 `defaults` 为上述 7 个新键补齐 zh-CN/zh-TW/en-US/ja-JP/ko-KR 五语默认译文，与 `Catalog.DefaultValue` 一致；种子完整性测试 `EnsureSeedAsync_Covers_Registered_ResourceKeys` 仍全绿（键注册表与种子双向校验）。
- **M3-04-C 占位符一致性校验**：新增 `src/Application/Localization/LocalizationPlaceholderValidator.cs`（`Regex \{([0-9]+)\}` 提取 `{n}` 占位符，`ExtractPlaceholders` + `AreConsistent` 比对基线/译文占位符集合）；`LocalizationController.SaveText` 在租户覆盖（targetTenant>0）写入前强制校验，不一致返回 400（中文错误文案含基线/译文占位符集合），平台基线（tenantId=0）免于校验。
- **M3-04-D UiTextResource 租户过滤纵深防御**：`src/Infrastructure/Persistence/SuperBIContext.cs` 为 `UiTextResource` 增加全局 QueryFilter（`!_tenantFilterEnabled || TenantId==_scopedTenantId || TenantId==0`），与 `SemanticLabel`/`Dashboard` 同模式；`LocalizationController` 的 `Texts`/`SaveText`/`ResetText` 经 `_db.ApplyTenantScope(targetTenant)` 显式启用作用域（传 0 即 no-op，保留平台基线视图）。须 `using SuperBuilder_AI.Services.Localization;`。
- **新增测试**：`tests/SuperBuilder_AI.Tests/LocalizationControllerTextsTests.cs` 3 例（占位符匹配成功/不匹配 400 拒绝/平台基线免校验）+ `tests/SuperBuilder_AI.Tests/UiTextResourceQueryFilterTests.cs` 2 例（未启用作用域见全部 / 启用后仅见 0+本租户）；**验证**：后端 + 三端(Components/Web/Maui) 构建 **0 error**（Maui 127、Web 0 警告）；全量回归 **698/698 通过 0 失败**（693+5）。主计划 M3-04 段标 ✅。
- **范围说明（递延）**：PlatformStrings/UiTextResources/前端 fallback 键空间合并、CI 硬编码/未知键/缺失译文/废弃键扫描、`localization:view/manage` 显式权限码与角色种子/菜单守卫统一，本轮未纳入，由 M3-05（全页面接入）与后续迭代承接。
- **待续**：M3-05 全页面接入、M3-06 用户语言偏好硬化；之后 M4 数据源与元数据闭环。本地提交（M3-02 `89d5862` + M3-03 `b716b13` + M3-04）均待用户推送 origin/master。

## M3-05 全页面接入（续批：共享反馈组件 + 页头稳定键 + 防漂移护栏）✅ 完成（本地提交待推送 origin/master）
- **背景**：M3-05 基础提交（`9923bc4`）已含基础设施+核心外壳+后端错误本地化，但共享反馈组件与页头尚未接入 L10n，且缺防漂移护栏（上轮已暴露种子/注册表脱节）。本批补齐。
- **M3-05-E 防漂移护栏**：新增 `tests/SuperBuilder_AI.Tests/ResourceKeyRegistryTests.cs` 6 例，校验「后端 `ResourceKeys` 常量 ↔ `Catalog` 元数据 ↔ zh-CN `ZhCnDefaults` ↔ RCL `Keys`/`Defaults`」四处方严格一致（键集合 + 中英双语文案）。护栏首次运行即抓出 `Nav.Dashboard`/`Nav.Admin` 缺失 zh-CN 基线，已补（避免清理遗留 hand dict 时静默退化成英文）。
- **M3-05-F 共享反馈组件本地化**：`ErrorState`/`LoadingState`/`SbEmptyState`/`SbAlert`/`PageHead` 接入统一 L10n（`Error.OperationFailed`/`CodeLabel`/`TraceIdLabel`/`Common.Retry`/`Close`/`Loading`/`Empty.NoData` 等），全部保留原中文 fallback 无回归。注意：组件新增 `@inject LocalizationService L10n` 后，**bUnit 组件测试须注册该服务**——否则渲染时 `L10n.T` 空引用。
- **M3-05-G 页头稳定键**：`PageHead` 新增稳定 `Key` 参数解析 `Page.Title.{Key}`（旧 `Page.Title.{中文标题}` 约定会退化成中文键、必然查不到，作为未迁移页回退）；登记 `Page.Title.*` 13 个键（五处同步：后端 `ResourceKeys` 常量+`Catalog`、`ZhCnDefaults`、RCL `Keys`/`Defaults`），并迁移工作台/管理后台 13 个页面 `<PageHead Key=...>`（Home/Profile/Tenants/TenantMembers/Identity/Quota/PlatformAdmins/PlatformAdminScopes/SelfRegistrationAdmin/SystemStatus/Localization/Themes/DemoData）。
- **测试修复**：`AuthGuardTests`/`SbDataTableTests` 补构造函数注册 `LocalizationService`（`new LocalizationService(null!, null!)`，RCL 版构造需 `IJSRuntime`+`IApiClient` 二参，测试工程无 Moq 故传 null!；`T()` 不触碰二者故安全）；`SbDataTableTests` 补 `using Microsoft.Extensions.DependencyInjection;`（否则 `AddSingleton` 扩展方法找不到，误报 `BunitServiceProvider` 缺方法）。
- **验证**：后端 + RCL + Web + Maui 四端构建 **0 error**；全量回归 **708/708 通过 0 失败**（698 基线 + M3-05-E 6 例，其中 3 例 AuthGuard/SbDataTable 因 L10n 缺失一度失败、已修）。主计划 M3-05 段补 E~G 交付清单。
- **仍递延**：Ask/Dashboards/Apps 详情页、BusinessModel/ComponentGallery/ThemeEditor/DataSources 列表等重内容页全量 L10n；前端按 `L10n.T("Error."+code, serverMessage)` 消费 `ApiError` 的友好提示层。
- **待用户推送**：本地领先 `origin/master` 现 **5 个提交**（M3-02 `89d5862`、M3-03 `b716b13`、M3-04、M3-05 基础 `9923bc4`、本批 M3-05 续批）。

## M3-05 续批2（内容页页头+共享动词）
- 登记 `Page.Title.*`+`Page.Desc.*` 18 键 + `Action.*`(10) + `Page.ThemeEditor.*`(2)，均五处同步（后端常量/Catalog、ZhCnDefaults、RCL Keys/Defaults）；`ResourceKeyRegistryTests` 护栏 8/8 通过（含种子覆盖）。
- 迁移 8 个静态标题内容页 `<PageHead Key=...>`（BusinessModel/BusinessModelEntityDetail/SemanticLabelDetail/ComponentGallery/ThemeEditor/DataSources/ModelAccounts/MetadataEntityDetail）；动态标题详情页不迁。
- `_Imports.razor` 全局注入 `LocalizationService L10n`（后续页面批次免逐页 @inject）。接入 ThemeEditor(浅色/深色/重置/保存主题/实时预览/调色板)、BusinessModel(新建实体)、DataSources(刷新/取消/保存并接入)。
- **修复**：`Catalog` 用索引器 `[k]=v` 语法，既有重复键（Empty.NoData/Nav.Dashboard）为覆盖语义不抛异常；新增 Action.Reset/Page.ThemeEditor.* 常量须同步加后端与 RCL `Page` 类否则 CS0117。
- **验证**：四端 0 error；全量 708/708 通过。仍递延：内容页正文/表格/状态标签全量 L10n + ApiError 前端友好提示层。

## M3-05 重内容页正文批次（BusinessModel / DataSources 家族）✅ 完成（本地提交待推送 origin/master）
- **范围**：用户"继续重内容页正文批次"，把 BusinessModel/DataSources 两个列表页及其详情页的静态正文全量接入 L10n（此前仅页头+少数动词）。
- **注册表**：后端 `ResourceKeys` 新增单级扁平常量类 `Content`（约 101 键，键名含页面前缀如 `Content.BusinessModelDomains`）；**关键约束**——必须单级（一层嵌套），否则 `All()` 的 `GetNestedTypes().SelectMany(GetFields)` 反射枚举会漏掉二级嵌套类导致护栏失败。`Catalog` 每键登记 en-US `DefaultValue`、`ZhCnDefaults` 补 zh-CN、RCL `Keys.Content` 常量 + `Defaults`(ZhCn/EnUs) 五处严格同步。
- **四页改写**：`BusinessModel.razor`(统计块/页签/搜索/空状态/无匹配/详情按钮/编辑器未就绪)、`BusinessModelEntityDetail.razor`(加载/面板/空状态/返回)、`DataSources.razor`(指标卡/连接资产/搜索/加载/空状态/**表格列 ID·名称·类型·状态·元数据表·字段**/**状态标签 运行中·已停用**/租户专属/管理连接/接入模态框/连接器描述/字段/保存并接入/各 Toast)、`DataSourceDetail.razor`(元数据·访问授权·行级安全三页签全部标签、表头、状态文案、授权/RLS 空状态与增删改 Toast)。Toast 含占位符用 `string.Format(L10n.T(key,"...{0}..."), arg)`。
- **踩坑（必记）**：`DataSources.razor` 的 `ConnectorDesc` 初版写成 `private static`，静态方法无法访问实例字段 `L10n` → CS0120 ×18；改为实例方法（`private string ConnectorDesc`）即解。另：新增键 `DataSourceLoadGrantsFailed` 因首版漏登记，必须五处补齐否则护栏 `All_RegisteredKeys_HaveCatalogMetadata`/`HaveZhCnBaseline` 红。
- **验证**：后端+RCL+Web+Maui 四端构建 **0 error**；`ResourceKeyRegistryTests` 6/6 护栏全绿（证明 101 新键五处零漂移）；**全量回归 708/708 通过 0 失败**。主计划 M3-05 段补 J 交付项，并收敛"仍递延"为 ModelAccounts/SemanticLabelDetail/ComponentGallery/ThemeEditor/MetadataEntityDetail 正文 + Ask/Dashboards/Apps 详情 + ApiError 前端提示层。
- **待用户推送**：本地领先 origin/master 现 **6 个提交**（M3-02 `89d5862`、M3-03 `b716b13`、M3-04、M3-05 基础 `9923bc4`、M3-05 续批 `0d1cef4`、M3-05 续批2 `6ce26c5`、本批）。按卫生规则提交排除 `.workbuddy/` 与根 `nul`。

## M3-05-M 残余硬编码中文扫描与本地化（增量批次，进行中）
- **扫描**：脚本扫描 `SuperBuilder_AI.Components` 全部 razor，去噪（剔除 `@* *@`/`//`/`/// <summary>`）后共 **614 处**用户可见中文串 / 53 文件。Admin 区域约 355 行（12 文件）为最大缺口，优先级最高；其次 Analysis 余页与 Account/Platform。
- **键命名纪律（关键）**：必须**扁平 camelCase**（如 `AdminIdentityTitle`、`CommonRefresh`），**严禁点号**（点号在 C# 标识符非法，且会破坏 `All()` 单层嵌套枚举护栏）。资源键字符串值可含点（`"Content.AdminIdentityTitle"`）。通用 CRUD 文案复用 `Common*` 键压低键数。
- **注册表插入坑（已踩）**：生成脚本的锚点必须匹配**整行**（含值与尾逗号），否则会把值挤到块之后成为游离行（RK catalog / ZH dict / KS defaults 曾因此损坏，已 git checkout 后用「整行锚点」重做修好）。安全锚点：`公共 const` 用 `public const string \w+ = "Content\.\w+";`；`Catalog` 用 `\[Content\.[\w.]+] = new\("Content", Page: "[^"]*", DefaultValue: ".*"\),`；`ZhCnDefaults` 用 `\["Content\.[\w.]+"\] = .+",`；`Keys.Defaults` 用 `\[Content\.[\w.]+] = new\("[^"]*", "[^"]*"\),`。
- **Batch 1 = `Admin/Identity.razor`**：新增 61 键（44 `AdminIdentity*` + 17 `Common*`，五处同步）。页标题经既有 `Page.Title.Identity` 机制本地化；描述因 `Page.Desc.Identity` 未注册，改用 `AdminIdentityDesc` 键（PageHead 回退到该属性值）。`@code` 中 Toast/错误提示用 `string.Format(L10n.T(key,...), arg)`。RCL 构建 0 error；`ResourceKeyRegistryTests` 6/6；全量回归 708/708。
- **待续批次顺序**：PlatformAdmins / Tenants / Localization / TenantMembers / DemoData / PlatformAdminScopes / SelfRegistrationAdmin / Audit / SystemStatus / Quota / Themes → Analysis 余页（SemanticLabels/Agent/AgentPlanDetail/DashboardDetail）→ Account/Platform（SelfRegistration/DataSources/ModelAccounts）。每批：生成键→接线→RCL 构建→护栏→（末批前）全量回归→提交（排除 `.workbuddy/` 与 `nul`）。
- **Batch 2 = `Admin/PlatformAdmins.razor`** ✅：新增 46 个 `AdminPlatformAdmins*` 键（五处严格同步：后端常量 + `Catalog` + `ZhCnDefaults`、RCL `Keys.Content` + `Keys.Defaults`，经 `C:/tmp/gen_pa_l10n.py` 单数据源生成脚本插入，规避手误漂移）。用户可见文本全部接入 L10n：页描述（经 `AdminPlatformAdminsDesc`；`Page.Desc.PlatformAdmins` 未注册故显式 `Desc` 回退，与 Identity 同模式）、页签/面板标题、空状态、新建/重置模态框字段与占位符、停用/启用 `SbConfirm`、表格列（用户名/显示名/邮箱复用 `Common*`；状态/创建时间/操作/操作人/结果/时间新增）、行操作与全部 `@code` 中 Toast/错误提示（含 `err ?? string.Format(L10n.T(key), status)` 友好层透传）。审计操作类型标签 `platform.admin.*` → `AdminPlatformAdminsAct*` 亦本地化；`ActionLabel` 由 `static` 改实例方法以接入 `L10n`。校验：RCL 0 error、护栏 6/6、全量回归 708/708、四端构建（`SuperBulider_AI.slnx`）0 error，均通过。本地提交待推送（排除 `.workbuddy/` 与 `nul`）。
- **Batch 3 = `Admin/Tenants.razor`** ✅：新增 67 个 `AdminTenants*` 键（五处严格同步：后端常量 + `Catalog` + `ZhCnDefaults`、RCL `Keys.Content` + `Keys.Defaults`，`Catalog.Page` 统一 `Tenants`；经 `C:/tmp/gen_tenants_l10n.py` 单数据源生成脚本插入）。用户可见文本全部接入 L10n：页描述（`AdminTenantsDesc`；`Page.Desc.Tenants` 未注册故显式 `Desc` 回退）、列表/搜索/空状态/加载态、新建/编辑/设置三套模态框字段与占位符/提示、统计磁贴、行操作、SbConfirm 标题/正文/确认按钮（标题用 `AdminTenantsEnableTitle/DisableTitle`、正文 `AdminTenantsConfirmMsg` 带 {0}{1} 插值、确认按钮复用 `AdminTenantsEnable/Disable`）、表格列与全部 `@code` Toast/错误提示（409 冲突 `租户编码已存在`/`配置 Key 已存在` 落专属键，通用 `保存失败` 复用 `AdminTenantsSaveFailed`）。通用 CRUD 复用 `CommonCancel`/`CommonSave`；`Page.Title.Tenants` 已注册故页标题自动本地化。踩坑：初版漏登记 `AdminTenantsEnableTitle/DisableTitle`（SbConfirm 标题用）致 CS0117，已补入 DATA 重跑脚本。校验：RCL 0 error、护栏 6/6、全量回归 708/708、四端构建 0 error，均通过。本地提交待推送（排除 `.workbuddy/` 与 `nul`）。
- **Batch 4 = `Admin/Localization.razor`** ✅：新增 51 个 `AdminLocalization*` 键（五处严格同步：后端常量 + `Catalog` + `ZhCnDefaults`、RCL `Keys.Content` + `Keys.Defaults`，`Catalog.Page` 统一 `Localization`；经 `C:/tmp/gen_loc_l10n.py` 单数据源生成脚本插入）。用户可见文本全部接入 L10n：页描述（`AdminLocalizationDesc`；`Page.Desc.Localization` 未注册故显式 `Desc` 回退，与 Identity/Tenants 同模式）、语言目录侧栏/待翻译/已停用芯片与上下移/编辑/启停图标 title、添加语言按钮、系统文本区说明/徽章/加载态、文本资源行的平台默认/平台文本/租户显示文本/租户专属/恢复继承/基线/已继承、添加/编辑语言与新增文本键三套模态框字段与占位符、全部 `@code` Toast/错误提示（`保存失败`/`创建失败`/`操作失败`/`排序失败` 等落专属键，通用 取消/保存 复用 `CommonCancel`/`CommonSave`）。`Page.Title.Localization` 已注册故页标题自动本地化；`租户专属` 等全角引号（「」）在五处均正确保留（整行锚点插入，无损坏）。`TEXT RESOURCE MATRIX` 为英文装饰 eyebrow，维持原样。校验：RCL 0 error、护栏 6/6、全量回归 708/708、四端构建 0 error，均通过。本地提交待推送（排除 `.workbuddy/` 与 `nul`）。
- **Batch 5 = `Admin/TenantMembers.razor`** ✅：新增 39 个 `AdminTenantMembers*` 键（五处严格同步：后端常量 + `Catalog` + `ZhCnDefaults`、RCL `Keys.Content` + `Keys.Defaults`，`Catalog.Page` 统一 `TenantMembers`；经 `C:/tmp/gen_tm_l10n.py` 单数据源生成脚本插入）。用户可见文本全部接入 L10n：页描述（`AdminTenantMembersDesc`；`Page.Desc.TenantMembers` 未注册故显式 `Desc` 回退）、列表标题/搜索框/空状态/加载态、添加成员按钮、三个统计磁贴、行操作、添加成员模态框字段与占位符/提示、SbConfirm 标题/正文/确认按钮、表格列与单元格状态徽标，以及全部 `@code` 中校验错误与 Toast 提示。通用 取消/添加 复用 `CommonCancel`/`CommonAdd`；`Page.Title.TenantMembers` 已注册故页标题自动本地化；SbConfirm 正文用带 `{0}{1}{2}` 插值的 `AdminTenantMembersRemoveMsg`。校验：RCL 0 error、护栏 6/6、全量回归 708/708、四端构建 0 error，均通过。本地提交待推送（排除 `.workbuddy/` 与 `nul`）。
- **待续批次顺序（剩余 Admin 区）**：DemoData / PlatformAdminScopes / SelfRegistrationAdmin / Audit / SystemStatus / Quota / Themes → Analysis 余页（SemanticLabels/Agent/AgentPlanDetail/DashboardDetail）→ Account/Platform（SelfRegistration/DataSources/ModelAccounts）。每批：生成键→接线→RCL 构建→护栏→（末批前）全量回归→提交（排除 `.workbuddy/` 与 `nul`）。

## M3-05-M 剩余规模盘点与「能否一次全量处理」评估（2026-09-05 晚）
- **规模实测**：48 文件 / 354 行残余中文；这些文件代码总行数 **5481 行 / 240,835 字符**（≈80K tokens 仅读取，读+写 ≈160K）。**残余行少 ≠ 成本低**——真正的成本是承载它们的代码体量（如 Ask.razor 仅剩 1 行残余却需处理 364 行文件）。
- **结论：不能一次全做**。硬约束＝上下文预算（全量重写 ≈160K tokens，超出单会话可靠工作预算，且还要留给构建/测试输出与五处注册表读写）；软约束＝错误排查成本非线性（Batch3 漏 2 键、Batch5 漏 1 处 SbListPage Title，仅在小批量时可秒定位）。
- **扫描口径修正（重要，避免误改）**：① `PageHead` 的 `Title="..."` 回退值**属设计保留**，非缺陷——`Page.Title.<Key>` 已注册时 PageHead 内部按键解析，外部再包 `L10n.T` 是重复且可能与 Key 机制冲突；扫描将其计入残余属误报（涉及 PlatformAdmins/BusinessModel/ComponentGallery/DataSourceDetail 等）。② 数据定义中的中文（如 `Ask.razor:337 name = "结果"`）是业务约定，非 UI 文案，不应本地化。
- **系统性陷阱（未动手，务必先记）**：`[Parameter] public string X { get; set; } = "中文"` **不能直接改成** `= L10n.T(...)`——字段初始化器不能引用实例属性 `L10n`（CS0236/CS0120，同 Batch1 `AppDetail._title` 坑）。共享组件默认值须改为：① `= null` + 渲染处 `?? L10n.T(...)` 兜底，或 ② 在 `OnInitialized` 内赋值。涉及 SbConfirm(确认操作/确认/取消)、SbListPage(刷新/搜索…/暂无数据)、SbSearch(搜索…/aria-label)、SbPagination(上一页/下一页/第X页·共Y条)、DataSourcePicker(数据源)、PermissionGuard(DenyText)、SqlViewer、AuthGuard、ResultTable 等。
- **分层加速策略（推荐替代逐文件 48 批，可压到约 8-10 批）**：
  - **L1 共享组件默认值（约 12 文件 / ~25 行）ROI 最高**——改一处全站受益，且多数可复用既有 `Common*` 键（确认/取消/搜索/刷新/暂无数据/上一页/下一页/关闭），实际新键很少；须先解上述字段初始化陷阱。
  - **L2 低密度页面（约 15 个大文件各剩 1-2 行 / ~20 行）**：用 Grep 定位 + `Read(offset/limit)` 局部读 + Edit 定点替换，**不读全文**，可将 300 行文件的读取成本从 ~14.7K 字符降到数百字符；一次会话可打包 6-8 个文件。
  - **L3 高密度页面（18 文件 / 292 行）**：必须 Read 全文 + Write 重写，按区域分组：Analysis 四大页(SemanticLabels36/Agent26/AgentPlanDetail21/DashboardDetail18 ≈101 行) → Admin 七页(≈102 行) → Account(SelfRegistration29/Profile12) → 其余。
- 扫描脚本：`C:/tmp/scan_remaining.py`（分组统计）、`C:/tmp/scale_check.py`（规模/token 估算）；明细 `C:/tmp/remaining_cn.txt`。

## M3-05-M L1 层（共享组件默认值本地化）✅ 完成（本地提交待推送）
- **策略切换**：用户采纳分层方案，由「逐文件 48 批」改为 **L1(共享组件) → L2(低密度页面) → L3(高密度页面)**。
- **范围**：`Shared/UI`+`Shared/Guard`+`Shared/BI`+`Shared/Analysis`+`Layout` 共 13 个共享组件，新增 **31 个 `Shared*` 键**（五处同步，`Catalog.Page="Shared"`，脚本 `C:/tmp/gen_l1_l10n.py`）。
- **字段初始化陷阱已实测解决**：模式 = 默认值改 `null` + 类型改 `string?` + 渲染处 `?? L10n.T(key, "原文")` 兜底。落地于 SbConfirm(Title/ConfirmText/CancelText)、SbDataTable(EmptyTitle)、SbListPage(SearchPlaceholder/EmptyTitle)、SbSearch(Placeholder)、PermissionGuard(DenyText)、DataSourcePicker(Label) 共 6 组件。参数由 `string` 改 `string?` 后**四端调用方零编译错误**（传值侧不受影响）。
- **透传陷阱（新发现）**：`SbListPage` 会把未解析的 `EmptyTitle` 传给 `SbDataTable`，故**两层都需兜底**，否则父级不传值时仍漏中文。
- **扫描漏报（新发现）**：`SqlViewer.razor:6` 的 `"查看生成的 SQL"` 位于 `<summary>` 标签内，被去噪逻辑当成 XML doc 剔除而漏报。**教训：每批收尾必须逐文件人工复查，扫描只用于排序优先级，不是验收门禁。**
- **保留项**：`AskTurnCard.razor` L231-238 的 `cmd.Contains("柱状"/"折线"/"隐藏图例"…)` 属后端指令识别语义约定，非 UI 文案，不本地化。
- **验证**：RCL 0 error、护栏 6/6、全量回归 708/708、四端构建 0 error，全绿。
- **待续**：L2（低密度页面，约 15 文件 / ~20 行，用 Grep+局部 Read+Edit，不读全文）→ L3（高密度页面，18 文件 / 292 行）。

## M3-05-M L2 层（低密度页面）✅ 完成（本地提交待推送）
- **范围**：残余 ≤7 行的页面 11 个，新增 **32 键**（五处同步，`Catalog.Page` 按页面分设；脚本 `C:/tmp/gen_l2_l10n.py` 支持 per-key Page）。
- **最大收益：先查键再接线**。`DataSourcesConnSqlServer`/`Mysql`/`Postgres`/`Oracle`/`Clickhouse`/`Mongodb` 六键**早已登记**（zh 默认值即页面硬编码文案），此前是「已登记未接线」，本批**零新增键直接覆盖 6 行**。**纪律：每批动手前先 grep 现有键前缀，能接线就不登记。**
- **误报识别规则（下次直接套用，省 8 处无效改动）**：带 `Key` 参数且 `Page.Title.<Key>`/`Page.Desc.<Key>` **均已注册**的 `PageHead`，其 `Title`/`Desc` 字面量是**回退值**——PageHead 内部按键解析，外面再包 `L10n.T` 是重复且可能与 Key 机制冲突。**判定方法**：`grep -oP 'Page\.Desc\.\w+' Keys.cs` 拿已注册清单比对。当前已注册 `Page.Desc.*` = BusinessModel/BusinessModelEntityDetail/ComponentGallery/DataSource/DataSources/MetadataEntityDetail/ModelAccounts/SemanticLabelDetail/ThemeEditor（**注意：Quota/Themes 的 Desc 未注册，故真实缺口**）。
- **字段初始化器陷阱的新变体（必记）**：① 集合字段初始化器 `private List<T> _x = new(){ ...含中文... }` 同样不能引用 `L10n`；② 若该集合**后续有写入**（如 `ModelAccounts._models` 在 `SetDefault` 里 `m.Default = m.Name == n`），**不可**改为只读计算属性 `=> new(){...}`（每次 new 会丢修改），必须「字段置空 + `OnInitialized` 填充」；③ 参与 `@bind` 的字段（如 `Login.BootstrapDisplayName`）必须保持可写，同样走 `OnInitialized` 赋值。
- **flaky 复现**：`RateLimitMiddlewareTests.Expired_Windows_Are_Evicted_To_Prevent_Unbounded_Growth` 在首轮全量回归中偶发失败（707/708），**隔离重跑 7/7 通过、全量复跑 708/708 全绿**——与 M2-03 记录一致，非本批引入。处理范式：遇此失败先隔离重跑确认，再复跑全量，不要改测试。
- **验证**：RCL 0 error、护栏 6/6、回归 708/708、四端构建 0 error，全绿。
- **待续**：L3 高密度页面（18 文件 / 292 行）——顺序建议 Analysis 四大页（SemanticLabels 36 / Agent 26 / AgentPlanDetail 21 / DashboardDetail 18 ≈101 行）→ Admin 七页（DemoData 23 / PlatformAdminScopes 23 / SelfRegistrationAdmin 19 / Audit 17 / SystemStatus 9）→ Account（SelfRegistration 29 / Profile 12）→ Home 19。

## M3-05-M L3 层（高密度页面本地化）✅ 全批次完成（C2 本地提交 `023f254` 待推送）
- **统一纪律**：每批「生成键（五处严格同步：后端 `ResourceKeys` 常量 + `Catalog` + `ZhCnDefaults`、RCL `Keys.Content` 常量 + `Keys.Defaults`）→ razor 接线 → RCL 构建 0 error → `ResourceKeyRegistryTests` 护栏 6/6 → 全量回归 708/708 → 四端（后端/RCL/Web/Maui-Windows）构建 0 error → 提交（排除 `.workbuddy/` 与根 `nul`）」。
- **L3-A1（Agent / DashboardDetail）** ✅：新增 46 键；修复 CS0236/CS0120（`Agent._columns` 改实例属性、`DashboardDetail._title` 字段置空 + `OnInitialized` 赋值）。
- **L3-A2（SemanticLabels / AgentPlanDetail）** ✅：新增 58 键（复用 Common*）；`ExtractSteps` 去 static；RCL 末键锚点动态定位（`DashboardDetailDefaultTitle`）。
- **L3-B1（SystemStatus / DemoData）** ✅：新增 35 键；`StatusBadge` 返回 `MarkupString`，替换须「旧串含外层引号、新串 `+ L10n.T` 拼接」。
- **L3-B2（PlatformAdminScopes / SelfRegistrationAdmin / Audit）** ✅（提交 `db2da94`）：新增 53 键（新增 CommonYes/CommonNo）；跨行正则匹配、SbStatTile 行含 Value、英文值内层双引号改单引号。
- **L3-C1（SelfRegistration / Profile）** ✅（提交 `4330ade`）：新增 42 键；补登记 `Page.Desc.Profile`；标签文本是错误提示前缀子串→「仅替换首次出现」；「已有账户？返回登录」`<a>` 无 class→按真实标记匹配。
- **L3-C2（Home / ThemeEditor）** ✅（提交 `023f254`）：新增 25 键（22 Home* + 2 ThemeEditor* + 1 Page.Desc.Home）；Home 22 处接线 + ThemeEditor 2 处；补登记 `Page.Desc.Home`。
- **L3 收尾验证（C2）**：RCL 0 error（128 既有 iOS IL2026 告警）、护栏 6/6、回归 708/708、Web/Maui-Windows 0 error 全绿。主计划 `docs/Master_Development_Plan.md` 补 L3-A1~C2 各段。
- **关键扫描复核**：重扫 RCL 残留中文 7498 字符/58 文件实为 `L10n.T(key,"中文fallback")` 回退值；未包裹中文仅 539 字符/30 文件，且全部为 ①`PageHead Title/Desc` 字面量回退值（设计保留，外裹 L10n.T 会与 `Page.Title.*` 冲突，属误报）②ComponentGallery 演示文本 ③ThemeEditor 设计令牌色名 ④AskTurnCard 语义指令串（刻意不本地化）。**结论：M3-05 页面本地化已实质完成，继续扫残留中文属低价值且有害，不应强制包裹。**

## M3-06 用户语言偏好硬化 ✅ 完成（本地提交 `cf2c706` 待推送 origin/master）
- **关键修正**：`UserLanguagePreferenceService.GetAsync` 原样返回存储文化、未重新校验是否仍属租户可用语言（仅前端 `AvailableCultures.Contains` 兜底，后端非权威）。改为读取时按 `ITenantLanguageService.GetAvailableCulturesAsync` 复核；越界（原语言被停用/平台下线）回退 `GetDefaultCultureAsync`（租户默认）。无记录仍返回 null（保留「无偏好→前端回退默认」契约）。
- **测试补齐（发布门禁）**：`UserLanguagePreferenceServiceTests.Get_FallsBackToTenantDefault_WhenStoredLanguageDisabled`（设 en-US→停用 en-US→读回退 zh-CN）、`Get_ReturnsStoredCulture_WhenStillEnabled`、`UserPreferenceControllerTests.Get_ReturnsTenantDefault_WhenStoredLanguageDisabled`（同场景经 API 返回有效文化）。覆盖刷新/换设备（服务端持久化读取）、越界回退、首次登录无偏好、语言停用回退四项门禁。
- **验证**：后端构建 0 error（25 既有 CS0618 HasCheckConstraint 告警）；`UserLanguagePreferenceServiceTests`(6)+`UserPreferenceControllerTests`(4)+`ResourceKeyRegistryTests`(6) 全绿；全量回归 **711/711（708+3 新增）**。主计划 `docs/Master_Development_Plan.md` 补 M3-06 硬化段。
- **当前状态**：M3（G0 + 01~06）全部完成。下一里程碑为 **M4 数据源与元数据闭环**（P1，依赖 M1 + M3-G0 核心子集）。本地领先 `origin/master` 现 **13 个提交**（L3-B2 `db2da94`/L3-C1 `4330ade`/L3-C2 `023f254`/M3-06 `cf2c706` + 前序 9 个），须用户本地 `git push origin master`（沙箱不通 github.com）。
