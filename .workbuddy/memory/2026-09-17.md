# 2026-09-17

## 数据源扫描优化（§L 版本生命周期）— 消费点过滤落地

继续 `docs/plans/active/2026-09-datasource-scan-optimization-final.md` 的 Step 2（步骤2：版本生命周期骨架）收尾。

- 新增 `SuperBuilder_AI/src/Application/Metadata/MetadataVersionQueryExtensions.cs`：`WhereActiveVersion(this IQueryable<MetadataTable|MetadataColumn|MetadataSemantic>, SuperBIContext db)`，以对 `DataSources` 的 EXISTS 关联子查询实现 §L.5 消费点过滤（`WHERE MetadataVersion == ds.ActiveMetadataVersion`）。语义等价于 SQL EXISTS，可与 Include/ThenInclude/既有 Where 自由组合。
- 存量数据 `ActiveMetadataVersion=0` 且所有行 `MetadataVersion=0` → 过滤退化为恒等，零回归。
- 已对以下 Ask/查询链消费点读取套用过滤（grep `MetadataTables/MetadataColumns/MetadataSemantics` 全量覆盖，仅限 Ask 侧，排除扫描器/种子/CI 假数据/向量索引维护/管理诊断控制器）：
  - MetadataSemanticSearchService（LoadTable/Column/SemanticVectorAsync、SearchByKeyword/SubstringAsync、ResolveTableByNameAsync）
  - MetadataSearchService.SearchAsync、AppQueryExecutor.LockPlanToTableAsync、LiveMetadataTool.ExecuteAsync
  - DisplayResolutionService.LoadColumnsAsync、QueryPlanContextBuilder、QueryPlanValidator、QueryPlanSecurityGate、RowLevelSecurityService、DimensionResolutionEvidenceService
  - AskCacheVersionProviders.MetadataVersionProvider：改为仅对 active 行求和 RowVersion（缓存键随激活翻版而失效）
- 主工程构建 0 错误 0 警告；测试工程 0 错误（32 个既有 warning 不变）。
- 待后台 `dotnet test` 回归结果确认（task vigp58）。

## 范围决策（已记录，避免后续误改）
- 故意未过滤：MetadataVectorIndexService（IndexAll/DetectOrphans/Validate，维护/孤儿检测全版本可见）、BusinessEntityService（绑定完整性校验，管理路径）、BusinessEntityGoldenFixtureController/DataSourcesController/LocalRuntimeDiagnosticsController/RowLevelSecurityController（管理/诊断，非 Ask 读取路径，staging 预览可能依赖全版本可见）。
- 向量过滤（§L.4 Qdrant payload metadata_version）由 `MetadataVersionFilterEnabled` 特性开关两阶段部署控制；SQL 消费点过滤按 §L.5 一律生效（存量恒等）。

## Step 2.5（§10.5 Ask 查询链）— 限定名/稳定别名/跨目录守卫

实现 §10.5：
- `QueryTable` 增 `CatalogName`/`SchemaName` 字段（构造点从解析到的 `MetadataTable.CatalogName/SchemaName` 透传）。
- `ISqlDialect` 新增 `QualifyTable(string? catalog, string? schema, string table)` + `AssertCatalogResolvable(string? catalog, string? currentCatalog)`；三方言实现：SQL Server `[cat].[sch].[tbl]`；MySQL `cat`.`tbl`（schema 必须为空或==catalog，否则抛 `InvalidOperationException`，因为 MySQL catalog==database、schema 固定为表名空间）；PostgreSQL `cat`.`sch`.`tbl`，且 `AssertCatalogResolvable` 在 catalog 非空时抛 `InvalidOperationException`（PG 不支持跨 catalog 引用，§10.5 #5）。
- 新增 `ConnectionStringDatabaseExtractor`：从连接串解析当前 catalog（SQL Server `Initial Catalog`/MySQL `Database`/PG `Database`/`SearchPath`），供执行层守卫用。
- 新增 `QueryCatalogGuard.GuardAsync`：PgSQL 下若 plan 含 catalog != 连接串当前库 → 抛 `CrossCatalogQueryException` 阻断。
- `SqlQueryBuilder` 重写为稳定别名 `t{MetadataTableId}` + `QualifyTable` 限定名（FROM/JOIN），字段引用统一走 `t0.col`；JOIN 按 MetadataTableId 校验存在性（缺失则抛 `InvalidOperationException`）。解决多数据源/多 schema 下表名歧义（MySQL 1052 类问题根因）。
- 执行层三处接入守卫：`AppQueryExecutor`/`QueryPlanWidgetDataResolver`/`AITestController` 注入 `ISecretStore` + 构建前 `QueryCatalogGuard.GuardAsync`。
- `FakeDialect`/`FakeSecretStore` 测试桩补齐；`SqlQueryBuilderTests` 4 条既有断言改为别名格式并新增 catalog/schema 单测。
- 主工程 + 测试工程均 0 错误（32 既有 warning）。建完待 `dotnet test` 回归（task h5nhwm）。

## Step 8（C2/C1 范围选择 + 保存并扫描）— 已完成

- 新增 `ScanScope` DTO（Databases/Schemas/ExcludeSystemDbs/ExcludeEmptyTables/ExcludePrefixes/ScanViews）。
- `MetadataScannerService.ScanAsync` 增 `ScanScope? scope` 入参 + `DiscoverScopedAsync`（跨库聚合、MySQL Databases==Schemas 同义）；外键/列分组重构为三键 `(CatalogName,SchemaName,TableName)` 避免跨库同名表碰撞。
- `MetadataController.ScanDataSource` 接收 `scope`，MySQL 双指定不一致拒 400，持久化 `ScopeJson`。
- 宿主 `ProcessJobAsync` 反序列化 `ScopeJson` 透传 scope。
- `DataSourcesController.Create` 增 `ScanAfterCreate`：真→201 建 `MetadataScanJob` 并入队；假→200 不入队。
- 构建 0 错误；全量单测 1275/1275。

## Step 9（C8 删除两档 + §10.7）— 已完成

- 新增权限 `metadata:delete`、`metadata:cleanup_metadata`（IdentityConstants + 目录 + 测试计数 35/27）。
- `DataSourcesController.Delete`：`mode=disable`(停用以保留) / `mode=cleanup`(先清 Restrict 引用 RLS/PhysicalBinding/AskSnapshot/QueryCorrectionRule/LearningRecord → 删源级联 → 同事务写 `MetadataVectorGcRequest` 含待删 point ID 快照)；cleanup 前 `VectorsBackfilled` 闸门（否则 409 `vector_backfill_required_before_cleanup`）；有依赖且 `!confirm` 返 409 `has_dependencies`。
- 构建 0 错误；全量单测 1275/1275。

## Step 10-11（C5/C10/C11 页面 + 测试补强）— 已完成

- 后端：`GET /metadata/scan/latest`（C5 进入页面恢复续显）；`ScanTelemetry` 增 `AddSuccess/AddDebug` + 扫描完成 `Success` 事件（C10 等级着色）；前端 `DataSourceDetail.razor` 操作栏重做（取消/重扫失败项/删除影响分析/状态恢复轮询、权限按钮 `PermissionGuard`），`PermissionCodes` 补 `MetadataCancel/Delete/Cleanup`，`app.css` 增 scan-event 等级色（is-success/is-debug）。
- i18n 四处一致：`Keys.cs`/`ResourceKeys.cs`/`LocalizationSeedService`/razor 消费，新增 18 键（去重 `DataSourceDeleted/DeleteFailed`）。
- §9 Fake 层集成补强：新增 15 个控制器测试（CancelScan Queued→Cancelled/Running→Cancelling/终态409/无权限403/未找到404；RetryFailedScan 建 OriginalJobId 续扫/同源去重409/未找到404；GetLatestScanJob 空/最新；Create ScanAfterCreate 201 入队/无则无任务；Delete disable 403/200、cleanup 409 未回填/200 删源并写 GC 待办）。seed 后 detach 避免跟踪冲突。
- **全量单测 1290/1290 通过，0 失败**（基线 +15）；主工程 + Components + 测试工程均 0 错误（仅既有 warning）。

## §9.1 真后端五类集成验收 — 未在本环境编写/运行

- 五类场景（同名表跨 schema Ask / 跨库 Ask / 向量单表失败 / 存量迁移 / 删除后重启 GC）强制需真实 MySQL/SQL Server/PostgreSQL/Qdrant 容器，规格明确 Fake 单测不可证。
- 本沙箱无容器，未编写该类测试；建议 CI 环境以 `[Trait("Category","RealBackend")]` 落地，本地可用 RealBackend 开关跳过。此为交付未闭环项（须 CI 补），非代码缺陷。
