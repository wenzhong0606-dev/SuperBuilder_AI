# 2026-09-03 工作日志

## 全面审计 + 后续开发计划（仅计划，未开发）
- 交付物：`docs/Audit_Report_and_DevPlan.md`（综合审计报告 + 分批开发计划，用户要求先形成计划不开发）
- 审计方法：4 个并行 Explore 子代理（前端页面 / 后端 API / 初始化数据+i18n / 文档待办）+ 关键缺陷源码二次确认
- **已源码确认的关键事实**：
  - 4 个前后端契约 404：`api/agent`(应 `api/agent/plans`)、`api/agent/tools-catalog`(应 `api/agent/tools`)、`api/business-model/entities/{id}`(后端无该路由)、`api/semantic-labels/{id}`(后端无 GET-by-id) → Agent/业务实体/语义标签详情页打不开
  - `AuthController.cs:139` 硬编码只放行 zh-CN/en-US（L-2）；生产代码 0 `Migrate()`
  - `Program.cs:274` `PlatformAdminBootstrapper.EnsureAsync()` 无 try/catch，内部 `SingleAsync`（:38/41/49/61/63）→ 全新库启动崩溃（I-1）
  - `tests/.../IdentityServiceTests.cs:47` `Assert.Equal(31, permCount)` 硬约束 → 新增权限码须同步改断言与 seed（L-11）
- **最高风险**：工作区 47 项未提交变更（含 12 新文件，部分实现平台需求基线）未经过 Golden 18/18 + 426 测试门禁
- 报告内编号体系：C-（契约）/I-（初始化）/L-（i18n）/S6-（前端发布整改）/SB-P1-/SB-P2-（后端企业/架构），可直接映射源码与文档
- 计划分批：A 发布阻塞(P0) → B 需求基线收口 → C 多语言产品化 → D 发布验收 → E 企业能力/架构 → F 平台扩展(P12/P13)
- 待用户裁决：bootstrap 匿名初始化治理冲突、是否支持多平台管理员、演示数据策略、i18n 工作量优先级、A3/A5 是否重启、P12/P13 排期

## 整合最终开发计划（Master Plan）
- 用户更新了 `docs/Frontend_DevChecklist.md`（升级为 P11.7 发布前整改清单，新增 S6-1~S6-9 九项，S1-S5 状态由 ✅ 改为 🟡 诚实标记）
- 交付物：`docs/Master_Development_Plan.md`（整合主计划 / 唯一执行参考）
- 整合来源：DevChecklist_Final.md（P0 已闭环、P1×16、P2×10）+ Frontend_DevChecklist(P11.7 S0-S6) + Audit_Report_and_DevPlan(C/I/L) + Platform_Product_Development_Requirements + DevPlan Stage1(A3/A5)
- 结构：0 总览就绪度 → 1 门禁纪律(约束1-4) → 2 里程碑路线总图 → 3 各里程碑详细工作项(M0-M8) → 4 统一状态追踪表(全部条目) → 5 决策点 → 6 提交门禁节奏
- 里程碑：M0 发布阻塞(P0) → M1 Sprint3(P1-01/02/03/04/07) → M2 Sprint4(P1-05/06/08/09/10/11) → M3 Sprint5(P1-12~16) → M4 架构治理(P2 + A5/A3) → M5 初始化+平台需求基线 → M6 前端发布验收(S6) → M7 多语言产品化 → M8 平台扩展(P12/P13)
- 测试基线统一为 426/426（后端411+前端bUnit13）动态下限
- `Audit_Report_and_DevPlan.md` 现作为审计细节溯源，主执行参考转为 `Master_Development_Plan.md`

## Ask「第一个对话跑不通」故障诊断（2026-09-03 下午）
- 现象：中文"给我最近的十张入库单"→ Medium(0.795/0.729/0.725) 卡死 + SB_BI_002(请补指标/维度)；澄清"入库单就是入库凭证"仍当新问题反复报错。
- 结论（已逐条源码复核）：**主因是代码缺陷，数据缺失为辅（必要不充分）**。
  - 决策门死锁：`QueryPlanDecisionGate.EvaluateMediumConfidence`(:270-301) 对 Medium 恒定 Confirm/ShouldExecute=false；High 阈值写死 0.80(`QueryPlanConfidenceService.cs:105,111`)；合法"实体+Limit+OrderBy、无 Metric/Dimension"明细列表结构性到不了 High → 永不进 SQL Builder。
  - SB_BI_002 误报：`QueryPlanBuilder.cs:1333-1337` 在 `plan.Fields.Count==0` 抛错→映射 SB_BI_002；默认字段补足(:617-819)仅限"最近且无 OrderBy"，明细列表补不到字段。
  - 别名确认缺失：`AskConversationService.Resolve`(:20-37) 仅"用户补充说明"拼接，无 EntityAliasConfirmation；conversationId 为空时 new Guid(:22)→找不到 pending→永远当新问句。
  - 数据面：入库凭证表 Synonyms 未见"入库单"种子；`BusinessSemanticMappingService` signals(:36-44) 不读 Synonyms。
- 计划已覆盖：M5-13「Ask 首用例」+ M6-03(别名/循环检测) + 旗舰回归(第180-188/409/451-452/722-723行)，但排期在 M5（依赖 M0→M1→M4），对用户 P0 阻塞偏晚。
- 用户选择 B（仅改计划，不动代码）：把"Ask 首通"从 M5 提前为 M0 后首批早期批次。
- **B 已落地**：V2 中 `Master_Development_Plan.md` 已含 `M0-09 首个 Ask 旗舰对话跑通`（第176-189行，P0 早期批次，依赖=无），即具体 7 步旗舰回归（入库单→入库凭证）。本轮补 3 处收紧：① M0-09 加"提前说明"——最小闭环、唯一前提=目标实体可解析、**不依赖 M4**；② §17.3 §10 Ask 覆盖行补 M0-09；③ M5-13 注明旗舰回归已提前至 M0-09，M5-13/M6-03 保留为通用化规则。三处 grep 验证通过。
- 待用户授权才进开发：3 处最小代码修复（决策门放行明细列表 / SB_BI_002 不再误报明细 / 别名确认分支 + 把两轮对话加为 Golden 回归）。

## M0-09 三个最小修复已落地（2026-09-03 傍晚，已开发）
- 用户授权"开始修"。5 处源码编辑 + 2 个测试文件，全部按 §2 工程门禁（Golden 18/18 不受影响、测试下限≥431、触碰 QueryPlan/Builder/Validation 需标注爆炸半径）。
- **改动清单**：
  1. `Domain/BiQuery/QueryPlanConfidence.cs`：新增 `IsExecutableDetailQuery` 属性。
  2. `Application/BiQuery/QueryPlanConfidenceService.cs:367-388`：判定"合法明细列表"（目标表已解析 + 非聚合 + 无指标/维度 + 含 Limit/Order + 无校验/Repair 异常）→ 置 `IsExecutableDetailQuery`。
  3. `Domain/BiQuery/QueryPlanDecisionGate.cs:294-315`：`EvaluateMediumConfidence` 在 Medium + `IsExecutableDetailQuery` 时改返回 **Proceed**（ShouldExecute=true, RequiresConfirmation=false）；否则维持原 Confirm。Hard Blocking 入口仍先拦截校验/Repair 异常。
  4. `Application/BiQuery/QueryPlanBuilder.cs:1333-1374`：Step11 `Fields.Count==0` 时若为合法明细列表，按 `GetPreferredDisplayColumns(table).Take(8)` 补默认字段再进 SQL Builder；仍空才抛原 SB_BI_002（保留真实空计划的报错）。
  5. `Application/BiQuery/AskConversationService.cs`：（3a）conversationId 空 → `FindRecentPending` 恢复最近 awaiting 轮次（不再 new Guid）；（3b）`TryParseAliasConfirmation`（"X 就是 Y"/"指的是"/"即"/"等同于"/"称作"/"叫做"，≤20 字/侧，去"我说的/你说的"等前缀）命中后用 `RewriteWithAlias` 改写原问句；合成串保留原始输入以满足既有测试契约。
- **新增/扩展测试**：
  - `tests/.../QueryPlanDecisionGateTests.cs`（新建）：Medium+明细→Proceed / 无明细标志→Confirm / 有校验错误→Reject。
  - `tests/.../AskConversationServiceTests.cs`（扩展）：别名确认改写原问句 + 空 conversationId 恢复最近 pending + 保留既有"补充说明拼接"契约。
- **验证结果**：API 构建 **0 error**；测试工程构建 **0 error**；全量单测 **436/436 通过**（≥431 下限，0 回归）；定向新测 **8/8 通过**。
- **构建坑（已解决）**：首轮 build 报 `MSB3021` 文件锁——`SuperBuilder_AI (PID 59056)` 是先前会话遗留的 API 调试进程锁住 `bin/.../SuperBuilder_AI.exe/.dll`；`Stop-Process -Id 59056` 后构建恢复。结论：本机若 build 报 MSB3021 锁文件，先查并停掉遗留 API 调试进程再构建。
- **Golden 处理（重要）**：按项目硬规则 `query-plan-golden-v1.json 不可删改`，**未**把两轮对话加进 Golden JSON；改为单测覆盖（等价回归意图，且不污染不可变契约）。Golden 18/18 由独立 `evaluation/golden-runtime` 端点跑（需 Qwen+Qdrant 实时），本沙箱不可跑；但本改动对 Golden 零影响：① 现有 18 例均含指标/维度的聚合/排名，不触发 `IsExecutableDetailQuery`；② Hard Blocking 仍先拦截异常；③ 默认路径逐字节不变。
- 下一步建议：用户在能连 Qwen+Qdrant 的环境复跑 `Golden 18/18` 做最终确认。
- **已提交**：`0557519`（fix(bi): 放行合法明细列表并修复 Ask 首通死锁 (M0-09)），8 文件 +379/−5，未含助手内部 memory 日志（保持未暂存）。工作树仅剩 memory 日志为未提交修改。

## 明细列表「只显示时间字段」修复（2026-09-03 晚间，续 M0-09）
- 用户页面实测：首轮"查询最近的十个入库单"只返回 come_time 一列；refine"显示更多字段"无变化。根因不是决策门，而是 **Builder 字段补全策略缺陷**。
- **根因**：`GetPreferredDisplayColumns`（Helpers.cs）对 metadata 标记稀疏的表（如入库单 wms_storage_receipt_info，18 列仅有 come_time 落入时间组）只产出 1 列；旧 Step5.5 单字段兜底 + Step11 `Take(8)` 直接采纳这 1 列 → 界面只显示 come_time。
- **修复**：
  1. `QueryPlanBuilder.cs` Step5.5（纯明细、`plan.Fields.Count==0`）：`GetPreferredDisplayColumns(table).Take(6)` 后，若 `plan.Fields.Count < FallbackTargetColumnCount(=8)`，调用新增 `GetFallbackDisplayColumns` 按业务相关性补足到 8 列；极端情况下仍为空才抛原 SB_BI_002。
  2. Step11 同样补「两层兜底」（首选列 <8 时按相关性补足），覆盖 refine「显示更多字段」路径（新增 `IsFieldExpansionRequested` 检测"显示更多字段/列/展开列/全部字段/详细字段/多显示/显示详细信息"等）。
  3. 新增 `GetFallbackDisplayColumns(MetadataTable, IEnumerable<string> alreadySelected)`：排除 `_by/_flag/del_flag/create_by/update_by/is_deleted` 及 blob/text/clob，按相关性排序——**数量/金额最高优先级(-1)** > 编码/编号/名称(0) > 时间(1) > id/主键(2) > 其它(3)；排除已选列避免重复。
  4. `AskController.ComposeRefinedQuestion`：当 instruction 命中字段扩展意图时，额外追加"请返回该表的主要业务字段，不要只返回时间字段"提示，强化 LLM 不再只选时间列。
- **端到端链路验证**：refine 端点 `composed` 串作为 `question` 传入 `AskAsync`，`QueryUnderstandingService` 以该串设置 `intent.OriginalQuestion`（:700），故"显示更多字段"能被 `IsFieldExpansionRequested` 捕获；纯明细首轮就直接补 8 列，refine 即使 LLM 仍只返回少量字段也强制扩展。
- **测试**：新增 `tests/.../QueryPlanBuilderDetailListTests.cs`（4 例：纯明细补 6 列 / 字段扩展指令扩到首选列 / 聚合不触发扩展 / metadata 稀疏兜底补列含 quantity）。
- **验证结果**：API 构建 0 error（仅既有可空性警告）；全量单测 **440/440 通过**（较 436 基准 +4 新用例，0 回归）；定向 12/12 通过。
- **已提交**：`d735df9`（fix(bi): 明细列表补全多列业务字段并支持 refine 显示更多字段），3 文件 +473/−108，未含助手内部 memory 日志。M0-09 链两连提交：0557519（首通死锁）+ d735df9（多列显示）。

## 明细列表「依旧只有 come_time」根因修正与彻底修复（2026-09-03 晚，续 d735df9）
- 用户复测 d735df9 后**首轮"最近的十个入库单"仍只有 come_time，refine 也无变化**。说明 d735df9 的根因假设（仅"metadata 稀疏"）不完整——真正的阻断更靠前，在补列逻辑被调用之前。
- **真实根因（覆盖 d735df9 假设）**：Step5.5 的纯明细补列逻辑被外层条件 `isRecentQuery && string.IsNullOrWhiteSpace(intent.OrderBy)` 包裹。实战中 Qwen 对"最近的十个入库单"**直接返回 OrderBy=come_time**，使整段 `if` 被跳过，内部补列从不执行；随后 Step9 只把 come_time 加进 `plan.Fields` → SELECT 仅 come_time 一列。d735df9 的补列只在该 `if` 内部（OrderBy 空时）生效，故对真实 LLM 输出完全无效。这同时解释了为何 refine 也无变化（合成串含"显示更多字段"但 LLM 仍返回 OrderBy 时，Step5.5 同样被跳过）。
- **修复（a4c4067）**：在 Step5 之后新增 **Step5.6**，把"纯明细补列"从 `OrderBy 是否为空` 解耦——只要意图无 指标/维度/过滤（纯明细列表）且 `plan.Fields` 不足 `FallbackTargetColumnCount(=8)`，即按首选展示列 + `GetFallbackDisplayColumns` 兜底补到 8 列。该逻辑对首轮与 refine 均生效；聚合/带指标维度查询不受影响（`isPureDetail` 要求 Filters 也为空）。Step11 的 refine 扩展分支保留不变。
- **新增回归测试** `Recent_detail_query_fills_columns_when_llm_returns_orderby`：模拟 LLM 已返回 `OrderBy=come_time` 的纯明细查询，断言仍补出 ≥6 列（receipt_no/come_time/material_code/quantity 等，排除 del_flag）。该用例在修复前必然 FAIL（仅 come_time），修复后 PASS——直接锁定用户实战 bug。
- **验证**：QueryPlanBuilderDetailListTests **5/5**；全量单测 **441/441 通过**（较 440 基准 +1，0 回归）；Golden json 未改动。
- **已提交**：`a4c4067`（fix(M0-09): 明细列表补列解耦 OrderBy 前置条件，修复"仅返回 come_time"），2 文件 +125/−0，未含 memory 日志。
- **运行环境提醒（关键）**：页面实测"依旧只有 come_time"可能叠加**本地 API 仍跑旧 dll**（d735df9/a4c4067 未重新 build+重启，或遗留 API 调试进程锁住旧 bin，历史 PID 59056/59588 曾导致 MSB3021）。必须：① `dotnet build` + 重启 API（Windows 报 MSB3021 时先停遗留 API 进程）；② 页面复测"最近的十个入库单"应出现多列（入库单号/come_time/物料编码/数量 等）。三提交（0557519/d735df9/a4c4067）均尚未 push。

## 字段扩展 refine 仍只返回时间列的再修复（2026-09-03 深夜，续 a4c4067）
- 用户新截图（对比模式）：首轮"查询最近的十条入库记录"已能返回 come_time/update_time/create_time 3 个时间字段；但 refine"显示更多字段"后**反而只剩 create_time 一列**，文本总结也承认"缺少关键业务字段"。说明 a4c4067 解耦了 OrderBy，但 refine 路径还有另一个门控失效。
- **根因**：Step11 的 `userRequestedMoreFields` 强依赖 `isDetailList`，而 `isDetailList` 要求 `plan.Dimensions.Count == 0`。实战中 LLM 对 refine 指令"显示更多字段"常误解析为带 dimension/metric，导致 `isDetailList=false`，字段扩展不触发；首轮 LLM 即便返回多个时间字段，也因 `plan.Fields.Count>0` 而不触发补列，结果只显示时间字段。
- **修复（87b2176）**：
  1. `QueryPlanBuilder.cs` Step11 新增 `fieldExpansionRequested` 判定，与 `isDetailList` 解耦：只要用户明确说"显示更多字段/列/展开列"等、非聚合、无指标、`plan.Fields` 不足 `FallbackTargetColumnCount`，即强制补足到目标列数，不再受 `plan.Dimensions` 影响。
  2. 将补列入口改为 `shouldExpandFields = isDetailList || fieldExpansionRequested`，统一处理"明细列表无字段"与"refine 字段扩展"两种场景；保留非明细/聚合查询的安全门（`plan.Fields.Count==0` 且未补列时仍抛 SB_BI_002）。
- **新增回归测试**（`QueryPlanBuilderDetailListTests.cs`）：
  - `Refine_field_expansion_forces_columns_when_only_one_time_field_selected`：refine 后仅 create_time 一列，仍补到 ≥6 列。
  - `Refine_field_expansion_works_even_when_llm_adds_dimension`：refine 后 LLM 误加 dimension，因字段扩展请求独立触发仍补到 ≥6 列。
- **验证**：本机构建时再次遇到遗留 API 进程 PID 60052 锁文件 MSB3021，`taskkill /PID 60052 /F` 后恢复；`QueryPlanBuilderDetailListTests` **7/7**；全量单测 **443/443 通过**（较 441 基准 +2，0 回归）；Golden json 未改动。
- **已提交**：`87b2176`（fix(M0-09): 字段扩展从 isDetailList 解耦，修复 refine"显示更多字段"仍只返回时间列），2 文件 +122/−42，未含 memory 日志。四提交链（0557519/d735df9/a4c4067/87b2176）均尚未 push。
