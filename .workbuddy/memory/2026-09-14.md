# 2026-09-14 工作日志

## O1 待决项回填（G1 前置）
- 用户确认由 AI 据源码/预验证证据填写 O1 的 1.4/1.5/1.9/1.12「请填写」列（文件 `docs/plans/active/2026-09-open-decisions-form.md`）。
- 落盘值（均标注「AI 建议，待 owner 确认」）：
  - 1.4 支持数据源类型 = **MySQL**（连接+扫描+查询三层验证）；SQL Server/PostgreSQL 仅连接分支，业务库扫描未实现，G1 不纳入验收。
  - 1.5 已验证层级 = MySQL 三层跑通 WMS `steccn_wms`；SQL Server/PostgreSQL 未做业务库扫描验证。
  - 1.9 浏览器 = Chromium 内核（Chrome/Edge 最新两大版本）；其余内核首批不认证。
  - 1.12 语言 = 首批 zh-CN，多语言需显式配置。
- 关键代码事实：`DataSourceConnectionFactory.cs` 连接层三分支 SQLSERVER/MYSQL/POSTGRESQL；`*MetadataReader*.cs` 仅 `MySqlMetadataReader` 一个实现。表单原 1.4 现状「已知 SQL Server/MySQL」已修正为含 PostgreSQL 三分支。
- 进度表 O1 由「部分确认」改为「已填（AI 建议待 owner 确认）」。
- 未做：§4 回填动作（写回 release-evidence.md / handover 占位符清理 / execution §2 置 CLOSED）须待 owner 对建议值签字后再执行。
- 追加：O1 段内新增「最低 vs 最优对照」表（1.4/1.5/1.9/1.12），明确当前最低=已验证承诺下限、目标最优=产品理想、差距动作=补实现或划 G1 后；G1 收口以最低为准。
- 修文档一致性：`docs/plans/active/README.md` 原称「唯一执行计划」仅登记 production-readiness，与实际 3 份（production-readiness / g1-execution / open-decisions-form）不符。已改为登记三份并标注分工（门禁映射 / 执行展开 / 待决回收），消除根因 #2。
- 填 O2/O3（用户确认「填 O2/O3」）：在 `open-decisions-form.md` 的 请填写 列落盘 AI 建议值（均标 待 owner/产品负责人确认）。
  - O2（2.1~2.11）：数据规模≤100表/5000向量、并发≤20人、Ask P95≤8s、实时查询 P95≤2s、失败率≤1%、成本≤¥0.05、RPO≤24h、RTO≤30min、可用性99.5%、告警用出厂基线、超限429。承诺类指标须实测/业务确认，不得反推。
  - O3（3.1~3.9）：场景=库存查询与入库分析；角色=仓库管理员+运营分析员；数据=WMS `steccn_wms` 近90天、无销售域；验收问题集6条；成效阈值建议。业务口径归产品负责人。
  - §5 进度表 O2/O3 由未回填改为已填（AI 建议待确认）。
- 现状：O1/O2/O3 全部已填（AI 建议待确认）；下一步=owner 签字→§4 回填 release-evidence/handover→g1-execution §2 置 CLOSED→解锁 PERF-01/M14-01/M14-08a/M14-07。
- 用户要求 O2 数据规模扩大 10×：2.1 由 ≤100 表/5000 向量点 改为 **≤1000 表/50000 向量点**（≈演练基线 19–47×）。已加连锁影响警示：PERF-01 须按此规模实测、参考硬件须复核容量、RTO 须按 50k 向量重算、高于实测须投入验证不得写进承诺。
- 容量决策（逐条问询 Q1）：用户选「维持现配置，PERF 实测验证」——参考硬件 8核/32GB/512GB 不变，由 PERF-01 按 10× 规模实测，不过再调。已写入表单 2.1 警示②。
- 逐条问询 Q2：O2 2.2–2.11 全部接受，O2 状态→「已接受（待 §4 回填 CLOSED）」。
- 逐条问询 Q3：O3 选「部分要调」，待定位具体项（3.7 人名必补）。
- 逐条问询 Q4：O3 仅 3.6（验收问题集）要调，其余 3.1/3.2/3.4/3.9 等按建议值接受；3.7 人名仍待补。
- 逐条问询 Q5（自由反馈）：用户指出 WMS **无金额字段、无近期数据**。据此修订 O3：3.6 验收问集去掉金额/改为数量+可用时间范围；3.5 指标计算方式删「入库金额」；3.4 时间窗改「可用数据范围（非固定近90天）」、补注无金额字段。O3 其余接受。
- 逐条问询 Q6：3.7 确认人用户选「暂用占位，CLOSED 前补」。但随后用户挑战「为什么非得用真名」——纠正：试点交接文档填**角色即可**（如产品负责人），非硬需求个人姓名。已把 3.7 改为「产品负责人（角色即可）」并去掉 CLOSED 阻塞；O1/O3 §5 状态推进为「已接受（待 §4 回填 CLOSED）」。O1/O2/O3 现全部已接受，无占位阻塞。
- 逐条问询 Q7：用户选「授权全部执行」§4 回填 + CLOSED。已执行：① g1-execution §2 三张 OPEN 行加 `→ CLOSED（2026-09-14 已回填）`；② 表单 §5 O1/O2/O3 状态→CLOSED；③ g1-execution §6 移除「唯一待办回填」改为已 CLOSED 解锁 PERF；④ 回填定稿去向：03-operations-runbook SLO 表（O2 值：99.5%/Ask≤8s/实时≤2s/失败≤1%/并发≤20/RPO≤24h/RTO≤30min/成本≤¥0.05）+双实例改单实例、05-backup-restore RPO/RTO、backup-restore-dr §3 加 O2 确认注、observability §3 注 O2 回填、release-evidence 加「O1 回填」段、Platform_Product §17 加 G1 边界、pilot-package 同步 O3；⑤ 清 handover `【待实测回填】` 占位符（03/05 已清，04 故障时限与 06 台账仍【待定稿】/【待填】，非 OPEN 范围）。07 状态改「部分清零」。
- 现状：O1/O2/O3 全 CLOSED 且已回填各定稿去向文档；下一步 = PERF-01 实测（按 ≤1000表/50000向量 规模）确认 O2 目标值 → M14-01/M14-08a/M14-07 定稿。
- 用户指令「先提交然后继续前进」：已 git commit 今日全部文档改动，5 个 commit（7047964 handover / 2b7b0b8 ops / 9a17cfa product / 454e613 plans / c9567b9 memory），工作树干净。
- PERF-01（M13-12）启动：拆 5 任务（T1 数据准备合成≤1000表/50000向量、T2 负载工具、T3 场景、T4 实测+超载、T5 验收报告）；产出执行方案 docs/plans/active/2026-09-perf-01-plan.md（场景设计阶段交付，对应 M13-12「先锁目标再场景再报告」）。T1 标记 in_progress。
- 沙箱边界（PERF 可行性）：无现成压测脚手架（Glob 无 Perf/Benchmark）、dotnet build 失败须用预编译 DLL+vstest、Ask 依赖 Qwen 受 HTTP_PROXY 限制、真实库仅 35 表远不足 1000 表目标→须合成元数据。
- 用户反馈「已多绑定几个数据源」+ 选「已绑定但未扫描」+「PERF 在真实环境跑」：T1 从纯合成改为**真实扫描优先（差额补合成）**，执行环境=真实环境。沙箱只出方案/脚本/报告模板，不碰真实库。
- 产出（未提交）：① 方案文档 `docs/plans/active/2026-09-perf-01-plan.md` §4/§5/§6/§9/§10 改真实环境口径；② `scripts/perf/load_test.py`（Python 并发负载器，端点占位待用户按真实调）；③ `docs/ops/perf-01-report.md`（验收报告模板，对照 O2 判据）。T1/T2/T4 描述已更新为真实环境执行。
- 下一步（用户侧）：真实环境触发扫描→统计规模→跑脚本(梯度1/5/10/20)→填报告→贴 AI 出稿。

## CI 失败诊断（2026-09-14 13:09 北京）
- 用户 push `3947186876` (feat(metadata): enrich live scan progress and telemetry) 致 run 34808590353 单元测试 job 红；编译检查/V2.6 Smoke/Web E2E 全绿。
- 根因：`MetadataScanJob.ProgressDetailsJson` 三处硬编码 `nvarchar(max)` 类型（`SuperBIContext.cs` OnModelCreating、`SuperBIContextModelSnapshot.cs` BuildModel、迁移 `20260914062000_M13_MetadataScanRichProgress.cs` Up()）。单元测试用 in-memory SQLite，解析 `nvarchar(max)` 的 `(max)` 报 `near "max": syntax error`，所有用 SuperBIContext 的测试类在 schema 初始化阶段同时崩溃（`Stage` 字段仅 HasMaxLength(64) 非问题）。
- 修复：三处去掉 `nvarchar(max)` 类型指定（保留 comment），EF 默认 SQL Server→nvarchar(max)、SQLite→TEXT，跨 provider 兼容，模型/快照/迁移一致。诊断文档 `docs/ops/ci-failure-34808590353.md`。
- 注：本地副本 HEAD=381d7b0 不含此提交（用户在另一工作区改），CI 错在远端；此副本领先远端 893 commit，非同一工作树。
