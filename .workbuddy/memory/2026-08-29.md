# 2026-08-29 工作日志

## 目标
继续 A4 收口 + 让 Golden 18/18 闸门在 LLM 抖动下稳定可复现。

## 已落地改动（待提交）
1. **QwenService LLM 超时加固**（之前被限流中断、未提交）
   - `src/Infrastructure/Llm/QwenService.cs` 构造器读取 `Qwen:TimeoutSeconds`（默认 180s），
     覆盖 HttpClient 默认 100s。背景：高延迟时 `qwen3.7-plus` 单次推理 >100s 被取消，
     GQ-005 曾因此判 ERROR（基础设施抖动，非逻辑失败）。
2. **Metric 漂移同构加固**（收口第三处 Runtime↔Resolution 集合漂移）
   - `src/Application/BiQuery/QueryPlanBuilder.SemanticResolution.cs` 的
     `ApplyMetricResolutions` 由 `plan.Metrics.Count != bindings.Count` 硬抛
     `InvalidOperationException` 改为与 Filter/Dimension 一致：
     SemanticText 对齐 + 丢弃无绑定幻影指标 + 用未消费 Resolution 绑定补全
     （保留 GQ-002 EntityCount→COUNT 强制覆盖）。1:1 时等价于原索引重写，行为不变。
   - `ApplyOrderResolutions` 不动：`BuildResolvedPlanSkeleton` 的 orders 由 `resolution.Orders`
     生成，plan.Orders.Count 恒等于 bindings.Count，结构上无漂移可能。
3. **清理调试脏代码**：删除 `BuildAsync` 中写 `C:/tmp/step10b.log` 的 `AppendAllText` 块。

## 验证
- `dotnet build -c Debug` → 0 error / 10 警告（警告均为既有 nullable，与本次无关）。
- Golden 18/18 全量复跑进行中（`Evaluation/artifacts/golden_run_20260829_fix3.json`，
  后台任务 b6xY1v）。此前 fix2 已 18/18；confirm 跑 17/18 因 GQ-005 LLM 超时（现已加固）。

## 关键事实/约定（长期）
- Golden `/run` 端点缓冲整个响应直到 18 case 全跑完（每 case 多次 Qwen ~13s/次），
  单次 HTTP >280s；用后台 curl + `--max-time 1800` 跑。
- 依赖：SQL Server(localhost/live/root/SuperBuilder_Platform) + Qdrant(localhost:6333/
  superbi_metadata=961 点) 均在线。
- 服务启动：`ASPNETCORE_URLS="http://localhost:5032" dotnet bin/Debug/net10.0/SuperBuilder_AI.dll`
  （默认 5000，必须显式设 5032）；跑 Golden 前须先停占用 DLL 的旧进程。
- 漂移修复统一模式（C.13.2「只消费已确认 Semantic Resolution」）：
  **SemanticText 对齐 + 丢弃无绑定幻影 + 用未消费绑定补全**，三位一体，已覆盖 Filter/Dimension/Metric。

## P3 Business Semantic Model 管线接线（收口，"剩下的"）
- 用户指令："A3和A5先不做，剩下的继续吧" → 跳过 A3(抽独立项目)/A5(限界上下文解耦)，收口 P3 管线接线。
- **核心缺口**：`QueryIntent.BusinessEntityHints` 已定型但零生产者、零消费者（"未接线"）。
- **零回归安全边界**：Golden 只调 `UnderstandAsync(question)` 无 tenant 重载；把业务实体增强做成"带 tenant 的可选重载"，Golden 路径代码零进入 → 行为零变化 → 18/18 安全。
- 增量改动（6 文件）：
  1. `QueryIntentNormalizer` 构造注入可选 `IBusinessSemanticMappingService?`；新增 `NormalizeWithBusinessEntitiesAsync(intent, tenantId)`（软信号，异常/无匹配静默跳过，绝不阻断主链路），填充 `intent.BusinessEntityHints`（词元重叠打分，不调 LLM，Top5）。
  2. `IQueryUnderstandingService` 新增 `UnderstandAsync(question, tenantId)`；`QueryUnderstandingService` 将原 `UnderstandAsync` 体抽为私有 `BuildRawIntentAsync`（不归一化），两个公开重载分别走 `Normalize` / `NormalizeWithBusinessEntitiesAsync`。
  3. `QueryPlan.BusinessEntityContext` 新增（仅供可读性与下游消费，不覆盖 Metadata 解析）。
  4. `QueryPlanBuilder`（1参实时路径）与 `QueryPlanBuilder.SemanticResolution`（2参 Golden 路径）均透传 `intent.BusinessEntityHints` → `plan.BusinessEntityContext`。
  5. `BIConversationService.ExecuteAsync` 实时路径由无 tenant 重载切换为 `UnderstandAsync(question, tenantId)`；Golden 路径保持无 tenant 重载不变。
- **验证**：`dotnet build -c Debug` → 0 error / 0 warning；服务启动监听 5032（DI 对新增可选依赖解析正常）；Golden 18/18 PASS（响应落盘 `/c/tmp/golden_run.json`，`scorecard.passed:true, failedGates:[]`, 18/18）。
- **文档**：`docs/PhaseChecklist.md` P3 区块全勾 ✅；`docs/DevelopmentPlan.md` Stage2/P3 → ✅ 完成；"立即下一步" → **P4 Multi-Tenant Platform Core**（A3/A5 暂缓）。
- 待提交：9 文件（6 代码 + 2 文档 + 无其余）。（已于本会话提交 `1d185a8`）

## P4 Multi-Tenant Platform Core（启动 · P4.1 基石已落地）
- 用户指令："接着启动 P4 的规划与落地吧"。
- **分闸策略**：P4 拆 P4.1(上下文抽象)/P4.2(Tenant扩展+Migration)/P4.3(全局租户过滤+单测)/P4.4(Runtime入口 PlatformContext 化)，每子阶段以 Golden 18/18 为硬闸门。
- **P4.1 已落地（零 schema 变更、不碰 gated 查询链路）**：
  - `src/Domain/Organization/TenantContext.cs`（租户运行时上下文 record，含 `System` 全局态）
  - `src/Domain/Organization/PlatformContext.cs`（聚合 Tenant/User/Workspace/Locale/Theme，先落地 Tenant）
  - `src/Application/Ports/Platform/IPlatformContextAccessor.cs` + `src/Application/Platform/PlatformContextAccessor.cs`（scoped 访问器）
  - `BIConversationService` 注入可选访问器；`ExecuteAsync` 内由 tenantId 建 `PlatformContext` 写入访问器（为 P4.3 奠基；下游 tenantId 透传不变）
  - `src/Api/Controllers/TenantManagementController.cs`（`api/tenant-management`：GET 列表/按Id、POST 创建、PATCH 启/停用）
  - `Program.cs` 注册 `IPlatformContextAccessor`（scoped）
- **验证**：`dotnet build` 0 error 0 warning；服务启动监听 5032 无 DI 错误；`GET /api/tenant-management` → 200（[Tenant1 WMS, Tenant3 CSV_FIXTURE]）。
- **Golden 策略**：P4.1 未触及 `Application/BI`、`Domain/{Metadata,BiQuery}`、`Infrastructure/{Vector,Database}`、Resolution → 不强制重跑；全量复跑在 P4.3 隔离落地后。
- 下一步 P4.2（Tenant 子实体 + Migration）。A3/A5 仍暂缓。
- **P4.2 已落地（Tenant 扩展 + Migration · 零行为变更 · build 绿 + Golden 18/18）**：
  - `src/Domain/Organization/TenantSetting.cs`（通用租户 KV 配置，统一承载 Setting/Locale/Theme/Workspace，Key 命名约定按域前缀；避免为每种配置单建表）
  - `SuperBIContext` 新增 `DbSet<TenantSetting>` + 配置（`Tenant` 级联删除、`(TenantId,Key)` 唯一索引、列注释）
  - Migration `20260829090341_P4_2_TenantSettings` 已 `dotnet ef migrations add` 生成并 `dotnet ef database update` 应用（含 P3 迁移一并补齐；SQL Server localhost 可达）
  - `TenantManagementController` 扩展 `GET/POST /api/tenant-management/{id}/settings`（运行时验证：POST 写→GET 读 往返 200）
  - **验证**：`dotnet build` 0 error（10 个预存 nullable 警告均位于未改动文件 QuerySemanticValidator/FieldResolver/PhysicalBindingResolver/GoldenDatasetRunner/BIConversationService:247，非本阶段引入）；**Golden 18/18 PASS**（HTTP=200, decision=PASS, total=18, passedCases=18, failedGates=[]，耗时 352s；首跑 300s 超时系 Qwen/Qdrant 延迟抖动，非代码回归）
- 下一步 **P4.3（SuperBIContext 全局 `HasQueryFilter` + 跨租户不可见单测，最高风险 · 必跑 Golden）**。A3/A5 仍暂缓。

## P4.3 全局租户过滤 + 跨租户单测（✅ 完成 · 零回归 · Golden 18/18）
- 用户指令："继续"（推进 P4）。
- **设计（纠偏后）**：`SuperBIContext` 增 `_tenantFilterEnabled`/`_scopedTenantId` 私有字段 + 显式 `ApplyTenantScope(long tenantId)` 方法（默认关闭=no-op）；`OnModelCreating` 对 4 个直接持有 TenantId 的根实体（`DataSource`/`MetadataTable`/`BusinessEntity`/`TenantSetting`）加 `HasQueryFilter(e => !_tenantFilterEnabled || e.TenantId == _scopedTenantId)`。
- **不读取 `IPlatformContextAccessor`**（避免 DbContext 构造期/请求期取值错位——前次失败根因之一即读取 `System` 上下文 `TenantId=0` 过度过滤）；由 `BIConversationService.ExecuteAsync` 在已知 tenantId 后显式调用 `ApplyTenantScope` 开启。**Golden 无租户路径从不调用 → 过滤恒为 no-op**。
- **DataSource.TenantId 为 long? 的坑**：首两版过滤器在 SQL Server 上触发 "Nullable object must have a value" 回归（18 case 全 ERROR）。根因 = `long? == long` 产生被提升的 `bool?` 与 `||` 组合破坏 EF 表达式处理。修复：DataSource 改用 `e.TenantId.HasValue && e.TenantId.Value == _scopedTenantId` 守卫；其余 long 列保持 `==`。
- **单测**：新增 `tests/SuperBuilder_AI.Tests/`（xunit + EF Core SQLite 内存库，离线可还原），`SuperBIContextTenantFilterTests` 覆盖 4 实体，验证 ① 无作用域=全可见 no-op ② 开启租户=仅本租户可见 ③ 跨租户不可见 ④ System/tenantId<=0=no-op；**4/4 通过**。
- **验收**：`dotnet build` 0 error 0 warning；**Golden 18/18 PASS（decision=PASS, passedCases=18/18, failedGates:None, overallPassRate=1.0, positivePassRate=1.0；3 个 ERROR 为预期负例 Qwen 403 抖动，expectedOutcome 满足）**。
- **环境恢复注记**：本沙箱 SQL Server TCP/1433 被禁用（`HKLM...\SuperSocketNetLib\Tcp\Enabled=0`）且服务重启需管理员权限（当前会话非管理员，无法改注册表/重启 MSSQLSERVER），故验证改用共享内存连接覆盖 `ConnectionStrings__DefaultConnection=Server=.` 启动服务；Qdrant gRPC 6334 正常，Qwen(dashscope) 可达。SQL TCP 恢复需管理员 `Set-ItemProperty ...Tcp Enabled=1` + 重启服务。
- 下一步 **P4.4（核心 Runtime 入口 PlatformContext 化：UnderstandAsync/QueryPlanPipeline 从 long tenantId 迁移为接受 PlatformContext；Golden 无租户重载不变）**。A3/A5 仍暂缓。
