# 2026-08-29 工作日志

## 目标
继续 A4 收口 + 让 Golden 18/18 闸门在 LLM 抖动下稳定可复现。

## 已落地改动（待提交）
1. **QwenService LLM 超时加固**（之前被限流中断、未提交）
   - `src/Infrastructure/Llm/QwenService.cs` 构造器读取 `Qwen:TimeoutSeconds`（默认 180s），
     覆盖 HttpClient 默认 100s。背景：高延迟时 `qwen3.7-plus` 单次推理 >100s 被取消，
     GQ-005 曾因此判 ERROR（基础设施抖动，非逻辑失败）。
2. **Metric 漂移同构加固**（收口第三处 Runtime↔Resolution 集合漂移）
   - `src/Application/BiQuery/QueryPlanBuilder.SemanticResolution.cs` 的
     `ApplyMetricResolutions` 由 `plan.Metrics.Count != bindings.Count` 硬抛
     `InvalidOperationException` 改为与 Filter/Dimension 一致：
     SemanticText 对齐 + 丢弃无绑定幻影指标 + 用未消费 Resolution 绑定补全
     （保留 GQ-002 EntityCount→COUNT 强制覆盖）。1:1 时等价于原索引重写，行为不变。
   - `ApplyOrderResolutions` 不动：`BuildResolvedPlanSkeleton` 的 orders 由 `resolution.Orders`
     生成，plan.Orders.Count 恒等于 bindings.Count，结构上无漂移可能。
3. **清理调试脏代码**：删除 `BuildAsync` 中写 `C:/tmp/step10b.log` 的 `AppendAllText` 块。

## 验证
- `dotnet build -c Debug` → 0 error / 10 警告（警告均为既有 nullable，与本次无关）。
- Golden 18/18 全量复跑进行中（`Evaluation/artifacts/golden_run_20260829_fix3.json`，
  后台任务 b6xY1v）。此前 fix2 已 18/18；confirm 跑 17/18 因 GQ-005 LLM 超时（现已加固）。

## 关键事实/约定（长期）
- Golden `/run` 端点缓冲整个响应直到 18 case 全跑完（每 case 多次 Qwen ~13s/次），
  单次 HTTP >280s；用后台 curl + `--max-time 1800` 跑。
- 依赖：SQL Server(localhost/live/root/SuperBuilder_Platform) + Qdrant(localhost:6333/
  superbi_metadata=961 点) 均在线。
- 服务启动：`ASPNETCORE_URLS="http://localhost:5032" dotnet bin/Debug/net10.0/SuperBuilder_AI.dll`
  （默认 5000，必须显式设 5032）；跑 Golden 前须先停占用 DLL 的旧进程。
- 漂移修复统一模式（C.13.2「只消费已确认 Semantic Resolution」）：
  **SemanticText 对齐 + 丢弃无绑定幻影 + 用未消费绑定补全**，三位一体，已覆盖 Filter/Dimension/Metric。
