# 2026-09-06 工作日志

## M4-05 扫描与同步：前端集成收尾 + 测试（完成 M4 闭环）

`scene#15:"日常开发"`，用户指令「继续M4的工作」。核查发现 M4-05 后端此前已落地（Channel 队列 + 后台 HostedService + `MetadataScanJob`/`ScanTelemetry` 模型 + 迁移 `20260906000108_M4_05_ScanJob` + `MetadataController` 的 POST 202 触发 / GET 轮询端点 + `Program.cs` 注册 + `MetadataScannerService` 孤儿安全清理），但**前端 `DataSourceDetail.razor` 仍用旧同步 `Api.PostAsync`**：对 202 误报“完成”、不取 jobId、不轮询、无进度/重试。

### 实施
- `DataSourceDetail.razor`：扫描改为异步任务模型——`StartScanAsync` 取 jobId → `PollScanAsync` 每 1.5s 轮询 `GetScanJobAsync` 至 `Succeeded`/`Failed`；`Cts` 在 `IDisposable.Dispose` 取消（导航离开即停）。扫描态面板显示状态徽标、进度条、已扫描表/字段/清理孤儿计数、起止时间、失败错误码+脱敏摘要、`重试` 按钮。`@implements IDisposable`。
- `app.css`：新增 `.scan-job*` 样式（成功/失败描边、进度条、统计、错误块），复用 `--sb-success`/`--sb-accent` 设计变量。
- `MetadataController.ScanDataSource`：202 响应 `status` 由硬编码 `"queued"` 改为 `job.Status.ToString()`（与 GET 枚举字符串一致）。
- `AuthStoreTests.StatusApiClient`：补齐 `IApiClient` 新增的 `StartScanAsync`/`GetScanJobAsync` 桩（前序 M4-05 接口扩展未同步导致测试工程编译失败，阻断全量测试）。

### 验证
- RCL（net10.0/android/ios）、API、Web 构建均 0 error。
- 新增 `MetadataScanControllerTests` 6 例（创建 Queued+入队 jobId 一致 / GET 轮询状态 / 未授权 403 / 缺 `metadata:scan` 权限 403 / 旧固定入口 410 / 缺令牌 401）；相关 9 例全绿。
- `docs/Master_Development_Plan.md` M4-05 标记 ✅，M4-01~M4-05 后端+前端闭环完成（M4-02/03/04 后端+前端经 M4-01 审计声明已就绪，本轮未独立重验）。

### 复用要点
- Blazor 轮询后台任务：用 `CancellationTokenSource` + `IDisposable.Dispose` 取消，避免导航离开后继续 `StateHasChanged`；`Task.Delay` 须传同一 `ct` 以响应取消。
- `StatusCode(code, value)` 返回 `ObjectResult`（非 `StatusCodeResult`），控制器测试断言状态须用 `ObjectResult` 读 `.StatusCode`。
- 扩展 `IApiClient` 接口后，须同步更新所有 Fake 实现（如 `AuthStoreTests.StatusApiClient`），否则测试工程编译失败——这是前序 M4-05 遗留的“接口漂移”坑。
- 项目根有 0 字节残留文件 `nul`（疑似 `> nul` 重定向误产物），非本次产生，未处理。

## M5 基础切片：M5-04 收缩 Builder + M5-03 Pipeline 阶段化（均完成、本地提交）

`scene#15:"日常开发"` 延续。用户经 AskUserQuestion 批准「M5 基础切片（M5-03 + M5-04）按方案执行」。两切片均经核实对 Golden 契约免疫（Golden 走 2 参 `BuildAsync` 且不进 Pipeline）。

### M5-04（提交 bcbe537，8 文件 +268/−58）
- 新增端口 `IQueryPlanDataSourceScope`（Interfaces.BI）+ 默认实现 `QueryPlanDataSourceScope`（Services.BI），把 `QueryPlanBuilder` 内联的选表前数据源候选收敛（P0-05 授权集合收敛 / P0-01 显式请求收敛）抽离。逻辑逐字节一致。
- `QueryPlanBuilder` 改为 ctor 注入 `IQueryPlanDataSourceScope`（必需依赖，杜绝生产静默丢失授权）；`Program.cs` 注册为 Scoped。
- 坑：`MetadataSemanticSearchResult` 命名空间是 `SuperBuilder_AI.Models.AI`（非 `Models.Metadata`）；接口 `ScopeAsync` 须含第 4 参 `string? question = null` 以匹配实现与调用点。
- 验证：API 0 error；M5-04 相关 4 类测试 45/45 全绿。

### M5-03（提交 fb38cea，14 文件 +920/−245）
- 新增 `IQueryPlanStage` + `QueryPlanPipelineContext`（Planning 命名空间）；把 `QueryPlanPipeline.RunAsync` 硬编码的 8 段链路（Build→Context→MetadataIntegrity→DetailProjection→SemanticValidation→Confidence→DecisionGate→Explainability）抽离为有序注入的 `IQueryPlanStage` 集合。管线仅负责构造上下文、按序执行、每阶段后查 `EarlyResponse` 短路、最终处理 Step 5.4 Decision Gate 阻断。三处提前返回语义不变。
- `QueryPlanPipeline` ctor 改为注入 `IEnumerable<IQueryPlanStage>`（MS DI 按注册顺序解析）；`Program.cs` 按执行顺序注册 8 阶段。
- `QueryPlanMetadataValidator.Validate` 标记 `virtual`（纯可测试性增强，无行为变更），以支持测试假对象。
- 新增 `QueryPlanStagesTests`：真实管线集成 4 分支（成功 / 元数据异常 / 语义无效 / Decision Gate 阻断）+ 各阶段隔离单测（14 例）。
- 验证：API 0 error；QueryPlan 相关测试 75/75 全绿（含 M5-03 新增 14 例）。

### 复用要点
- 阶段化模式：共享可变上下文 + 每阶段可置 `EarlyResponse` 短路，比硬编码链更易测试与扩展（M5-05~M5-13 可在此骨架上挂阶段）。
- `dotnet test --filter "FullyQualifiedName~Xxx"` 可只跑单类/前缀，避免全量 500+ 用例耗时；filter 运行前会先编译整个测试工程，可顺带验证无编译破坏性改动。
- `dotnet` 绝对路径：`C:/Program Files/dotnet/dotnet.exe`（系统运行时），避免依赖 managed 运行时 PATH。

## M5-07 Decision Gate 状态化（提交 cf6e5db，8 文件 +204/−61）

用户指令「按计划继续推进」。M5-03/M5-04 已落地，按开发计划顺序推进 M5-07（Decision Gate 状态化），因其是 M5-05/06 表达拒绝/降级结果的前置基础且复用刚建好的 `QueryPlanDecisionGateStage`，风险最低。

### 实施
- 枚举 `QueryPlanDecisionType` 规范为 5 态：`Reject=0` / `RequireApproval=1`（原 Confirm）/ `Allow=2`（原 Proceed）/ `AskClarification=3` / `LimitedExecution=4`。整型值向后兼容；后两者为新增扩展态（供 M5-05 列级安全、M5-06 成本治理注入）。
- `QueryPlanDecision.ShouldExecute` / `RequiresConfirmation` 改为由 `Decision` 枚举派生的表达式属性（单一事实来源），新增 `IsReject/IsRequireApproval/IsAllow/IsAskClarification/IsLimitedExecution` 语义访问器。
- `QueryPlanDecisionGate` 仅设置 `Decision`（+`Confidence`+`Reason`），移除冗余布尔赋值；`QueryPlanDecisionTrace` 仍独立记录 `ShouldExecute/RequiresConfirmation`（从 decision 派生值填充，已恢复被脚本误删的两处）。
- 同步：Golden 评估器（`QueryPlanEvaluationConfidenceService`/`GoldenConfidenceCalibrationEvaluator`）与既有测试断言 `Proceed→Allow`、`Confirm→RequireApproval`。

### 验证
- API 0 error（28 既有警告，无关）。
- 决策/状态/管线相关测试 33/33 全绿：既有 `QueryPlanDecisionGateTests`（High→Allow / Medium→RequireApproval / Low→Reject 行为不变）+ `QueryPlanStagesTests`（4 分支集成）+ 新增 `QueryPlanDecisionStateTests`（5 态派生 + 语义访问器 + gate 映射回归 9 例）。
- 对 Golden 契约免疫：Golden 不进 Pipeline；枚举名在 `GoldenConfidenceCalibrationEvaluator` 内 `Enum.TryParse(decision.ToString())` 字符串回路自洽（两边同名）。

### 坑
- Python 脚本批量删 `ShouldExecute =`/`RequiresConfirmation =` 赋值时，把 `AttachTrace` 中给 `QueryPlanDecisionTrace` 自身属性赋值的同名行也误删（16 对=14 decision + 2 trace）。必须单独恢复 trace 的两处赋值，否则决策轨迹丢失 ShouldExecute/RequiresConfirmation。
- 改名 enum 成员后，所有 `new QueryPlanDecision { ShouldExecute = ... }` 测试假对象会编译失败（属性已无 setter）→ 改为 `new() { Decision = QueryPlanDecisionType.Allow }` 等。
- Edit 工具对同文件并行写会偶发 EBUSY（资源锁），重试即可。

## M5-05 Column-Level Security（提交 0fc4a31，11 文件 +728/−1）

用户指令「继续」。M5-03/04/07 已落地，按计划推进 M5-05（列级安全）。

### 实施
- 新增管线阶段 `QueryPlanColumnSecurityStage`（Services.BI），接入 Pipeline（Context 之后、MetadataIntegrity 之前），复用 `ValidationContext.TableColumns` 做 Filter/Metric 列名→Id 反查。
- 三件套端口+实现：`IColumnSensitivityClassifier`（列是否受限）+ 默认 `DenyNothingColumnClassifier`（false，零影响）；`IColumnSecurityPolicy`+`ColumnSecurityPolicy`（遍历 Plan 六类列引用剔除未授权受限列，主投影全清空→RequiresRejection）；`IColumnSecurityContextResolver`+`DefaultColumnSecurityContextResolver`（从 `IDataSourceExecutionIdentityAccessor` 解析身份，授权白名单留治理扩展点默认空）。
- `ColumnSecurityContext`/`ColumnSecurityResult`/`BlockedColumnReference`/`ColumnMaskingStrategy` 枚举（None/Redact/Hash/PartialMask，脱敏投影预留）。
- `Program.cs`：阶段顺序 Build→Context→**ColumnSecurity**→...；注册三件套 Scoped。

### 关键设计
- 零 schema 变更：`MetadataColumn` 不新增字段（规避受控迁移）；敏感/授权判定全由注入策略接口完成。默认 classifier 不敏感→零默认行为变更→对 Golden 免疫。
- 拒绝路径复用 `ErrorCodes.QueryPlanSecurityRejected`（403），未新增错误码。
- 主投影空才拒绝（deny-by-default）；部分剔除后 Plan 仍有效则不拒绝。

### 验证
- API 0 error；新增 `QueryPlanColumnSecurityTests` 11 例全绿；QueryPlan 相关 96/96 全绿（零回归）。

### 复用要点
- 敏感/权限判定走"注入策略接口"而非"实体字段"，可规避 DB 迁移，适合治理策略后续接入（实现 classifier 读敏感标记、resolver 加载授权列 ACL）。
- `new[] { long, long?, long? }` 推断 `long?[]`，foreach 的 id 不能直接传 `RestrictedAsync(long, ...)` → 过滤后 `.Select(x => x!.Value)` 转 long。

## M5-06 Query Cost Governance（提交 d9e01a1，13 文件 +725/−1）

用户指令「继续执行」。M5-05 之后按计划推进 M5-06（查询成本治理）。

### 实施
- 新增管线阶段 `QueryPlanCostGovernanceStage`（Services.BI），接入 Pipeline（**DecisionGate 之后、Explainability 之前**），复用 M5-07 的 5 态决策与管线既有 EarlyResponse 短路。
- 三件套端口+实现：`IQueryCostClassifier`+`StructuralQueryCostClassifier`（抽取 JOIN 数/无界性/请求行数上限等确定性结构信号，模型成本评级默认 Low 未接遥测）；`ICostGovernancePolicy`+`ThresholdCostGovernancePolicy`（阈值裁决：JOIN 超硬上限→Reject，超预警阈值→Degrade，无界→注入防御性 Limit 降级）；`ICostGovernanceContextResolver`+`DefaultCostGovernanceContextResolver`（从 `IDataSourceExecutionIdentityAccessor` 解析租户，特权豁免留扩展点默认不豁免）。
- 新增领域类型：`QueryCostAssessment`/`QueryCostSeverity`/`ModelCostTier`/`CostGovernanceVerdict`/`CostGovernanceAction`/`CostGovernanceContext`（Domain/BiQuery 与 Models.BI）。
- 新增 `CostGovernanceOptions`（Application/Common/Options，SectionName "CostGovernance"），经 `Program.cs` `Configure<CostGovernanceOptions>` 绑定。
- `ErrorCodes` 新增 `QueryPlanCostGoverned = "SB_COST_001"` + 友好文案。

### 关键设计
- **默认全部关闭**（EnableUnboundedGuard/EnableJoinGuard/EnableModelCostGuard 均 false）→ 零默认行为变更、对 Golden 免疫、本地 checkpoint 安全；由配置显式开启（渐进式治理）。
- 阶段语义：拒绝→改写 `Decision=Reject` + 置 `EarlyResponse`（403, SB_COST_001）；降级→注入防御性 Limit（取 min(现有, 上限)）且 `Decision=LIMITED_EXECUTION`（沿用 ShouldExecute 通路）。
- **重要语义边界**：无界守卫仅在 `plan.Limit`/`intent.Limit` 均为 null（IsUnbounded=true）时触发；已设 Limit 的"有界查询"不受无界守卫治理。

### 验证
- API 0 error；新增 `QueryPlanCostGovernanceTests` 15 例全绿（分类器 3 + 策略 7 + 阶段集成 3 + 自定义策略 2）；QueryPlan 相关 111/111 全绿（零回归）。
- 一处测试前提写错（假设"有界计划 Limit=50 + 无界守卫"会降级）已修正为正确语义（有界不触发）；并补 `FakeDegradePolicy` 覆盖 `Math.Min` 分支。

### 复用要点
- M5 阶段化骨架价值兑现：M5-06 完全"挂新阶段"完成，未改动管线/Builder/Decision 任何既有代码。
- 治理控制默认关闭是本仓库（受控迁移/零回归）场景下的稳妥模式，M5-05 列级安全同样采用（默认 DenyNothing classifier）。
- 同程序集跨命名空间仍需显式 `using`（如 Options 文件引用 `Models.BI.ModelCostTier`）。

## M5-01 Canonical Semantic Model（提交 1bb1d63，5 文件 +329）

用户指令「需要，然后按推荐顺序继续推进」（先修订计划插入治理启用里程碑）。M5-01 是 M5 真实治理策略的前置地基。

### 实施
- 领域类型 `CanonicalSemanticModel.cs`（Models.BI）：`SemanticKind` 枚举（Entity/Metric/Dimension/Filter/Binding）+ `CanonicalSemanticId`（`readonly record struct`，`Kind`+`Key`，稳定串 `<KIND>:<key>`，含 `Parse` 与工厂方法，空 key/非法串抛异常）+ 五类定义记录（CanonicalEntity/Metric/Dimension/Filter/Binding）+ 容器 `CanonicalSemanticModel`（`FindId(nameOrAlias)` 跨种类按名称/别名查找）。
- 端口 `ICanonicalSemanticResolver`（Interfaces.BI.Planning）+ 默认 `PassThroughCanonicalResolver`（Services.BI，名称即标识、零外部依赖），`Program.cs` 注册为 Scoped。
- 计划修订（提交 b151550）：插入 M5-08「Governance Policy Enablement」（原编号 SB-P1-12，真实 classifier/成本配置/遥测/逐租户灰度）；原 M5-08~M5-13 顺延 M5-09~M5-14；同步更新行 190/770/806 三处引用；补充执行顺序 M5-01→M5-02→M5-08→M5-09→M5-10→M5-11→M5-12/13/14。

### 验证
- API 0 error；新增 `QueryPlanCanonicalSemanticModelTests` 11/11 全绿；QueryPlan 相关 122/122 零回归；对 Golden 免疫。

### 坑
- 构建期遇 `MSB3027` 文件锁（残留 dev server PID 20644 占用 `SuperBuilder_AI.exe`），释放后重建通过。
- 测试工程未启用 ImplicitUsings，补 `using System;`；git 路径须 `SuperBuilder_AI/src/...` 与 `tests/...` 完整相对路径。

## M5-02 统一字段解析规则（提交 07aacf8，6 文件 +470/−101）

用户指令「继续」（M5-01 之后按计划推进 M5-02）。

### 实施
- 共享词汇：`SemanticFieldReference`（运行时字段引用：Kind + SemanticText + PhysicalColumn + ColumnId，含 `From`/`FromPlan` 工厂与 `HasToken`）+ `SemanticFieldBinding`（权威绑定侧，含 `From(QueryPlanXxxResolution)` 工厂），均位于 Domain/BiQuery（Models.BI）。
- 单一匹配算法 `SemanticFieldBindingMatcher`（Services.BI，纯静态）：优先级 P1 SemanticText 精确 → P2 物理列↔绑定 SemanticText/物理列 → P3 SemanticText↔绑定物理列（维度互换情形）→ P4 位置回退（仅 HasToken）。取代 Builder 中 `MatchMetricBinding`/`MatchFilterBinding`/`MatchDimensionBinding` 三套近义、且有细微分叉的匹配逻辑（维度旧逻辑缺 HasToken 守卫 → 潜在幽灵维度，已统一修复）。算法为三套旧逻辑并集，不削弱任何已有匹配路径。
- Validator：`ValidateMetrics`/`ValidateFilters` 的"字段物理解析"告警改为以 `SemanticFieldReference.FromPlan(...)` + `IsPhysicallyResolved(...)` 表达（口径与原有列名匹配完全一致，零行为变更）。
- 无 DI 注册（纯静态，零引入依赖，零回归）。

### 验证
- API 0 error；新增 `QueryPlanUnifiedFieldResolutionTests` 18/18 全绿；QueryPlan 相关 140/140 零回归；对 Golden 免疫。

### 复用要点
- 治理"统一规则"的标准形态：将分散在多处、近义且易漂移的判断收敛为单一纯算法 + 共享值对象，三阶段（Understanding/Builder/Validator）以同一词汇与同一算法表达，结果天然一致。
- 位置回退统一要求 HasToken，是根治"空引用误绑"类幽灵项的关键不变量。

## M5-08 Governance Policy Enablement（提交 146f738，15 文件 +712/−9）

用户指令「继续」（M5-01/M5-02 之后按计划推进 M5-08，将 M5-05/06 安全默认提升为可配置启用的真实治理）。

### 实施
- **列级真实分类器**：新增 `ColumnSecurityOptions`（Application/Common/Options，SectionName "ColumnSecurity"）：`ClassifierMode`(DenyNothing/PolicyDriven 默认 DenyNothing) + `RestrictedColumnNames` + `IncludeBuiltInPiiHeuristics`(默认 true) + `TenantOverrides`(Dictionary<long, TenantColumnSecurityOverride> 逐租户覆写)；`ColumnClassifierMode`/`TenantColumnSecurityOverride` 同文件。`BuiltInPiiCatalog`（Services.BI，小写 PII 列名集：id_card/password/phone/email/salary/...）。`PolicyDrivenColumnClassifier`（Services.BI，按列名+内置PII+配置名+租户覆盖判定，`IColumnSensitivityClassifier`）。`ColumnSecurityModeResolver`（Services.BI，全局/租户模式单一事实来源）。`ConfigurableColumnClassifier`（Services.BI，注册为 `IColumnSensitivityClassifier`，在 `DenyNothing`↔`PolicyDriven` 间按模式切换；默认 DenyNothing 逐字节等价旧行为）。
- **成本治理灰度 + 遥测**：`CostGovernanceOptions` 增 `Mode`(Off/Threshold 默认 Off 灰度主开关) + `TelemetryMode`(None/Log 默认 None)；`CostGovernanceMode`/`CostTelemetryMode` 同文件。`ThresholdCostGovernancePolicy` 增主开关短路（Mode≠Threshold→NoAction）。新增端口 `IModelCostTelemetry`（Interfaces.BI.Planning）+ `NoOpModelCostTelemetry`(默认) + `LogModelCostTelemetry`(ILogger Warning 级) + `ConfigurableModelCostTelemetry`（Services.BI，按 TelemetryMode 切换）；`QueryPlanCostGovernanceStage` ctor 增 `IModelCostTelemetry`+`IOptions<CostGovernanceOptions>`，非 NoAction 裁决写入遥测（默认 NoOp 零行为）。
- **DI**：`Program.cs` 注册 `Configure<ColumnSecurityOptions>`/`Configure<CostGovernanceOptions>`、列分类器三件套、`IModelCostTelemetry→ConfigurableModelCostTelemetry`、NoOp/Log 两个具体实现。
- **测试同步**：M5-06 测试 9 处 `new CostGovernanceOptions{...}` 补 `Mode = Threshold`（主开关默认 Off 后需显式 opt-in，否则原守卫测试返回 NoAction 失败）；`BuildStage` 与 `FakeDegradePolicy` 构造补遥测/Options 参数。

### 验证
- API 0 error；M5-08 新增 `QueryPlanGovernanceEnablementTests` 13/13 全绿；M5-06 回归 15/15 全绿；QueryPlan 相关 153/153；**完整套件 821/821 零回归**（含 Golden，确认 M5-08 免疫）。
- 默认零行为变更：默认配置下 `ConfigurableColumnClassifier` 等价于 DenyNothing、成本主开关 Off、遥测 None。

### 关键设计 / 坑
- **测试兼容性陷阱**：给 `CostGovernanceOptions` 加主开关 `Mode`（默认 Off）会令既有 M5-06 测试（默认 options 启用守卫）因主开关未开返回 NoAction 而失败——必须同步给所有启用守卫的 `new CostGovernanceOptions` 补 `Mode=Threshold`。这是新增"默认关闭的主开关"时的必然联动，落地前须审计全部构造点。
- 复用 M5-05/06「可配置委托」模式（`ConfigurableColumnClassifier`/`ConfigurableModelCostTelemetry`）：默认分支完全等价旧实现（DenyNothing/NoOp），启用仅由配置切换，保证零默认行为变更 + 对 Golden 免疫。
- 测试工程未启用 ImplicitUsings，FakeLogger 需 `using System;`（IDisposable/Exception/Func）。
- 计划修订（b151550）+ M5-01(1bb1d63) + M5-02(07aacf8) + M5-08(146f738) 均为本地 checkpoint 提交，未推送 origin。

## M5-09 AI Decision Audit（提交 afd1135，11 文件 +883/−38）

用户指令「继续」（M5-08 之后按计划推进 M5-09，决策全过程可追踪审计）。

### 实施
- **审计记录模型** `QueryPlanDecisionAuditRecord`（Domain/BiQuery，Models.BI）：八维字段——问题(`Question`)/意图(`IntentType`+`IntentSummary`)/计划(`PlanTableNames`/`PlanFields`/`PlanFilterCount`/`PlanOrderCount`/`PlanJoinCount`/`PlanLimit`/`PlanIsAggregate`)/SQL(`Sql` 预留 null)/修复(`HasRepair`/`RepairStatus`/`RepairAttempts`/`RepairChangedPlanCount`/`RepairStopReason`/`RepairHistoryCount`)/置信度(`ConfidenceScore`/`ConfidenceLevel`/`CanProceed`/`IsExecutableDetailQuery`/`ValidationErrorCount`/`RepairCount`)/决策(`DecisionType`/`DecisionReason`/`ShouldExecute`/`RequiresConfirmation`/`DecisionTraceSummary`)/模型(`Model` 预留 null)/结果(`Outcome`:`AuditOutcome` 枚举 Executed/RequiresApproval/AskClarification/Rejected/EarlyResponse + `ErrorMessage`)。`AuditId`(Guid)/`CorrelationId`(请求级)/`EvaluatedAt`(UTC) 标识。
- **端口 + 三件套**：`IDecisionAuditSink`（Interfaces.BI.Planning，`RecordAsync(record, ct)`）+ `NoOpDecisionAuditSink`(默认零行为) + `LogDecisionAuditSink`(ILogger Information 级结构化日志) + `ConfigurableDecisionAuditSink`（按 `DecisionAuditOptions.Mode` 切换 None/Log，默认 None）。`DecisionAuditOptions`（Application/Common/Options，SectionName "DecisionAudit"，`Mode` 默认 None）。
- **记录构建器** `DecisionAuditRecordBuilder`（Services.BI，纯静态工厂 `Build(ctx, outcome, correlationId, errorMessage, model)`）：从 `QueryPlanPipelineContext` 提取全部字段，优雅处理 null（EarlyResponse 短路时上下文不完整 → 缺失字段为 null/空列表）。列表类字段初始化为非空空列表（便于序列化）。
- **采集点**：`QueryPlanPipeline.RunAsync` 注入 `IDecisionAuditSink`；方法内生成 `correlationId`(Guid)，在 `try/finally` 的 finally 中（三路径：阶段 EarlyResponse 短路 / Decision Gate 阻断 / 成功）调用 `_auditSink.RecordAsync(...)`；finally 内 try/catch 包裹 sink 调用——**审计旁路故障绝不破坏主流程返回**。Decision Gate 阻断态按 `MapDecisionOutcome` 映射为对应 `AuditOutcome`（Reject→Rejected/RequireApproval→RequiresApproval/AskClarification→AskClarification，可执态→Executed）。
- **DI**：`Program.cs` 注册 `Configure<DecisionAuditOptions>(GetSection)` + `IDecisionAuditSink→ConfigurableDecisionAuditSink`（默认 None→NoOp）。

### 验证
- API 0 error；M5-09 新增 `QueryPlanDecisionAuditTests` 4/4 全绿（默认 NoOp 零行为 / Collecting 八维字段正确 / EarlyResponse 同样被审计 / NoOp 不抛）；QueryPlan 相关 157/157；**完整套件 825/825 零回归**（含 Golden，确认 M5-09 免疫）。
- 测试工程构造器同步：`QueryPlanStagesTests` 的 `new QueryPlanPipeline(stages)` 补 `NoOpDecisionAuditSink` 第二参。

### 关键设计 / 坑
- **SQL/模型预留**：管线本身不生成 SQL 文本（SQL 在下游 `BIResponse.Sql` 生成）、上下文无 LLM 模型字段，故审计记录以"计划要素摘要"（表/字段/过滤/排序/Limit）作为 SQL 可重建源，并预留 `Sql`/`Model` 字段供上游填充。忠实覆盖验收口径而不引入新依赖。
- **只读属性陷阱**：`QueryPlanPipelineContext.Intent` 与 `QueryPlanDecision.ShouldExecute` 均为只读/派生属性，测试中 seed 阶段不能赋值——改为 `RunAsync` 调用处传入完整 `QueryIntent`，`ShouldExecute` 由枚举派生（Allow→true）。
- **列表字段空 vs null**：记录设计上 `PlanTableNames` 等非空（序列化友好），缺失时为 `Empty` 而非 null——测试断言须用 `Assert.Empty` 而非 `Assert.Null`。
- 计划修订（b151550）+ M5-01(1bb1d63) + M5-02(07aacf8) + M5-08(146f738) + M5-09(afd1135) 均为本地 checkpoint 提交，未推送 origin。

## M5-10 Production Feedback（提交 af88953，13 文件 +669/−1；计划标记 0211dc7）

用户指令「继续」（M5-09 之后按计划推进 M5-10，Feedback→Candidate→Review→Baseline→Regression 闭环）。

### 实施
- **领域聚合** `ProductionFeedback`（Domain/BiQuery，Models.BI）：`FeedbackId`(Guid)/`AuditId`(关联 M5-09)/`CorrelationId`/`Question`/`Category`(`FeedbackCategory` 枚举 Other/Correctness/Performance/Security/Usability)/`Description`/`Source`(`FeedbackSource` UserManual/AuditAuto)/`SubmittedAt`/`Status`(`ProductionFeedbackStatus` Received→InReview→AcceptedAsCandidate→Baselined→RegressionVerified / Rejected)/`Reviewer`/`ReviewNote`/`ReviewedAt`/`CandidateId`/`BaselineVersion`/`RegressionRef`/`RejectReason`。`FeedbackDrivenCandidate`（CandidateId/FeedbackId/SourceAuditId/Title/ProposedChange/CreatedAt）。
- **端口三件套**：`IProductionFeedbackStore`（Interfaces.BI.Planning，默认内存桩零落盘）/ `IProductionFeedbackSink`（`RecordAsync` + `RecordFromAuditAsync(audit)`）/ `IFeedbackBaselineGateway`（注册基线 + 校验回归，**默认 NoOp 解耦既有 Golden Baseline 服务**）。
- **配置** `ProductionFeedbackOptions`（Application/Common/Options，SectionName "ProductionFeedback"）：`Mode`(Off/Store 默认 Off 灰度主开关) + `EnableAuditDerivedFeedback`(默认 false)。`ProductionFeedbackMode` 同文件。
- **三件套 Sink**：`NoOpProductionFeedbackSink`(默认) + `StoreProductionFeedbackSink`（写入 store；`EnableAuditDerivedFeedback` 时由 `QueryPlanDecisionAuditRecord` 派生自动反馈 `Source=AuditAuto`，按 `Outcome` 推导 `Category`如 Rejected→Correctness）+ `ConfigurableProductionFeedbackSink`（按 Mode 切换，默认 Off→NoOp）。
- **闭环状态机** `ProductionFeedbackLoop`（Services.BI）：`ReceiveAsync`→`StartReviewAsync`→`AcceptAsync`(生成 Candidate)→`BaselineAsync`(经 Gateway 注册)→`VerifyRegressionAsync`(经 Gateway 校验)；`RejectAsync` 终止；前置状态不符抛 `InvalidOperationException`。
- **DI**：`Program.cs` 注册 `Configure<ProductionFeedbackOptions>` + `IProductionFeedbackStore→InMemoryProductionFeedbackStore` + `IFeedbackBaselineGateway→NoOpFeedbackBaselineGateway` + `IProductionFeedbackSink→ConfigurableProductionFeedbackSink` + `ProductionFeedbackLoop`。**不接管线默认路径，对 Golden 免疫**。

### 验证
- API 0 error；M5-10 新增 `QueryPlanProductionFeedbackTests` 9/9 全绿（默认 NoOp 零行为 / Store 持久化 / 审计派生开关 / Configurable Off·Store / 完整闭环 / Reject / 错误态抛异常）；QueryPlan 相关 166/166；**完整套件 834/834 零回归**（含 Golden，确认 M5-10 免疫）。
- 默认零行为变更：Mode=Off 时 Sink 与历史逐字节等价；闭环 Baselined/RegressionVerified 经 NoOp Gateway 返回占位标识，不触达真实 Golden 回归。

### 关键设计 / 坑
- **Gateway 解耦 Golden**：闭环的 Baselined/Regression 步骤通过 `IFeedbackBaselineGateway` 抽象，默认 `NoOpFeedbackBaselineGateway` 返回占位串，不调用 `GoldenBaseline*` 服务——既满足"闭环"语义，又保证对既有 Golden 体系零侵入、对 Golden 契约免疫。生产启用时再注入真实网关（接 `GoldenBaselineRegressionService`）。
- **复用 M5-09 审计记录**：`AuditId`/`CorrelationId` 作为关联键，`RecordFromAuditAsync` 在 `EnableAuditDerivedFeedback` 时把失败类审计（Rejected/EarlyResponse）转化为自动反馈，形成"审计→反馈"可选衔接，闭环数据来源闭环。
- 测试工程未启用 ImplicitUsings，需 `using System;`、`using Microsoft.Extensions.Options;`（`Options.Create`）。
- 提交序列未推送 origin：b151550(计划修订) → 1bb1d63(M5-01) → 07aacf8(M5-02) → 146f738(M5-08) → afd1135(M5-09) → af88953(M5-10) → 0211dc7(计划标记)。

## M5-11 AI BI E2E（提交 0d81281，1 文件 +330；计划标记待提交）

用户指令「继续」（M5-10 之后按计划推进 M5-11，AI BI 全链 NL→API→Plan→SQL→Test DB→Result 覆盖）。

### 实施
- 新增 `tests/SuperBuilder_AI.Tests/QueryPlanAiBiE2ETests.cs`（330 行，3 例 Fact）：
  - `E2E_Plan_To_Sql_To_TestDb_To_Result_RealExecution`：真实 SqlQueryBuilder(PostgreSQL 方言)→真实 QueryExecutionService(Dapper, SQLite)→真实 ResultUnderstandingService，断言 SQL 含 `"WarehouseReceipts"`/`"come_time"`/`ORDER BY`/`LIMIT 10`、data.Rows.Count==10、按 come_time 倒序、answer.Success。
  - `E2E_NL_To_Plan_Deterministic`：NL→Intent→Plan 确定性映射字段断言。
  - `E2E_NL_To_Api_To_Plan_To_Sql_To_Result_FullChain`：经 `BIConversationService.ExecuteAsync("最近的十张入库凭证", TenantId, requestedDataSourceId)` 全链贯通断言。
- **确定性隔离设计（核心）**：LLM(Qwen) 与 metadata 向量检索是链路唯一非确定性源，全部用桩隔离——`FakeUnderstanding`(NL→Intent)、`FakePlanBuilder`(绕过向量检索直接产 Plan)、`FakeQwen`(明细分支兜底)、`FileConnectionFactory`(自定义 `IDataSourceConnectionFactory` 返回同一文件库 SQLite 连接)；真实组件为 SqlQueryBuilder/QueryExecutionService/ResultUnderstandingService/BIConversationService。注入的 `QueryPlanPipeline` 仅含 `QueryPlanBuildStage`，不触发 metadata validator（其已由 M5 各单测覆盖）。
- **离线可还原库**：文件型 SQLite 临时库（EF `SuperBIContext` 与 Dapper 共享），`EnsureCreated()` 后种子 Tenant(1)+DataSource(1, POSTGRESQL, Enabled)+`WarehouseReceipts` 15 行；Dispose 删除。PostgreSQL 方言生成的 `"col"` + `LIMIT n` SQL 在 SQLite 原生可执行。
- 明细列表（`IsDetailList(plan)`）经 `ResultUnderstandingService` 确定性分支（"已返回 N 条记录"），对 LLM 完全免疫——旗舰场景"最近的十张入库凭证"为明细 TopN，故 Result 环节确定。

### 验证
- 系统 `dotnet`(`/c/Program Files/dotnet/dotnet`) 构建 0 error（57 警告，均预存在，与本次无关）。
- **M5-11 过滤集 3/3 全绿**；**完整套件 837/837 零回归**（M5-10 基线 834 + 3），含 Golden，确认 M5-11 免疫。

### 坑
- **SQLite 连接池文件锁**：初版 `Dispose` 删临时库报 "file used by another process"——根因 `Microsoft.Data.Sqlite` 默认 `Pooling=true`，连接池持有文件句柄。`Pooling=false` 让连接关闭即释放；`Dispose` 内 `SqliteConnection.ClearAllPools()` 兜底。修复后 3/3 通过。
- 测试工程未启用 ImplicitUsings，需显式 `using System;`/`using Microsoft.Data.Sqlite;`/`using Microsoft.EntityFrameworkCore;`。
- `dotnet` 用系统运行时绝对路径，避免依赖 managed 运行时 PATH（目录名含特殊空格且 managed 未安装）。

### 提交序列（本地 checkpoint，未推送 origin）
- 0d81281(feat M5-11) → 计划标记待 `docs(M5-11)` 提交（含 0d81281）。

## M5-12 GQ-006 物料缺独立主表→DirectKey 真实解析（提交 be6ac70，2 文件 +253；计划标记待 docs 提交）

用户经 AskUserQuestion 批准「按方案实现（推荐）」。M5-11 收尾后按计划推进 M5-12（GQ-006 能力缺口）。

### 根因（RED 证明）
`DimensionResolutionEvidenceService.ResolveAsync` 无主表分支内，`HasDimensionEvidence(x, "物料")` 过滤要求列元数据含维度词；`material_id`(FK) 列名英文无"物料"被排除 → `keyCandidates` 空 → 跳过 DirectKey → 落 `NotResolved`。评估器层旧兜底 `ResolveDimensionFallbackAsync` 以 display 列 `material_name` 降级 DirectKey，致 GQ-006 维度键错位。

### 实施（GREEN）
- 无主表分支（`if (bestKey is not null){...}` 之后、`fallback` 之前）注入 `else`：调用新增 `DiscoverAlignedDimensionKey(fact, displayCandidates)`，命中则返回 `DirectKey` 证据（`ExecutionCapability=Executable`、`FactKeyColumn=alignedKey.ColumnName`）。
- 新增两静态方法：`DiscoverAlignedDimensionKey`（取 display 候选的命名根集合，从事实表全列过滤 `IsPrimaryKey || LooksLikeForeignKey` 且命名根对齐者，优先 FK 后 PK）+ `StripDimensionSuffix`（剥离 `_id/_code/_name/_no/_key` 等后缀取共享根，如 `material_id`/`material_name`/`material_code` → `material`）。
- **契约合规**：仅增强服务层，零改动 `QueryPlanEvaluator`/GQ-011 Ranking/Ranking Contract/`QueryJoin`/SQL Builder；已 MasterJoin/DirectKey 案例逐字节不变 → 零默认行为变更。符合 Phase 2.7 D03 冻结第 4 条（无主表不得直接 NotResolved，须检查事实表稳定 Dimension Key/Code/Name）。

### 验证
- 系统 `dotnet` 构建 0 error；新增 `DimensionResolutionEvidenceServiceTests` 3 例：
  1. `Material_Without_MasterTable_Resolves_To_DirectKey_On_AssociationId`：断言 `ResolutionType=="DirectKey"`、`ExecutionCapability=="Executable"`、`FactKeyColumn=="material_id"`（GREEN 前为 NotResolved/RED，证明根因）。
  2. `Material_With_No_Dimension_Evidence_Stays_NotResolved`：搜"供应商"无证据 → 断言 `null || NotResolved || Ambiguous`（安全边界不变，已保绿）。
  3. `GQ006_Evaluator_Resolves_Material_Dimension_To_DirectKey_On_AssociationId`：构造 GQ-006 `GoldenQueryCase`（入库数量/Sum，物料维度，IsAggregate/Ranking/AggregateRanking/Limit=10）→ `SemanticApplicabilityEvaluator.EvaluateAsync` → 断言 `State=="Resolved"`、`dim.ResolutionType=="DirectKey"`、`dim.DimensionKeyColumn=="material_id"`（GREEN 前为 material_name/RED）。
- 修复前 2 RED 证明根因；修复后过滤集 3/3 全绿；**完整套件 840/840 零回归**（M5-11 基线 837 + 3），含 Golden，确认 M5-12 免疫。

### 坑
- 编译错误 CS1061：`DimensionResolutionEvidence` 无 `DimensionKeyColumn` 属性（维度键在证据模型即 `FactKeyColumn`/`FactKeyColumnId`）→ 服务层测试断言改用 `FactKeyColumn`，评估器层 `SemanticApplicabilityDimensionResolution.DimensionKeyColumn` 合法保留。
- 首编译因上述错误未含新测试 DLL，`--no-build` 跑旧产物报"没有测试匹配" → 去掉 `--no-build` 重新构建运行。
- Edit 锚点匹配：插入辅助方法须用精确含 "D8: " 前缀的注释文本定位，避免与 `LooksLikeForeignKey`/`NormalizeIdentifier` 之间的其他方法块混淆。

### 提交序列（本地 checkpoint，未推送 origin）
- be6ac70(feat M5-12) → 计划标记 + 本条目待 `docs(M5-12)` 提交（含 be6ac70）。

## M5-13 完成 Phase 2.7 Regression（计划标记 99eb6c4 后追加；本条目随 docs 提交）

用户经 AskUserQuestion 批准「标记 M5-13 完成并继续 M5-14」。

### 就绪核查（只读）
- **唯一缺口已闭合**：Phase 2.7 全文档（`docs/plans/archive/Phase2.7-*`）中仅 GQ-006 一处 BLOCK/NotResolved（Runtime-02 归档为能力缺口，归因"物料无独立主表→旧逻辑直接 NotResolved"），现由 M5-12（be6ac70）以 D03 第 4 条合规的 DirectKey(material_id) 解析闭合；无其它遗留缺口。
- **Golden 契约兼容**：`query-plan-golden-v1.json` GQ-006（行 183-226）期望为**排名结构**（metric=入库数量/sum、dimension=物料、order DESC、limit=10、isAggregate/isRanking/isAggregateRanking），**未钉死维度解析态为 NotResolved**。M5-12 翻转为 DirectKey 后产出一致结构 → 既非"为提升通过率强制改 Resolved"（D03 第 5 条明令禁止），又履约 D03 第 4 条。
- **回归全绿**：完整套件 840/840（含 Golden 18/18）零回归，确认 Phase 2.7 Regression 证据完整。

### 结论
M5-13 实质条件（证据完整后宣布 Phase 3.1 Frozen）已满足回归层面。但计划仍列 M5-14（Ask 首用例）未做，故**暂不宣布 Phase 3.1 Frozen**，仅标记 Phase 2.7 Regression 完成。后续按推荐顺序推进 M5-14。

### 提交序列（本地 checkpoint，未推送 origin）
- 计划标记（M5-13 行）+ 本条目随 `docs(M5-13)` 提交。

## M5-14 Ask 首用例（提交 42fc64f，1 文件 +181；计划标记随 docs 提交）

用户经 AskUserQuestion 批准「标记 M5-13 完成并继续 M5-14」。M5-13 收尾后推进 M5-14（Ask 首用例）。

### 核查（实现前提已落地）
- `AskController`(POST /api/ask) → `IBIConversationService.AskAsync` 直接委托 `ExecuteAsync`，走完整管线（理解→Pipeline→SQL Builder→执行→结果理解）；M5-11 E2E `E2E_NL_To_Api_To_Plan_To_Sql_To_Result_FullChain` 已用「最近的十张入库凭证」证明全链。
- 明细列表语义（M0-09）：`QueryPlanBuilder.IsDetailListQuery` + Step5.6 补列（`plan.Tables.Any(MetadataTableId>0) && 非聚合 + Limit/OrderBy` → 自动补首选/兜底展示列）；`QueryPlanDecisionGate` 对 `IsExecutableDetailQuery` 的 Medium 直接 Allow 进入 SQL Builder。
- `SB_BI_002`(BiNoQueryableField) 触发点：`QueryPlanBuilder.cs:1451` 仅当 `plan.Fields.Count == 0`（确实无可用字段）抛「没有任何可查询字段」，由 `UnifiedExceptionMiddleware:89` 映射为 SB_BI_002；无搜索结果时更早抛「Metadata语义搜索没有找到任何结果」（不同错误，非 SB_BI_002）。

### 实施
- 新增 `tests/SuperBuilder_AI.Tests/AskDetailListFirstUseCaseTests.cs`（2 例，真实 `QueryPlanBuilder` + 内存 SQLite + FakeSearch/FakeJoin，复用 M0-09 DetailList 建制）：
  1. `Ask_First_UseCase_DetailList_Resolves_To_ValidPlan_Without_SB_BI_002`：明细意图（实体 wms_receipt + Limit=10 + OrderBy=come_time、无指标/维度）→ 断言 `plan.Tables` 含 `MetadataTableId>0`、`Metrics/Dimensions` 空、`Fields.Count>0`（未误报 SB_BI_002）、不含 del_flag。
  2. `Ask_First_Use_Case_Unformable_Query_Triggers_SB_BI_002`：全内部列表（del_flag/is_deleted/create_by/update_by）+ 明细意图 Limit=10 → 断言抛 `InvalidOperationException` 含「没有任何可查询字段」（→SB_BI_002）。复现口径同 `QueryPlanBuilderQaRegressionTests.X2`。

### 验证
- 系统 `dotnet` 构建 0 error（预存在 CS8767/CA2017 等警告无关）；M5-14 过滤集 2/2 全绿；**完整套件 842/842 零回归**（M5-13 基线 840 + 2），含 Golden，确认 M5-14 免疫。
- 测试工程未启用 ImplicitUsings，须补 `using SuperBuilder_AI.Models.AI;`（MetadataSemanticSearchResult 命名空间）+ `using SuperBuilder_AI.Interfaces;`（IMetadataSemanticSearchService 命名空间，非 Interfaces.BI）。

### 坑
- 负向用例初版用「空搜索结果」意图，Builder 更早抛「Metadata语义搜索没有找到任何结果」（非 SB_BI_002）→ 断言落空 FAIL。修正为「实体已解析但全内部列」场景，命中真正的 SB_BI_002 抛点（同 X2）。

### 提交序列（本地 checkpoint，未推送 origin）
- 42fc64f(feat M5-14) → 计划标记（M5-14 行）+ 本条目随 `docs(M5-14)` 提交。

## M6-02 Ask 输入与状态契约硬化（提交 70724b4，3 文件 +84/−11；计划标记随 docs 提交）

M5 全闭环后按推荐顺序推进 M6（Ask 企业化），M6-02 为基础性、低风险首步（不引入 Redis 等外部依赖，为 M6-03/04 打底）。

### 实施
- `SuperBuilder_AI/src/Domain/BiQuery/BIResponse.cs`：新增 `ConversationStatus` 枚举（AwaitingClarification/Completed/Failed/Expired/Cancelled）；`BIResponse.ConversationStatus` 由 `string` 改为该枚举，`[JsonConverter(typeof(JsonStringEnumConverter))]` 字符串序列化（wire 值 "AwaitingClarification"/"Completed" 不变）。
- `AskController.cs`：补 `using SuperBuilder_AI.Models.BI;`；Question 长度契约 2–2000（早于鉴权，400）；ConversationId 长度护栏 ≤128（400）；Refine `History` 轮数护栏 ≤20（400）；`response.ConversationStatus` 改用枚举（替代魔法字符串）。
- `AskControllerTests.cs`：占位 `Question="q"`（1 字符）改为 `请查询销售额` 以适配 2 字符下限（机械调整，非行为变更）；新增 4 例（过短/过长 Question、过长 ConversationId、Refine 超轮 → 400）。

### 关键设计 / 坑
- **双 BIResponse 副本**：后端 `SuperBuilder_AI.Models.BI.BIResponse`（src/Domain/BiQuery）与前端 `SuperBuilder_AI.Components.Models.BIResponse`（string 副本）是独立类型；仅改后端枚举 + 字符串转换器，线格式不变，前端无需改动。项目已有 `JsonStringEnumConverter` 用法（PlatformAdminService/ GoldenQueryDatasetSerializer），风格一致。
- **AskController 缺 using**：原文件未 `using SuperBuilder_AI.Models.BI;`（BIResponse 靠全局 using 解析），新增枚举须显式补 `using` 否则 CS0103。
- 线格式兼容性：`JsonStringEnumConverter` 确保枚举序列化为成员名（非数字），与历史 JSON 客户端契约一致。

### 验证
- 系统 `dotnet` 构建 0 error；AskController+E2E 过滤集 17/17；**完整套件 846/846 零回归**（M5-14 基线 842 + 4），含 Golden，确认 M6-02 免疫。

### 提交序列（本地 checkpoint，未推送 origin）
- 70724b4(feat M6-02) → 计划标记（§10 M6-02 小节）+ 本条目随 `docs(M6-02)` 提交。

## 登录页缺陷修复：租户下拉无数据 + 语言切换器缺失（提交 8ddea3f）

`scene#日常开发`，用户报告「登录页没有语言选择，并且租户下拉框也没有数据」。

### 诊断（根因）
- **租户下拉无数据**：`Auth:ShowTenantDirectory` 在 `appsettings.json` 缺省 → M0-08 默认隐藏完整租户目录，`LoginOptions` 返回 `[]`；而 `DoLogin` 守卫 `TenantId<=0` → 无目录即无法选租户、登录死局。
- **无语言选择**：运行实例 DB 未播种 `UiLanguages`（或种子步骤在该实例失败）→ `public/languages` 仅返回 ≤1 启用文化 → `LanguageSwitcher` 门控 `AvailableCultures.Count>1` 隐藏。经确认非登录代码 bug：种子已在 `Program.cs:430` 启动序列**无条件执行**（try/catch 包裹）。修复 = 重启后端即自动播种 5 语言（zh-CN/zh-TW/en-US/ja-JP/ko-KR），刷新即出现语言切换器，**无需改登录代码**。

### 实施（用户选定「开发环境放开+手动兜底」方案）
- `appsettings.Development.json`：Auth 节新增 `"ShowTenantDirectory": true`（开发/演示可见完整下拉，与生产测试配置一致）。
- `AuthController.TenantByCode`（`[HttpGet("tenant-by-code")] [AllowAnonymous]`）：按已知编码解析租户，**不枚举目录**；排除 platform 与停用租户；空编码 400、不存在/停用/未知 404、命中返回 `{Id,TenantCode,Name}`。
- `AuthMiddleware.IsAnonymousPath`：白名单新增 `/api/auth/tenant-by-code`（位于 login-options 之后）。
- `Login.razor`：`_tenantOptions.Count>0` 渲染原 `<select>`；否则渲染手动租户编码 `<input>`（提示「目录未公开，请输入已知编码」）；`DoLogin` 在 `TenantId<=0` 且目录为空时先 `ResolveTenantByCodeAsync` 解析再登录（失败给友好错误）。
- `ApiClient`/`IApiClient`：新增 `ResolveTenantByCodeAsync(code)`（GET `api/auth/tenant-by-code?code=`，解析 Id/Code/Name/Error）；同步更新 `AuthStoreTests.StatusApiClient` 测试 mock（接口扩展必须同步，否则测试工程编译失败）。
- 新增 `tests/SuperBuilder_AI.Tests/AuthControllerTenantByCodeTests.cs`（5 例：空编码400 / 启用租户200+Id / 停用404 / platform404 / 未知404），SQLite 内存库 + 真实 DbContext，沿用 `AuthControllerLoginOptionsTests` 建制。

### 验证
- 全量 `dotnet build`：**0 个 CSxxxx 编译错误**（仅 183 警告，均为预存 nullable/CA/旧 MSB3026 复制重试）。构建末段 4 个 `MSB3021/MSB3027` **是文件复制锁错误，非编译错误**——VS 调试会话（devenv 28668）持有 Web `bin` 的 `SuperBuilder_AI.Components.dll`、运行中的 API 主机（pid 56784）持有自身 exe；我的 C#/Razor 全部编译通过。
- `dotnet test --filter AuthController`：**10/10 通过**（5 LoginOptions + 5 TenantByCode），零回归。
- 本地 checkpoint 提交 `8ddea3f`（8 文件 +198/−2），**未推送 origin**。

### 坑 / 备注
- 工作树残留 phantom 未跟踪项 `nul`：`Get-Item -LiteralPath nul` 解析为 Windows NUL 设备（非真实文件），已排除出提交，勿纳入。
- 语言切换器修复依赖「重启后端播种」，非代码改动；若重启后仍不显示，先查 `public/languages` 是否返回 ≥2 条 Enabled 文化、再查 `Program.cs` 种子步骤是否抛错（仅标记 SeedIncomplete，不中断启动）。
- Blazor 事件属性含 C# 字符串须单引号（`@onclick='...'`），`@bind="_manualTenantCode"` 绑定字符串字段正常。

## M6-03-A 澄清与语义学习（行为分类 + 结构化候选项 + 循环检测，提交待办）

M6-02(70724b4) 之后按推荐顺序推进 M6-03（Ask 企业化—澄清与语义学习）。M6-03 是多增量里程碑，本回合交付自包含核心增量 M6-03-A，未做大改。

### 实施
- **模型层（双副本同步）**：
  - 后端 `src/Domain/BiQuery/BIResponse.cs`：新增枚举 `AskBehavior { NewQuestion, Clarification, Correction, Confirmation, Cancel }`（`JsonStringEnumConverter` 字符串序列化）；新增 `ClarificationDetail`（OriginalQuestion / PendingSlot / CandidateEntities·CandidateDimensions·CandidateMetrics·CandidateTime: List<string> / AuthorizedDataSourceIds: List<long> / LoopDetected: bool / RepeatCount: int）；`BIResponse` 增 `AskBehavior AskBehavior`（默认 NewQuestion）与 `ClarificationDetail? Clarification`。
  - 前端 `Components/Models/BIResponse.cs`：镜像 `string AskBehavior`（默认 "NewQuestion"）+ `ClarificationDetail? Clarification` + 同字段 `ClarificationDetail` 类（字符串副本，线格式一致，无需行为改）。
- **会话服务重写** `src/Application/BiQuery/AskConversationService.cs`：
  - 接口 `IAskConversationService.Resolve/Record` 加可选 `AskResolveContext? context = null`（向后兼容）；`AskResolveContext { AuthorizedDataSourceIds, PendingSlot, CandidateEntities }`。
  - `AskConversationTurn` record 增 `AskBehavior Behavior`（init NewQuestion）、`ClarificationDetail? Clarification`（init null）。
  - `Pending` 富化：Question/Limit/Order/Filter/CandidateEntities/AuthorizedDataSourceIds/PendingSlot/ClarifyCount。
  - `Resolve` 行为分类：无 pending→NewQuestion；`IsCancel`（取消/清空/结束/不用了/算了/终止，≤12 字）→Cancel 清 pending；待澄清中别名确认→Confirmation（带候选实体）；`IsCorrection`（改成/改为/纠正/修正为/更新为/不对/应该是）→Correction；其余→Clarification。`const int LoopThreshold = 3`，`ClarifyCount>=3` 置 `LoopDetected`=true。
  - `Record` 保留上一轮 `ClarifyCount`（prevCount+1）避免循环检测失效；用 context 富化候选/授权源/槽位。
- **控制器接线** `src/Api/Controllers/AskController.cs`：`Resolve`/`Record` 调用加 `new AskResolveContext { AuthorizedDataSourceIds = access.AuthorizedDataSourceIds }`；响应写 `response.AskBehavior = turn.Behavior` 及（非空时）`response.Clarification = turn.Clarification`。
- **资源键一致性修复（登录回归）**：`Keys.cs`(Components)/`ResourceKeys.cs`(Domain)/`LocalizationSeedService.ZhCnDefaults` 三处补注册 `Login.TenantCodePlaceholder`/`Login.TenantCodeHint`/`Login.TenantCodeInvalid`（zh-CN/en-US 一致），否则 `ResourceKeyRegistryTests.Literal_TranslationKeys_Used_By_Components_AreRegistered` 失败（登录提交 8ddea3f 引入的回归，首轮只补 2 键仍 FAIL，次轮补第 3 键 `Login.TenantCodeInvalid` 后转绿）。
- **测试** `tests/SuperBuilder_AI.Tests/AskConversationServiceBehaviorTests.cs`（新增 10 例）：NoPending→NewQuestion / Pending 补充→Clarification+OriginalQuestion+RepeatCount=1 / 别名确认→Confirmation+候选实体 / 纠正→Correction / 取消清 pending 后 NewQuestion / 无 pending 取消→Cancel / 循环检测第3轮 LoopDetected / context 授权源透传 / context PendingSlot 透传 / 跨租户 pending 不可见。

### 验证
- 系统 `dotnet` 构建 0 CSxxxx 编译错误。
- **完整套件 869/869 零回归**（含 Golden，确认 M6-03-A 免疫）。
- 验证前须停止锁定 bin 的运行 API 主机（pid 57704 持 `SuperBuilder_AI.exe`）：`Stop-Process -Id 57704 -Force` 后重跑 `dotnet test tests/SuperBuilder_AI.Tests/SuperBuilder_AI.Tests.csproj`；VS 调试会话（devenv 28668）仍锁 Web bin，用测试工程单独验证即可避开。

### 范围决策（M6-03 后续增量，显式推迟）
- **①管线结构化候选 threading**：当前 `QueryPlanDecision`/`Explanation` 仅暴露 `RequiresConfirmation` 布尔，指标/维度/时间候选项未结构化外透（埋于 `QueryPlanConfidenceEvidence`/`DimensionResolutionEvidenceService` 等）；M6-03-A 的 `ClarificationDetail` 已预留 `CandidateMetrics/CandidateDimensions/CandidateTime` 字段，但需管线深层改动才能填充 → 划为后续增量。
- **②`TenantSemanticAlias` 持久化**：实体别名「X 就是 Y」确认仅当前租户复用（M6-03-A 已在 `Resolve` 内做内存别名确认）；长期别名须持久化 + 管理员提议/审核/启停/审计 → 后续增量。
- **③确认重放**：确认后"恢复并重放原 QueryPlan 不走普通首问校验"需在 BI 层落地（M6-03-A 已生成 Confirmation 改写文本，但重放机制未完成）。
- 三项均保持零回归与 Golden 免疫、本地 checkpoint、不推送 origin。

### 提交序列（本地 checkpoint，未推送 origin）
- M6-03-A 改动（含 3 资源键）待 `git commit`（排除 phantom `nul`）。

> **更正**：M6-03-A 已于本回合作本地 checkpoint 提交 **cf04bef**（10 文件 +459/−18），计划标记 **976fe7c**；基线 869/869 零回归。

## M6-04 Ask 语义缓存 7 维版本上下文（提交 a7b1b41，7 文件 +547/−5）

M6-03-A(cf04bef) 之后按推荐顺序推进 M6-04（SB-P1-09）：将缓存键从「权限指纹+RLS 策略指纹」扩展为 7 维版本上下文，撤权/策略/语言/模型/语义/元数据/数据源集合任一变化后不复用旧结果。

### 实施
- **接口/模型** `src/Interfaces/BI/IAskCacheVersionProvider.cs`：`AskCacheVersionContext` record（归一化问题 + `BuildCacheKey` 拼接 7 维指纹，段分隔符 `\u001f` 防歧义）+ `Legacy(perm, policy, culture)` 工厂（新增维度补 `"na"`/当前文化，保留 `policy:` 段以兼容旧测试）+ `IAskCacheVersionProvider`/`ISemanticVersionProvider`/`IMetadataVersionProvider`/`IDataSourceCatalogVersionProvider` 四接口。
- **选项** `src/Application/Common/Options/QwenOptions.cs`：`SectionName="Qwen"`，`Model`(默认 "qwen-plus") + `EmbeddingModel`（供 ModelVersion 维度）。
- **组合提供器** `src/Application/BiQuery/AskCacheVersionProviders.cs`：
  - `CompositeAskCacheVersionProvider`（ctor 各源可空）→ `ResolveAsync` 组合 PermissionFingerprint + RlsPolicyFingerprint + `CultureInfo.CurrentUICulture.Name` + `QwenOptions.Model` + SemanticVersion + MetadataVersion + DataSourceVersion；各子源 try/catch 降级为 `legacy`/`na`。
  - `MetadataVersionProvider`：`MetadataTables.RowVersion.Sum + MetadataColumns.RowVersion.Sum`（按租户）字符串聚合。
  - `SemanticVersionProvider`：`SemanticLabels.Where(TenantId==tenant || TenantId==0).RowVersion.Sum`（全局标签 TenantId==0 对所有租户生效）。
  - `DataSourceCatalogVersionProvider`：`DataSources.Where(TenantId==t).Select(Id,Enabled,RowVersion)` 排序拼接 `"Id:Enabled:RowVersion;"`；空集合返回 `"empty"`。
- **控制器接线** `src/Api/Controllers/AskController.cs`：ctor 新增第 7 参 `IAskCacheVersionProvider? versionProvider`；`BuildCacheKeyAsync` 在未注入时走 `Legacy` 分支（保留 `policy:` 段），注入时走 7 维——对主查询链路零侵入、对 Golden 免疫。
- **DI** `src/Api/Program.cs`：`Configure<QwenOptions>` + 注册三子提供器 Scoped + `CompositeAskCacheVersionProvider` Scoped。
- **测试** `AskCacheVersionProviderTests.cs`(9) + `AskControllerTests.Ask_WithVersionProvider_BuildsFullSevenDimensionCacheKey`(1)：含 BuildCacheKey 稳定性/单维变化/Legacy 补 na、Composite 全维度解析/Null 各源降级、SQLite 真实 EF 验证（metadata/semantic/ds 聚合 + 改 Enabled 致目录指纹变化 + 租户隔离 + 全局标签）。

### 验证
- 系统 `dotnet` 构建 0 CSxxxx 编译错误（28 预存警告无关）。
- **完整套件 879/879 零回归**（M6-03-A 基线 869 + 10 例：9 CVP + 1 控制器 7 维键断言），含 Golden，确认 M6-04 免疫。

### 坑 / 复用要点
- **测试工程 SQLite 内存库建制**：必须用 `DataSource=:memory:`（**不可 `Pooling=false`**——后者每次 open 新建空库致 EnsureCreated 的表丢失）+ `connection.Open()` 显式持连 + 测试内 `await using var _ = conn` 保持连接存活（沿用 `AgentControllerTests.CreateContext` 建制）。初版 `Pooling=false` 报 "no such table: DataSources"。
- **SQLite FK 约束**：`MetadataTable`→`DataSource` 是复合 FK `{DataSourceId, TenantId}` 对 `DataSource` 备用键 `{Id, TenantId}`；种子须用导航 `tbl.DataSource = ds`（而非写 `DataSourceId=0`）。`DataSource`/`MetadataTable.TenantId` 对 `Tenant` 有必需 FK → 测试须先 seed `Tenant{Id=7,8}`（显式 Id）。
- **数据源目录指纹含尾 `;`**：断言须用动态 `ds.Id`（自增实际值未必为 1，本次实测 Id=2），避免硬编码 `"1:..."`。
- `SuperBIContext` 命名空间 `SuperBuilder_AI.Data`（非 `Infrastructure.Persistence`）；`Tenant` 在 `SuperBuilder_AI.Models.Organization`。
- phantom `nul` 仍排除出提交（0 字节误产物）。

### 提交序列（本地 checkpoint，未推送 origin）
- a7b1b41(feat M6-04) → 计划标记（§10 M6-04 小节）+ 本条目随 `docs(M6-04)` 提交。
- M6 推进顺序：M6-02(70724b4) → M6-03-A(cf04bef) → M6-04(a7b1b41) → 下一步 M6-05（审计/指标/真实 E2E）。

## M6-05 Ask 审计 + 分段指标 + 脱敏（提交 1a1198e，14 文件 +727/−25）

M6-04(a7b1b41) 之后按推荐顺序推进 M6-05（审计/指标/真实 E2E）。审计三件套完全镜像 M5-09（默认 NoOp 零行为，对 Golden 免疫）；分段指标在 `BIConversationService.ExecuteAsync` 粗粒度填充；脱敏复用 M5-08 的 `BuiltInPiiCatalog`。

### 实施
- **Ask 审计模型** `src/Domain/BiQuery/AskAuditRecord.cs`：
  - `AskAuditOutcome` 枚举（Completed/Failed/RequiresConfirmation/AskClarification/Rejected/Cancelled）。
  - `AskSegmentTimings` record（MetadataUnderstand/Plan/SqlBuild/DbExec/ResultUnderstand 5 段 long + `TotalMs` 派生）。
  - `AskAuditRecord` record（17 位置参：AuditId/CorrelationId/ConversationId/TurnIndex/OriginalQuestion/RewrittenQuestion/AuthorizedDataSourceIds/DecisionType/SqlSummary/Model/DurationMs/Cost/Status/Outcome/ErrorMessage/SegmentTimings/ResultSample）。
- **端口三件套**：`src/Interfaces/BI/IAskAuditSink.cs`(`RecordAsync(record, ct)`) + `NoOpAskAuditSink`(默认零行为) + `LogAskAuditSink`(ILogger Information) + `ConfigurableAskAuditSink`（按 `AskAuditOptions.Mode` 切换 None/Log，默认 None）。`AskAuditOptions`(`src/Application/Common/Options/AskAuditOptions.cs`，SectionName "AskAudit"，`Mode=None`）。
- **脱敏** `src/Application/BiQuery/AskPiiRedactor.cs`：复用 `BuiltInPiiCatalog.Contains`（M5-08 内置 PII 列名集）+ 可选 `restrictedColumns`；`RedactText`（屏蔽 phone/email 正则 + PII 关键词替换 `***`）/`RedactSql`（PII 列名置 `***`）/`RedactResultSample`（取首行，PII 列值置 `***`，保留非 PII）。
- **记录构建器** `src/Application/BiQuery/AskAuditRecordBuilder.cs`：纯静态 `Build(correlationId, turn, authorizedDataSourceIds, response, errorMessage, outcome, durationMs, segmentTimings, redactor)`——落库前经 redactor 脱敏文本/SQL/结果样本；`DecisionType` 取 `response.Explanation?.Decision?.Decision`；`Status` = `"{ConversationStatus}.{AskBehavior}"`。
- **分段计时** `src/Domain/BiQuery/BIResponse.cs` 加法字段 `DurationMs`/`SegmentTimings`；`src/Application/BiQuery/BIConversationService.cs` 用 `Stopwatch` 包裹 5 阶段（metadataUnderstand/plan/sqlBuild/dbExec/resultUnderstand），早期返回（EarlyResponse）也填充 `DurationMs`/`SegmentTimings`；最终 `BIResponse` 注入两字段。
- **控制器旁路审计** `src/Api/Controllers/AskController.cs`：ctor 新增第 8 参 `IAskAuditSink? askAudit = null`；`Ask`/`Refine` 改 `try { work } catch { throw } finally { RecordAuditSafeAsync(...) }` 计时+审计；`DeriveAuditOutcome`（由 Success/AskBehavior.Cancel/DecisionType 推导）+ `RecordAuditSafeAsync`（`_askAudit==null` 直接返回；sink 调用包 try/catch 空 catch——**审计失败绝不破坏主响应**）。
- **DI** `src/Api/Program.cs`：M5-09 注册后加 `Configure<AskAuditOptions>` + `IAskAuditSink→ConfigurableAskAuditSink`。
- **测试**：`AskPiiRedactorTests.cs`(5) + `AskControllerTests.cs`(+3：`Ask_WithAuditSink_RecordsAuditOnSuccess`/`_RedactsSensitiveFields`/`_RecordsFailure_WhenBiThrows`)；`FakeBi` 增 `SqlToReturn`/`Decision` 注入、`FakeAskAuditSink`(记 `List<AskAuditRecord>`)、`FakeBiThrowing`(抛 `InvalidOperationException`)。

### 验证
- 系统 `dotnet` 构建 0 CSxxxx 编译错误（预存警告无关）。
- **M6-05 过滤集 8/8 全绿**（AskPiiRedactor 5 + Ask_WithAuditSink 3）。
- **完整套件 887/887 零回归**（M6-04 基线 879 + 8 例），含 Golden，确认 M6-05 免疫。

### 坑 / 复用要点
- **CS1739 命名参数后跟位置参数**：`AskAuditRecordBuilder.Build` 初版用 `model: null` 命名参后跟位置参 `durationMs` → C# 禁止 → 改纯位置调用。
- **Mirror M5-09 模式**：默认 NoOp + 可配置切换是零回归红线；审计旁路 try/finally + 空 catch，绝不污染主响应。
- **脱敏复用 BuiltInPiiCatalog**（非新建连接串脱敏器）：降低新建依赖、与列级安全共享同一"内置 PII 启发"事实来源。
- **分段计时放 BIConversationService.ExecuteAsync 粗分段**：不改 `QueryPlanPipeline` 内部，避免污染既有管线测试（Golden 走 2 参 BuildAsync 不进管线，天然免疫）。
- **审计放 AskController 而非服务内部**：try/finally 旁路，主路径零耦合；审计记录经 `AskAuditRecordBuilder` 脱敏后才落库。
- phantom `nul` 仍排除出提交（0 字节误产物，Windows NUL 设备）。
- M6 推进顺序：M6-02(70724b4) → M6-03-A(cf04bef) → M6-04(a7b1b41) → M6-05(1a1198e)；M6 余下增量与 M7（Dashboard/App/Agent 占位清零）待办。

## M7-01 Dashboard 草稿/发布隔离 + 可追踪回滚（提交 7aa731e，8 文件 +3927/−3）

M6 全闭环后按推荐顺序推进 M7（Dashboard/App/Agent 占位清零）。M7-01 为 M7 首章，是后续 M7-02(App) 版本化的前置地基。

### 实施
- **快照实体** `src/Domain/Dashboard/DashboardVersion.cs`：`DashboardId`/`TenantId`(作用域列，不建 Tenant 外键，因 TenantId=0 全局模板)/`Version`(从1自增)/`Code`/`Title`/`Description`/`ThemeKey`/`DslVersion`/`DslJson`(固化快照)/`PublishedAt`/`PublishedBy`/`RolledBackFromVersion`(回滚来源)；继承 `BaseEntity`。
- **Dashboard 扩展** `src/Domain/Dashboard/Dashboard.cs`：加 `PublishedDslJson`(null=未发布)/`PublishedVersion`(0=未发布)/`PublishedAt`/`PublishedBy`。草稿(`DslJson`)与发布态物理隔离。
- **DbContext** `src/Infrastructure/Persistence/SuperBIContext.cs`：`DashboardVersions` DbSet + 配置：唯一索引 `(DashboardId,Version)`、租户索引 `(TenantId,DashboardId)`、FK→Dashboards `OnDelete(Cascade)`、各列注释/maxLength（对齐 Dashboard 风格）。
- **受控迁移** `dotnet ef migrations add M7_01_DashboardVersion`（EF 10.0.10，无 DesignTimeDbContextFactory，靠 Program.cs 宿主构建模型、不连库）：`20260906131855_M7_01_DashboardVersion`(+Designer) + `SuperBIContextModelSnapshot` 自动更新；新增 DashboardVersions 表 + Dashboards 4 列。EF Tools 版本(10.0.10) 低于 Runtime(10.0.11) 仅 warning，无碍。
- **控制器** `src/Api/Controllers/DashboardController.cs`：新增 `Publish(id)`(固化快照、自增版本、置 Published+PublishedAt+PublishedBy、插 DashboardVersion)/`Rollback(id,version)`(恢复历史快照为发布态并再固化新版本、记 RolledBackFromVersion)/`Versions(id)`(倒序列出版本链带 IsCurrent 标记)；`Render` 改为优先 `PublishedDslJson`（仅当 Status=Published 且非空）；`Actor()` 取 `User.Identity?.Name ?? "system"`；`DashboardSummary` 加 `PublishedVersion`/`PublishedAt`。租户隔离沿用 `ScopeTo`（未认证主体退化为以请求租户为有效租户，测试可直接传 tenantId）。

### 验证
- 系统 `dotnet` 构建 0 CSxxxx 编译错误（预存 nullable/CA 警告无关）。
- **M7-01 过滤集 7/7 全绿**（`DashboardControllerVersioningTests`：发布快照/草稿隔离/回滚恢复/版本列表/空草稿拒绝/未知版本404/租户隔离）。
- **完整套件 894/894 零回归**（M6-05 基线 887 + 7），含 Golden 18/18，确认 M7-01 免疫（平台管理面，不触 BI 查询链路）。

### 坑 / 复用要点
- **EF 生成迁移**：本仓无 `IDesignTimeDbContextFactory`，`dotnet ef migrations add` 靠 Program.cs 宿主构建模型（UseSqlServer 仅存连接串不连库），成功生成且与模型逐字节一致；快照自动更新，避免手写出错。
- **强类型 DTO 扩展**：`DashboardSummary` 改为 11 参位置记录，唯一构造点在 `ToSummary`（无其他引用），扩展无破坏性；测试断言走 `PublishResult`/`DashboardVersionSummary`（同命名空间 `SuperBuilder_AI.Controllers`）。
- **测试工程未启用 ImplicitUsings**：所有 using 须显式（含 `SuperBuilder_AI.Models.Organization` for `PlatformContext`/`PlatformContextAccessor.Current`）。
- **SQLite 内存库建制**：`DataSource=:memory:` + `connection.Open()` + 持连；`EnsureCreated` 按模型建全表（含 DashboardVersions），无需应用迁移。
- **Update 调用陷阱**：`Update(id, request, [tenantId=0], ct)` 第 3 参是 `long tenantId`、第 4 才是 `CancellationToken`——直调时须显式传 `0` 否则 CS1503（CancellationToken→long）。
- **未认证主体 ScopeTo**：`TenantDataPlanePolicy.ResolvePlatformScope(null,...)` 返回 `authorized=true` 且有效租户=请求租户，测试可自由传 tenantId；隔离由 `ApplyTenantScope` 查询过滤保证（租户7 仪表盘对租户8 的 GetById/Versions 返回 404）。
- **Fake 接口签名须精确**：`IDashboardRenderer.RenderAsync(DashboardDsl, PlatformContext, ct)` 不能用 `object` 桩，否则不实现接口（CS errors）。
- phantom `nul` 仍排除出提交（0 字节误产物）。

### 提交序列（本地 checkpoint，未推送 origin）
- 7aa731e(feat M7-01) → 计划标记（§11 M7-01 进度）待 `docs(M7-01)` 提交（含 7aa731e）。
- 推进顺序：M6-02→M6-03-A→M6-04→M6-05 → **M7-01**；M7 余下 M7-02~M7-11 待办。

## M7-02 App 草稿/发布隔离 + 可追踪回滚（代码完成，**待 shell 恢复后构建/测试/提交**）

M7-01(7aa731e) 之后按推荐顺序推进 M7-02（复用 M7-01 版本化模式，风险最低）。**本会话 Bash/PowerShell 工具全部不可用（harness 级报错：Bash「command undefined」、PowerShell「reading 'split'」），无法运行 `dotnet ef`/`dotnet test`/`git`，故 M7-02 仅完成代码、未验证零回归、未提交。**

### 实施（已落地，未验证）
- **快照实体** `src/Domain/AppBuilder/AppVersion.cs`：镜像 `DashboardVersion`，字段 `AppId`/`TenantId`(作用域列，不建 Tenant 外键)/`Version`(从1自增)/`Code`/`Name`(AppPlan 用 Name 非 Title)/`Description`/`ThemeKey`/`DslVersion`/`DslJson`(固化快照)/`PublishedAt`/`PublishedBy`/`RolledBackFromVersion`(回滚来源)；继承 `BaseEntity`。
- **AppPlan 扩展** `src/Domain/AppBuilder/AppPlan.cs`：加 `PublishedDslJson`/`PublishedVersion`/`PublishedAt`/`PublishedBy`；草稿(`DslJson`)与发布态物理隔离（**满足 M7-02 验收「不直接覆盖线上版本」**——`Update` 仅改 `DslJson` 不碰发布态）。
- **DbContext** `src/Infrastructure/Persistence/SuperBIContext.cs`：`AppVersions` DbSet + 配置（唯一索引 `(AppId,Version)`、租户索引 `(TenantId,AppId)`、FK→AppPlans `OnDelete(Cascade)`、列注释/maxLength 对齐 AppPlan）。
- **迁移** `20260906214300_M7_02_AppVersion.cs`（手写 Up/Down，镜像 M7-01）+ 主文件已补 `[DbContext(typeof(SuperBIContext))]`/`[Migration("20260906214300_M7_02_AppVersion")]` 使迁移可被发现；`SuperBIContextModelSnapshot.cs` 已加 AppVersion 实体块 + AppPlan 4 列。
- **迁移 Designer 待重建**：shell 不可用无法 `dotnet ef` 生成 Designer（全模型 ~2000 行，手抄风险高且无法验证），故主文件 `BuildTargetModel` 沿用基类空实现。待 shell 恢复后执行 `dotnet ef migrations remove && dotnet ef migrations add M7_02_AppVersion` 重建 Designer 并校准快照（模型配置已正确，重生结果一致）。
- **控制器** `src/Api/Controllers/AppBuilderController.cs`：新增 `Publish(code)`/`Rollback(code,version)`/`Versions(code)`（路由 `POST api/apps/{code}/publish` 等，与 Dashboard 用 id 不同——App 用 code 定位）；`Actor()`=`User.Identity?.Name ?? "system"`；`AppSummary`/`AppDetail` 加 `PublishedVersion`/`PublishedAt`；`Update` 维持仅改草稿。
- **测试** `tests/SuperBuilder_AI.Tests/AppBuilderControllerVersioningTests.cs`（7 例，镜像 M7-01）：发布快照/草稿隔离/回滚恢复/版本列表/空草稿拒绝/未知版本404/租户隔离。用真实 `AppDslSerializer` + `FakeAppBuilderAgent`(AppDsl→AppPlan 映射，不调 LLM)；SQLite 内存库 `EnsureCreated` 建全表（含 AppVersions）。

### 待办（shell 恢复后）
1. `dotnet build` 确认 API/测试工程 0 CS 错误（重点：AppVersion 命名空间、迁移主文件特性、测试 Fake 接口签名 `IAppBuilderAgent.BuildFromDslAsync(long,AppDsl,string?)`）。
2. `dotnet ef migrations remove && dotnet ef migrations add M7_02_AppVersion` 重建 Designer + 校准快照。
3. `dotnet test --filter FullyQualifiedName~AppBuilderControllerVersioningTests` 确认 7/7；全量确认零回归（预期 894→901）。
4. `git add` 显式文件（排除 phantom `nul`）+ 两次提交（feat + docs）；不推送 origin。

### 坑（本会话特有）
- **shell 工具不可用**：Bash 与 PowerShell 均报 harness 级错误，无法执行任何命令（含 git）。所有文件编辑经 Read/Write/Edit 工具完成；构建/测试/提交须待环境恢复。
- **迁移发现依赖 [Migration] 特性**：该特性原属 Designer 分部；手写迁移须在主文件补 `[DbContext]`/`[Migration]` 否则 EF 不识别迁移。

## M7-02 续：shell 恢复后收尾完成（2026-09-06）
- 实测：前序"死 shell"会话的 M7-02 实现代码（AppVersion/AppPlan/SuperBIContext/AppBuilderController/手写迁移/测试）其实**已被提交**（git 状态干净），仅测试编译失败 + 迁移缺 Designer 未验证。
- 测试编译修复：AppBuilderController 的 DTO 为**嵌套类型**，测试须 `using static SuperBuilder_AI.Controllers.AppBuilderController;`；但 `PublishResult` 与 DashboardController 顶层 `PublishResult` 重名 → 仅在该两处用 `AppBuilderController.PublishResult` 消歧（其余靠 using static）。**不能把 DTO 移到命名空间级**（会与 Dashboard 的 PublishResult 冲突）。
- 迁移 Designer 重建：`dotnet ef migrations remove` **只回退快照、未删手写 .cs（因缺 Designer EF 无法完整处理该迁移）**；需手动 `rm` 孤儿 .cs，确认 `grep -c AppVersion` 快照归零后，再 `dotnet ef migrations add M7_02_AppVersion` 生成 `.cs + .Designer.cs + 校准快照`（新 id 20260906141902）。
- 验证：AppBuilderControllerVersioningTests 7/7；全量 **901/901** 零回归、含 Golden 18/18（894→901）。
- 提交（不推送）：`fd895c9` feat(M7-02) + `a2e0636` docs(M7-02)；Master_Development_Plan.md §11 M7-02 由 🟡→✅。phantom `nul` 始终排除。

## M7-03 Agent Runtime 受控执行框架（feat 提交；docs 同本次）

M7-02(a2e0636) 之后按推荐顺序推进 M7-03（Agent 占位清零——受控运行时）。经诊断，Agent 子系统此前仅有「编排/编写面」（AgentPlan DSL + ToolRegistry + AgentPlanner + CRUD 端点），**无运行时执行**；M7-03 交付"受控执行框架"，工具以诚实信封呈现而非伪造成功，契合 M7-04「无假成功按钮」。

### 实施
- **领域** `src/Domain/Agent/AgentRun.cs`：`AgentRunStatuses`(7 态 queued/running/succeeded/approval_pending/approved/failed/rejected) + `AgentRun` 实体（`BaseEntity`，TenantId/PlanCode/Status/CurrentStepOrder/Actor/GrantedToolsJson/Approved/StartedAt/FinishedAt/ResultSummary/StepLogJson，含 `GetStepLog()` 反序列化）+ `AgentRunStepRecord`（Order/Tool/Risk/Succeeded/Attempts/Output/Error/Mode/PendingApproval）。
- **端口** `src/Application/Ports/Agent/Runtime/`：`ToolContracts`(ToolRisk Safe/Read/Write + ToolContext/ToolResult+Ok/Fail + TransientToolException)、`ITool`/`IToolCatalog`/`IToolPermissionPolicy`(deny-by-default)/`IRetryPolicy`(RetryOutcome<T>)/`IAgentRuntime`(StartRun/Approve/Reject)。
- **运行时** `src/Application/Agent/Runtime/`：`AgentRuntimeException`(StatusCode)、`ControlledToolCatalog`(按 Tool 索引)、`ToolPermissionPolicy`(授权集含工具才放行)、`RetryPolicy`(仅重试 TransientToolException，零退避)、`AgentRuntime`(核心编排：加载 DSL→按序执行→权限闸门→审批挂起→重试执行→持久化 StepLogJson)、`ControlledTools`(ControlledToolBase 诚实信封 + 8 具体工具，Write 默认 RequiresApproval)。
- **DbContext** `SuperBIContext.cs`：`AgentRuns` DbSet + 配置（ToTable/索引/属性注释）；查询过滤器**不放行 TenantId==0**（AgentRun 为租户专属）。
- **迁移** `dotnet ef migrations add M7_03_AgentRuntime`：`20260907004450_M7_03_AgentRuntime`(+Designer) + `SuperBIContextModelSnapshot` 校准（AgentRuns 表 + 2 索引）。
- **端点** `AgentController.cs`：新增 `POST plans/{code}/run`(approval_pending 返 202 否则 200)、`GET runs/{id}`、`POST runs/{id}/approve`、`POST runs/{id}/reject`；`Program.cs` 注册 IToolPermissionPolicy/IRetryPolicy/8×ITool/IToolCatalog=ControlledToolCatalog/IAgentRuntime 全部 AddScoped。

### 验证
- API 构建 0 CS 编译错误；`AgentRuntimeTests` 12/12 全绿（权限 deny-by-default / 审批闸门挂起·通过·拒绝 / 瞬态重试至成功 / 非瞬态直接失败 / 状态机·隔离·诚实信封 / 持久化回读 / 跨租户审批拒绝）。
- **完整套件 913/913 零回归**（M7-02 基线 901 + 12），含 Golden 18/18，确认 M7-03 免疫。

### 坑 / 复用要点
- **中文转义断言**：`System.Text.Json` 默认将非 ASCII 转义为 `\uXXXX`；受控信封 note 的中文在 `StepLogJson` 中以字面转义存储，反序列化后 `step.Output` 仍持 `\uXXXX`，故断言须用 ASCII 安全子串（如 "M7-04"）而非中文原文。
- **非瞬态异常 Attempts**：`RetryPolicy` 仅捕获 `TransientToolException`，其余异常（如 InvalidOperationException）穿透至 `AgentRuntime` catch 块，该块硬编码 `Attempts=0`——断言须与"异常路径记录 Attempts=0"一致（仅瞬态重试成功路径带真实计数）。
- **IndexBuilder 无 HasComment**：`HasIndex(...).HasComment(...)` 报错 → 移除索引 `.HasComment()`，保留索引本身。
- **FailRunAsync 用实例方法**：初版误用 `run.AssertDb()`（不存在）→ 改私有 `FailRunAsync` + `_db.SaveChangesAsync`。
- **DSL 校验拒绝自定义工具**：测试须用受支持工具（AgentTools.Query 等）映射到 FakeTool，未知分支改为"受支持但目录未注册"。
- **跨租户审批 403**：`ApproveAsync` 校验 `run.TenantId != tenantId` → 403；断言用 `Assert.ThrowsAsync<AgentRuntimeException>`。
- phantom `nul` 始终排除（0 字节误产物）。

### 提交序列（本地 checkpoint，未推送 origin）
- feat(M7-03) 源码+迁移+测试 → docs(M7-03) Master Plan §11 M7-03 🟡→✅ + 本条目。

## M7-04 Agent 工具接真实后端（Safe 工具翻 live；其余诚实信封）

M7-03(ae9401d/75a99ca) 之后按推荐顺序推进 M7-04（Agent 新建/工具/运行接真实端点，红线「无假成功按钮」）。运行时框架零改动——仅把确定性、只读、无 LLM 的 Safe 工具（metadata/semantic）翻为 live 模式，接真实 SuperBIContext；其余 Read/Write 工具（query/dashboard/forecast/report/alert/workflow）真实后端在 M7-05~M7-08 接入前继续诚实受控信封（connected:false + backendMilestone 标注）。

### 实施
- **新建** `src/Application/Agent/Runtime/LiveMetadataTool.cs`：`_db.ApplyTenantScope(context.TenantId)` → `MetadataTables.AsNoTracking().Include(t=>t.Columns).OrderBy(TableName)`；输出 `mode="live"`/`connected=true`/`backend="SuperBIContext.MetadataTables"` + `tables[]`（tableName/comment/businessDomain/columns[{columnName,dataType,nativeType,comment,isPrimaryKey}]）→ `ToolResult.Ok(output,"live",RequiresApproval=false)`。
- **新建** `src/Application/Agent/Runtime/LiveSemanticTool.cs`：`SemanticLabels.AsNoTracking()` 按 ConceptType/Id/SortOrder 排序 → GroupBy 概念 → 输出 `mode="live"`/`connected=true`/`concepts[]` → `ToolResult.Ok(output,"live",...)`（全局共享标签 TenantId==0 经过滤器放行）。
- **改** `ControlledTools.cs`：诚实信封 `note` 字典 `["mode"]="controlled"`/`["connected"]=false`/`["backendMilestone"]="M7-05~M7-08"` + 说明未接后端不声称业务结果；类注释标注 metadata/semantic 已翻 live。
- **改** `ToolRegistry.cs`：`AgentToolDescriptor` 增 `BackendStatus`/`BackendMilestone` 两字段；Catalog 8 项标注 `metadata`/`semantic=live("M7-04")`，其余 6 个 `pending("M7-05~M7-08")`。
- **改** `Program.cs`(~275)：`AddScoped<ITool, MetadataTool/SemanticTool>()` → `LiveMetadataTool/LiveSemanticTool`；其余 6 个受控注册不变。

### 验证
- API 构建 0 CS 错误；`AgentRuntimeTests` 15/15（12 既有 + 3 新增：`LiveMetadata_ReturnsRealTables_NotFakeSuccess` / `LiveSemantic_ReturnsRealLabels_NotFakeSuccess` / `ToolsCatalog_HonestBackendStatus` 断言 2 live/6 pending）。
- **完整套件 916/916 零回归**（M7-03 基线 913 + 3），含 Golden 18/18，确认 M7-04 免疫。
- 受控信封断言改 ASCII 安全子串 `"connected":false`；live 断言用 `"t_sales"`/`"SalesAmount"`。

### 坑 / 复用要点
- **Live 工具 using 缺失**：`SuperBIContext`(CS0246)→补 `using SuperBuilder_AI.Data;`；`AgentTools`(CS0103)→补 `using SuperBuilder_AI.Models.Agent;`（两文件各补）。
- **Tenant 无 Name 属性**：测试播种 `new Tenant{Id,Name=...}`(CS0117)→移除 `Name`，仅 `{Id=tenantId}`（与 M6-04 一致）。
- **中文转义断言**：延续 M7-03 教训，`System.Text.Json` 默认 `\uXXXX`，live/controlled 断言一律用 ASCII 锚点。
- **运行时框架零改动**：M7-03 的 `ControlledToolCatalog` 经 `IEnumerable<ITool>` 自动聚合，翻 live 仅需替换 DI 注册，无需改 AgentRuntime/RetryPolicy/状态机。
- phantom `nul` 持续排除（0 字节误产物）。

### 提交序列（本地 checkpoint，未推送 origin）
- feat(M7-04) 源码+测试（LiveMetadataTool/LiveSemanticTool/ControlledTools/ToolRegistry/Program.cs DI/LiveMetadataTool 等）→ docs(M7-04) Master Plan §11 M7-04 标记完成 + 本条目。

## M7-05 BusinessModel 实体 CRUD 真实持久化闭环（前端三端 0 error；后端 923/923 零回归）

M7-04 之后按推荐顺序推进 M7-05（BusinessModel 新建/编辑/详情闭环 — CRUD 真实持久化）。**持久化层此前已存在**：`BusinessEntity` 聚合早已落地 `SuperBIContext` DbSet；`IBusinessEntityService` 已具 `CreateAsync/UpdateAsync/DeleteAsync`（真实 EF + 租户隔离），缺口仅为「无写端点 + 前端占位 Toast + 无测试」。

### 实施
- **后端** 新建 `BusinessEntityUpsertRequest.cs`（标量 DTO，BusinessKey/Name 必填）；`BusinessModelController.cs` 注入 `IBusinessEntityService` 第三参，新增 `POST/PUT/DELETE api/business-model/entities`（沿用 `TenantDataPlanePolicy.Resolve` 租户隔离；必填 400 / 跨租户 403 / 404）。
- **PUT 乐观并发陷阱**：初版用脱管新实体 `ToEntity` 映射 → `RowVersion=0` → `UPDATE … WHERE RowVersion=0` 命中 0 行抛 `DbUpdateConcurrencyException`。修复：PUT 先 `_entities.GetAsync` 取回带正确 `RowVersion` 的实体 → `MapDto(existing,dto)` → `UpdateAsync(existing)`（保留 RowVersion）。`BusinessModelControllerTests` 10/10 验证。
- **前端（RCL）**：`BusinessModel.razor` 占位 `Create()` Toast → `SbModal` 创建表单（BusinessKey/Name/DisplayName/Description/BusinessDomain/SemanticText/Status），`IApiClient.PostAsync` 后 `Reload()`；`BusinessModelEntityDetail.razor` 增「编辑」(预填 `SbModal`→`PUT`) 与「删除」(`DELETE`→`Nav.NavigateTo("business-model")`)。遵循 Blazor 约束：事件单引号属性、`OnAfterRenderAsync` 取数、`VisibleChanged` 双向。

### 关键回归发现（务必记入复用要点）
- **本地化护栏**：`ResourceKeyRegistryTests.Razor_Markup_ContainsNo_Unlocalized_Cjk_TextNodes_Or_Placeholders` 禁止裸中文——仅捕获 `placeholder="CJK"` 与 `>CJK<` 文本节点（`Label`/`Hint`/`Title` 属性豁免，同 `PageHead` Desc；`@code` 内字符串字面量豁免）。前置「用字面中文串规避改 Keys.cs」的捷径**直接触发该测试 FAIL**（923→922）。
- **修复**：4 向注册 5 个新键（RCL `Keys.cs`[const+`Defaults`]、后端 `ResourceKeys.cs`[const+`Catalog` DefaultValue]、`LocalizationSeedService.ZhCnDefaults`），须满足 `All()`(反射 const)/`Catalog`/`ZhCnDefaults`/`RclMirror` 全一致；占位改英文、`<option>`/「取消」/删除确认用 `L10n.T(Keys.Content.BusinessModelStatus*/BusinessModelDeleteConfirm, "…")` 与 `L10n.T(Keys.Action.Cancel,"取消")`。`Literal_TranslationKeys_Used_By_Components_AreRegistered` 要求 `L10n.T("key")` 的 key 必在 `Keys.Defaults`。
- **新键清单**：`Content.BusinessModelStatusActive/Draft/Unresolved/Deprecated` + `Content.BusinessModelDeleteConfirm`（删除确认长句）。`Action.Cancel/Save/Edit/Delete` 复用既有键。

### 验证
- Components（net10.0/android/ios）+ Web 构建 **0 error**（仅预存 CS0414 `_loading` 未用等警告）。
- **完整套件 923/923 零回归**（M7-04 基线 916 + 7 写测试；`BusinessModelControllerTests` 10/10、`TenantDataPlanePolicyTests` 因第三构造参补 `EntityServiceStub` 修复）；含 Golden 18/18，确认 M7-05 免疫。

### 提交序列（本地 checkpoint，未推送 origin）
- feat(M7-05)（BusinessEntityUpsertRequest + BusinessModelController + 两 Razor + 3 本地化文件 + 两测试）→ docs(M7-05)（Master_Development_Plan.md §11 M7-05 🟡→✅ + 本条目 + 工作日志）。phantom `nul` 始终排除。
- M7 推进顺序：M7-01→M7-02→M7-03→M7-04→**M7-05**；M7 余下 M7-06~M7-11（占位清零）待办。

## M7-06 ThemeEditor 接 Theme API（现状盘点 + §11 计划块已落，待用户确认后动手）

M7-05 收尾后启动 M7-06（占位清零批次首件）。遵循「先写 §11 计划块再动手」节奏，**本轮仅落计划块、未实施**。

### 现状盘点（Explore 只读 + 本回合核验）
- 后端 `api/themes` 已完整可用（迁移 `20260830034528_P7_1_Theme` + `SuperBIContext.Themes` + `Theme` 实体）：POST/GET/GET{key}/PUT/DELETE/`{key}/assign-default`/`{key}/copy`/`editor/blueprint`；写端点经 `TenantDataPlanePolicy.ResolvePlatformScope` 隔离 + `ThemeDslSerializer.TryDeserialize` 版本白名单校验；内置主题不可改/删。`assign-default` 写 `TenantSetting "theme:defaultKey"`，级联 `ThemeResolver` 被 Dashboard/App 取用。
- `ThemeDsl` 语义键：`Brand{Primary,Accent}`、`Color{Primary,Success,Warning,Danger,Neutral,Background,Surface,Text,TextMuted,Border}` + 其余令牌（Typography/Layout/Border/Radius/Shadow/ChartPalette/Component/DashboardTemplate）。
- 前端 `ThemeEditor.razor` 仅硬编码 `Presets` 8 键（primary/accent/success/warning/info/bg/text/border）+ `Save()` 占位 `Toast.Info("…P11.3 收口")`；未注入 `IApiClient`、无 ThemeDsl 映射/序列化/发布。本地化约 12 键已注册，`ThemeEditorSavedToast` 仍含占位文案。
- 键错位需映射：8 前端键 ↔ 10 DSL 色板键（`primary→Color.Primary`、`accent→Brand.Accent`、`success→Color.Success`、`warning→Color.Warning`、`info→Color.Neutral`、`bg→Color.Background`、`text→Color.Text`、`border→Color.Border`）。

### 计划要点（详见 Master_Development_Plan.md §11 M7-06 块）
- 前端注入 `IApiClient`；`OnAfterRenderAsync` 取 blueprint + 主题清单；下拉选主题回填 `_tokens`（用 `System.Text.Json.JsonNode` 解析 DslJson 改 color/brand）；新增 Key/Name 输入；`Save()` 真实 POST/PUT（JsonNode 改 color/brand 后 camelCase 序列化）；「指派为默认」`POST assign-default`；内置主题只读、删除仅非内置；预览 `--sb-{key}` 不变。
- 本地化 4 向新增/修订（含替换占位 `ThemeEditorSavedToast`）。
- 红线「无假成功按钮」；范围外：copy 前端入口、`Typography/...` 逐令牌 UI、多租户管理面、深色持久化。
- 零回归：后端 `ThemeControllerTests`(11) 已覆盖，本里程碑不加后端测试；前端护栏 + 手动 E2E；预期 923 基线零回归、build 0 error、Golden 18/18 不变。

### 交付（2026-09-07，已实施并提交）
- 实施 `ThemeEditor.razor`：注入 `IApiClient`+`AppState`；`OnAfterRenderAsync` 加载 `editor/blueprint` 与 `GET /api/themes?tenantId`；色板 8 键经 `JsonNode` 映射 `ThemeDsl.Color/Brand`（primary→color.primary、accent→brand.accent、success→color.success、warning→color.warning、info→color.neutral、bg→color.background、text→color.text、border→color.border）后 camelCase 序列化；新建 `POST /api/themes`、编辑 `PUT /api/themes/{key}`、指派 `POST /api/themes/{key}/assign-default`（级联 `TenantSetting "theme:defaultKey"`）、删除 `DELETE`；内置主题下拉标灰只读；深色预览用 `.sb-preview-dark { filter: invert(0.92) hue-rotate(180deg); }` 本地反相（不落库）；移除占位 `Toast.Info("…P11.3 收口")`，失败显式 `Toast.Error`+错误 alert（无假成功按钮）。
- 本地化 4 向新增/修订 10 键（ThemeEditorSelectTheme/New/Saved/SaveFailed/Key/Name/KeyInvalid/DeleteConfirm/Assigned + Action.ThemeAssignDefault；`ThemeEditorSavedToast` 占位文案替换为「主题已保存。」）。`ResourceKeyRegistryTests` 9/9 全绿。
- 构建期踩坑：`JsonValue` 歧义（`SuperBuilder_AI.Components.Models.JsonValue` vs `System.Text.Json.Nodes.JsonValue`）→ 用全限定名 `System.Text.Json.Nodes.JsonValue` 修复。
- 验证：RCL（net10.0/android/ios）+ API build 0 error；全量测试 **923/923 零回归**（含 Golden 18/18）。
- 双提交 `feat(b2ed887)` + `docs(9955d17)`（含 Master Plan §11 M7-06 标 ✅、工作日志）；未推送 origin；phantom `nul` 排除。M7 余下 M7-07~M7-11（占位清零）待办。

