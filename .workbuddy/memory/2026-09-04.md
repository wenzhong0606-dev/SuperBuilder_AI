# 2026-09-04 工作日志

## 明细列表只显示时间字段 Bug 收尾 + 独立回归发现

- **表结构确认**：用户提供 `wms_storage_receipt` 真实 25 列。核心业务字段 `code`(入库单号)、`es_supplier_code`(供应商编码)、`source_code`/`erp_receipt_code`；审计时间 `create_time`/`update_time`；审计人 ID `create_by`/`update_by`。
- **前序提交（均未 push）**：`0557519`/`d735df9`/`a4c4067`/`87b2176` 解耦 OrderBy 前置条件 + refine 字段扩展，但首轮仍只出时间字段。

### 根因（本轮定位）
- 首轮补列入口 Step5.6 的 `isPureDetailList`（QueryPlanBuilder.cs:805-808）要求 Metrics/Dimensions/Filters 三者全空；**Step11 的 `isDetailList` 同样要求 `plan.Dimensions.Count==0`（第二道锁）**。
- 实战 Qwen 对「最近的十个入库单」顺手返回 `Dimensions={status}` → 两道锁都被跳过 → SELECT 仅 `come_time,create_time,update_time,affirm_time,status`。
- 复现测试实测场景 B：`Fields(2)=status,come_time`，与线上吻合。

### 工程师修复（寇豆码 / software-engineer-2）
- 新增 `IsDetailListQuery(intent,plan)`：聚合一律 false（拦截 SUM/COUNT/AVG/MAX/MIN + `plan.IsAggregate`），否则要求 Limit/OrderBy 明细特征；**不再要求 Dimensions/Filters 为空**。Step5.6/Step11 均改用之。
- 重写 `GetPreferredDisplayColumns`：优先级 单据号/编码 > 类型/状态 > 外键 > 名称 > 数量金额 > 业务时间 > 审计时间 > 主键 > 兜底；全程去重（旧版各组间重复产出同一列浪费 Take(8) 配额）；审计时间排业务时间后。
- 新增 `IsAuditColumn`/`IsInternalColumn` 判据。
- 修复后场景 A/B = 9 列：`code,source_code,erp_receipt_code,es_supplier_code,type,status,source_type,warehouse_id,come_time`（业务字段主导）。
- 自报全量 446/446，但**自测环境有缓存不可信**（见下回归）。

### ⚠️ 独立回归发现（关键）
- 我独立跑全量：**462/468，6 失败**；Golden json 未改。
- QA 严过关因 429 限流失败未回报，但**已写入 `tests/SuperBuilder_AI.Tests/QueryPlanBuilderQaRegressionTests.cs` 并真实捕获 6 回归**（真实信号非误报）：
  - **R2g/R2f（严重）**：`IsAggregation`(Helpers.cs:240-261) 白名单漏 `DISTINCTCOUNT`/`DISTINCT_COUNT`/`COUNT_DISTINCT`，去重计数聚合被当明细补列。
  - **R1a**：`IsAuditColumn`(Helpers.cs:52-61) 用 `StartsWith("created"...)` 前缀，误伤业务时间 `created_order_time`。
  - **R1c**：`IsInternalColumn`(Helpers.cs:38-46) 漏 `creator_id`/`modifier_id`，审计人 ID 抢占业务外键位。
  - **R2b**：单字段+Limit 过度补列（与 P0 场景同构的设计争议，已决定保留补列、放宽该断言）。
- **已派工程师修复这 4 硬 bug + 调整 R2b**，要求全量回到 468 全绿。

### 限流
- QA 子 agent 撞 429 频率限制，重置时间 2026-09-04 13:51 UTC+8。

### 待办
- 等工程师修复回归 → 我独立跑全量确认 468 → 用户 build+重启 API+页面复测「最近的十个入库单」应出现 code/入库单号等。
- 本轮提交仍**未 push**，待确认后统一推。
- Golden 18/18 仍需用户在可连 Qwen+Qdrant 环境复跑 `evaluation/golden-runtime/release-gate`。
- 遗留 API 进程 PID 65948 已杀（曾锁 bin 致 MSB3021）。
- **已提交 `a53cfc1`**（4 文件 +1097/−27，含 QA 独立回归测试 `QueryPlanBuilderQaRegressionTests.cs`），全量 468/468 主理人独立重跑确认（13s，0 失败）。5 个 M0-09 提交（0557519/d735df9/a4c4067/87b2176/a53cfc1）本地已就绪（master ahead origin/master = 5）。
- **push 因认证失败**：当前非交互环境无 github.com 凭据（仅有 gitee.com generic provider）、无 SSH key；`git push origin master` 报 `could not read Username`。待用户在自己本地终端（GCM 已登录）push，或提供 GitHub PAT 后由 AI 用临时 URL 推送。

## 基于 Master_Development_Plan.md 规划「接下来的工作」（基线核验）
- **核验结论**：Master_Development_Plan.md 基线（431/431，2026-09-03）已过时。当前实测 **476/476 全绿、build 0 error**；M0-09（Ask 旗舰对话）实质上已完成，仍有 16 个未提交改动（含新增 `DetailQueryProjectionPolicy.cs`/`AuthStoreTests.cs`/`ResultUnderstandingServiceTests.cs`）属 M0-09 收尾 + M0-04(Ask 可靠性 UI) + M0-03(权限守卫测试) 的尾批。
- **M0 现状**：M0-09 ✅(进行尾批提交)；M0-03/M0-04 🟡 进行中；M0-01(凭据轮换)/M0-02(4 路由契约)/M0-05(受控 Migration)/M0-06(字段越权/关系链)/M0-08(匿名端点+限流) 仍为 🔴 未做。
- **规划建议（执行顺序）**：① 先按 M0-07 把 16 个未提交改动复核并作为可追溯批次提交（含 476 门禁证据），清脏基线；② 收口 M0-03/M0-04；③ 依次啃 M0-02(路由契约,高可见404)→M0-06(越权链路)→M0-05(受控迁移)→M0-08(匿名/限流)→M0-01(凭据轮换,需密钥轮换流程)；④ M0 全绿后转入 M1(字段/数据完整性)→M2/M3(平台治理+多语言)→M4→M5/M6→M7→M8→M9→M10。
- **注意**：M0-01 含密钥轮换属流程动作（先当已泄露轮换+扫描 Git 历史），不能仅靠代码；M0-07 要求每里程碑前记录 git/测试/构建基线。

## M0-02 补齐 4 处前后端契约不一致（已完成并提交）

- **提交 `f4df66c`**：fix(M0-02)，7 文件 +278/−2，全量 **483/483 通过**（基线 476 + 新增 7 用例），build 0 error。
- 四处不一致：① Agent 列表前端 URL `api/agent`→`api/agent/plans`；② Agent 工具 `api/agent/tools-catalog`→`api/agent/tools`；③ 后端缺 `GET api/business-model/entities/{id}`（新增 `BusinessModelController.GetEntity`，复用 `IBusinessEntityRegistryService.GetAsync` 注册表租户隔离）；④ 后端缺 `GET api/semantic-labels/{id}`（新增 `SemanticLabelController.GetById` + `ISemanticLabelService/SemanticLabelService.GetByIdAsync`，显式租户过滤 `TenantId==tenantId || TenantId==0` 含共享标签可见）。
- 新增契约测试 `BusinessModelControllerTests`(3) + `SemanticLabelControllerTests`(4)，覆盖 200 所属租户可见 / 200 共享标签可见 / 404 不存在 / 403 跨租户越权（403 由 `TenantDataPlanePolicy.Resolve(User,"tid")` 判定）。
- 测试坑（可复用）：① 遗留 API 进程（PID 46616）曾锁 bin 致 MSB3027，进程退出后自愈；② `ControllerBase.User` 只读，注入 tid claim 须 `controller.ControllerContext = new ControllerContext { HttpContext = new DefaultHttpContext { User = user } }`，不能对象初始化器 `{ User = user }`。
- 下一步（M0 剩余 🔴）：M0-06 字段越权/关系链 → M0-05 受控 Migration → M0-08 匿名端点+限流 → M0-01 凭据轮换（含密钥轮换流程）。
