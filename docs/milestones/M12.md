# SuperBuilder AI — 里程碑 M12（前端功能闭环与 RBAC 收口）

> 本文件是 `M12` 的里程碑详情。Master 维护里程碑总状态，Backlog 维护当前未完成项，Active Plan 维护当前施工顺序；本文件保留该里程碑的范围、验收与历史交付证据。

## 0. 来源与定位

- **消费来源**：用户提供的《前端页面整改计划》（2026-09-09 会话，基于 `master` 实际页面与权限模型整理）。
- **与 MDP 关系**：新增里程碑，承载前端 RBAC 收口 + 功能闭环（P0/P1）。MDP §17.5 已登记。
- **去重边界**：
  - **M2**（RBAC 基础 / 后端权限模型）已 ✅ —— M12 不重建权限模型，只消费。
  - **M3**（i18n 产品化）已 ✅ —— M12 文案须入统一多语言资源。
  - **M7-11**（App 应用闭环 RBAC）已 ✅ —— M12-02 直接复用 `app:*` 已同步的前端常量。
  - **M8**（前端体验 / 视觉层）已 ✅ —— M12 不重做视觉，只做权限与交互闭环。
  - **M7**（Dashboard/App/Agent 占位清零）已 ✅ 后端写操作 —— M12-09/10/12 清掉残留 Toast 占位。

## 1. 关键事实核验（落地前已回查 master 源码）

| 断言 | 证据 | 结论 |
|---|---|---|
| 后端已定义细粒度权限码 | `src/Domain/Identity/IdentityConstants.cs:15-19`（dashboard:view/create/edit/publish/delete）、`:28-30`（agent:create/manage/delete）、`:33-34`（theme:edit/publish）、`:36-38`（metadata:view/edit/scan） | ✅ 后端完整 |
| 前端常量层滞后 | `SuperBuilder_AI.Components/Components/Constants/PermissionCodes.cs` 全文仅含 `app:*` + platform/localization/identity/audit/theme:view；dashboard/agent/theme:edit/publish/metadata 全部缺失（菜单用字符串字面量如 `"dashboard:view"`） | ❌ 须补 |
| SystemStatus 缺 Page Guard | `Components/Pages/Admin/SystemStatus.razor:6` `<PermissionGuard>` 未传 `Require` → 空 Require 恒 true，仅 `<AuthGuard>` 查登录 | 🔴 真实越权 |
| Agent 新建/Run 是 Toast 占位 | `Components/Pages/Analysis/Agent.razor:117/120` | ✅ 属实（归 P1） |
| DataSource 测试连接是 Toast | `Components/Pages/.../DataSources.razor:185` + `Keys.cs:1713` "P12 后端计划" | ⚠️ 部分（见 M12-12 修正） |
| 后端 TestConnection 已实现 | `src/Api/Controllers/DataSourcesController.cs:328` `TestConnection(long id, …)` + `tests/…/DataSourcesControllerTests.cs:224-255`（NotFound/BadRequest/Refused 三用例） | ✅ 后端已存在 |
| `component:*` 权限域不存在 | `IdentityConstants.cs` 无 `component:*`；`PermissionCodes.cs` 亦无 | ⚠️ 需前后端同步新增 |

## 2. 对原始计划的前提修正

1. **"纯前端接线" 不成立** → 实为两步：①前端 `PermissionCodes.cs` 先对齐后端 `IdentityConstants`（补 dashboard/agent/theme:edit/publish/metadata:edit/scan 常量）；②按钮级 `<PermissionGuard Require=…>` 包裹。
2. **P0-UI-08 置顶** → 它是唯一"直接 URL 绕过菜单权限"的真实安全缺陷，其余为体验问题，排 P0 第一。
3. **`component:*` 是新增域** → 不能只改前端；需同步后端 `IdentityConstants` + 前端 `PermissionCodes` + 种子（归 M12 扩展项，默认 P2）。
4. **P1-04（Test Connection）修正** → 后端已能对**已保存**数据源 `TestConnection(id)`；缺口是：前端新建态仍 Toast、且无"按连接串预测试"端点。方案二选一：A) 先保存再调 `TestConnection(id)`；B) 新增 `TestConnectionString(connStr)` 端点。

## 3. P0 清单（M12-01 ~ M12-08）

### M12-01 Dashboards 按钮 RBAC（原 P0-UI-01）
- 页面 `/dashboards`（`Dashboards.razor`）。
- 映射：`dashboard:view`→页；`create`→新建；`edit`→编辑；`publish`→发布；`delete`→删除。
- 证据：后端 `IdentityConstants.cs:15-19` 已定义；前端缺 → 先补常量再包裹。
- 验收：Viewer 仅见查看；Member 见新建/编辑/发布；TenantAdmin 全；**无权限按钮隐藏而非点击 403**。

### M12-02 Apps 按钮 RBAC（原 P0-UI-02）
- 页面 `/apps`、`/apps/{code}`、`AppDetail.razor`。
- 映射：`app:view/create/edit/publish/delete`。前端 `PermissionCodes.cs` 已含 `app:*` ✅，直接包裹（复用 M7-11）。

### M12-03 Agent 按钮 RBAC（原 P0-UI-03）
- 页面 `/agent`（`Agent.razor`）。
- 映射：`agent:view/create/manage/delete`（后端 `IdentityConstants.cs:28-30`）；前端缺 → 补。
- 注：新建/Run 按钮当前是 Toast 占位 → 功能闭环归 M12-09/10（P1），本项只做权限包裹。

### M12-04 DataSources metadata 权限分离（原 P0-UI-04）
- 页面 `/data-sources`（`DataSources.razor`）。
- Viewer/Member：`metadata:view` only；TenantAdmin：+ `metadata:edit/scan`。
- 后端 `IdentityConstants.cs:36-38` 已定义；前端缺 `metadata:*` → 补。新建/测试连接/扫描按钮需 `metadata:edit/scan`。

### M12-05 ThemeEditor view/edit/publish 分离（原 P0-UI-05）
- 页面 `/themes`（`ThemeEditor.razor`）。
- 映射：`theme:view`→查看；`theme:edit`→New/Edit/Copy/Save；`theme:publish`→Assign Default/Publish。
- 后端 `IdentityConstants.cs:33-34` 已定义；前端已含 `theme:view`，缺 edit/publish → 补。Viewer/Member 应为 Preview only。

### M12-06 BusinessModel metadata 权限分离（原 P0-UI-06）
- 页面 `/business-model`（`BusinessModel.razor`）。`metadata:view/edit` 分离。

### M12-07 SemanticLabels metadata 权限分离（原 P0-UI-07）
- 页面 `/semantic-labels`。`metadata:view/edit` 分离（产品命名建议改"业务术语"，归 P2 文案）。

### M12-08 SystemStatus Page Guard 缺口修复（原 P0-UI-08，置顶）
- 页面 `/admin/system`（`SystemStatus.razor:6`）。
- 当前 `<PermissionGuard>` 未传 `Require` → 空 Require 恒 true → 仅登录即可直链绕过菜单权限。
- 修复：`<PermissionGuard Require="new[]{ PermissionCodes.PlatformDiagnosticsView }">`。
- **真实安全缺陷，优先于所有体验项。**

## 4. P1 功能闭环（M12-09 ~ M12-18）

| 编号 | 功能 | 关键事实 / 修正 |
|---|---|---|
| **M12-09** | Agent 新建接真实 API | 替换 `Agent.razor` Toast → 弹窗采集意图 → `POST api/agent/plan` → 成功跳转编辑器。**✅ 已实施（2026-09-11，CI run 34570740857 编译检查+矩阵全绿）** |
| **M12-10** | Agent Run 执行真实 Workflow | 替换 `Agent.razor` Toast → `POST api/agent/plans/{code}/run`，按 200/202 区分成功/待审批并 Toast 反馈后刷新。**✅ 已实施（2026-09-11，同 run）** |
| **M12-11** | Agent Runs 页面 | 执行历史 / 工具调用 / 时长 / 错误 / 待审批。**✅ 已实施（2026-09-11，CI run 34576806667 编译检查+E2E 全绿；38 新 i18n 键过 ResourceKeyRegistryTests）** |
| **M12-12** | DataSource 测试连接真实执行 | 方案 B 已落地：后端 `TestConnectionString`（`:367`）+ 前端新建态 `TestConn`（`DataSources.razor:201`）→`POST /api/data-sources/test-connection` 均真实调用；清理死键 `DataSourcesTestSubmitted`。**✅ 已收口（2026-09-11）** |
| **M12-13** | Dashboard 可视化设计器 | 拖拽画布（12 栅格 CSS Grid），DSL 退 Source Mode。**✅ 已实施（2026-09-11，commit `c677d10`，CI run 34596673160 编译检查+E2E 全绿；70 新 i18n 键过 ResourceKeyRegistryTests + 2 个 bUnit 用例，全量 1073/1073 绿）** |
| **M12-14** | App 可视化设计器 | Component Tree / Canvas / Data Binding / Theme / Permissions（Events 因 App DSL 无模型按红线剔除，Permissions 渲染为说明面板）。**✅ 已实施（2026-09-11，commit `83ea583`；复用 Dashboard 12 栅格拖拽运行时，60 新 i18n 键过 `ResourceKeyRegistryTests` + 2 个 bUnit 用例，全量 1075/1075 绿；CI run 34600842945 编译检查+E2E 全绿，GQ-007 独立失败）** |
| **M12-15** | Semantic Model 关系图 | 实体关系、点节点进 Entity Detail。**✅ 已实施（2026-09-11，commit `5699f71`；新增只读关系端点 + 零依赖 SVG 关系图，节点=实体/边=关系，点击进实体详情；4 新 i18n 键 + 7 单测（3 后端 + 4 bUnit），全量 1082/1082 绿；CI run 34605047205 编译检查+E2E 全绿，GQ-007 独立失败）** |
| **M12-16** | 指标中心 Metric Center | 指标 / 维度 / 计算字段治理。**✅ 已实施（2026-09-11，commit `5010fb6`；新增只读治理端点 `metrics`/`dimensions` + 投影 record，前端 `/metrics` 三 Tab 只读治理视图（指标 / 维度 / 计算字段）；30 新 i18n 键 + 9 单测（5 后端 + 4 bUnit），全量 1091/1091 绿；CI run 34608796259 编译检查+E2E 全绿，GQ-007 独立失败）；**后由「增量 3-2」（2026-09-12，迁移 `M12_18`）补齐指标/维度编辑（计算口径 Expression + 数据类型 DataType），全量 1140/1140 绿** |
| **M12-17** | Identity 增加组织 / 部门 / 用户组 | 关联 M2 已建 RBAC。**✅ 已实施（2026-09-11，commit `3e1330c`；新增 6 表（Organization/Department/UserGroup/UserGroupMember/UserGroupRole/UserDepartmentMember）+ EF 迁移，`api/identity-directory` 11 端点（服务端强制 `identity:manage`），组角色并入 `GetPermissionsAsync` 使「用户→组→角色→权限」真实生效；前端 `/admin/identity-directory` 三 Tab；49 新 i18n 键 + 17 单测，全量 1108/1108 绿；CI run 34613229420 编译检查+E2E 全绿）；**后由「增量 3-1」（2026-09-12，无新迁移）补齐重命名/启停/删除/列表分页，全量 1132/1132 绿** |
| **M12-18** | 数据权限 / RLS 可视化管理 | 复用项目已有 RLS。**✅ 已实施（2026-09-11，commit `70a3395`；后端新增 `GET api/data-policies/row/manage`（跨数据源富化列表：数据源名/表名/列名/类型/主体显示名）与 `POST api/data-policies/row/{id}/toggle`（启用停用，省略 body 取反），复用既有 `ScopeAsync`（强制 `identity:manage`+`metadata:edit`）与 `RlsVocabularyValidator`，不改执行链路；前端 `/admin/data-policies` 统计瓦片 + 筛选 + 表/列级联新建编辑 + 启用切换 + 删除；65 新 i18n 键 + 11 单测（7 后端 + 4 bUnit），全量 1119/1119 绿；CI run 34617329087 编译检查+E2E 全绿，GQ-007 独立失败）** |

## 5. 产品页面结构建议（设计输入，非强制交付）

原始计划建议最终结构（供 M12 设计参考，不强制本里程碑一次性落地）：

```
工作台 / Ask BI
分析：仪表盘 / 应用 / 智能体
数据与语义：数据源 / 业务模型 / 业务术语 / 指标中心
设计：应用设计器 / 组件库 / 主题设计器
企业管理：用户与组织 / 角色与权限 / 数据权限 / 模型与账号 / 企业主题 / 多语言 / 审计日志
平台管理：平台总览 / 租户 / 租户成员 / 平台管理员 / 管理员范围 / 配额 / 自助注册 / 演示数据 / 多语言 / 系统状态
个人：个人设置
```

## 6. 统一规则与验收标准

- **四层权限贯穿**：`AuthGuard` → Page Permission → Action Permission → API Permission。
- 无权限用户**不看到按钮**（而非点击 403）。
- Viewer / Member / TenantAdmin 三角色端到端体验正常。
- 全量单测绿 + 三端 build 0 error + Golden 18/18 不变。
- `ResourceKeyRegistryTests` 等 i18n 护栏全绿（M3 已 ✅）。

## 7. 红线

- 不改动后端权限模型（已在 `IdentityConstants` 完整定义），M12 仅前端同步 + 包裹。
- `component:*` 为新增域，须同步后端 `IdentityConstants` + 前端 `PermissionCodes` + 种子（默认 P2，不阻塞 P0）。
- 不得引入新占位 / 假数据；M12-09/10/12 的 Toast 占位必须被真实调用替换。
- 权限常量集中 `PermissionCodes.cs`，禁止 `.razor` 内联字符串字面量绕过（PR 评审项）。

## 8. 状态

> **实施状态（2026-09-11 源码复核）**：M12-P0（M12-01~08）的常量对齐（`PermissionCodes.cs` 已含 `dashboard:*`/`agent:*`/`theme:edit|publish`/`metadata:*`）与按钮级 `PermissionGuard` 包裹**已在代码中落地**。逐页核实：SystemStatus(`PlatformDiagnosticsView`)、Dashboards、Apps、DataSources、ThemeEditor、BusinessModel、SemanticLabels 均正确包裹；唯一遗漏 `Agent.razor:33` 的 Run 按钮未包 `agent:manage` 已于本日补全。

> **权限矩阵 E2E（2026-09-11 编写 + 实跑）**：`tests/SuperBuilder_AI.E2E.Tests/PermissionMatrixE2ETests.cs` 覆盖 M12-P0 全部按钮级守卫的双向断言——管理员可见、读者隐藏（拆分为 `AdminVisibleRoutes` 7 项 + `ReaderHiddenRoutes` 8 项，路由已校正为真实 `@page`：`/dashboards`、`/apps`、`/agent`、`/data-sources`、`/themes`、`/business-model`、`/semantic-labels`、`/admin/system`）；并对 `Agent.razor` Run 按钮（`agent:manage`）单列专项回归（读者必隐藏 + 管理员有种子时必可见）。复用既有 `LoginHelper.ApiLoginAsync` + `SkippableFact/SkippableTheory` 范式，未改任何 razor。
> **实跑结果（2026-09-11，本地拉起整套栈 + 注入 SB_E2E_*）**：经 `dotnet vstest` 对预编译 DLL 实跑，**15 通过 / 1 失败 / 1 跳过**。凭据：`e2eadmin`/`e2ereader`（租户 4 = `e2eapp`，口令 `longping00`）。
> **CI 实跑结果（run 34568568564 / commit 86a2245，2026-09-11）**：**16 通过 / 0 失败 / 1 跳过（无智能体种子，诚实跳过）**，全绿 ✅。
> - ✅ 通过：6 条管理员可见断言（dashboards/apps/agent/data-sources/business-model/semantic-labels，均 <1s）+ 8 条读者隐藏断言（含 /themes、/admin/system）。
> - ⏭️ 跳过：`Admin_Agent_RunButton_Visible_WhenAgentsExist`（E2E 租户无智能体种子，读者侧隐藏断言已覆盖回归）。
> - ❌ 失败：`Admin_Sees_GuardedAction("/themes","保存 主题")` 等待按钮可见 20s 超时。
> **CI 复跑根因（run 34566854796 / commit 4511564，2026-09-11）**：矩阵并非只 1 例失败，而是 **17 中 5 通过 / 12 失败**。全 12 失败均源于 **登录 429（`SB_TOO_MANY_REQUESTS`）**：`RateLimitMiddleware` 默认 `LoginLimit=10`/`60s`，且登录限流键为 `ip:127.0.0.1:<UA哈希>`——CI 中所有 E2E 登录同源自 127.0.0.1 同一 Playwright UA，共用一个桶，矩阵 ~19 次登录瞬间触顶，后续用例登录即被限流。此外 admin `/themes` 还叠加一个真实组件崩溃：`ThemeEditor` 的 `_tokens` 初值为空字典，且 `OnAfterRenderAsync(firstRender)` 在会话自举前触发 `api/themes/editor/blueprint`（以 `authTenant=0` 匿名发出→401→`GetJsonAsync` 抛异常被 catch→`_tokens` 仍空），调色板渲染 `_tokens[key]` 抛 `KeyNotFoundException 'primary'`。两者均**非守卫缺陷**。
> **修复（已落源码 + CI 复跑全绿）**：① `.github/workflows/dotnet-build.yml`「启动 Web API」步骤注入 `RateLimit__LoginLimit=500`/`RateLimit__GlobalLimit=1000`（与 Local 配置等价，仅 CI）；② `ThemeEditor.razor` 将 `_tokens` 防御性初始化为 `TokenOrder.ToDictionary(k=>k,"")`，并仿 `AppPreview`/`AppRun` 在 `OnInitialized` 订阅 `State.SessionRestoredChanged`、`OnAfterRenderAsync` 仅 `State.SessionRestored` 后取数；③ 测试侧 `WaitForLoadStateAsync(NetworkIdle)` 时序解耦保留。CI run 34568568564（commit 86a2245）= **16 通过 / 1 跳过 / 0 失败**，全绿。

> **⚠️ 本地实跑环境注**：本沙箱内 `Environment.GetFolderPath(CommonApplicationData)` 返回 null（`ProgramData` 已知文件夹不可解析），导致 NuGet `XPlatMachineWideSetting` → `ConfigurationDefaults` 静态构造抛 `Value cannot be null (path1)`，从而 `dotnet restore`/`dotnet build`/`dotnet test` 全部失败（`NUGET_PACKAGES`/`ProgramData` 等环境变量设置均无效）。**绕过法**：对预编译 DLL 直接 `dotnet vstest <dll>`（不经 NuGet/SDK 还原管线）可正常实跑。CI（干净环境）不受此影响，`dotnet test` 直接可用。
> **结论**：P0 由「草案/未排期」转为**「已实施 + 权限矩阵 E2E 已在 CI 实跑全绿（16 通过 / 1 跳过 / 0 失败，run 34568568564）」**；P1（M12-09~18）推进中：M12-09/10/11/12 已交付（Agent 新建/Run 接真实 API、Agent Runs 详情页、DataSource 测试连接方案 B），M12-13（Dashboard 可视化设计器）、M12-14（App 可视化设计器）、M12-15（Semantic Model 关系图）、M12-16（指标中心）已实施，M12-17（Identity 组织目录）、M12-18（数据权限 / RLS 可视化管理）已实施，**M12-P1（M12-09~18）全部闭环**。

- 🟢 P0 已实施 + 权限矩阵 E2E 已在 CI 实跑全绿（16/17 通过、1 跳过、0 失败，run 34568568564，2026-09-11）；P1 推进中：M12-09/10/11 已实施并 CI 验证（run 34576806667，编译检查+E2E 全绿，38 新 i18n 键过护栏）；M12-12 已收口（方案 B，commit `80b4317`，CI run 34580960129 编译检查+E2E 全绿）；M12-13 已实施并 CI 验证（可视化设计器，commit `c677d10`，run 34596673160 编译检查+E2E 全绿；70 新 i18n 键 + bUnit 2 用例，全量 1073/1073 绿）；M12-14 已实施并 CI 验证（App 可视化设计器，commit `83ea583`，60 新 i18n 键 + bUnit 2 用例，全量 1075/1075 绿；CI run 34600842945 编译检查+E2E 全绿）；M12-15 已实施并 CI 验证（Semantic Model 关系图，commit `5699f71`，4 新 i18n 键 + 7 单测，全量 1082/1082 绿；CI run 34605047205 编译检查+E2E 全绿）；M12-16 已实施并 CI 验证（指标中心，commit `5010fb6`，30 新 i18n 键 + 9 单测，全量 1091/1091 绿；CI run 34608796259 编译检查+E2E 全绿，GQ-007 独立失败）；M12-17 已实施并 CI 验证（Identity 组织目录，commit `3e1330c`，6 新表 + EF 迁移 + 11 端点，49 新 i18n 键 + 17 单测，全量 1108/1108 绿；CI run 34613229420 编译检查+E2E 全绿，GQ-007 独立失败）；M12-18 已实施并 CI 验证（数据权限 / RLS 可视化管理，commit `70a3395`，2 个只读/写端点 + 前端 `/admin/data-policies`，65 新 i18n 键 + 11 单测，全量 1119/1119 绿；CI run 34617329087 编译检查+E2E 全绿，GQ-007 独立失败）。**M12-P1（M12-09~18）至此全部闭环。** 其后两项**产品化增量**已交付：**增量 3-1**（组织目录 重命名/启停/删除/列表分页，commit `8bca05a`，9 端点 + 14 新 i18n 键，全量 1132/1132 绿）；**增量 3-2**（指标/维度编辑 计算口径 Expression + 数据类型 DataType，commit `80f34cd`，迁移 `M12_18`，全量 1140/1140 绿，开发库迁移 **45/45** pending 0 / drift 0；CI run 34658011942 编译检查+E2E 全绿，GQ-007 独立失败）。
- P0（M12-01~08）验收建议紧随 M7-11 验收之后；P1（M12-09~18）按产品优先级分批。

### M12-12 交付记录（2026-09-11）

- 采用方案 B：新增 `POST api/data-sources/test-connection`，请求为 `dbType` / `connectionString`；令牌租户与用户必填，要求 `metadata:edit`。仅探测，不创建数据源、不写访问授权。已保存数据源的原端点保持原契约。
- 三种现有驱动（SQLSERVER / MYSQL / POSTGRESQL）打开连接并执行 `SELECT 1`，15 秒超时；返回 `status`、`elapsedMs`、脱敏 `errorCode`，不返回连接串或异常消息。拒绝空连接串、超长连接串及不支持的类型。
- 新建弹窗移除 Toast 占位，区分 HTTP 成功和连接测试成功；测试期间禁用重复测试、保存及连接参数编辑。连接串输入隐藏字符。修正 SQL Server 显示名称到 SQLSERVER 的类型码转换，保存同步使用该转换。
- 验证：Release 全量单测 **1071/1071 通过，0 失败、0 跳过**；新增 8 个后端用例（权限、输入校验、三驱动失败脱敏、不持久化）及 2 个 bUnit 界面用例（成功/失败、类型码、仅预测试请求）。修复已有 AuthStoreTests 测试替身遗漏 PostJsonAsync 导致的编译失败。
- 构建：API / 共享组件随测试构建通过；Web Release 0 警告、0 错误；MAUI Windows Release 2 个既有 MainPage 弃用警告、0 错误。
- 验证边界：未执行连接真实外部数据库的成功预测试、真实浏览器 E2E 或 Android/iOS 设备验收；成功 UI 分支使用 API 替身验证。未部署、未提交或推送。完整生产验收不据此标记完成。

### M12-13 交付记录（2026-09-11）

- 新增页面 `Components/Pages/Analysis/DashboardDesigner.razor`（路由 `/dashboards/{Id:long}/design`），双模式：**设计**（12 栅格拖拽画布）与 **源码**（`DashboardDsl` JSON 直编，`JsonNode.Parse` 校验失败回退并提示）。
- 画布实现零第三方依赖：原生 Pointer Events + CSS Grid（`grid-template-columns: repeat(12,1fr)`），按 `getComputedStyle` 换算像素位移为 (x/y/w/h)，松手经 `[JSInvokable] OnWidgetGridChanged` 回写 `position`；调色板 HTML5 拖放经 `OnPaletteDrop(type,x,y)` 落位。
- 运行时脚本 `Components/wwwroot/js/dashboard-designer.js`（`window.SuperBuilder.designer.attach/detach`），已在 `SuperBuilder_AI.Web/Pages/_Host.cshtml` 与 `SuperBuilder_AI.Maui/wwwroot/index.html` 注册；样式 `.sb-dz-*` 追加至 `Components/wwwroot/css/app.css`（复用 `--sb-*` 令牌，含 1100px 响应式降级）。
- 属性面板支持 6 类组件（chart/table/kpi/filter/text/aiInsight）通用 + 专属字段编辑；保存走 `PUT api/dashboards/{id}`，发布走 `POST api/dashboards/{id}/publish`；入口在 `Dashboards.razor` 行操作区（`PermissionGuard Require=DashboardEdit`）。
- i18n：新增 **70** 个 `Content.DashboardDesigner*` 键，后端 `ResourceKeys`（const + Catalog）/ RCL `Keys`（const + Defaults）/ `LocalizationSeedService` 四处严格一致，经 `ResourceKeyRegistryTests` 通过；页面内无裸中文（全部 `L10n.T(Keys.Content.*, fallback)`）。
- 验证：全量单测 **1073/1073 通过，0 失败、0 跳过**（基线 1071 + 新增 2 个 bUnit 用例）；`DashboardDesignerUiTests` 覆盖「DSL 渲染组件 + 调色板新增」「源码模式切换」；Components / Tests / Web 宿主三个工程 build 0 错误。
- 验证边界：仅 bUnit 渲染级验证 + 本地构建，未做真实浏览器拖拽手势 E2E（Pointer Events 换算依赖真实布局）；未在 MAUI 真机验收。
- CI（commit `c677d10`，`workflow_dispatch` run **34596673160**）：✅ 编译检查 success（三端 build 0 error + 70 新键过 `ResourceKeyRegistryTests`）；✅ Web/Blazor E2E (Playwright) success；❌ V2.6 Evaluation Controller Runtime Smoke failure —— **仅**步骤「Controller C.13.3 Multi-Metric Evaluation」(GQ-007 `SemanticApplicabilityGate` BLOCK)，属用户「选 B」登记的独立既有故障，与 M12-13 无关。

### M12-14 交付记录（2026-09-11）

- 新增页面 `Components/Pages/Analysis/AppDesigner.razor`（路由 `/apps/{Code}/design`），复用 M12-13 的 Dashboard 12 栅格拖拽运行时（`window.SuperBuilder.designer.attach/detach` + `.sb-dz-*` 样式，零第三方库）。左侧组件树、中部调色板+12 栅格画布、右侧属性面板。
- 属性面板六类编辑：应用设置（name/description/themeKey）、常规（id/title/order/position x/y/w/h）、数据绑定（entity/dataSourceId/tableId/metrics[field+aggregation]/dimensions/filters[field+operator+value]/sort[field+direction]/limit）、呈现参数（properties 键值）、样式（palette/background/showBorder/padding）、权限说明（引用 RBAC 码 AppCreate/AppView/AppEdit/AppPublish/AppDelete 的纯说明面板）。
- 双模式：**可视化**（拖放/调色板经 `OnPaletteDrop`/`OnWidgetGridChanged` 回写 `components[]`）+ **源码**（`JsonNode.Parse` 校验，失败回退提示）；保存 `PUT api/apps/{code}`（body `{dslJson}`），发布 `POST api/apps/{code}/publish?tenantId=`（取 `version` Toast）。
- 入口接线：`Apps.razor` 行操作区与 `AppDetail.razor` 头部新增「设计」按钮，`PermissionGuard Require=AppEdit` → `/apps/{code}/design`。
- **红线遵守**：App DSL 无 Events 模型 → 按不造假占位原则剔除 Events 编辑，Permissions 渲染为说明面板（非伪表单）。
- i18n：新增 **60** 个 `Content.AppsDesigner*` 键（含 `AppsDesignerSaving`），后端 `ResourceKeys`（const + Catalog）/ RCL `Keys`（const + Defaults）/ `LocalizationSeedService` 四处严格一致（脚本校验 `OK: all 59 AppsDesigner keys consistent`），经 `ResourceKeyRegistryTests` 通过；页面内无裸中文。
- 验证：全量单测 **1075/1075 通过，0 失败、0 跳过**（基线 1073 + 新增 2 个 bUnit 用例）；`AppDesignerUiTests` 覆盖「DSL 渲染 2 组件→调色板新增 3 个」「源码模式切换→`.sb-dz-source textarea`」；Components（net10.0 目标）/ Tests 工程 build 0 错误。
- 验证边界：仅 bUnit 渲染级验证 + 本地构建，未做真实浏览器拖拽手势 E2E；未在 MAUI 真机验收；沙箱缺 Android SDK 致 RCL 的 net10.0-android/ios 目标本地无法构建（CI 环境有 SDK，不受影响）。
- CI（commit `83ea583`，`workflow_dispatch`/push 触发 run **34600842945**）：✅ 编译检查 success（三端 build 0 error + 60 新键过 `ResourceKeyRegistryTests`）；✅ Web/Blazor E2E (Playwright) success；❌ V2.6 Evaluation Controller Runtime Smoke failure —— **仅**步骤「Controller C.13.3 Multi-Metric Evaluation」(GQ-007 `SemanticApplicabilityGate` BLOCK)，属用户「选 B」登记的独立既有故障，与 M12-14 无关。M12-14 在自身范围内 CI 全绿（编译检查 + E2E + i18n 护栏）。

### M12-15 交付记录（2026-09-11）

- 后端（只读，无破坏性契约变更）：新增 `GET api/business-model/relationships?tenantId=`，复用既有 `BusinessEntityRelationship` 聚合（SourceEntityId/TargetEntityId/Name/RelationshipType/Cardinality），租户隔离由 `TenantDataPlanePolicy` + 仓储「两端实体 TenantId 均须匹配」双重保证。`IBusinessEntityRepository` / `IBusinessEntityRegistryService` 增 `ListRelationshipsAsync`，实现于 `BusinessEntityRepository`（Include 两端实体）。
- 关键发现：既有 `BusinessEntityService.ListAsync` **未 Include 关系**，故实体列表不含关系；关系图必须走独立端点（未改既有 entities 列表契约，属纯增量）。
- 前端（零第三方库）：新增 `Components/Pages/Analysis/SemanticModelGraph.razor` —— SVG 关系图，节点=实体（**按业务域分列**布局，列内纵向堆叠），边=实体关系（带箭头 marker），点击节点 → `/business-model/entities/{id}`；`Entities.Count==0` 走空状态、有实体无关系时显示诚实说明（不造假边）。`BusinessModel.razor` 新增「关系图」Tab（Badge=关系数），`Reload()` 同步拉取 relationships。`app.css` 追加 `.sb-graph-*`（复用 `--sb-*` 令牌）。
- i18n：新增 **4** 个 `Content.BusinessModelGraph*` 键（Title/Empty/NoRelationships/ClickHint），`ResourceKeys`（const+catalog）/`Keys`（const+Defaults）/`LocalizationSeedService` 四处严格一致，过 `ResourceKeyRegistryTests`；页面无裸中文。
- 验证：全量单测 **1082/1082 通过，0 失败、0 跳过**（基线 1075 + 3 后端 + 4 bUnit）；`BusinessModelControllerTests` +3（403 跨租户 / 200 返回关系 / 空集）；`SemanticModelGraphUiTests` +4（节点+边渲染 / 点击导航 / 无关系 / 无实体）。后端主工程、Components(net10.0)、Tests、Web 宿主四工程 build 0 错误。
- 验证边界：仅 bUnit 渲染级 + 本地构建验证；未做真实浏览器交互 E2E、未在 MAUI 真机验收；沙箱缺 Android SDK 致 RCL 的 android/ios 目标本地无法构建（CI 环境有 SDK，不受影响）。
- 数据现状说明（诚实）：业务术语治理层（`BusinessEntities`/`BusinessEntityRelationships`）在演示租户多为空集，故关系图实际展示为「节点（若有实体）+ 无关系说明」；关系图能力就绪，待治理层数据填充后即呈现真实边。
- CI（commit `5699f71`，push 触发 run **34605047205**）：✅ 编译检查 success（三端 build 0 error + 4 新键过 `ResourceKeyRegistryTests`）；✅ Web/Blazor E2E (Playwright) success；❌ V2.6 Evaluation Controller Runtime Smoke failure —— **仅**步骤「Controller C.13.3 Multi-Metric Evaluation」(GQ-007 `SemanticApplicabilityGate` BLOCK)，属用户「选 B」登记的独立既有故障，与 M12-15 无关。M12-15 在自身范围内 CI 全绿。

### M12-16 交付记录（2026-09-11）

- 后端（只读，纯增量，无破坏性契约变更）：新增 `GET api/business-model/metrics?tenantId=` 与 `GET api/business-model/dimensions?tenantId=`；`IBusinessEntityRepository` / `IBusinessEntityRegistryService` 增 `ListMetricsAsync` / `ListDimensionsByTenantAsync`，实现于 `BusinessEntityRepository`。租户隔离：指标经所属 `BusinessEntity.TenantId`（EF INNER JOIN），维度自带 `TenantId` 直接过滤；均复用 `TenantDataPlanePolicy`（跨租户 403）。
- 关键设计：新增只读投影 record `BusinessMetricView` / `BusinessDimensionView`（`Application/Ports/BI/Entity/BusinessModelGovernanceViews.cs`）——**刻意不返回 EF 实体**：`BusinessEntityMetric.BusinessEntity` 与 `BusinessEntity.Metrics` 互为反向导航，直接序列化存在循环引用风险；投影同时一次算好实体名/业务域/`PhysicalBindings.Count`。
- 前端（零第三方库）：新增 `Components/Pages/Analysis/MetricCenter.razor`（路由 `/metrics`）——统计瓦片（指标总数 / 计算字段 / 已绑定物理列）+ 三 Tab（指标 / 维度 / 计算字段）+ 搜索过滤 + 指标行操作跳 `/business-model/entities/{id}`；计算字段 Tab 由 `isCalculated` 过滤。`NavMenuItems` 「分析」组新增 `metrics` 入口（Permission `metadata:view`）。
- i18n：新增 **30** 个键（`Nav.MetricCenter` + 29 个 `Content.MetricCenter*`），四处严格一致（ResourceKeys 58 = 29 const + 29 catalog、Keys 58、Seed 29；Nav 2/2/1），过 `ResourceKeyRegistryTests`；页面无裸中文。
- 验证：全量单测 **1091/1091 通过，0 失败、0 跳过**（基线 1082 + 5 后端 + 4 bUnit）；`BusinessModelControllerTests` +5（403 跨租户 / 投影字段 / 跨租户排除 / 维度域名 / 403）；`MetricCenterUiTests` +4（渲染 / 计算字段过滤 / 行操作导航 / 空状态）。后端主工程、Components(net10.0)、Tests、Web 宿主四工程 build 0 错误。
- 验证边界：仅 bUnit 渲染级 + 本地构建验证；未做真实浏览器交互 E2E、未在 MAUI 真机验收；沙箱缺 Android SDK 致 RCL 的 android/ios 目标本地无法构建（CI 环境有 SDK，不受影响）。
- 范围边界（诚实）：本项交付**只读治理视图**；指标与维度的编辑（含计算口径）需扩展 `BusinessEntityUpsertRequest` 写契约，属更大改动，留待后续里程碑（**已由「增量 3-2」补齐，见下文；`MetricCenterEditHint` 面板文案同步改写**）；页面以 i18n 说明面板（`MetricCenterEditHint`）明示，不做假占位。
- 数据现状说明（诚实）：治理层（`BusinessEntities` → `BusinessEntityMetrics` / `BusinessEntityDimensions`）在演示租户多为空集，指标中心实际展示为「统计瓦片全 0 + 空状态」；能力就绪，待治理层数据填充后即呈现真实指标/维度。
- CI（commit `5010fb6`，push 触发 run **34608796259**）：✅ 编译检查 success（三端 build 0 error + 30 新键过 `ResourceKeyRegistryTests`）；✅ Web/Blazor E2E (Playwright) success（含 M12-P0 权限矩阵步骤）；❌ V2.6 Evaluation Controller Runtime Smoke failure —— **仅**步骤 20「Controller C.13.3 Multi-Metric Evaluation」(GQ-007 `SemanticApplicabilityGate` BLOCK)，属用户「选 B」登记的独立既有故障，与 M12-16 无关。M12-16 在自身范围内 CI 全绿。

### M12-17 交付记录（2026-09-11）

- 域模型（6 张新表）：`Organization`（租户内组织结构单元）、`Department`（隶属组织，`ParentId` 树形）、`UserGroup`（横切职能集合）、`UserGroupMember`（组员）、`UserGroupRole`（组 → 角色）、`UserDepartmentMember`（用户 → 部门）；均带 `TenantId` + 全局查询过滤器。EF 迁移 `20260911143327_M12_17_IdentityOrganizationUnits`（由 `dotnet-ef` 生成，快照同步，`has-pending-model-changes` = None）。
- 后端服务：新增 `Interfaces.Identity.IIdentityDirectoryService` + `Services.Identity.IdentityDirectoryService`；所有查询用 `IgnoreQueryFilters()` + 显式 `TenantId` 过滤，**行为不依赖调用方是否开启租户作用域**（比依赖作用域更可预期）。
- **RBAC 关联（本项核心）**：`IdentityService.GetPermissionsAsync` 由单一 UserRole 查询扩展为 **有效角色 = 直接 `UserRole` ∪ 组角色 `UserGroupRole`**（经 `UserGroupMembers` 关联），使「用户 → 用户组 → 角色 → 权限」链路真实生效，而非仅做组织架构展示。
- 端点：新增 `IdentityDirectoryController`（`api/identity-directory`，11 端点：organizations GET/POST、departments GET/POST、user-groups GET/POST、user-groups/{id}/roles PUT、user-groups/{id}/members POST/DELETE、users/{id}/department PUT）。**服务端强制 `identity:manage`**（缺 claim 直接 403，`User.HasClaim("perm", …)`）+ `TenantDataPlanePolicy` 数据面单租户恒等（跨租户 403）。注：既有 `IdentityController` 仅有前端 `PermissionGuard`、无服务端权限码校验；本控制器改为服务端强制，是 M12-P0 已知缺口方向上的局部收敛。
- 前端（零第三方库）：新增 `Components/Pages/Admin/IdentityDirectory.razor`（路由 `/admin/identity-directory`）——统计瓦片 + 三 Tab（组织 / 部门 / 用户组）+ 新建组织/部门/用户组 + 组角色编辑（全量替换）+ 组员添加 + 部门成员分配；`NavMenuItems` 管理后台组新增入口（`identity:manage`）。
- i18n：新增 **49** 个键（`Nav.IdentityDirectory` + 48 个 `Content.IdentityDirectory*`），四处严格一致（ResourceKeys 98 = 48 const + 48 catalog + Nav 2、Keys 98、Seed 49），过 `ResourceKeyRegistryTests`；页面无裸中文。
- 验证：全量单测 **1108/1108 通过，0 失败、0 跳过**（基线 1091 + 13 后端 + 4 bUnit）；后端主工程、Components(net10.0)、Tests、Web 宿主四工程 build 0 错误。
- 工具链注记（沙箱）：`dotnet-ef` 已全局安装（10.0.10），但沙箱 `APPDATA` 为空会使 `dotnet nuget` / `dotnet ef` 报 `Value cannot be null (Parameter 'path1')`；显式 `export APPDATA="C:\Users\ThinkPad  X1\AppData\Roaming"` 后可直接调用 `~/.dotnet/tools/dotnet-ef.exe`。这是本项目首次在沙箱内**用真实 EF 工具生成迁移**（此前手工迁移易漏快照）。
- 范围边界（诚实）：本项交付组织/部门/用户组的**新建与关联**闭环；（后续由「增量 3-1」补齐重命名 / 启停 / 删除 / 列表分页，见下文）；**部门树 UI 仍未做**，页面不显示未实现的假操作入口。演示租户的组织目录为空集，页面实际展示「瓦片全 0 + 空状态」；能力就绪，待数据填充。
- CI（commit `3e1330c`，push 触发 run **34613229420**）：✅ 编译检查 success（三端 build 0 error + 49 新键过 `ResourceKeyRegistryTests`）；✅ Web/Blazor E2E (Playwright) success；❌ V2.6 Evaluation Controller Runtime Smoke failure —— **仅**步骤 20「Controller C.13.3 Multi-Metric Evaluation」(GQ-007 `SemanticApplicabilityGate` BLOCK)，属用户「选 B」登记的独立既有故障，与 M12-17 无关。M12-17 在自身范围内 CI 全绿。
### M12-18 交付记录（2026-09-11）

- 后端（复用既有 RLS，纯增量，无破坏性契约变更）：新增 `GET api/data-policies/row/manage?tenantId=`
  （可选 `dataSourceId` 过滤），在原 `RowLevelSecurityPolicy` 之上补齐 `dataSourceName` / `tableName` /
  `columnName` / `dataType` / `subjectDisplayName`（User→DisplayName、Role→Name、Attribute→key=value）；
  新增 `POST api/data-policies/row/{id}/toggle`（启用/停用，body 省略时取反，Version 自增）。
- 授权与隔离：两类端点复用既有 `ScopeAsync` —— 服务端强制 `identity:manage` **且** `metadata:edit`，
  数据面按 `TenantId` 恒等；跨租户策略不出现在列表、toggle 返回 404。运算符仍由
  `RlsVocabularyValidator` 受控词表校验，**未改动执行链路**（`QueryPlanSecurityGate` 消费同一张表）。
- 前端（零第三方库）：新增 `Components/Pages/Admin/DataPolicies.razor`（路由 `/admin/data-policies`）——
  统计瓦片（总数/已启用/已停用/覆盖数据源）+ 筛选（数据源/状态/关键字）+ 表格（含条件与状态列）+
  新建/编辑抽屉（数据源→表→列级联、主体类型 Everyone/User/Role/Attribute、受控运算符、
  IS NULL / IS NOT NULL 免填值）+ 启用切换 + 删除。导航「管理后台」组新增「数据权限」入口。
- i18n：新增 **65** 键（`Nav.DataPolicies` + 64 个 `Content.DataPolicies*`），四处严格一致
  （精确计数 2/2/1，无孤儿键），过 `ResourceKeyRegistryTests`。
- 验证：全量单测 **1119/1119 通过**（基线 1108 + 7 后端 + 4 bUnit）；后端主工程、
  Components(net10.0)、Tests、Web 四工程 build 0 错误。
- 范围边界（诚实）：本页仅管理**行级**策略；**列级（字段）授权尚未提供**，页面以说明文案明示，
  不做假占位。仅 bUnit 渲染级 + 本地构建验证，未做浏览器交互 E2E。
- CI（commit `70a3395`，push 触发 run **34617329087**）：✅ 编译检查 success（三端 build 0 error + 65 新键过 `ResourceKeyRegistryTests`）；✅ Web/Blazor E2E (Playwright) success；❌ V2.6 Evaluation Controller Runtime Smoke failure —— **仅**步骤 20「Controller C.13.3 Multi-Metric Evaluation」(GQ-007 `SemanticApplicabilityGate` BLOCK)，属用户「选 B」登记的独立既有故障，与 M12-18 无关。M12-18 在自身范围内 CI 全绿。

### M12-17 增量 3-1 交付记录（2026-09-12）

- 定位：M12-P1（09~18）闭环后的产品化增量，补齐 M12-17 组织目录在「范围边界」中明示未做的 **重命名 / 启停 / 删除 / 列表分页**。**无新迁移**（纯 API + 前端 + 服务逻辑）。
- 后端（纯增量，`IIdentityDirectoryService` / `IdentityDirectoryService`）：新增
  `UpdateOrganizationAsync`、`SetOrganizationEnabledAsync`、`DeleteOrganizationAsync`（有部门时拒绝）；
  `UpdateDepartmentAsync`（可改挂组织/上级，校验同租户且防自环）、`SetDepartmentEnabledAsync`、
  `DeleteDepartmentAsync`（有成员或子部门时拒绝）；`UpdateUserGroupAsync`、`SetUserGroupEnabledAsync`、
  `DeleteUserGroupAsync`（级联清理成员与角色关联）。
- 端点：`IdentityDirectoryController` 新增 **9** 个（`PUT /{id}`、`PUT /{id}/enabled`、`DELETE /{id}` × 3 类），
  全部复用既有 `RequireIdentityManage` + `TenantDataPlanePolicy`（服务端强制 `identity:manage`，跨租户 403）。
- 分页：三个列表端点改返回分页信封 `DirectoryPage<T>{items,total,page,pageSize}`；`pageSize<=0` 时返回全量（`OfAll`），向后兼容。
- 语义收敛：`IdentityService.GetPermissionsAsync` 改为**仅已启用用户组**参与权限计算（停用组的角色不再生效）。
- 前端（零第三方库）：`IdentityDirectory.razor` 组织/部门/用户组行操作统一加「重命名 / 启停 / 删除」；
  新增通用重命名弹窗（名称 + 描述，编码不可变）；列表解析兼容分页信封与纯数组（`ItemsOf`）。
- i18n：新增 **14** 键（`CommonDelete`/`CommonEdit`/`CommonSaveSuccess` + 11 个 `IdentityDirectory*`），四处严格一致。
- 验证：`IdentityDirectoryControllerTests` +12（重命名/启停/删除校验/级联/分页切片/跨租户改挂拒绝）
  → 25/25 绿；`IdentityDirectoryUiTests` +1（分页信封解析）并同步断言行操作数量；全量 **1132/1132 绿**。
- 提交：`8bca05a`（未单独跑 CI，随下一条 push 一并验证）。
- 范围边界（诚实）：**部门树 UI** 仍未做；仅 bUnit 渲染级 + 本地构建验证，未做浏览器交互 E2E。

### M12-16 增量 3-2 交付记录（2026-09-12）

- 定位：补齐 M12-16 在「范围边界」中明示留待后续的**指标 / 维度编辑**（含计算口径与数据类型）。M12-16 原为**只读**治理视图，本增量使其可写闭环。
- 域模型 + 配置：`BusinessEntityMetric` 新增 `Expression`（计算口径，`IsCalculated=true` 时有意义）/ `DataType`；
  `BusinessEntityDimension` 新增 `Expression`（可分组字段或时间粒度表达式）/ `DataType`；
  配置 `Expression nvarchar(1000)` / `DataType nvarchar(50)`（均可空）。
- 仓储投影：`BusinessMetricView` / `BusinessDimensionView` 补传 `Expression` / `DataType`（供前端弹窗预填）。
- Service（`IBusinessEntityService`，新增 2 方法）：`UpsertMetricsAsync(tenantId, entityId, metrics)` /
  `UpsertDimensionsAsync(tenantId, domainId, dimensions)` —— 按 `Name`（忽略大小写）**全量合并**：
  命中则更新标量字段并**保留指标物理绑定**，未命中新增，传入列表外的存量项删除（级联清物理绑定）。
  租户隔离双重保证：Controller `TenantDataPlanePolicy` + Service 校验父级（实体/业务域）归属，缺失抛
  `KeyNotFoundException` → 404。
- 端点：`PUT api/business-model/entities/{id}/metrics`、`PUT api/business-model/domains/{domainId}/dimensions`
  （400 空列表 / Name 必填；403 跨租户；404 实体或域不存在或不属于当前租户；204 成功）。
- 前端（零第三方库）：`MetricCenter.razor` 指标/维度行操作加「编辑」按钮 + 两个编辑弹窗
  （指标：Name/DisplayName/SemanticType/Aggregation/IsCalculated 开关/Expression/DataType；
  维度：Name/Description/Expression/DataType）；编辑时按所属实体/业务域整组替换提交。
  **「详情」（导航）保持在行操作首位**，编辑为次操作。
- i18n：新增 **5** 键（`MetricCenterColExpression`/`MetricCenterColDataType`/`MetricCenterNameRequired`/
  `MetricCenterDimNameRequired`/`MetricCenterSaveFailed`）并改写 `MetricCenterEditHint`（去掉「编辑将随后续里程碑开放」），四处严格一致。
- 迁移：`20260911231209_M12_18_MetricDimensionExpression`（4 个 `AddColumn`，**纯 additive**，
  快照 diff 精确 16 行）；**开发库 43→44→45 已应用，pending 0**；`scripts/schema/schema-version.json`
  44→**45**（程序集 ↔ 清单集合差 0）。
- 验证：全量单测 **1140/1140 通过，0 失败、0 跳过**（基线 1132 + 3 `BusinessEntityServiceTests` + 5 `BusinessModelControllerTests`）；
  另修复 4 个 `MetricCenterUiTests`（bUnit 需注册新注入的 `ToastService`）与 2 个 404 用例
  （原误传 `tenantId≠认证租户` → 被数据面拦成 403；改为「认证租户=调用方、目标属他租户」）。
  Api/Components/Web 三工程 build 0 error（MAUI 因沙箱 `path1` 资产校验限制未纳入，本增量未改 MAUI）。
- **历史提交证据**：`80f34cd`（特性）+ `69db2fc`（记忆）。
- **历史 CI 证据**：push run **34658011942**（当时提交 `69db2fc`）：✅ 编译检查 success（三端 build 0 error + 新键过
  `ResourceKeyRegistryTests`）；✅ Web/Blazor E2E (Playwright) success；❌ V2.6 Evaluation Controller Runtime Smoke
  failure —— **仅**步骤 20「Controller C.13.3 Multi-Metric Evaluation」(GQ-007 `SemanticApplicabilityGate` BLOCK)，
  属用户「选 B」登记的独立既有故障，与本增量无关。本增量在自身范围内 CI 全绿。
- 范围边界（诚实）：口径表达式**仅登记不执行**（系统不在治理层求值，供生成层消费）；
  治理层数据在演示租户仍多为空集，编辑能力就绪待数据填充；仅 bUnit 渲染级 + 本地构建验证，未做浏览器交互 E2E。
