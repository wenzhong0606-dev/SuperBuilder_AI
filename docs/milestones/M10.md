# SuperBuilder AI — 里程碑 M10（自 Master_Development_Plan.md §14 抽出）
> 本文件是 `Master_Development_Plan.md` 的拆分详情。**状态以 `Master_Development_Plan.md`（唯一事实来源）为准**；本文件仅承载 M10 的展开说明，不应被当作独立执行计划。

> **⚠️ 范围说明（2026-09-11 校正）**：MDP §3 总表 M10 的「主要交付」列历史上写作「自定义组件、多数据库、多模型、外部身份」，易被误读为 M10 待办含自定义组件。实际：
> - **自定义组件库已于 M7-09 交付**（`Domain/Components/CustomComponentDefinition.cs` + `CustomComponentVersion` + `CustomComponentDslSerializer` + `ComponentsController` @ `api/components` + 迁移 `20260907053931_M7_09_CustomComponents` + `tests/CustomComponentsTests.cs`，前端 `ComponentGallery.razor` 接真实编辑器）。
> - **自定义风格/主题已于 M7-10 交付**（主题在 App / Dashboard / 主题资产间可授权复用，`AppDsl.ThemeKey` 引用打通）。
> - 故 M10 只剩下方 01~03 三项。**唯一残留关联缺口**：App DSL 的 `AppComponentTypes` 为 6 类硬编码白名单、无 `CustomComponentRef`，自定义组件尚不能被 App 引用 —— 归 **App DSL 演进**（M12 增量项），不属 M10。

---

- 建立 IDataSourceConnector：连接测试、列举 Catalog/Schema/Table/Column、查询执行、类型归一化。
- DataSourceConnectionFactory 使用注册式连接器，不在业务服务散落 switch。
- 每种数据库实现 Dialect、参数化、分页、标识符转义、超时和确定性离线测试。
- 禁止硬编码业务表和字段。

### M10-02 多 AI 模型 BYO（P13）

- 建立 ILLMProvider、ModelCatalog、TenantModelBinding、UserModelBinding。
- API Key 使用 AEAD 或密钥引用，日志/审计脱敏。
- 定义模型能力、上下文、成本、区域、状态、降级；用户选择不得越过租户政策。

### M10-03 外部身份提供方

- 本地账号生命周期稳定后评估 OIDC/SAML/企业目录。
- 外部身份仍必须经过 Tenant Membership、Role、Permission、DataSourceGrant。

---

