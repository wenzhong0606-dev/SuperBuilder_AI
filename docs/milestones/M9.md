# SuperBuilder AI — 里程碑 M9（自 Master_Development_Plan.md §13 抽出）
> 本文件是 `Master_Development_Plan.md` 的拆分详情。**状态以 `Master_Development_Plan.md`（唯一事实来源）为准**；本文件仅承载 M9 的展开说明，不应被当作独立执行计划。


| ID | 原编号 | 任务 | 验收 |
|---|---|---|---|
| M9-01 | SB-P2-01 | 拆分 God ApiClient | BI/Dashboard/App/Agent/Identity/Admin 可独立测试 |
| M9-02 | SB-P2-02 | 统一 Namespace 与目录边界 | 依赖方向检查通过 |
| M9-03 | SB-P2-03 | BusinessTerm 强类型化 | 编译期约束替代自由字符串 |
| M9-04 | SB-P2-04 | Production/Internal Diagnostics 分区 | 生产默认不暴露内部诊断 |
| M9-05 | SB-P2-05 | 扩展 Plan/LLM/DB latency、Repair/Reject rate | 可观测且无高基数泄漏 |
| M9-06 | SB-P2-06 | 统一前后端错误码 | UI 不依赖错误字符串判断 |
| M9-07 | SB-P2-07 | Dev/Test/Prod 配置校验 | 生产缺配置 fail-fast 且不泄密 |
| M9-08 | SB-P2-08 | Web/Blazor 自动化 | 关键流程进入 CI |
| M9-09 | SB-P2-09 | Dashboard DSL 版本兼容 | 旧 DSL 可加载、升级、回滚 |
| M9-10 | SB-P2-10 | App/Agent DSL 版本兼容 | 旧版本可升级和回滚 |
| M9-11 | A5 | 限界上下文解耦、SharedKernel、重复 DTO/枚举合并 | 无 Metadata↔Organization 循环 |
| M9-12 | A3 | 拆 Domain/Application/Infrastructure/Api 四项目 | 每步 build+Golden 通过 |
| M9-13 | 新增 | OpenAPI/Swagger 与契约生成 | 公开 API 有鉴权、DTO、错误说明 |
| M9-14 | 新增 | 备份恢复和灾备演练 | RPO/RTO、步骤和演练证据明确 |
| M9-15 | SB-P1-12 | Migration/Seed/SchemaVersion/升级回退体系 | 新旧环境可重复验证 |

> **交付 M9-06**（2026-09-09；统一前后端错误码·务实子集，feat，未推送 origin）：
> - **范围确认（与用户对齐）**：M9-06 原验收为「UI 不依赖错误字符串判断」。经确认采用务实子集：① 所有 ApiClient 调用点返回 `code`（与 `BiApiClient` 既有 `Code` 透传对齐）；② 新增前端 `ErrorCodes` 常量类镜像后端 `ErrorCodes` + `ErrorDecisions`；③ 仅在 UI 真正需要分支时按 `code` 判断，不强制全量改造现有 `error` 字符串展示。
> - **现状盘点**：后端 `src/Api/Errors/ErrorCodes.cs` 已定义 `SB_*` 常量与 `ErrorDecisions`（PermissionDenied / DataSourceUnauthorized / PolicyBlocked / RequiresClarification）；前端门面 `IApiClient`/`ApiClient` 及聚焦客户端 `IIdentityApiClient`/`IAdminApiClient`/`IdentityApiClient`/`AdminApiClient` 的 5 个方法（LoginAsync / SwitchTenantAsync / RegisterSelfAsync / SetUserLanguageAsync / InstallDemoDataAsync）此前返回 2 元组 `(Result, Error)`，未透传 `Code`；UI 调用点以 `var (r, err)` 解构、仅展示 `err` 字符串。
> - **实施（常量类）**：新增 `SuperBuilder_AI.Components/Components/Constants/ErrorCodes.cs`（`SuperBuilder_AI.Components.Components.Constants` 命名空间），含全部 `SB_*` 常量（BadRequest / Unauthorized / Forbidden / NotFound / Conflict / Unsupported / Internal / ServiceUnavailable / TooManyRequests / AuthInvalidCredential / Bi* / App* / SB_PFM_* / SB_AUTHZ_* / SB_SECURITY_* / SB_COST_*）与嵌套 `Decision` 类（PermissionDenied / DataSourceUnauthorized / PolicyBlocked / RequiresClarification），与后端保持语义对齐。
> - **实施（透传）**：将 5 个方法签名由 2 元组升级为 3 元组 `(Result, Error, Code)`，沿调用链贯通——聚焦接口（`IIdentityApiClient`/`IAdminApiClient`）、实现（`IdentityApiClient`/`AdminApiClient` 经 `ParseApiError` 取 `code` 透传）、门面（`IApiClient`/`ApiClient` 委托透传）；5 处 Razor 调用点（Login / PlatformLogin / SelfRegistration / DemoData / TenantSwitcher）解构补 `, _` 吞掉第三元素，行为零变化。
> - **验收（构建+测试）**：RCL 多目标（net10.0 / android / ios / windows / maccatalyst）0 错误；`SuperBuilder_AI.Tests` 工程 0 错误（2 个既有警告与本次无关）；相关单测（FocusedApiClientTests / ApiClientBaseAndFacadeTests / AuthStoreTests）43/43 全绿；既有 39 调用点零改动、零回归。
> - **红线/诚实声明**：仅做 `code` 透传与常量类落地，未改动任何 UI 文案或分支逻辑；`ParseApiError` 解析多形态错误体（ApiError / `{error}` / `{errors:[]}` / 纯文本）行为不变；本次变更纯前端 RCL + 单测，未触碰后端、未触碰运行实例。
> - **范围与后续**：3 元组的第三元素（Code）当前被全部调用点 discard，纯为后续按 `code` 分支（如 `SB_AUTHZ_*` 跳转权限申请、限流码提示重试）预留能力，无需再改签名即可接入；完整 `Error.{code}` → L10n 映射层与全量调用点分支梳理为 M9-06 后续增强，不在本提交范围。

> **交付 M9-07**（2026-09-09；Dev/Test/Prod 配置校验统一化，fail-fast 且不泄密，未推送 origin）：
> - **范围确认（与用户对齐）**：采用「务实全集」。既有 `Auth:SigningKey`（`AuthSigningKeyPolicy`）、`Cors`（`CorsOriginPolicy.Resolve`）、`RateLimit:Redis`、`SecretStore:MasterKey` 四处已分别 fail-fast，但 `ConnectionStrings` / `Qwen:ApiKey` / `Embedding:ApiKey` / `Qdrant` 四类关键配置**此前无启动校验**，生产若残留 `__REQUIRED__` / `__SET_VIA_ENV__` 占位符会带不安全默认值启动。本项建统一框架集中覆盖该缺口。
> - **实施（模型）**：新增 `src/Application/Common/Configuration/ConfigValidationReport.cs`（`SuperBuilder_AI.Application.Common.Configuration`，Application 层纯逻辑、不反向依赖 Api/Middleware，满足架构门禁）。含 `ConfigSeverity`（Error/Warning）、`ConfigIssue(Key,Message,Severity)`（仅携带键路径与原因）、`ConfigValidationReport`（Errors/Warnings/HasErrors 派生）。
> - **实施（校验器）**：`StartupConfigurationValidator.Validate(IConfiguration, IHostEnvironment)` —— 严格模式（非 Development 且非 Test，即 Production/Staging/自定义）下，主库连接串（`DefaultConnection`/`SuperBI`）、`Qwen:ApiKey`、`Embedding:ApiKey`、`Qdrant:Host` 缺失或为未替换占位符 → `Error`；`AllowedHosts:"*"` 在生产 → `Warning`。开发/测试环境同类问题 → 降级 `Warning`，不阻断启动。占位符哨兵（`__REQUIRED__` / `__SET_VIA_ENV` / `_OR_appsettings.Local.json__`）仅用于识别未替换，绝不出现在消息中。
> - **实施（接入）**：`Program.cs` 在 `builder.Build()` 前调用校验器；`Warning` 经 `Console.Error` 输出（仅含键路径），`HasErrors` 时抛 `InvalidOperationException`（消息仅含键路径与原因，绝不输出配置值/密钥）拒绝启动；风格与既有 fail-fast 抛错一致。
> - **验收（构建+测试）**：后端 0 错误；新增 `StartupConfigurationValidatorTests`（9 例：生产缺主库/LLM 密钥/向量库端点均 fail-fast、全合法零错误、开发/测试环境降级告警、AllowedHosts 通配仅告警、占位符与真实密钥值绝不回显）；全量单测 **1051/1051 全绿**（含架构门禁通过），Golden 契约 18/18 未改。
> - **红线/诚实声明**：未触碰前端、未触碰运行实例；仅新增 Application 层纯逻辑 + Program.cs 接入，既有值返回型校验（SigningKey/Cors/RateLimit:Redis/SecretStore）维持原样未改动；`IHostEnvironment.IsDevelopment()` 判定沿用现有范式，Test 环境显式等同 Development 降级。
> - **范围与后续**：统一框架已就位，后续新增必需配置只需在 `Validate` 内加一条 `Check*` 规则即可获得环境感知 + 不泄密保障；M9-08 Web/Blazor 自动化为下一项。

> **交付 M9-08**（2026-09-09；Web/Blazor 自动化接入 CI，未推送 origin）：
> - **现状盘点**：M8-06 已建 E2E 工程 `tests/SuperBuilder_AI.E2E.Tests`（Playwright + xunit + Xunit.SkippableFact，README 明定「CI 接入归 M9-08」）；但既有 CI `dotnet-build.yml` 仅起 API 打 evaluation 端点，**从未跑 Playwright E2E**；且 `PlaywrightFixture` 在 `SB_E2E_BASE_URL` 已设但 Chromium 缺失时 `Chromium.LaunchAsync()` 无兜底 → 整组报红（M7-11 契约记载缺口）。
> - **实施（CI 接入）**：`dotnet-build.yml` 新增 `e2e` 作业——复用既有 SQL Server + Qdrant 容器与迁移，额外起 **Web(Blazor,5080)** + API(5050)（`ApiBaseUrl=http://127.0.0.1:5050`），装 `Microsoft.Playwright.CLI` + `playwright install --with-deps chromium`，跑 `dotnet test` E2E 工程；凭据经 `secrets.SB_E2E_*` 注入，缺失时认证类用例（`E2EConfig.Require`）诚实跳过，无认证流（未登录重定向 / 登录页 axe / 四断点视觉基线）照常执行；视觉基线产物上传 artifact。
> - **实施（夹具健壮性）**：`PlaywrightFixture.InitializeAsync` 对 `Playwright.CreateAsync`/`Chromium.LaunchAsync` 包 try/catch，浏览器不可用（未装 / 缺系统依赖 / 驱动不匹配）→ 降级 `_skip` 跳过并附 `_skipReason` 诊断，封堵 M7-11 缺口。
> - **验收（构建+测试）**：E2E 工程 0 错误 0 警告；本环境（无 `SB_E2E_BASE_URL`）`dotnet test` 10/10 干净跳过、exit 0；全量单测 **1051/1051 全绿**，0 回归；CI YAML 结构与既有 `controller-runtime` 作业一致。
> - **红线/诚实声明**：CI 实际浏览器执行需运行实例 + Chromium + demo 凭据（沙箱不具备）；本交付仅完成**接线与健壮性封堵**；数据源授权/元数据/Ask/CRUD 等端到端用例按 M8-06 计划「后续补齐」，不在本提交范围。
> - **交付后真实浏览器实跑（2026-09-09，用户追问"不需要跑一下吗"）**：沙箱本地具备 SQL Server(`SuperBuilder_Platform`,`live/root`)+qdrant(已启用)+Chromium(已装) 可顶替 docker。同进程树内起 API(5050)+Web(5080)、清代理、绑 127.0.0.1、`SB_E2E_BASE_URL=http://127.0.0.1:5080` 实跑 → **2 通过 / 7 跳过 / 1 失败**。通过：`VisualBaselineTests`(真实 Chromium 截 4 视口 `/login` PNG，产物已生成)+`Invalid_Login_ShowsError`；跳过：7 例(缺 axe.min.js / 缺 `SB_E2E_*` 凭据)诚实 Skip；失败 1 例：`Unauthenticated_Access_Protected_RedirectsToLogin` 超时——根因回查 `AuthGuard.razor`/`MainLayout.razor` 确认为**客户端路由跳转**(`Nav.NavigateTo("/login")`，需 `SessionRestored&&!IsAuthenticated`)，无头环境该跳转未触发（页面停在 /ask，web.log 无报错），属**应用/环境行为，非 M9-08 接线缺陷**；测试等待策略已顺带修正为 `WaitForFunction(location endsWith /login)`（SPA aware），仍暴露该应用层问题。结论：M9-08 的 CI 接线与 `PlaywrightFixture` 健壮性封堵**已被真实浏览器验证有效**；那 1 失败建议作为独立应用缺陷跟进。
> - **范围与后续**：关键流程 E2E 已接入 CI 并可诚实跳过；下一步 M9-09 Dashboard DSL 版本兼容。

> **交付 M9-09**（2026-09-09；Dashboard DSL 版本兼容——旧 DSL 可加载 / 升级 / 回滚，未推送 origin）：
> - **现状盘点（回查源码）**：`DashboardController` 的 `Publish`/`Rollback`/`Versions` 已实现**回滚**——`Publish` 固化 `PublishedDslJson`、自增 `PublishedVersion` 并写不可变 `DashboardVersion` 快照（`RolledBackFromVersion` 记来源）；`Rollback` 将历史快照恢复为新版本（`DashboardControllerVersioningTests.Rollback_Restores_Previous_Version_And_Creates_NewVersion` 已覆盖）。故回滚**已完成**，M9-09 的真正缺口是「**旧 DSL 可加载 / 升级**」——`DashboardDslSerializer` 此前仅有 `Validate` 的白名单拒绝未知版本（第 99-100 行），**无 in-place 升级 / 归一化**，旧版文档不会在读时归一化为当前版本。
> - **实施（`DashboardDslSerializer.TryDeserialize`）**：在反序列化链路中于校验前插入 `Upgrade(dsl)` 步骤，顺序为「① 受支持版本白名单拦截未知版本（拒绝，与 `Validate` 一致）→ ② `Upgrade` 归一化为 <see cref="DslVersions.Current"/> → ③ 对归一化结果跑完整 `Validate`（先校验后信任）→ ④ 返回」。其中 `Upgrade` 以 `switch (dsl.Version)` 分流：当前仅 `DslVersions.V1` 分支调用 `NormalizeV1`，并**显式不做「版本==Current 即跳过」的早返回**（因 V1==Current，对所有受支持版本——含当前——都应执行结构归一化，保证内存模型完整）。`NormalizeV1` 做一项向前兼容的默认值物化：将缺失（<c>null</c>）的页面布局补全为默认 `LayoutKinds.Grid`（12 栅格），使渲染器无需再判空；未来新增 V2 时在此 `switch` 注册 `UpgradeFromV1` 等转换（字段重命名 / 默认值补全 / 结构迁移），并同步把新版本加入 `DslVersions.Supported`。
> - **验收（构建+测试）**：后端 0 错误；`DashboardDslSerializerTests` 新增 3 例——`TryDeserialize_SupportedV1Version_LoadsAndNormalizesToCurrent`（历史 V1 反序列化后 `Version==Current`）、`TryDeserialize_UnsupportedVersion_IsRejected`（反序列化边界拦截未知版本的回归守护）、`TryDeserialize_NullPageLayout_IsNormalizedToDefault`（缺省布局被物化为默认 Grid 12 栅格）；因 `DashboardController.Publish` 会解析并重新序列化（落库归一化形态），`DashboardControllerVersioningTests.SampleDslJson` 夹具补 `Layout = new LayoutDsl()` 以对齐；全量单测 **1055/1055 全绿**（较基线 +3 新增 / +20 净增，0 回归），Golden 契约 18/18 未改。
> - **红线/诚实声明**：未触碰前端、未触碰运行实例、未改动回滚既有实现；行为影响仅限「加载时内存模型与落库 JSON 中页面布局从 `null` 变为默认 Grid 12 栅格」（渲染器对两者输出一致，已回查 `DashboardLowcodeRenderer.RenderPageAsync` 确认默认值相同）；架构门禁（Application 不反向依赖 Api/Middleware）经 `ArchitectureTests` 仍通过。
> - **范围与后续**：M9-09 交付「可加载 + 升级 + 回滚」完整闭环；App/Agent DSL 版本兼容（M9-10）为独立项，DSL 升级框架可复用本模式。

> **交付 M9-10**（2026-09-09；App / Agent DSL 版本兼容——旧 DSL 可加载 / 升级，未推送 origin）：
> - **现状盘点（回查源码）**：与 M9-09 同源，App/Agent 两个序列化器（`AppDslSerializer` / `AgentDslSerializer`）此前均**仅有 `Validate` 的白名单拒绝未知版本，无 in-place 升级 / 归一化**；运行时取数路径确认均经 `TryDeserialize`（`AppBuilderController` Create/Update/Import/Copy/Render + `AppBuilderAgent`；`AgentPlanner` + `AgentRuntime`×2），故归一化在加载/导入/运行期生效。两端回滚现状：App 回滚**已完成**——`AppVersion` 不可变快照 + `AppBuilderController.Publish/Rollback/Versions`（M7-02，测试 `AppBuilderControllerVersioningTests` 全绿）；Agent **无回滚体系**（`AgentPlan` 无 `PublishedVersion/PublishedDslJson`、无 `AgentVersion` 实体、`AgentController` 无 Publish/Rollback/Versions）。
> - **实施（复用 M9-09 框架）**：在 `AppDslSerializer.TryDeserialize` 与 `AgentDslSerializer.TryDeserialize` 的反序列化链路中、校验前插入 `Upgrade(dsl)`，顺序同 Dashboard——「① 受支持版本白名单拦截未知版本 → ② `Upgrade` 归一化为 `*DslVersions.Current` → ③ 对归一化结果跑完整 `Validate` → ④ 返回」；`Upgrade` 以 `switch(dsl.Version)` 分流，`NormalizeV1` 后统一 `dsl.Version = Current`，**显式不做「版本==Current 即跳过」的早返回**（V1==Current，须对所有受支持版本执行结构归一化）。`AppDslSerializer.NormalizeV1` 做真实前向兼容物化：缺省（`null`）的 `PagePlan.Layout` 补全为默认 `AppLayoutKinds.Grid`（12 栅格）——渲染器对 `null` 与默认值输出一致（字段注释明示「为 null 时采用 Grid 默认值」），行为零变化；`AgentDslSerializer.NormalizeV1` 为 V1==Current 且无等价可空结构字段下的**结构就绪占位**（V2 演进预留迁移点，不修改任何字段，保证旧文档无损升级）。
> - **验收（构建+测试）**：后端 0 错误；`AppDslSerializerTests` 新增 2 例（含 `TryDeserialize_NullPageLayout_IsNormalizedToDefault` 验证 null 布局物化为 Grid 12）、`AgentDslSerializerTests` 新增 1 例（`TryDeserialize_SupportedV1Version_LoadsAndNormalizesToCurrent`）；因 `AppBuilderController.Publish` **直接复制原始 `DslJson`、不经序列化器重序列化**（与 Dashboard 不同），`AppBuilderControllerVersioningTests` 精确 JSON 比较用例不受影响——已实跑 49/49 全绿佐证；全量单测 **1058/1058 全绿**（较 M9-09 基线 1055 +3 新增，0 回归），Golden 契约 18/18 未改，架构门禁 `ArchitectureTests` 通过。
> - **红线/诚实声明**：未触碰前端、未改动 App 既有回滚实现、未构建 Agent 回滚子系统（超出「DSL 升级框架」范围）；行为影响仅限 App「加载时内存模型页面布局从 `null` 变为默认 Grid 12 栅格」（输出零变化）。**待跟进**：Agent 回滚需另立「Agent 版本子系统」子项（`AgentVersion` 实体 + 迁移 + `AgentController.Publish/Rollback/Versions` + 测试），不在 M9-10 内。
> - **范围与后续**：M9-10 交付 App/Agent「可加载 + 升级」框架（App 回滚已具备）；下一里程碑 M9-11~M9-15（OpenAPI / 备份灾备 / Migration 体系）未启动。

> **交付 M9-11**（2026-09-09；限界上下文解耦——消除 Metadata↔Organization 循环依赖，未推送 origin）：
> - **现状盘点（回查源码，确认环的精确结构）**：`ArchitectureTests` 反射校验仅覆盖层间方向（Domain/Application/Api/Middleware），此前无「限界上下文间循环」不变量。交叉引用 grep 证实循环发生在 **Domain 层两上下文之间**：
>   - `Models.Metadata`（`DataSource.cs` / `MetadataLearningRecord.cs`）→ 引用 `Models.Organization.Tenant`（FK 导航属性 `Tenant` + `TenantId`）；
>   - `Models.Organization`（`Tenant.cs`）→ 引用 `Models.Metadata.DataSource`（`ICollection<DataSource> DataSources` 集合导航）；
>   - 即 `Tenant.DataSources` 是 Organization→Metadata 的**唯一边**，构成 `Metadata ↔ Organization` 双向循环。
>   - 影响面核查：`Tenant.DataSources` 定义仅一处（`Domain/Organization/Tenant.cs:46`）；应用层**无任何** `tenant.DataSources` 属性读取或 `Include(t => t.DataSources)`（全部 `.DataSources` 引用均为 `context.DataSources` DbSet 访问，与实体导航无关）；`SuperBIContext.cs:240` 经 Fluent `HasOne(...).WithMany(x => x.DataSources)` 配置关系。**移除导航零业务影响**。
> - **实施（最小安全解，仅改架构、不改业务语义）**：
>   - `Domain/Organization/Tenant.cs` 移除 `using SuperBuilder_AI.Models.Metadata;` 与 `ICollection<DataSource> DataSources` 集合导航属性（环的唯一来源）。
>   - `SuperBIContext.cs:240` Fluent 配置 `WithMany(x => x.DataSources)` → `WithMany()`，关系改由从属侧 `DataSource.Tenant` + `TenantId` 定义，**数据库结构（FK/索引/级联）完全不变**。
>   - `ArchitectureTests` 新增不变量 `Organization_Should_Not_Depend_On_Metadata`（`FindViolations("SuperBuilder_AI.Models.Organization", "SuperBuilder_AI.Models.Metadata")` 须为空），锁定验收「无 Metadata↔Organization 循环」；Metadata→Organization 方向（FK 导航）保持合法。
> - **验收（构建+测试）**：后端 0 错误；`ArchitectureTests` 13/13 绿（含新增不变量）；全量单测 **1059/1059 绿**（基线 1058 → 1059，+1 新增架构不变量，0 回归）；Golden 契约 18/18 未改；EF 模型正常构建（含依赖 SuperBIContext 的测试）。
> - **红线/诚实声明**：未触碰前端、未触碰运行实例、未改动数据库 Schema；仅移除**未使用**的集合导航（行为零变化）。**已知债务（不在本提交范围，归 M9-15 Migration 体系）**：移除导航使 EF 模型与既有迁移快照 `SuperBIContextModelSnapshot.cs` / 最新 `*.Designer.cs` 中 `.WithMany("DataSources")` + `b.Navigation("DataSources")` 不一致——测试套件无「模型 vs 迁移快照」一致性校验故不影响构建/测试，但后续 `dotnet ef migrations add` 会生成空迁移，将在 M9-15 统一对账。
> - **范围与后续**：M9-11 验收（消除循环）已达成，聚焦的是「环」而非「SharedKernel / 重复 DTO·枚举合并」——后者为更大范围的独立审计（全仓 DTO/枚举重复扫描），建议另立子项，不在本提交内。下一里程碑 **M9-12（拆 Domain/Application/Infrastructure/Api 四项目，A3）** 或 **M9-13（OpenAPI/Swagger）** 待推进。

> **交付 M9-13**（2026-09-09；OpenAPI/Swagger 契约生成——公开 API 鉴权 / DTO / 错误说明，未推送 origin）：
> - **范围确认（与用户对齐）**：M9-12（拆四项目，A3 高风险重构）按用户决策**跳过**，转进 M9-13。验收口径「公开 API 有鉴权、DTO、错误说明」三要素：① 契约声明统一 Bearer 鉴权方案（与 `AuthMiddleware` 解析 `Authorization: Bearer` / `X-Api-Token` 对齐）；② DTO（请求/响应模型）由 XML 注释进入契约文档；③ 统一错误响应说明（401/403/422 + `ApiError` 错误体，对齐 `ErrorCodes`）。
> - **实施（包与文档化）**：`SuperBuilder_AI.csproj` 新增 `Microsoft.AspNetCore.OpenApi` 10.0.10 `PackageReference`（本地 NuGet 缓存离线可用，规避沙箱代理联网风险）；开启 `GenerateDocumentationFile` + `NoWarn;1591`，使 DTO/Operation 的 XML 注释进入契约。
> - **实施（接线）**：`Program.cs` 在 `AddControllersWithViews` 之后注册 `builder.Services.AddOpenApi("v1", options => { options.AddDocumentTransformer<OpenApiSecurityTransformer>(); options.AddDocumentTransformer<OpenApiErrorResponsesTransformer>(); })`，并在路由映射后 `app.MapOpenApi("/openapi/v1.json")`。
> - **实施（鉴权声明）**：新增 `src/Api/OpenApi/OpenApiSecurityTransformer.cs`（`IOpenApiDocumentTransformer`）——注册 `bearerAuth`（Type=Http, Scheme=bearer, BearerFormat=JWT）安全方案，并为**全部 operation** 追加全局安全要求；描述显式说明令牌由 `Authorization: Bearer <token>` 或 `X-Api-Token` 提供、`AuthMiddleware` 解析后注入身份与租户上下文，非标准 ASP.NET 认证方案。
> - **实施（错误说明）**：新增 `src/Api/OpenApi/OpenApiErrorResponsesTransformer.cs`——为每个 operation 补 `401`（未认证：SB_AUTH_001 等）/ `403`（未授权：SB_AUTHZ_002、PolicyBlocked 等）/ `422`（语义校验失败：SB_APP_DSL_INVALID、SB_BI_* 等）响应，并注册 `ApiError` schema（字段 `code` / `message` / `traceId` / `decision` / `errors`，其中 `errors` 为字段级校验明细 `field`/`code`/`message`），与 `UnifiedExceptionMiddleware` 统一错误体对齐。
> - **验收（构建+实例）**：**干净重建 0 错误**（增量构建曾掩盖 `SecuritySchemes` 类型不匹配的编译错误——应为 `IDictionary<string,IOpenApiSecurityScheme>` 而非 `Dictionary<string,OpenApiSecurityScheme>`，干净重建后暴露并修复，此前端点 500 NRE 即源于此陈旧二进制）；启动实例（`Development` 环境，5111 端口）`GET /openapi/v1.json` → **HTTP 200 / 497KB**；结构化校验：openapi 3.1.1、162 paths / 200 operations **全部**带 `security` + 401/403/422、`ApiError` schema 已注册。导出契约快照 `docs/api/openapi-v1.json` 与可读查看页 `docs/api/openapi-v1.html`（Redoc 内嵌 spec，离线可用）。
> - **验收（测试）**：全量单测基线 **1059/1059 绿**（本次纯新增 OpenApi 命名空间 + Program.cs 两处接线，未改既有行为，0 回归）；Golden 契约 18/18 未改；端点纯反射元数据、不依赖 DB（`SchemaProbe` 失败仅日志，不阻断启动），对既有测试零影响。
> - **红线/诚实声明**：未触碰前端、未触碰既有业务逻辑、未触碰运行实例既有行为；仅新增两个 `IOpenApiDocumentTransformer` + `Program.cs` 两行接线 + csproj 包引用与 XML 文档开关；Swagger UI 未启用（无 wwwroot，契约以 JSON + Redoc 查看页承载），不影响生产（端点受 `AuthMiddleware` 与 `[ApiAuthorizationFilter]` 守卫，仅 Development 经 `Cors:AllowAnyOrigin` 可跨域访问）。
> - **范围与后续**：M9-13 交付「公开 API 鉴权 + DTO 文档 + 错误说明」契约闭环；**M9-14 已交付（见下）**；M9-15（Migration/Seed/SchemaVersion/升级回退体系）未启动。

> **交付 M9-14**（2026-09-09；备份恢复与灾备演练——RPO/RTO、步骤、演练证据明确，未推送 origin）：
> - **范围确认（与用户对齐）**：M9-14 验收口径为「RPO/RTO 目标明确、步骤明确、演练证据明确」。交付以**文档 + 可复用脚本 + 沙箱内可执行的真实证据**为主，非生产环境代码改动（与平台运行态零耦合）。
> - **组件清单**：关系库（`SuperBIContext`，多引擎 SQL Server/MySQL/PostgreSQL，42 个 EF 迁移）、Qdrant 向量库（集合 `superbi_metadata`，1024 维）、配置/密钥（`appsettings*.json` + 环境变量，**占位符无明文**）、种子/语义 CSV、迁移程序集。
> - **RPO/RTO 目标（建议值，待平台 owner 裁定）**：关系库 RPO 15min / RTO 30min；Qdrant RPO 4h / RTO 1h（可由元数据重嵌入兜底）；配置/密钥 RPO 0 / RTO 15min；种子/语义 RPO 24h / RTO 15min。明确标注为推荐目标，非 SLA 承诺。
> - **脚本集（6 个，UTF-8 BOM，PowerShell 5.1 解析全过）**：`scripts/dr-backup/backup-db.ps1`（多引擎自动识别：sqlpackage BACPAC / mysqldump / pg_dump，产出 manifest+SHA256）、`restore-db.ps1`（配对还原）、`backup-qdrant.ps1` / `restore-qdrant.ps1`（Qdrant REST 快照）、`verify-backup.ps1`（SHA256+大小一致性校验）、`backup-all.ps1`（关系库+Qdrant+配置归档编排）。
> - **恢复手册（§5）**：先架构（EF 迁移 / 幂等 `restore-schema-from-migrations.sql`）→ 再数据还原 → 再配置下发（密钥由密钥管理器，不从备份恢复明文）→ 一致性校验（迁移数=42、核心表行数、Qdrant vectors 一致）→ 切换流量。
> - **验收（沙箱真实证据）**：① `dotnet ef migrations script --idempotent` 生成 `scripts/dr-backup/restore-schema-from-migrations.sql`（367KB / 42 迁移全量建表）——"迁移即恢复"架构级演练证据；② 6 个 PS 脚本经 Windows PowerShell 5.1 `Parser` 解析 **6/6 PARSE_OK**（修复初版因 UTF-8 无 BOM+中文在 PS5.1 误报解析失败，统一改 UTF-8 BOM 后通过）；③ 环境探测确认沙箱**无可达 DB/Qdrant**（1433/5432/3306/6333/6334 无监听，仅 `sqlcmd` 在），端到端数据级演练为**环境门禁**（与 M7-11 P0-D1/D2 同类），已在 `docs/ops/drill-evidence-20260909.md` 诚实标注，未夸大。
> - **红线/诚实声明**：未触碰应用代码、未改运行实例；交付物为 `docs/ops/backup-restore-dr.md`（DR 计划）、`docs/ops/drill-evidence-20260909.md`（证据）、`scripts/dr-backup/*`（6 脚本 + 幂等恢复 SQL）；备份/归档**绝不**含明文密钥；全量演练须在隔离测试实例执行；端到端数据级演练因环境阻塞未执行，明确列为待办而非已完成。
> - **⚠️ 勘误（2026-09-10）**：上条「端到端数据级演练因环境阻塞未执行」中的**关系库部分结论不成立**。当时仅扫描 1433/5432/3306 标准端口未见监听即判定不可达，但本机 SQL Server 以**命名实例/共享内存**提供（`sqlservr.exe` 实际运行），`Server=localhost` 可正常连接（M9-15 已实证：实例成功读取 `__EFMigrationsHistory`、5 步种子全部成功）。**经用户指出并实测复核，「Qdrant 不可达」同样不成立**：Qdrant **1.19.0 实际可达**（`curl http://localhost:6333/` → HTTP 200，返回 `qdrant - vector search engine`），集合 `superbi_metadata` 含 **961 个点**（1024 维 Cosine）。**根因：本沙箱内 `ss`/`netstat` 无法看到宿主监听，端口扫描方法整体不可用**——判定服务可达性必须做真实连接实测。已同步在 `docs/ops/backup-restore-dr.md` §8 与 `docs/ops/drill-evidence-20260909.md` E3 加勘误块。

> **交付 M9-15**（2026-09-10；Migration / Seed / SchemaVersion / 升级回退体系——新旧环境可重复验证，未推送 origin）：
> - **范围确认（与用户对齐）**：对应 DevChecklist `SB-P1-12`，验收为「明确 EF Migration、Seed、Schema Version、升级与回退策略；新环境可自动初始化；旧环境升级可验证」。
> - **现状盘点（回查源码）**：EF 迁移 **42 个**（`CreateDB`→`20260909025001_M7_11_PublishIdempotency`）；**全仓无 SchemaVersion 概念**（仅 EF `__EFMigrationsHistory`）；启动自动迁移由 `Startup:MigrateOnStartup` 控制且**默认 false**（`Program.cs:481`）；`SchemaProbe` 区分 `DatabaseUnreachable`/`SchemaNotCreated`/`Ready` 三态；启动 **5 步种子固定顺序**（Identity/Permission → UiLanguage/Text → Quota+Theme → TenantLanguages → 管理员引导），逐步异常隔离并置 `SeedIncomplete`。
> - **Schema Version 策略**：定义 `schemaVersion` = 最新迁移 ID（当前 `20260909025001_M7_11_PublishIdempotency`）；新增权威清单 `scripts/schema/schema-version.json`（42 个有序迁移 ID）。升级判定：环境版本 < 清单即需升级，集合不一致即漂移。
> - **Seed 策略（幂等性源码核实）**：`LocalizationSeedService.EnsureSeedAsync` ✅ 先查已有 Culture 集合仅补缺失、文本键 `existingKeys.Contains` 跳过；`PlatformAdminBootstrapper.EnsureAsync` ✅ `GetStatusAsync` 先查 `UserRoles.AnyAsync`，已有平台管理员直接返回 false **不重复创建**。其余 3 步依注释声明幂等，**未逐一回查**（诚实标注）。
> - **升级/回退策略**：升级 = `dotnet ef database update`（或应用 M9-14 幂等 SQL）；回退 = `dotnet ef database update <target>`，并可用 `dotnet ef migrations script <from> <to>`（to 早于 from）**先生成降级脚本再审执**——已实产 `scripts/schema/rollback-one-step.sql`（事务包裹、含删表/删列/清理迁移历史）。明确标注回退限制：依赖迁移 `Down`、删列删表会丢数据、须先备份、代码须同步回退。
> - **可重复校验工具**：`scripts/schema/verify-schema.ps1`（离线/在线双模式，退出码 0=一致 / 2=漂移）。**离线正向**：42/42 一致 EXIT=0（真实执行）；**离线负向**：篡改清单去掉 1 个迁移 → 正确报 `DRIFT + 20260909025001_M7_11_PublishIdempotency` 且 EXIT=2（证明非橡皮图章）；**在线实证**：本机 SQL Server 可达，检出**真实漂移 5 个 Pending 迁移**（`M1_ClosureIntegrity`、`M7_01_DashboardVersion`、`M7_02_AppVersion`、`M7_03_AgentRuntime`、`M7_11_PublishIdempotency`）。
> - **安全修复（顺带，M9-13 遗留）**：构建期 NuGet 审计 `NU1903` 报 `Microsoft.OpenApi 2.0.0` **高危漏洞**（GHSA-v5pm-xwqc-g5wc），系 M9-13 新增 `Microsoft.AspNetCore.OpenApi 10.0.10` 的传递依赖。已显式提升 `Microsoft.OpenApi` 至 **2.7.5**（本地缓存离线可解析）；验证：构建 **0 错误且 NU1903 消失**、全量单测 **1059/1059 绿**、M9-13 契约端点复验与已提交契约**结构化完全一致**（3.1.1 / 162 paths / 200 ops / 全 security+401/403/422 / ApiError）。
> - **工程坑（已固化）**：① PowerShell 5.1 下原生命令 stderr（如 NuGet 告警）在 `$ErrorActionPreference='Stop'` 会被误判终止错误 → 需在调用处临时降级 `Continue`；② `Write-Host` 走 host 流**无法被赋值/重定向捕获**（PS5.1），校验工具改 `Write-Output`；③ 重复 BOM 编码会叠加 BOM 使 `<#` 不在首位 → 帮助块失效并引发级联解析错误，须先 `lstrip('\ufeff')` 再写单 BOM。
> - **红线/诚实声明**：未触碰应用业务逻辑、未执行任何迁移变更（**未擅自应用**检出的 5 个 Pending 迁移，避免改动开发库）；交付物为 `docs/ops/migration-seed-schemaversion.md`、`docs/ops/schema-verify-evidence-2026-09-10.md`、`scripts/schema/*`（清单+校验脚本+回退 SQL）+ csproj 一处安全包提升。
> - **Qdrant 复核（用户指出后实测）**：Qdrant **1.19.0 可达**（`curl :6333/` → 200），集合 `superbi_metadata` 含 **961 个点**；快照 API 经 **curl 与 PowerShell 双路径**验证均可创建（≈592MB，带 checksum），且**已全部清理**（`GET /snapshots` → `[]`）。`backup-qdrant.ps1` 的**下载步骤**因快照体积（≈592MB）在沙箱未跑通（属资源限制，非能力缺失），已在证据文档 E7 诚实标注并列为待办。**待用户确认**：是否对开发库应用 5 个 Pending 迁移以完成「旧环境升级可验证」闭环。

> **交付 M9-05**（2026-09-09；扩展管线 latency / Repair / Reject rate 可观测性，零 Golden 回归，未推送 origin）：
> - **范围确认（与用户对齐）**：采用「务实全集」。复用既有 `RequestMetricsCollector` 的有界内存 + 异常静默范式，将 BI 管线 5 段延迟（P95/avg/count/max）与结果分类计数（Reject / EarlyReturn / Repair）聚合进 `GET /metrics`，零新增高基数维度。
> - **现状盘点**：`RequestMetricsCollector`（`src/Api/Middleware`）已按路由聚合请求数/错误/P95/avg/max，且为 `Singleton`（`Program.cs:398`）；但 BI 管线 5 段耗时（`BIConversationService` 已用 `sw` 计时入 `BIResponse.SegmentTimings`）与 Reject/Repair 结果分类从未上报指标；`QueryPlanPipelineResult` 无 `WasRepaired`/`WasRejected` 标志；架构门禁 `Application_Should_Not_Depend_On_Api_Or_Middleware` 禁止 `Services.BI` 反向依赖 `Middleware`。
> - **实施（端口抽象）**：新增 `src/Application/Ports/BI/IPipelineMetricsSink.cs`（`SuperBuilder_AI.Interfaces.BI`，端口层），声明 `RecordStage(string,long)` / `RecordOutcome(string,bool)` 与固定分段/分类键常量（StageUnderstand/Plan/Sql/Db/Result、OutcomeReject/EarlyReturn/Repair）。`RequestMetricsCollector` 实现该接口（移除自身重复常量，复用既有 `RouteMetrics` 有界采样 + `__other__` 桶 + 异常静默）；新增 `StageSnapshot()`/`OutcomeSnapshot()` 快照方法，`Reset()` 同步清空。
> - **实施（线程字段）**：`QueryPlanPipelineResult` 新增纯观测字段 `WasRepaired`/`WasRejected`（默认 false，向后兼容，零 Golden 契约改动）；`QueryPlanPipeline.RunAsync` 在 Decision Gate 阻断时置 `WasRejected = decision.IsReject`，成功路径置 `WasRepaired = SemanticValidation.RepairTrace.TotalAttempts > 0`。
> - **实施（埋点）**：`BIConversationService` 构造注入 `IPipelineMetricsSink?`（可选，默认 null，DI 注册见下）；`ExecuteAsync` 在 5 段计时点调用 `RecordStage`，并在管线返回后调用 `RecordOutcome(Reject/EarlyReturn/Repair)`。
> - **实施（DI + 端点）**：`Program.cs` 将同一 `RequestMetricsCollector` 单例额外注册为 `IPipelineMetricsSink`（`GetRequiredService<RequestMetricsCollector>()`，保证中间件/端点/编排服务写入同一份内存）；`GET /metrics` 响应新增 `pipelineStages` 与 `outcomes` 两段，`catch` 分支同步补空数组。
> - **验收（构建+测试）**：后端 0 错误；新增 `RequestMetricsCollectorTests`（6 例：分段聚合/独立键/计数条件/累计/Reset/空键兜底）+ `QueryPlanStagesTests` 扩展 4 例（`WasRejected` 在 Reject 阻断时置位、`WasRepaired` 在 `RepairTrace.TotalAttempts>0` 时置位及反向用例）；全量单测 **1042/1042 全绿**（含架构门禁 `Application_Should_Not_Depend_On_Api_Or_Middleware` 通过——因经接口而非具体类依赖，零回归）；Golden 契约 18/18 未改。
> - **红线/诚实声明**：未触碰前端、未触碰运行实例；`WasRepaired`/`WasRejected` 为纯新增字段，所有既有调用方（含 `QueryPlanAiBiE2ETests` 的 `new BIConversationService(...)` 7 参构造）因可选参数默认 null 零改动；指标采集全链路异常静默，主链路语义未受影响。
> - **范围与后续**：管线分段延迟已从 `BIResponse.SegmentTimings`（单次响应）提升为进程级聚合指标；下一步 M9-07 配置校验。

> **交付 M9-04**（2026-09-09；诊断分区，零侵入）：
> - **现状盘点**：`src/Api/README.md` 声明诊断/测试/夹具控制器为「非生产代码，应限制路由前缀 `evaluation/*`、`test/*`」。回查 `Program.cs:58-62` 发现 M0-09 / SB-P0-09 已实现 `ProductionEvaluationRouteConvention`：非 Development 环境下，从 endpoint discovery **移除所有 `evaluation/*` 前缀控制器** → Production 返回 404（端点不存在）。但 `AITestController`（`[Route("test")]`）未被覆盖——其测试 `ProductionEvaluationRouteConventionTests` 明确断言 `IsEvaluationRoute("test")==false`，故 `test` 前缀控制器在 Production **仍暴露**。
> - **实施（缺口补齐）**：扩展 `ProductionEvaluationRouteConvention.IsEvaluationRoute` 纳入 `test` 与 `test/*` 前缀，与 `evaluation/*` 一致地在非 Development 环境从 endpoint discovery 移除；同步更新单测（`test` 由 DoesNotMatch 移入 Matches，新增 `test/foo` 用例）。
> - **验收（构建+测试）**：后端 0 错误；`ProductionEvaluationRouteConventionTests` 全绿（evaluation/* + test/* 均移除，api/* 保留）；全量单测保持绿。
> - **范围与后续**：仅补齐 `test` 前缀缺口，未改动既有 evaluation/* 移除逻辑、未引入新过滤器；Release 构建排除 / 移至 `tests/` 模块为 README 中记录的后续可选增强，本项不阻塞。M9-06 统一前后端错误码。

A5/A3 在功能和数据约束稳定后分步执行，不得在同一提交中同时改变架构和业务语义。

> **交付 M9-01**（2026-09-08；拆分 God ApiClient 为 7 个聚焦域客户端 + 向后兼容门面，双提交 feat+docs，未推送 origin）：
> - **现状盘点**：`SuperBuilder_AI.Components/Services/ApiClient.cs` 为 761 行单体「上帝类」，横跨 Identity/Admin/BI/App/Dashboard/Agent/DataSource 七域共 26 个 `IApiClient` 方法；39 个调用点全部经 `@inject IApiClient` 注入。验收要求「各域可独立测试」无法满足——改动任一域需重建整类且易牵连其他域。
> - **实施（拆分）**：新增 `Services/Clients/` 目录。抽象基类 `ApiClientBase` 持有命名 HttpClient 工厂 + `AppState`，统一 Bearer 头注入、`OnUnauthorized` 会话回收、`ParseApiError` 多形态错误体解析（ApiError / 旧 `{error}` / `{errors:[]}` / 纯文本）与通用读（GetAsync/GetJsonAsync/GetTextAsync）/写（SendAsync/Post/Put/Patch/Delete）原语。7 个聚焦客户端（Identity/Admin/BI/App/Dashboard/Agent/DataSource）各含接口 `I*ApiClient` + 实现 `*ApiClient : ApiClientBase`，仅补充本域方法；Dashboard/Agent 直接复用基类原语。
> - **实施（门面）**：`ApiClient` 重构为 `IApiClient` 向后兼容门面，构造注入 7 个聚焦接口，29 个 `IApiClient` 方法逐一对委托；旧 DTO（AuthResult/SelfRegistration*/Demo*/TenantSwitchResult/RefineTurn/ScanJobView）保留于 `ApiClient.cs`。39 个既有调用点零改动。
> - **实施（DI）**：两 Head（Web `Program.cs` / MAUI `MauiProgram.cs`）由单一 `AddScoped<IApiClient,ApiClient>()` 扩展为「7 个聚焦客户端注册 + 门面注册」。
> - **验收（构建+测试）**：RCL 多目标（net10.0 / android / ios / windows / maccatalyst）0 错误；新增聚焦客户端单测（隔离 HTTP 替身，不触达真实 API）覆盖七域行为 + 基类错误解析多形态 + 门面委派路由（URI 级证明）+ 401 会话回收跨域生效；既有单元零回归。
> - **红线/诚实声明**：门面委托纯透传，既有 39 调用点行为未变；新增测试与本环境运行实例无关（纯单元、替身隔离），未触碰后端。
> - **后续**：M9-02 Namespace 边界治理；聚焦客户端为按域替换/打桩奠定基础（M7-12 Agent 剩余 6 工具接真实后端可在 `IAgentApiClient` 增量扩方法，不影响其他域）。

> **交付 M9-02**（2026-09-06；统一 Namespace 与目录边界，双提交 feat+docs，未推送 origin）：
> - **现状盘点（BOM 容错重算）**：后端 `src/` 共 542 有效 .cs / 74 命名空间 / 71 目录，采用「目录按分层、命名空间按关注点」双轨（`Models`=Domain、`Services`=Application、`Controllers`=Api、`Interfaces`=Ports、`Data`/`Infrastructure`=Infra）。原有正则分析因 UTF-8 BOM 误报「全局命名空间」，重算后真实缺陷为：① `src.` 非法前缀（24 个迁移文件 `SuperBuilder_AI.src.Infrastructure.Persistence.Migrations`）；② `Api/Diagnostics` 错置 16 个 `Controllers` 文件；③ `Interfaces/BI` 2 个游离文件与 `Application/Ports/BI` 同名跨目录；④ 关注点命名空间内的游离文件（`Application.BiQuery`×1、`Application.Metadata`×2、`Configuration`×2、Domain 中 4 个误用 `Services.BI`、SharedKernel 1 个 `Models.BI`、`Infrastructure/Database` 3 个误用 `Services.Database`、`SuperBuilder_AI.Migrations`×16）；⑤ 跨目录同名空间碰撞 5 处（均由上述导致）。主流命名空间（`Services.BI` 99 引用、`Models.BI` 248、`Data` 135）保留不改 → 波及面小、git 可逆。
> - **实施（Tier1 明确缺陷）**：移除 `src.` 前缀（24→canonical）；`git mv` 16 个 Controller `Api/Diagnostics`→`Api/Controllers`（Diagnostics 仅余 `SchemaProbe`/`StartupDiagnostics` 两个 `Api.Diagnostics` 文件）；`git mv` 2 个 `Interfaces/BI` 游离文件→`Application/Ports/BI`。
> - **实施（Tier2 归一化）**：全局唯一游离命名空间全仓 `using`+`namespace`+限定名替换（`Application.BiQuery`→`Services.BI`、`Application.Metadata`→`Services`、`Configuration`→`Application.Common.Options`、`Services.Database`→`Infrastructure.Database`、`SuperBuilder_AI.Migrations` 16+快照 与 `src.` 24 共 41 → `Infrastructure.Persistence.Migrations`；目录实际 81 个迁移文件现全部统一）；`Services.BI`/`Models.BI` 因与保留命名空间共享，按文件路径精确编辑 `Domain/BiQuery` 4 文件→`Models.BI`、`SharedKernel` 1 文件→`Models`（避免误伤 69/46 个保留文件）；全仓 `using` 去重（消除重命名引入的重复指令）。
> - **实施（Tier3 防回归）**：`ArchitectureTests` 新增 4 个不变量——`NoNamespaceContainsIllegalSrcPrefix`、`AllMigrationsShareSingleNamespace`、`EverySourceFileHasSuperBuilderAiNamespaceExceptProgram`、`DomainLayerUsesModelsNamespace`；并保留原 4 条依赖方向校验（反射，基于关注点命名空间）。`Program.cs`/`SuperBIContext` 配置：迁移命名空间统一后 EF 经程序集扫描发现，未使用 EF 10 不可用的 `MigrationsNamespace` API。
> - **验收（构建+测试）**：后端 0 错误；RCL 多目标 0 错误；Web 0 警告 0 错误；MAUI Windows 0 错误（2 既有警告）；单元套件 999/999 全绿（基线 994 + 新增 4 架构不变量 + 1 既有），0 回归；依赖方向检查通过（M9-02 验收口径）。
> - **红线/诚实声明**：`SuperBuilder_AI.Data`（`SuperBIContext`）命名空间保留未改，其 `git status` 的 M 为既有 CRLF 行尾规范化伪改动，已按红线排除、未纳入提交；`nul` phantom 未跟踪、未提交；迁移命名空间统一仅改声明与 `using`，0 外部引用、EF 运行时经程序集扫描发现不受影响（本环境未实跑迁移应用，归部署流水线验证）；重命名为机械替换+全量构建门禁，git 可逆。
> - **覆盖范围与后续**：双轨命名空间约定已固化，跨目录碰撞仅剩 `SuperBuilder_AI.Services` 跨 Application/Infrastructure（关注点约定的有意设计，非缺陷）；M9-03 BusinessTerm 强类型化。

> **交付 M9-03**（2026-09-06；BusinessTerm 强类型化，双提交 feat+docs，未推送 origin）：
> - **现状盘点**：业务术语全程以自由 `string`/`List<string>` 流转，全仓无 `BusinessTerm` 强类型（仅 `BusinessTermExtractor` 抽取器类，无 class/struct/record/enum）。引用面：6 个源文件 + 1 个测试，全部为 `List<string>` 形态——`BusinessTermExtractor.CollectBusinessTerms`/`CollectBusinessTermsFallback` 返回 `List<string>`、`QueryPlanBuilder.Diagnostics.BusinessTerms` 为 `List<string>`、候选表选择与评分链路 `SelectBestTable`/`ScoreTableCandidate`/`CalculateTableBoost`/`CalculateLocalMatchCount` 均收 `List<string> businessTerms`。命名无冲突，引入 `BusinessTerm` 类型零碰撞。
> - **实施（强类型）**：新增 `Domain/BiQuery/BusinessTerm.cs`（`SuperBuilder_AI.Models.BI`，Domain 层）——`readonly struct` 值类型，构造即规范化（去首尾空白、折叠内部连续空白），值相等以 `OrdinalIgnoreCase` 为准（与既有 `Distinct(StringComparer.OrdinalIgnoreCase)` 去重语义一致，可作字典键/集合去重依据）；提供到 `string` 的隐式转换使下游（语义搜索、文本归一化、关联判断）无缝消费；配套 `JsonConverter` 使 `List<BusinessTerm>` 在诊断端点序列化为纯 `string[]`，保持与 `List<string>` 线格式兼容（`QueryPlanDiagnostics` 经 `AITestController.BuildWithDiagnosticsAsync` 暴露，须避免契约破坏）。
> - **实施（链路收敛）**：`BusinessTermExtractor` 两处 `Collect*` 返回 `List<BusinessTerm>`、`SearchMetadataAsync` 收 `List<BusinessTerm>`；`QueryPlanBuilder` 三个 partial（Search/TableSelection/Diagnostics）与 `TableSelector` 的业务术语签名由 `List<string>` 收敛为 `List<BusinessTerm>`（含 `SelectBestTable` 及其 `ScoreTableCandidate`/`CalculateTableBoost`/`CalculateLocalMatchCount` 内部方法）。内部抽取逻辑、评分公式、召回/去重行为逐字节不变。
> - **实施（防回归）**：`ArchitectureTests` 新增 3 条不变量——`BusinessTerm_Should_Be_ValueType_NotStringAlias`（须值类型而非 string 别名）、`QueryPlanDiagnostics_BusinessTerms_ShouldBe_ListOfBusinessTerm`、`BusinessTermExtractor_CollectBusinessTerms_ShouldReturn_ListOfBusinessTerm`。
> - **验收（构建+测试）**：后端 0 错误；RCL 多目标 0 错误（127 警告为既有多目标裁剪提示）；Web 0 警告 0 错误；MAUI Windows 0 错误（2 既有警告）；单元套件 1002/1002 全绿（基线 999 + 新增 3 架构不变量），0 回归。
> - **红线/诚实声明**：`SuperBIContext.cs` 无改动、未纳入；`nul` phantom 与 `tmp_ns_analysis` 临时脚本未纳入；诊断端点 JSON 线格式经 JsonConverter 保持 `string[]` 不变（未实跑 HTTP 验证，仅静态契约保持）。
> - **覆盖范围与后续**：业务术语已实现编译期约束替代自由字符串；M9-04 Production/Internal Diagnostics 分区。

---

