# OPEN 待决项回填表（G1 前置）

> Status: ACTIVE（**待平台 owner / 产品负责人填写**）
> Last Updated: 2026-09-13
> 上游：`docs/plans/active/2026-09-g1-execution.md` §2「开工前置：OPEN 待决项回填清单」

---

## 0. 为什么需要这张表

G1（企业试点发布门禁）有若干项**不是代码问题，而是需要业务/平台侧拍板的口径**。这些口径未定，下游任务无法验收（只能做骨架）。本表把它们从「散落在计划里的 OPEN 行」变成**可逐项填写、可回收、可校验**的清单。

**填写人**：支持环境与性能/恢复目标 → 平台 owner；试点业务口径 → 产品负责人。
**回收路径**：按下表「回填去向」写回对应文档，并在 `2026-09-g1-execution.md` §2 把该行状态改为 `CLOSED`。

## 1. 总览

| # | OPEN 项 | 填写人 | 阻塞的任务 | 定稿门槛 |
|---|---|---|---|---|
| O1 | 支持环境 | 平台 owner / 运维 | E2E-01(M13-09) 范围、PERF-01(M13-12) 范围、双实例支持结论 | 集成验证前 |
| O2 | 性能和恢复目标 | 平台 owner | PERF-01(M13-12)、DR-01 目标值、OBS-01 生产阈值、M14-07 服务目标 | PERF 执行前 |
| O3 | 试点业务口径 | 产品负责人 | M14-01（试点产品包）、M14-08a（成效测量方案）、M14-07 场景说明 | Golden/业务验收标准调整前 |

> 已 CLOSED：密钥与升级安排（随 SEC-01/G0 收口）。

---

## O1 · 支持环境（填写人：平台 owner / 运维）

**定稿去向**：`docs/ops/release-evidence.md`（支持环境段）+ `docs/product/Platform_Product_Development_Requirements.md`（能力边界）+ `2026-09-g1-execution.md` §2 置 CLOSED。

| # | 待填项 | 我方建议/现状 | 请填写 |
|---|---|---|---|
| 1.1 | 部署 OS | 待目标服务器安装验证 | Windows Server 2022 x64（用户同意推荐，2026-09-14） |
| 1.2 | 实例数 | 应用拓扑，不代表所有依赖同机 | 单实例（用户确认，2026-09-14） |
| 1.3 | 是否支持双实例 | 其他部署组合后续考虑 | 首批不支持（用户确认，2026-09-14） |
| 1.13 | 安装方式 | Windows 托管方式和依赖服务部署需实施核验 | 直接安装（用户确认，2026-09-14） |
| 1.4 | 支持的数据源类型（业务库） | 连接层：SQL Server / MySQL / PostgreSQL 三分支（源码 `DataSourceConnectionFactory.cs`）；扫描层仅 MySQL 实现（已验证） | **MySQL**（连接→扫描→查询三层完整实现并验证）；SQL Server / PostgreSQL 仅连接分支存在，业务库元数据扫描未实现，**G1 不纳入验收**（AI 建议，2026-09-14 待 owner 确认） |
| 1.5 | 已验证层级 | MySQL：连接→扫描→查询 三层已验证；SQL Server/PostgreSQL 仅连接分支存在 | MySQL 三层已验证（WMS `steccn_wms` 真实库跑通）；SQL Server / PostgreSQL 未做业务库扫描验证，不计入 G1 验收（AI 建议，2026-09-14 待 owner 确认） |
| 1.6 | 元数据库引擎与版本 | 可与应用同机，需配置内存上限 | SQL Server 2022（参考配置已确认） |
| 1.7 | Qdrant 版本 | 1.19（1024 维） | |
| 1.8 | 大模型供应方 | 阿里云百炼（DashScope 兼容模式） | |
| 1.9 | 浏览器支持范围 | 本机 Windows 11 Chromium 登录+权限矩阵 20 项 19 通过 1 跳过（`docs/ops/browser-preflight-20260914.md`） | **Chromium 内核（Chrome / Edge 最新两个大版本）**；其余内核（Firefox/Safari）首批不做认证（AI 建议，2026-09-14 待 owner 确认） |
| 1.10 | 参考硬件 | 非最低配置认证；容量待实测 | 8 核 x64 / 32 GB / 512 GB SSD，备份另存（用户确认） |
| 1.11 | 是否要求离线/内网部署 | 业务库沿用已有服务器，无本地大模型/GPU | 当前参考配置使用外部模型服务，不属于完全离线方案 |
| 1.12 | 语言支持范围 | 现状：新租户默认仅 `zh-CN`；多语言需显式配置（见 `handover/02` §3） | **首批 zh-CN**；多语言界面需显式配置语言，不在 G1 验收范围（AI 建议，2026-09-14 待 owner 确认） |

**影响**：E2E-01 的场景边界；PERF-01 的测试前提；M14-07 的「支持环境清单」；双实例结论。
**⚠️ 注意**：1.3 若答「支持双实例」，需先完成共享限流存储 + 缓存一致性增强，属**范围扩张**，须评估排期。

### O1 · 最低 vs 最优对照（针对 1.4 / 1.5 / 1.9 / 1.12）

> 「当前最低」= 上表已填入并验证的承诺下限；「目标最优」= 产品理想范围；「差距动作」= 从最低走到最优需补的工作或范围划分决策。**G1 收口以「当前最低」为准，超出部分不得写进 G1 验收。**

| # | 当前最低（已填值） | 目标最优 | 差距动作 |
|---|---|---|---|
| 1.4 | MySQL（连接→扫描→查询三层验证）；SQL Server/PostgreSQL 仅连接分支，G1 不纳入 | SQL Server / PostgreSQL 业务库扫描验证，三库全支持 | 实现 `SqlServerMetadataReader` / `PostgreSqlMetadataReader` 并跑通真实库；或明文划为 G1 后支持（**推荐后者**，避免范围扩张触发门禁违规） |
| 1.5 | MySQL 三层已验证（WMS `steccn_wms` 真实库） | 三库均完成连接→扫描→查询验证 | 随 1.4 差距动作一并补齐两库扫描实现 + 验证 |
| 1.9 | Chromium 内核（Chrome / Edge 最新两大版本） | 跨浏览器（Firefox / Safari）+ 移动端 WebView 认证 | 扩展 E2E 测试矩阵，认证非 Chromium 内核；试点受控环境可暂不做 |
| 1.12 | 首批 zh-CN；多语言需显式配置 | 多语言界面（en-US 等）可切换 | 修「新租户语言」缺陷（创建 UI 缺语言多选），补 seed/资源四地一致；属已知缺口，不在 G1 |

---

## O2 · 性能和恢复目标（填写人：平台 owner）

**定稿去向**：`docs/ops/observability-troubleshooting.md` §3（生产阈值）+ `docs/handover/03-operations-runbook.md` §1（SLO）+ `05-backup-restore.md` §3.3（RTO）+ `docs/ops/backup-restore-dr.md` §3（RPO/RTO）+ §2 置 CLOSED。

| # | 待填项 | 我方建议/实测基线 | 请填写（AI 建议，待 owner 确认） |
|---|---|---|---|
| 2.1 | 目标数据规模（行数/表数） | 演练基线：52 表 / ~1061 向量点 | **≤ 1000 表 / ≤ 50000 向量点**（用户要求扩大 10×；≈ 演练基线 19–47×，须 PERF-01 按此规模实测，非压测认证） |
| 2.2 | 目标并发（用户/请求数） | 【待填】 | 试点 ≤ 20 并发用户 / ≤ 50 请求·分钟⁻¹（单实例 Blazor Server，受控试点） |
| 2.3 | Ask 接口 **P95** 上限 | 【待填；实测后回填】 | P95 ≤ 8s（受 LLM 延迟主导；上线前须实测回填，不得用目标反推实测） |
| 2.4 | 实时查询接口 **P95** 上限 | 【待填】 | P95 ≤ 2s、P99 ≤ 5s（不含 LLM，直查业务库解析结果） |
| 2.5 | 请求失败率上限 | 【待填】 | ≤ 1%（不含 429 限流拒绝；429 属预期超载行为） |
| 2.6 | 每请求成本上限 | 【待填】 | Ask 单次 ≤ ¥0.05（待按 token 用量实测回填；LLM 调用主导） |
| 2.7 | **RPO**（允许数据损失） | 建议值见 `backup-restore-dr.md` §3；备份耗时 2.3s 不是 RPO；须核对备份/日志频率及可恢复数据时间点 | ≤ 24h（每日备份；元数据为扫描驱动、变化低频，试点可接受） |
| 2.8 | **RTO**（恢复时间） | 组件耗时：库还原 0.124s、Qdrant 109s@592MB；完整业务 RTO 须计入发现、准备、恢复及业务验证 | ≤ 30 min（单实例试点，含发现+准备+还原+业务验证；Qdrant 还原 109s 为主项） |
| 2.9 | 月度可用性目标 | 【待填】 | 99.5%（单实例无 HA，不承诺 99.9%；含计划维护窗口） |
| 2.10 | 生产告警阈值（错误率/失败率/积压） | 出厂基线：路由错误率 20%/50%、登录失败 30%、Ask 失败 20%、积压 ≥5 | 采用出厂基线（路由 20%/50%、登录 30%、Ask 20%、积压 ≥5），上线前按需收紧 |
| 2.11 | 超载行为（限流/排队/拒绝） | 现状：限流 `RateLimit:LoginLimit`/`GlobalLimit` | 超限返回 429 + 拒绝，不雪崩；`LoginLimit`/`GlobalLimit` 按试点并发调参 |

**影响**：PERF-01 验收；DR-01「目标值」列；OBS-01 生产阈值；**M14-07 全部服务目标**（判据 4「承诺不超过实测能力」）。
**⚠️ 注意**：2.3/2.7/2.8 须先确定业务目标，再分别由性能测试与完整恢复演练验证；不得用实测结果自动改写目标；若业务期望高于实测，应调整目标或投入优化，**不得直接写进合同**。

**⚠️ 注意（2.1 扩大 10× 的连锁影响）**：目标规模升至 ≤1000 表 / ≤50000 向量点后——① PERF-01 须按此规模准备测试数据并实测 P95/RTO，否则验收不成立；② 参考硬件（8 核 / 32GB / 512GB SSD）**维持现配置**（2026-09-14 决策：由 PERF-01 按 10× 规模实测验证，不过再调），容量风险待 PERF 结论；③ RTO（2.8 ≤30min）随数据量增长，Qdrant 还原 109s@592MB 须按 50k 向量重新测算；④ 该目标高于当前实测能力，按门禁规则须投入验证/优化，**不得直接写进对外承诺**。

---

## O3 · 试点业务口径（填写人：产品负责人）

**定稿去向**：`docs/product/pilot-package.md`（M14-01 交付物）+ §2 置 CLOSED。

| # | 待填项 | 我方建议/现状 | 请填写（AI 建议，待产品负责人确认） |
|---|---|---|---|
| 3.1 | 试点业务场景（1 个） | 建议从已验证场景（**库存 / 入库**）中锁定 1 个 | **库存查询与入库分析**（WMS MySQL `steccn_wms` 已连接→扫描→查询三层跑通） |
| 3.2 | 目标用户角色 | 【待填】 | 仓库管理员 + 运营分析员（非技术，替代手工 SQL/Excel 取数） |
| 3.3 | 要解决的业务问题 | 【待填】 | 非技术人员用自然语言查库存/入库趋势，降低取数门槛与等待 |
| 3.4 | 数据要求（表/字段/时间范围） | 【待填】 | WMS MySQL `steccn_wms`（24 `wms_*` + 11 `QRTZ_*`，**无销售域、无金额字段**）；时间范围=业务库可用数据范围（非固定近 90 天，无近期数据时按历史全量） |
| 3.5 | 指标计算方式 | 【待填】 | 入库数量、库存存量、库龄（基于 `wms_*` 表字段聚合；**业务库无金额字段，不含入库金额**，语义层已定义） |
| 3.6 | 验收问题集（3~10 问） | 【待填】 | ① 指定日期的入库数量（按仓库）② 各仓库当前库存总量 ③ 某时间段的入库趋势（按业务库可用数据范围，非固定近 N 天）④ Top10 物料入库**数量**（非金额）⑤ 某仓库库龄分布 ⑥ 指定期间出入库**数量**对比。**时间参数为可用数据范围、不假设近期；指标不含金额** |
| 3.7 | 参考结果确认人 | 【待填】 | 产品负责人（角色即可，无需个人真名） |
| 3.8 | 已支持 / 未支持边界 | 已知：**无销售域**（业务库无销售相关表） | 支持：库存/入库类；**不支持：销售/营收类**（业务库无相关表，属预期） |
| 3.9 | 成效测量口径阈值 | 首效用时 / 首接成功率 / 任务正确率 / 人工修正 / 周活 / 节省时间 / 支持投入 / 成本 / 付费意愿 | 首接成功率 ≥ 80%、任务正确率 ≥ 90%、节省取数时间 ≥ 50%、周活 ≥ 5 人（试点 4 周口径，待产品确认） |

**影响**：M14-01 试点产品包；M14-08a 测量方案；M14-07 中「试点范围」与「支持入口」的表述。

---

## 4. 回填后的动作

| # | 动作 | 产出 |
|---|---|---|
| 1 | 更新 `2026-09-g1-execution.md` §2 对应行 → `CLOSED` | 计划同步 |
| 2 | 数值写入各「定稿去向」文档 | 单一真相 |
| 3 | **清除** `docs/handover/*` 中的 `【待实测回填】` 占位符 | M14-07 定稿门槛 1 |
| 4 | 触发对应任务开工/验收（PERF-01 → M14-01 → M14-08a → M14-07 定稿） | Backlog 状态流转 |
| 5 | 若出现与实测冲突（如目标高于实测能力） | 记录为范围/投入决策项，**不得静默降级承诺** |

## 5. 回填进度

| OPEN | 状态 | 填写人 | 回填日期 | 备注 |
|---|---|---|---|---|
| O1 支持环境 | **CLOSED（§4 已回填，2026-09-14）** | 用户 / AI 建议 | 2026-09-14 | 1.1~1.3/1.6~1.8/1.10/1.11/1.13 用户已确认；1.4/1.5/1.9/1.12 用户早期「同意，继续」认可 |
| O2 性能和恢复目标 | **CLOSED（§4 已回填，2026-09-14）** | 用户 / AI 建议 | 2026-09-14 | 2.1~2.11 全接受；容量决策=维持现配置 PERF 实测；承诺类指标（2.3/2.7/2.8/2.9）须实测或业务确认，不得由实测反推目标 |
| O3 试点业务口径 | **CLOSED（§4 已回填，2026-09-14）** | 用户 / AI 建议 | 2026-09-14 | 3.1~3.9 由库存/入库场景反推；3.6/3.5/3.4 按「无金额/无近期数据」反馈修订；3.7 用角色「产品负责人」即可，无需真名 |

已确认配置与验证步骤见 [Windows 单实例参考配置](../../ops/windows-pilot-profile.md)。O1 表单项已补全（1.4/1.5/1.9/1.12 为 AI 建议待 owner 确认）；剩余真实施工待落实项：目标服务器到货验证、Qdrant 服务部署位置（与应用同机或独立）、以及最终 owner 对建议值签字。
