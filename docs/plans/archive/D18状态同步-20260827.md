> **HISTORICAL SNAPSHOT / 历史快照**：本文是归档资料，只描述记录当时的计划、状态或审计判断。文中的“当前、唯一、已完成、未完成、风险、测试基线、HEAD”等均不得解释为现在的项目状态。当前事实请按 `docs/README.md` 的治理顺序核验。\n\n# D18 状态同步（2026-08-27）

> 本文件用于同步 D14 Golden Regression 从 BLOCK 到 PASS 的完整修复链事实基线，并记录 D15-D18 修复链进入 master 后的最终状态。

## 1. 当前源码基线

- Branch：`master`
- 当前阶段基线：`6a5f7d6`（D15-D18 修复链已提交）
- 结果文件：`SuperBulider_AI/golden_result_d18.json`
- Golden Dataset：`query-plan-golden` v1.3

> 历史记录中的 `252cf2b` 与“D15-D18 尚未提交 master”仅代表本文件最初生成时的状态，不再作为当前基线。

## 2. D18 最终证据

| 指标 | 结果 | 阈值 | 判定 |
|---|---|---|---|
| Overall Pass Rate | **100%**（18/18） | ≥90% | ✅ |
| Positive Pass Rate | **100%**（11/11） | ≥95% | ✅ |
| Negative Detection | 100%（5/5） | — | ✅ |
| Ambiguous Detection | 100%（1/1） | — | ✅ |
| Unresolved Detection | 100%（1/1） | — | ✅ |
| failedGates | **[]** | — | ✅ |

## 3. D15-D18 修复链（已进入 master）

| 轮次 | 修复内容 | 最终状态 |
|---|---|---|
| D15/D16 | 供应商 MasterJoin 语义对齐、QueryJoin 语义字段、事实侧 FK 锚定、Label GROUP BY 契约 | 已提交 master |
| D17 | EntityCount `MetricType` 传播与 `COUNT` 确定性绑定；回填 `plan.DataSourceId` | 已提交 master |
| D18 | `ApplyDimensionResolutions` 确定性补全 LLM 漏产出 Dimension，保持严格数量断言 | 已提交 master |

## 4. 核心模式

1. **Resolution 是权威绑定**：SemanticApplicability 的 Resolution 结果是唯一事实源，LLM intent 的漂移在 QueryPlanBuilder 层用确定性规则覆盖，不允许放宽评分器断言。
2. **语义契约与物理执行分离**：`SemanticText` 与 `TableName/ColumnName` 双轨；MasterJoin 维度运行时锚定事实侧 FK，展示使用 Label 列。
3. **LLM 波动的确定性兜底**：Resolution 已确认的 Filter / Dimension / Metric 聚合，在 Runtime 缺失时由 Resolution 补全。

## 5. 当前 Freeze / Exit 状态

```text
master 6a5f7d6
      ↓
D15-D18 已提交
      ↓
Build PASS
      ↓
Startup PASS
      ↓
Golden 18/18 PASS
      ↓
failedGates=[]
      ↓
D14-D20 = FROZEN
      ↓
D21 Exit Review = PASS
      ↓
Phase 2.7 = READY TO CLOSE
```

## 6. 历史失败根因簇 → 最终状态

| 根因簇 | Case | 最终状态 |
|---|---|---|
| Multi-DB DirectKey（供应商） | GQ-005 / GQ-009 | PASS |
| Ranking Eval | GQ-006 / GQ-010 | PASS |
| EntityCount Ambiguous | GQ-002 / GQ-008 | PASS |
| Multi-Metric | GQ-007 | PASS |

## 7. 下一步

正式关闭 Phase 2.7 前，将本状态与主 Phase 计划、Phase 2.7 计划保持一致；关闭后再进入 Phase 3。
