@page "/model-accounts"
<h2>模型与账号（BYO）</h2>
<p class="muted">§13.2 多 AI 模型 + 用户自绑定账号（BYO）：列出可用模型、选择当前模型、绑定自有 API Key（掩码）、设为默认。后端 <code>ILLMProvider</code> 抽象 + <code>UserModelBinding</code> 计划于 P13 实现。</p>
